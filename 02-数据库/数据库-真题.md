---
title: 数据库 · 真题
type: 系统
role: 真题筛题与组卷入口
database_type: 真题聚合
module: 全科
subject: 系统
tags: [化竞, 数据库, 真题, Dataview]
updated: 2026-06-15
---

# 数据库 · 真题

> 本文件是所有已入库真题的统一操作台。
> 现在默认先用页首的交互组卷面板；页内只保留仍有直接使用价值的查询入口。

> **页面边界（2026-06-15）**：本页负责”题怎么筛、怎么组、怎么按字段浏览”。
> - 面板已扩展至全题库（真题 + 化学原理 + 教材习题 + 教学改编题），支持来源筛选
> - 想知道当前哪批课最该补题：去 [[备课思路/备课与教学思路待办]]
> - 想知道某专题上课时该先讲哪题：回对应专题页 / 课堂执行页
> - 想看短期修库任务：去 [[活跃任务]]

---

## 推荐入口（先用这里）

> 高频使用场景直接从这里进入，不必再往下翻。
> 支持关键词、模块、难度梯度、轮次预设、查询模板复制、候选题导出。

![[02-数据库/片段-真题组卷面板]]

**读完本页后通常下一跳**

- 想按字段精筛：[[02-数据库/真题.base]]
- 想从备课侧判断哪页最该补题：[[备课思路/备课与教学思路待办]]
- 想做学生练习卷：[[06-学生侧材料/练习卷/README]]

---

## 一、全题库概览

```dataview
TABLE WITHOUT ID
  link(file.name, title) as 题目,
  year as 年份,
  module as 模块,
  question_type as 题型,
  difficulty as 难度,
  teaching_level as 层次,
  join(knowledge_points, "、") as 核心知识点
FROM "04-题库"
WHERE type = "题目"
SORT difficulty ASC, year DESC, file.name ASC
```

---

## 二、按知识点交叉筛选

> 低频参考视图。用于观察高频考点交叉覆盖；真要组卷时，优先回到页首交互面板。

```dataviewjs
const pages = dv.pages('"04-题库"').where(p => p.type == "题目").array();
const counter = new Map();

for (const page of pages) {
  const points = Array.isArray(page.knowledge_points)
    ? page.knowledge_points
    : (page.knowledge_points ? [page.knowledge_points] : []);
  for (const point of points) {
    const key = String(point ?? "").trim();
    if (!key) continue;
    counter.set(key, (counter.get(key) ?? 0) + 1);
  }
}

const rows = [...counter.entries()]
  .sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0], "zh-Hans-CN"))
  .slice(0, 30)
  .map(([point, count]) => [point, count]);

if (!rows.length) {
  dv.paragraph("未找到可统计的 knowledge_points。");
} else {
  dv.table(["知识点", "出现次数"], rows);
}
```

---

## 三、组卷操作台（基础 / 综合 / 挑战）

> 这一节直接服务于 [[模板-专题练习卷]]。
> 如果只是要筛题，请优先使用页首“推荐入口”里的交互面板。
> 使用方法：
> 1. 先把查询里的 `目标KP` / `专题关键词` 改成当前专题的核心词
> 2. 若是多知识点专题，用 `OR contains(string(knowledge_points), "...")` 继续补条件
> 3. 第一轮通常取 `基础 3-4 题 + 综合 1-2 题`
> 4. 第二轮通常取 `基础 2 题 + 综合 2-3 题 + 挑战 1 题`

### 3.1 基础巩固池（difficulty = 2）

```dataview
TABLE WITHOUT ID
  link(file.path, title) AS 题目,
  year AS 年份,
  module AS 模块,
  question_type AS 题型,
  difficulty AS 难度,
  join(knowledge_points, "、") AS 知识点
FROM "04-题库"
WHERE type = "题目"
  AND contains(string(knowledge_points), "目标KP")
  AND difficulty = 2
SORT year DESC, file.name ASC
LIMIT 12
```

### 3.2 综合应用池（difficulty = 3）

```dataview
TABLE WITHOUT ID
  link(file.path, title) AS 题目,
  year AS 年份,
  module AS 模块,
  question_type AS 题型,
  difficulty AS 难度,
  join(knowledge_points, "、") AS 知识点
FROM "04-题库"
WHERE type = "题目"
  AND contains(string(knowledge_points), "目标KP")
  AND difficulty = 3
SORT year DESC, file.name ASC
LIMIT 12
```

### 3.3 拓展挑战池（difficulty >= 4）

```dataview
TABLE WITHOUT ID
  link(file.path, title) AS 题目,
  year AS 年份,
  module AS 模块,
  question_type AS 题型,
  difficulty AS 难度,
  join(knowledge_points, "、") AS 知识点
FROM "04-题库"
WHERE type = "题目"
  AND contains(string(knowledge_points), "目标KP")
  AND difficulty >= 4
SORT difficulty ASC, year DESC, file.name ASC
LIMIT 10
```

### 3.4 多知识点专题模板（可直接改词）

> 适合“一个专题不止一个 KP 名称”的情况，例如化学平衡、晶体结构、酸碱理论。

```dataview
TABLE WITHOUT ID
  link(file.path, title) AS 题目,
  year AS 年份,
  question_type AS 题型,
  difficulty AS 难度,
  join(knowledge_points, "、") AS 知识点
FROM "04-题库"
WHERE type = "题目"
  AND (
    contains(string(knowledge_points), "专题关键词A")
    OR contains(string(knowledge_points), "专题关键词B")
    OR contains(string(knowledge_points), "专题关键词C")
  )
  AND difficulty >= 2
  AND difficulty <= 4
SORT difficulty ASC, year DESC
LIMIT 20
```

### 3.5 按模块直接组卷（少改词版）

> 当你只是想快速从某一模块抓一组题时，用这一段最快。

#### 化学原理

```dataview
TABLE WITHOUT ID
  link(file.path, title) AS 题目,
  year AS 年份,
  question_type AS 题型,
  difficulty AS 难度,
  join(knowledge_points, "、") AS 知识点
FROM "04-题库"
WHERE type = "题目"
  AND module = "化学原理"
  AND difficulty >= 2
  AND difficulty <= 4
SORT difficulty ASC, year DESC
LIMIT 18
```

#### 无机和结构化学

```dataview
TABLE WITHOUT ID
  link(file.path, title) AS 题目,
  year AS 年份,
  question_type AS 题型,
  difficulty AS 难度,
  join(knowledge_points, "、") AS 知识点
FROM "04-题库"
WHERE type = "题目"
  AND module = "无机和结构化学"
  AND difficulty >= 2
  AND difficulty <= 4
SORT difficulty ASC, year DESC
LIMIT 18
```

#### 分析化学

```dataview
TABLE WITHOUT ID
  link(file.path, title) AS 题目,
  year AS 年份,
  question_type AS 题型,
  difficulty AS 难度,
  join(knowledge_points, "、") AS 知识点
FROM "04-题库"
WHERE type = "题目"
  AND module = "分析化学"
  AND difficulty >= 2
  AND difficulty <= 4
SORT difficulty ASC, year DESC
LIMIT 18
```

#### 有机化学

```dataview
TABLE WITHOUT ID
  link(file.path, title) AS 题目,
  year AS 年份,
  question_type AS 题型,
  difficulty AS 难度,
  join(knowledge_points, "、") AS 知识点
FROM "04-题库"
WHERE type = "题目"
  AND module = "有机化学"
  AND difficulty >= 2
  AND difficulty <= 4
SORT difficulty ASC, year DESC
LIMIT 18
```

### 3.6 组卷前快速检查

| 检查项 | 建议 |
|:---|:---|
| 是否全是同一种题型 | 至少混入 2 种题型，避免整张卷只练一种动作 |
| 是否连续 2 题考同一小点 | 尽量错开，避免“一错全错” |
| 是否只有高难题 | 第一轮/第二轮先保留 2-3 道可稳拿分的题 |
| 是否缺少题目来源跨度 | 尽量混合不同届次，避免同一套卷风格过于单一 |
| 是否能回链到 KP | 题目优先保留 `knowledge_points` 完整的条目 |

### 3.7 DataviewJS 交互组卷面板

> 主面板已前置到页首，不再在此处重复渲染。
> 如需维护交互脚本源码，请编辑 [[02-数据库/片段-真题组卷面板]]。

## 四、有机专项（当前冲刺重点）

> 有机化学是基础要求最后一块拼图，此处单独列出有机相关真题以便集中突破。

### 4.1 有机真题列表

```dataview
TABLE WITHOUT ID
  link(file.name, title) as 题目,
  year as 年份,
  question_type as 题型,
  difficulty as 难度,
  join(knowledge_points, "、") as 核心知识点
FROM "04-题库"
WHERE type = "题目"
  AND module = "有机化学"
SORT year DESC, difficulty ASC
```

### 4.2 有机真题涉及的知识点覆盖

```dataview
TABLE WITHOUT ID
  knowledge_points as 知识点,
  count(file.name) as 考查次数,
  join(map(rows, (r) => link(r.file.name, r.title)), "、") as 关联题目
FROM "04-题库"
WHERE type = "题目"
  AND module = "有机化学"
FLATTEN knowledge_points
GROUP BY knowledge_points
SORT count(file.name) DESC
```

---

## 五、数据质量检查

### 5.1 缺少 knowledge_points 的题目（需补全）

```dataview
TABLE WITHOUT ID
  link(file.name, title) as 题目,
  year as 年份,
  module as 模块,
  question_type as 题型,
  difficulty as 难度
FROM "04-题库"
WHERE type = "题目"
  AND (!knowledge_points OR length(knowledge_points) = 0)
SORT year DESC, file.name ASC
```

### 5.2 缺少 syllabus_codes 的题目（需补全）

```dataview
TABLE WITHOUT ID
  link(file.name, title) as 题目,
  year as 年份,
  module as 模块,
  knowledge_points as 知识点
FROM "04-题库"
WHERE type = "题目"
  AND (!syllabus_codes OR length(syllabus_codes) = 0)
SORT year DESC, file.name ASC
```

---

## 六、最近更新

```dataview
TABLE WITHOUT ID
  link(file.name, title) as 题目,
  year as 年份,
  module as 模块,
  difficulty as 难度,
  updated as 更新日期
FROM "04-题库"
WHERE type = "题目"
  AND updated >= date("2026-05-07")
SORT file.mtime DESC
LIMIT 10
```

---

## 图例与快捷操作

| 图标 | 含义 |
|:---:|:---|
| ⭐ | 难度 5（竞赛拔高） |
| 📐 | 计算题 |
| 🔬 | 推断题 |
| 🧪 | 方程式题 |
| 📝 | 机理题 |

**快捷导航**：[[数据库-知识点]] · [[数据库-工作流]] · [[数据库-外部资料]] · [[真题总索引]]（04-题库 原版索引）

---

*本数据库页保留“组卷与筛题入口”；更细的字段筛选可回到 [[02-数据库/真题.base]]。*
