## 交互组卷面板

> 直接在本页输入关键词即可筛题。支持：
> - 多关键词：用空格分隔
> - 来源筛选：全部 / 真题 / 化学原理 / 教材习题 / 教学改编题
> - 模块筛选：全部 / 四大模块
> - 难度筛选：基础 / 综合 / 挑战
> - 轮次预设
> - 查询模板复制
> - 当前候选题清单导出

```dataviewjs
const root = dv.el("div", "", { cls: "dv-pack-panel" });

const title = document.createElement("p");
title.innerHTML = "<strong>交互组卷面板</strong>：输入知识点关键词，实时筛选“基础 / 综合 / 挑战”候选题。";
root.appendChild(title);

const controls = document.createElement("div");
controls.style.display = "grid";
controls.style.gridTemplateColumns = "2fr 1fr 1fr 1fr";
controls.style.gap = "12px";
controls.style.marginBottom = "12px";
root.appendChild(controls);

const keywordWrap = document.createElement("label");
keywordWrap.style.display = "flex";
keywordWrap.style.flexDirection = "column";
keywordWrap.style.gap = "4px";
keywordWrap.textContent = "关键词（空格分隔）";
const keywordInput = document.createElement("input");
keywordInput.type = "text";
keywordInput.placeholder = "例如：化学平衡 平衡常数 反应商";
keywordInput.style.padding = "6px 8px";
keywordInput.style.border = "1px solid var(--background-modifier-border)";
keywordInput.style.borderRadius = "6px";
keywordWrap.appendChild(keywordInput);
controls.appendChild(keywordWrap);

const moduleWrap = document.createElement("label");
moduleWrap.style.display = "flex";
moduleWrap.style.flexDirection = "column";
moduleWrap.style.gap = "4px";
moduleWrap.textContent = "模块";
const moduleSelect = document.createElement("select");
[
  "全部模块",
  "化学原理",
  "无机和结构化学",
  "分析化学",
  "有机化学"
].forEach(name => {
  const opt = document.createElement("option");
  opt.value = name;
  opt.textContent = name;
  moduleSelect.appendChild(opt);
});
moduleSelect.style.padding = "6px 8px";
moduleSelect.style.border = "1px solid var(--background-modifier-border)";
moduleSelect.style.borderRadius = "6px";
moduleWrap.appendChild(moduleSelect);
controls.appendChild(moduleWrap);

const sourceWrap = document.createElement("label");
sourceWrap.style.display = "flex";
sourceWrap.style.flexDirection = "column";
sourceWrap.style.gap = "4px";
sourceWrap.textContent = "来源";
const sourceSelect = document.createElement("select");
[
  "全部来源",
  "真题",
  "化学原理",
  "教材习题",
  "教学改编题"
].forEach(name => {
  const opt = document.createElement("option");
  opt.value = name;
  opt.textContent = name;
  sourceSelect.appendChild(opt);
});
sourceSelect.style.padding = "6px 8px";
sourceSelect.style.border = "1px solid var(--background-modifier-border)";
sourceSelect.style.borderRadius = "6px";
sourceWrap.appendChild(sourceSelect);
controls.appendChild(sourceWrap);

const limitWrap = document.createElement("label");
limitWrap.style.display = "flex";
limitWrap.style.flexDirection = "column";
limitWrap.style.gap = "4px";
limitWrap.textContent = "结果上限";
const limitInput = document.createElement("input");
limitInput.type = "number";
limitInput.min = "5";
limitInput.max = "60";
limitInput.value = "24";
limitInput.style.padding = "6px 8px";
limitInput.style.border = "1px solid var(--background-modifier-border)";
limitInput.style.borderRadius = "6px";
limitWrap.appendChild(limitInput);
controls.appendChild(limitWrap);

const bucketWrap = document.createElement("div");
bucketWrap.style.display = "flex";
bucketWrap.style.gap = "16px";
bucketWrap.style.marginBottom = "12px";
root.appendChild(bucketWrap);

function makeCheck(label, checked = true) {
  const wrap = document.createElement("label");
  wrap.style.display = "flex";
  wrap.style.alignItems = "center";
  wrap.style.gap = "6px";
  const input = document.createElement("input");
  input.type = "checkbox";
  input.checked = checked;
  wrap.appendChild(input);
  wrap.appendChild(document.createTextNode(label));
  bucketWrap.appendChild(wrap);
  return input;
}

const baseCheck = makeCheck("基础（difficulty 2）", true);
const midCheck = makeCheck("综合（difficulty 3）", true);
const highCheck = makeCheck("挑战（difficulty 4-5）", true);

const presetRow = document.createElement("div");
presetRow.style.display = "flex";
presetRow.style.alignItems = "end";
presetRow.style.gap = "12px";
presetRow.style.marginBottom = "12px";
root.appendChild(presetRow);

const presetWrap = document.createElement("label");
presetWrap.style.display = "flex";
presetWrap.style.flexDirection = "column";
presetWrap.style.gap = "4px";
presetWrap.textContent = "轮次预设";
const presetSelect = document.createElement("select");
[
  "自定义",
  "第一轮（基础班）",
  "第二轮（提高班）",
  "第三轮（深化）",
  "第四轮（冲刺）"
].forEach(name => {
  const opt = document.createElement("option");
  opt.value = name;
  opt.textContent = name;
  presetSelect.appendChild(opt);
});
presetSelect.style.padding = "6px 8px";
presetSelect.style.border = "1px solid var(--background-modifier-border)";
presetSelect.style.borderRadius = "6px";
presetWrap.appendChild(presetSelect);
presetRow.appendChild(presetWrap);

const applyPresetBtn = document.createElement("button");
applyPresetBtn.textContent = "应用预设";
applyPresetBtn.style.padding = "6px 10px";
applyPresetBtn.style.border = "1px solid var(--background-modifier-border)";
applyPresetBtn.style.borderRadius = "6px";
applyPresetBtn.style.cursor = "pointer";
presetRow.appendChild(applyPresetBtn);

const copyBtn = document.createElement("button");
copyBtn.textContent = "复制当前查询模板";
copyBtn.style.padding = "6px 10px";
copyBtn.style.border = "1px solid var(--background-modifier-border)";
copyBtn.style.borderRadius = "6px";
copyBtn.style.cursor = "pointer";
presetRow.appendChild(copyBtn);

const actionMsg = document.createElement("span");
actionMsg.style.color = "var(--text-muted)";
actionMsg.style.fontSize = "0.9em";
presetRow.appendChild(actionMsg);

const hint = document.createElement("p");
hint.style.marginTop = "0";
hint.style.color = "var(--text-muted)";
hint.textContent = "提示：关键词会在 knowledge_points、标题、来源中做模糊匹配。搜索范围覆盖全部题库（真题 + 化学原理 + 教材习题 + 教学改编题）。";
root.appendChild(hint);

const summary = document.createElement("div");
summary.style.margin = "8px 0 12px";
root.appendChild(summary);

const resultMount = document.createElement("div");
root.appendChild(resultMount);

const exportMount = document.createElement("div");
exportMount.style.marginTop = "16px";
root.appendChild(exportMount);

const pages = dv.pages('"04-题库"').where(p => p.type == "题目").array();

function sourceCategory(p) {
  const path = p.file.path;
  const src = p.source ?? "";
  if (path.includes("真题")) return "真题";
  if (path.includes("化学原理")) return "化学原理";
  if (path.includes("教学改编题")) return "教学改编题";
  if (path.includes("教材习题")) return "教材习题";
  if (src.includes("届")) return "真题";
  if (src.includes("普通化学原理")) return "化学原理";
  if (src.includes("无机化学 例题") || src.includes("无机化学例题")) return "教材习题";
  if (src.includes("ABOC")) return "教材习题";
  return "其他";
}

function difficultyMatch(p) {
  const d = Number(p.difficulty ?? 0);
  return (baseCheck.checked && d === 2)
    || (midCheck.checked && d === 3)
    || (highCheck.checked && d >= 4);
}

function moduleMatch(p) {
  return moduleSelect.value === "全部模块" || p.module === moduleSelect.value;
}

function sourceMatch(p) {
  if (sourceSelect.value === "全部来源") return true;
  return sourceCategory(p) === sourceSelect.value;
}

function keywordMatch(p, keywords) {
  if (!keywords.length) return true;
  const haystack = [
    p.title ?? "",
    p.module ?? "",
    p.question_type ?? "",
    p.source ?? "",
    Array.isArray(p.knowledge_points) ? p.knowledge_points.join(" ") : String(p.knowledge_points ?? "")
  ].join(" ").toLowerCase();
  return keywords.every(k => haystack.includes(k));
}

function bucketLabel(d) {
  if (d === 2) return "基础";
  if (d === 3) return "综合";
  if (d >= 4) return "挑战";
  return "其他";
}

function presetConfig(name) {
  if (name === "第一轮（基础班）") {
    return { limit: 10, base: true, mid: true, high: false, hint: "建议选 3-4 道基础 + 1-2 道综合" };
  }
  if (name === "第二轮（提高班）") {
    return { limit: 14, base: true, mid: true, high: true, hint: "建议选 2 道基础 + 2-3 道综合 + 1 道挑战" };
  }
  if (name === "第三轮（深化）") {
    return { limit: 16, base: false, mid: true, high: true, hint: "建议选 1 道基础（可选） + 2 道综合 + 2 道挑战" };
  }
  if (name === "第四轮（冲刺）") {
    return { limit: 16, base: false, mid: true, high: true, hint: "建议偏重挑战题，综合题用于热身与查漏" };
  }
  return { limit: Number(limitInput.value || 24), base: baseCheck.checked, mid: midCheck.checked, high: highCheck.checked, hint: "当前为自定义模式" };
}

function setMessage(text) {
  actionMsg.textContent = text;
  if (!text) return;
  clearTimeout(setMessage._timer);
  setMessage._timer = setTimeout(() => {
    actionMsg.textContent = "";
  }, 2500);
}

function buildTemplate(keywords) {
  const words = keywords.length ? keywords : ["目标KP"];
  const wherePart = words
    .map(k => `contains(string(knowledge_points), "${k}")`)
    .join("\n    OR ");
  return [
    "```dataview",
    "TABLE WITHOUT ID",
    '  link(file.path, title) AS 题目,',
    '  year AS 年份,',
    '  module AS 模块,',
    '  question_type AS 题型,',
    '  difficulty AS 难度,',
    '  join(knowledge_points, "、") AS 知识点',
    'FROM "04-题库"',
    "WHERE (",
    `    ${wherePart}`,
    "  )",
    "  AND difficulty >= 2",
    "  AND difficulty <= 4",
    "SORT difficulty ASC, year DESC",
    "LIMIT 20",
    "```"
  ].join("\n");
}

function render() {
  resultMount.innerHTML = "";
  exportMount.innerHTML = "";
  const keywords = keywordInput.value.trim().toLowerCase().split(/\s+/).filter(Boolean);
  const limit = Math.max(5, Math.min(60, Number(limitInput.value || 24)));

  const rows = pages
    .filter(p => difficultyMatch(p))
    .filter(p => moduleMatch(p))
    .filter(p => sourceMatch(p))
    .filter(p => keywordMatch(p, keywords))
    .sort((a, b) => {
      const da = Number(a.difficulty ?? 0);
      const db = Number(b.difficulty ?? 0);
      if (da !== db) return da - db;
      return Number(b.year ?? 0) - Number(a.year ?? 0);
    })
    .slice(0, limit);

  const counts = { 基础: 0, 综合: 0, 挑战: 0 };
  rows.forEach(p => {
    const label = bucketLabel(Number(p.difficulty ?? 0));
    if (counts[label] != null) counts[label]++;
  });

  const preset = presetConfig(presetSelect.value);
  summary.innerHTML = `命中 <strong>${rows.length}</strong> 题`
    + ` · 基础 ${counts["基础"]}`
    + ` · 综合 ${counts["综合"]}`
    + ` · 挑战 ${counts["挑战"]}`
    + ` · 预设建议：${preset.hint}`;

  if (!rows.length) {
    const empty = document.createElement("p");
    empty.textContent = "没有匹配结果。建议减少关键词，或放宽模块/难度限制。";
    resultMount.appendChild(empty);
    return;
  }

  const overallTitle = document.createElement("p");
  overallTitle.innerHTML = "<strong>总览表</strong>";
  resultMount.appendChild(overallTitle);

  dv.table(
    ["层级", "来源", "题目", "年份", "模块", "题型", "难度", "知识点"],
    rows.map(p => [
      bucketLabel(Number(p.difficulty ?? 0)),
      sourceCategory(p),
      dv.fileLink(p.file.path, false, p.title),
      p.year ?? "",
      p.module ?? "",
      p.question_type ?? "",
      p.difficulty ?? "",
      Array.isArray(p.knowledge_points) ? p.knowledge_points.join("、") : (p.knowledge_points ?? "")
    ]),
    resultMount
  );

  const groups = [
    { label: "基础", match: (d) => d === 2 },
    { label: "综合", match: (d) => d === 3 },
    { label: "挑战", match: (d) => d >= 4 }
  ];

  for (const group of groups) {
    const subset = rows.filter(p => group.match(Number(p.difficulty ?? 0)));
    if (!subset.length) continue;

    const titleEl = document.createElement("p");
    titleEl.style.marginTop = "18px";
    titleEl.innerHTML = `<strong>${group.label}候选题</strong>（${subset.length} 题）`;
    resultMount.appendChild(titleEl);

    dv.table(
      ["来源", "题目", "年份", "模块", "题型", "难度", "知识点"],
      subset.map(p => [
        sourceCategory(p),
        dv.fileLink(p.file.path, false, p.title),
        p.year ?? "",
        p.module ?? "",
        p.question_type ?? "",
        p.difficulty ?? "",
        Array.isArray(p.knowledge_points) ? p.knowledge_points.join("、") : (p.knowledge_points ?? "")
      ]),
      resultMount
    );
  }

  const exportTitle = document.createElement("p");
  exportTitle.style.marginTop = "18px";
  exportTitle.innerHTML = "<strong>导出当前候选题清单</strong>";
  exportMount.appendChild(exportTitle);

  const exportLines = rows.map((p, idx) => {
    const kpText = Array.isArray(p.knowledge_points) ? p.knowledge_points.join("、") : (p.knowledge_points ?? "");
    return `${idx + 1}. [${bucketLabel(Number(p.difficulty ?? 0))}] ${sourceCategory(p)}｜${p.title}｜${p.year ?? ""}｜${p.module ?? ""}｜${p.question_type ?? ""}｜难度 ${p.difficulty ?? ""}${kpText ? `｜${kpText}` : ""}`;
  });

  const exportBox = document.createElement("textarea");
  exportBox.value = exportLines.join("\n");
  exportBox.rows = Math.min(Math.max(rows.length + 2, 8), 20);
  exportBox.style.width = "100%";
  exportBox.style.padding = "8px 10px";
  exportBox.style.border = "1px solid var(--background-modifier-border)";
  exportBox.style.borderRadius = "6px";
  exportBox.style.fontFamily = "var(--font-monospace)";
  exportBox.style.fontSize = "0.9em";
  exportMount.appendChild(exportBox);

  const exportBtn = document.createElement("button");
  exportBtn.textContent = "复制当前候选题清单";
  exportBtn.style.marginTop = "8px";
  exportBtn.style.padding = "6px 10px";
  exportBtn.style.border = "1px solid var(--background-modifier-border)";
  exportBtn.style.borderRadius = "6px";
  exportBtn.style.cursor = "pointer";
  exportMount.appendChild(exportBtn);

  exportBtn.addEventListener("click", async () => {
    try {
      await navigator.clipboard.writeText(exportBox.value);
      setMessage("已复制候选题清单");
    } catch {
      exportBox.select();
      setMessage("剪贴板失败，已选中文本");
    }
  });
}

[keywordInput, moduleSelect, sourceSelect, limitInput, baseCheck, midCheck, highCheck].forEach(el => {
  el.addEventListener("input", render);
  el.addEventListener("change", render);
});

applyPresetBtn.addEventListener("click", () => {
  const preset = presetConfig(presetSelect.value);
  limitInput.value = preset.limit;
  baseCheck.checked = preset.base;
  midCheck.checked = preset.mid;
  highCheck.checked = preset.high;
  render();
  setMessage(`已应用：${presetSelect.value}`);
});

copyBtn.addEventListener("click", async () => {
  const keywords = keywordInput.value.trim().split(/\s+/).filter(Boolean);
  const text = buildTemplate(keywords);
  try {
    await navigator.clipboard.writeText(text);
    setMessage("已复制查询模板");
  } catch {
    setMessage("复制失败，请手动选取");
  }
});

keywordInput.value = "目标KP";
render();
```
