# Agent 执行规范

## MCP Server

本 vault 配备 KB Vault MCP Server（`kb-vault-mcp/`），提供 12 个 kb_* 工具。**原生 Read/Write/Edit/Bash 已禁用**，所有文件操作必须通过 MCP 工具。

### 启动流程

1. `kb_session init` — 初始化或恢复会话
2. `kb_gate action=create` — 创建 Task 实例
3. `kb_gate action=confirm` — 确认计划，进入 EXECUTING
4. `kb_write` / `kb_edit` / `kb_move` / `kb_delete` — 文件操作（自动状态检查）
5. `kb_gate action=verify` → `action=close` → `action=archive` — 完成归档

### 关键约束

- `kb_write` 在 ASSESSING 或 PLANNING 状态调用会被拒绝（TASK_NOT_EXECUTING）
- USE 类型 Task 写入 03-知识点/ 会被拒绝（USE_CANNOT_WRITE_KP）
- `kb_move` 会检查反向引用，有引用则阻断
- 每 10 次文件操作自动写检查点（`checkpoint.jsonl`）

### 设计文档

`00-首页/Agent生命周期与MCP工具层设计.md` — 完整架构文档（Session/Task 状态机、钩子系统、MCP 工具规范）

## Word 讲义写作入口

- 生成或修改讲义源 Markdown 前，先参考：`11-模板/MD通用规范（Word-PDF共同最优子集）.md`
- 生成或修改 Word 讲义前，优先参考：`00-首页/Word友好 Markdown 符号速查表.md`
- 需要看整份讲义的结构样板时，再看：`11-模板/MD讲义格式参考（原子结构样板）.md`
- 新写学生讲义时，直接复制填充：`11-模板/学生讲义模板（v1.2 填充式）.md`；生产流程与验收：`00-首页/讲义生产SOP与双向映射规范.md`
- 找图入口：`10-索引与统计/00-图谱雷达总入口.md`（总导航）+ 学科分流索引（`01-有机化学图谱总索引.md`、`02-结构与无机化学图谱总索引.md`、`03-物化与分析化学图谱总索引.md`）+ `00-首页/规则-配图来源优先级.md`（先复用后新找，最终落地 `媒体仓库/` 哈希原名）
- 核心规则：简单就 Unicode，复杂就整段 math，严禁半截 math
- 高风险写法示例：`Na$^+$`、`2s$^2$2p$^3$`、`4f$_{+3}$`、`2p$_0$`、`$I_1$(Na)`

### 搜图与图谱调用规范 (Image Search Protocol)
当需要为讲义配图或检索化学图表时，请严格遵守以下操作：
1. **第一步（学科分流索引定位）**：严禁盲目游荡目录！**必须优先使用 `grep_search` 工具搜索对应的学科分流雷达**：
   - **有机化学讲义**（机理/人名反应/立体化学/合成） $\to$ 搜索 `10-索引与统计/01-有机化学图谱总索引.md`
   - **结构与无机讲义**（晶胞/点群/配位/MO/元素） $\to$ 搜索 `10-索引与统计/02-结构与无机化学图谱总索引.md`
   - **物化与分析讲义**（相图/动力学/热力学/滴定/光度） $\to$ 搜索 `10-索引与统计/03-物化与分析化学图谱总索引.md`
   - **跨学科/未知分类** $\to$ 搜索 `10-索引与统计/全库核心图谱总索引.md` 或 `00-图谱雷达总入口.md`
2. **第二步（逆向文本发掘/备用方案）**：如果在分流索引中未找到理想的高质量分子结构图或模型图，**请务必对 `03-教材书籍`（如 Clayden、Weller、周公度等）等原始 OCR 目录进行 `grep_search` 全文搜索**。输入相关的化学概念或文字描述（如“卟啉”、“Diels-Alder”），由于 OCR 转换特性，原始原著中的高清图片哈希 `![[hash.jpg]]` 必定会紧挨着这些文字描述出现。这能帮你发掘出隐藏在库中但未被收录的孤儿高清原图！
3. **第三步（提取与验证）**：命中目标后，**必须提取“物理哈希文件名”**（完整 64 位哈希，如 `08f2e0e6...jpg`）。强烈建议使用 `view_file` 等视觉能力工具亲自打开图片进行“核验”，排除 OCR 错位导致的假阳性（比如将公式当成图片）。**绝对禁止**使用英文标签别名（如 `zhao_methyl.jpg`）作为插入名！
4. **第四步（插入）**：严格遵循 Obsidian 的解析格式，在最终生成的 Markdown 讲义中写入 `![[哈希文件名.jpg]]`。Obsidian 底层会自动在 `媒体仓库/` 中完成渲染；**状态列为 `✅在媒体仓库` 的条目可直接用于 Word 管线**。如果写入别名将导致全盘断链。

## 工作目录

```bash
cd product-merge && claude -p "..."
```

## Token节约规则

### 结构化学题库操作

1. **先读速查**：操作题库前，先读 `04-题库/教材习题/结构化学基础/题库格式速查.md`
2. **用摘要替代OCR**：需要原始题目内容时，读 `07-资料提炼/习题提炼/结构化学基础-习题章节摘要.md`
3. **禁止全读OCR**：775KB的OCR源文件只在摘要不足时才读取特定章节（用limit限制行数）
4. **批量操作**：审计/修复多个文件时，用一个agent处理多个文件，不要每个文件一个agent
5. **审计参考**：质量审计时，先读 `04-题库/教材习题/结构化学基础/题库审计清单.md`

### 通用规则

1. **Read调用加limit**：除非明确需要全文，否则用limit=100限制读取行数
2. **用Glob+Edit替代Read+Write**：小修改直接Edit，不要先Read全文再Write
3. **先检查再读取**：用kb_read检查文件是否变化，避免重复读取
4. **批量Grep**：搜索时用Grep而非逐文件Read
5. **避免重复读取同一文件**：如果文件刚读过且未修改，跳过读取

### 量化目标

| 操作类型 | 目标token | 说明 |
|----------|-----------|------|
| 单题审计 | <5K | 读速查+摘要+目标文件 |
| 批量审计(10题) | <30K | 读速查+摘要+批量frontmatter |
| 创建新题 | <10K | 读速查+OCR特定章节+写入 |
| 知识点修正 | <5K | 读速查+目标文件+Edit |

---

*最后更新：2026-07-27*
