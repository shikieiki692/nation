---
title: 讲义 docx 产物目录说明
type: 说明
tags: [学生讲义, 产物, 目录规范]
created: 2026-09-07
updated: 2026-09-18
---

# 06-学生侧材料/讲义 — 讲义 docx 产物目录

## 定位

讲义 Word 产物的**唯一当前落点**。2026-09-07 起，`11-模板/scripts/build-all-handout-docx.py` 的默认输出指向这里（此前是 `00-首页/学生讲义Word`，导航区不再堆产物）。

- **源**：`04-课件/学生讲义/{模块}/*.md`（已按学科模块分子目录）
- **生成**：`python -X utf8 11-模板/scripts/build-all-handout-docx.py --batch-root`（批量，自动按源模块写入对应子目录）；单文件 `--path` 导出默认落根级，需手动归入模块子目录
- **性质**：派生物，删除可随时重建；改需求一律改 md 源，不要直接改 docx

## 当前产物布局（按课程计划 6 模块分子目录，2026-09-18 收敛）

根级不堆文件，全部归入子目录。**模块子目录与课程计划 6 模块一一对应**（同名、同序）：

| 目录 | 内容 | 能否用 |
|:--|:--|:--|
| `1-化学基础知识/` | 课程计划模块一讲义（4） | ✅ |
| `2-结构化学/` | 课程计划模块二讲义（22） | ✅ |
| `3-物理化学/` | 课程计划模块三讲义（16） | ✅ |
| `4-分析化学/` | 课程计划模块四讲义（5） | ✅ |
| `5-无机化学/` | 课程计划模块五讲义（12） | ✅ |
| `6-有机化学/` | 课程计划模块六讲义（23） | ✅ |
| `_数学工具/` | 数学工具模块讲义：第 1~7 讲 + 附录速查卡（8） | ✅ |
| `_综合/` | 跨模块合集：分子轨道式相关真题小问总集、真题模拟拆解（2） | ✅ |
| `_archive/` | 历史归档（旧版产物、旧代输出、无源 md 旧产物、弃用产物，共 218 件，不发放） | ❌ 仅供回溯 |
| `media/` | 讲义图片 | ✅ |

> 模块归属规则：docx 名 = md 源名，按源 md 所在模块子目录归位；跨模块或题组类产物归 `_综合`；数学前置工具类归 `_数学工具`；历史产物统一在 `_archive/` 内分层归档。
>
> **`_archive/` 子层**（均为 ❌ 不可发放）：
> | 子目录 | 内容 | 件数 |
> |:--|:--|:--|
> | `旧版产物-202606/` | 2026-06 及更早历史产物（含手工拷贝、旧版导出、`试印版/`） | 122 |
> | `旧代-06-讲义输出-20260831/` | 上一代输出目录整体迁入 | 68 |
> | `无源md-旧产物/` | 无 1:1 源 md、不可重建的旧产物（内含 README 说明） | 21 |
> | `弃用产物/` | 源 md 已废弃的产物 | 7 |

## 为什么旧物不删而归档

同名对比显示：两边同名的文件里，当前产物**全部**新于旧物（旧物无一更新），理论上旧物可删。但其中大量为历史独有文件（对应 `04-课件/学生讲义/_归档/` 里的旧命名 md），保留可回溯，故统一移入 `_archive/` 分层归档而非删除。

## 使用提醒

- 找讲义进对应 `模块/` 子目录；文件名 = md 源名（含「超级充实版（自学完整）」「Word清稿」「新课」等版本后缀）。
- 看到 `_` 开头的子目录（`_archive` / `_综合` / `_数学工具`）中的 `_archive` 一律不要发给学生。
- 重导出请用 `--batch-root`，产物会自动落到对应模块子目录，维持本布局。
- 需要「课堂填空」版？目前无源可生成，须先定挖空规则，再从对应的「（自学完整）」md 生成（见 `_archive/README-孤儿课堂填空.md`）。
