# 表格内公式乱码治理 —— 剩余任务评估报告

> 生成：2026-09-20 10:0x ｜ 基线：`44e7ccd25`（已推送，远端同步）
> 口径：全库 md 扫描 `<table>…</table>` 块 + 逐表跑 `convert_table()` 取 reason
> **判据独立于工具**：扫描用自写正则（不复用工具的 iter_files），可转性用工具真实函数

---

## 一、总盘（修正前序口径）

| 域 | 文件 | 表 | 含公式表 | 可转 | 拒收 |
|---|---:|---:|---:|---:|---:|
| **可动域**（非 EXCLUDE_PREFIX） | 244 | 1,529 | 714 | — | — |
| **排除域**（EXCLUDE_PREFIX 内） | 177 | 548 | 93 | — | — |
| **合计** | 421 | 2,077 | 807 | — | — |

> ⚠️ **口径修正**：之前报的「剩余 816 表 / 230 文件」是把**含公式表**当待办量。实际
> 全库 HTML 表 **2,077 表**，其中含公式 **807 表** —— 而**工具默认只转含公式表**
> （`math_only=True`），故真实待办基数是 **807**，不是 2,077。

---

## 二、按「能否转」重新分桶（核心结论）

对含公式的 807 表逐表跑 `convert_table()`：

| 分桶 | 表数 | 说明 |
|---|---:|---|
| ✅ **可转** | ~347 | 真数据表 / 简单布局表，四口径可过 |
| ❌ **拒收·img** | ~197 | 表内含 `<img>`（图+公式混排）→ 转 pipe 会丢图 |
| ❌ **拒收·width>8** | ~87 | 答题卡/评分卡（9~10 列，含 colspan/rowspan） |
| ❌ **拒收·prose** | ~62 | 整格长文本答案卡 |
| ❌ **拒收·narrow1** | ~16 | 单列「题干+答案」问答块，非表格 |
| ❌ **拒收·entity/hardprose/halfwrap** | ~39 | 实体未解码 / 单格超长 / 半包裹 |

### 逐目录可转量（按可直接执行排序）

| 目录 | 含公式表 | **可转** | 拒收主因 | 性质 |
|---|---:|---:|---|---|
| `07-资料提炼` | 61 | **47** | img13 / entity1 | **正典提炼层**，优先级高 |
| `09-AI工作区` | 53 | **43** | img8 / width10 1 / entity1 | 已归档旧产物，价值低 |
| `06-外部资料导入` | 428 | **156** | img155 / width9 55 | **外部导入源**，非自产 |
| `mineru` + `mineru02` | 134 | **~68** | img21+ / width10 | OCR 源 |
| `04-课件/学生讲义` | 4 | **4** | — | ⚠️ 其他会话产物（mtime 09-19 20:24） |
| `04-课件/习题集` | 14 | **0** | width9 4 / prose201 3 / narrow1 2 / hardprose 1 | ✅ 本轮已尽转（剩余全合理拒收） |
| `04-题库` | 18 | **0** | prose189 9 / width9 5 / narrow1 2 / hardprose 1 / entity 1 | ✅ 本轮已尽转（剩余全合理拒收） |
| `北斗40套` / `人教版初中` / `高考化学` / 其他小目录 | ~104 | **~70** | 混杂 | 外部资料 |

---

## 三、拒收是否「误杀」——已逐类目检

| reason | 目检样本 | 判定 |
|---|---|---|
| `width9`（4x, `第一轮原理·4-化学动力学`） | 带 `colspan="7"` 的**评分卡**，格内含 `$\frac{…}$` 长推导 | ✅ **合理拒收**（转 pipe 会破坏评分结构） |
| `prose201`（3x, `第一轮原理·8-氧化还原`） | 2 列答案卡，格内 `$\begin{array}{l}…\end{array}$` 多行公式 | ✅ **合理拒收**（多行公式塞进 pipe 单元格不可读） |
| `narrow1`（2x, `第一轮原理·5-化学平衡`） | **单列**，内容 = 「题干」+「答案与评分」 | ✅ **合理拒收**（本质是问答块，非表格） |
| `img`（`07-资料提炼/…学生版`） | 表内有 `![[…]]` 图片引用 | ✅ **合理拒收**（转 pipe 会丢图或破坏） |

**结论：现有拒收全部合理，无误杀。** 上一轮修掉 `narrow(width≤2→≤1)` 与
`prose(无条件→仅窄表)` 两条误杀后，剩余拒收均属「本就不该转成 pipe table」的形态。

---

## 四、真正的遗留问题（拒收表内的公式仍不渲染）

⚠️ **这是本轮最重要的发现**：被拒收的 ~460 表（img/width9/prose/narrow1）**内部仍含
`$…$` 且仍不渲染** —— 拒收只是避免了「转坏」，**问题本身没解决**。

需要**第二套处置策略**（不是本次工具的范围）：

| 形态 | 规模 | 建议处置 |
|---|---:|---|
| `narrow1` 单列问答块 | ~16 | **去 `<table>` 包裹** → 转成正文段落/引用块（公式即可渲染） |
| `prose/hardprose` 答案卡 | ~101 | 同上，或保留表格但**把公式移到表外** |
| `width>8` 评分卡 | ~87 | 同上（或维持现状，接受不渲染） |
| `img` 图混排 | ~197 | 需 **`<table>` → 图片+文字分离** 的专用处理 |
| `entity` | ~7 | 直接 `--allow-entities` 可解（**唯一可立即做**） |

---

## 五、`\AA`（埃）宏 —— 独立待办

pandoc `texmath` 不识别 `\AA` → Word 里残留源码。与表格无关，属**公式宏兼容专项**。
需先普查规模（`\AA` / 其他非标准宏）。

---

## 六、建议执行顺序（供拍板）

| # | 事项 | 规模 | 风险 | 建议 |
|---|---|---|---:|---|
| **A** | `07-资料提炼` 47 表 | 22 文件 | 低（四口径✅） | ⭐ **先做**（正典提炼层，用户已多次说「07 可动」） |
| **B** | `09-AI工作区` 43 表 | 12 文件 | 低 | 可做（但属**已归档**产物，价值低） |
| **C** | `entity` 批次（全库 ~7 表） | ~5 文件 | 低 | **一并做**（`--allow-entities`） |
| **D** | 拒收表「去表格包裹」专项 | ~460 表 | **中**（改动语义结构） | 需**新方案 + 授权** |
| **E** | `06-外部资料导入`+`mineru`（~224 表可转） | ~180 文件 | **高**（外部源，非自产，改动会失真） | ⚠️ **不建议**（应保持原貌） |
| **F** | `\AA` 宏专项 | ? | 独立问题 | 另立 |

---

## 七、待用户拍板的三件事

1. **A+B+C**（`07-资料提炼` 47 + `09-AI工作区` 43 + entity ~7 ≈ **97 表**）是否执行？
2. **D**（拒收表去包裹，~460 表）是否立项？需先出**单文件试点**再批量。
3. **E**（外部导入源 ~224 表）确认**不做**（保持原貌）？
