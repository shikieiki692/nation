---
title: 公式渲染缺陷工单 2026-09-21
type: 审计
created: 2026-09-21
updated: 2026-09-21
tags: [公式渲染, 工单, 内容线]
---

# 公式渲染缺陷工单 · 2026-09-21

> **这份工单解决什么**：「有 1 份文件公式渲染不出来」是个没法动手的数字；
> 本文把它拆成「**哪些域、多少份、坏在哪、谁先做、哪些不该做**」，
> 且每份都附**闸门给的原因原文** —— 拿起来就能改，不用先自己扫一遍。
>
> **一句话**：共 **1** 份 —— 🟡 P2 阅读/提炼 **1** 份；
> 其中**实测可净化 0 份**，其余 **1 份必须回原书/原式校正**。
>
> **复跑**（改完一批后刷新本表，同一条命令）：
> `python -X utf8 11-模板/scripts/render_health.py --raw-dir .workbuddy/tmp/health_final --work-order <本文件路径>`
>
> **口径**：排除 `_归档/`（死文件）；文档类域（审计报告 / 数据库表 / 索引 / README）不检查
> —— 那里 `$` 常是价格、散文描述、DataviewJS 语法。判据 = `render_gate.py`（产物字面 `$` == 0 且无 texmath 转换失败）。

## 一、汇总（与闸门对账）

| 域 | 受检 | 失败 | 允许 | 处置建议 |
|:--|--:|--:|--:|:--|
| `06-学生侧材料/` | 34 | 0 ✅ | 0 | 🔴 P0 交付路径 |
| `04-课件/习题集/` | 214 | 0 ✅ | 0 | 🔴 P0 交付路径 |
| `04-课件/学生讲义/` | 85 | 0 ✅ | 0 | 🔴 P0 交付路径 |
| `13-教案/` | 59 | 0 ✅ | 0 | 🔴 P0 交付路径 |
| `04-课件/新授课/` | 111 | 0 ✅ | 0 | 🟢 P3 其他课件（实测未见交付产物，需按需定级） |
| `04-课件/备课大纲/` | 68 | 0 ✅ | 0 | 🟢 P3 其他课件（实测未见交付产物，需按需定级） |
| `04-课件/专题课/` | 8 | 0 ✅ | 0 | 🟢 P3 其他课件（实测未见交付产物，需按需定级） |
| `04-课件/复习课/` | 9 | 0 ✅ | 0 | 🟢 P3 其他课件（实测未见交付产物，需按需定级） |
| `04-课件/工具卡/` | 11 | 0 ✅ | 0 | 🟢 P3 其他课件（实测未见交付产物，需按需定级） |
| `04-专题与题型/` | 176 | 0 ✅ | 0 | 🟡 P2 阅读/提炼 |
| `03-知识点/` | 946 | 0 ✅ | 0 | 🟠 P1 KP 页 |
| `07-资料提炼/` | 516 | **1** | 0 | 🟡 P2 阅读/提炼 |
| `05-真题库/` | 41 | 0 ✅ | 0 | 🟢 P3 其他课件（实测未见交付产物，需按需定级） |
| `高考化学/` | 224 | 0 ✅ | 0 | 🟡 P2 阅读/提炼 |
| `02-考纲条目/` | 73 | 0 ✅ | 0 | 🟡 P2 阅读/提炼 |
| **合计** | **2575** | **1** | **0** | |

> **对账**：分组明细合计 **1** == 各域失败数合计 **1** ✅
> （不一致只可能来自原始输出解析失败，见文末「解析告警」。）

## 二、按**报错片段**分组（启发式，只用于排优先级）

| 组 | 份数 | 其中实测可净化 | 备注 |
|:--|--:|--:|:--|
| A栏｜仅字面 `$`（无 texmath 报错；多为算式不配平） | 1 | 0 |  |

> **「实测可净化」的算法**：对每份文件真跑一遍 `md_sanitize.sanitize()`，
> **前后有变化**才算 —— 不是「看起来像某个已知形态」。
> 而**分组名**依据的是 `render_gate` 报出的**报错片段**（截断 90 字符），属**启发式**：
> 同组内可能混有不同成因，**一律以明细里的「原因原文」为准**，不要按组名下结论。

## 三、明细（按「域处置建议 → 份数」排序）

### `07-资料提炼/` —— 1 份（🟡 P2 阅读/提炼）

| # | 文件 | 闸门原因（原文） |
|--:|:--|:--|
| 1 | `07-资料提炼/网课资料/无机化学-新课-周坤-2020-难度适中/笔记/09.气体与溶液_笔记.md` | 产物字面 $ ×17 |

## 四、怎么用这张工单

1. **先看「建议不修」的域** —— 那是原始 OCR 存档，修存档不如重新导入；
   把它们当作「导入质量」的观测值，**不要排进工作队列**。
2. **再从 P0/P1 往下做**，每改完一批就**复跑**（见文开头命令）刷新本表；
   份数下降即进度，新出现的项即回归。
3. ⚠️ **本批没有任何一份能被机械净化**（实测：`sanitize()` 改动量为 0）
   —— **全部得回原书/原式校正**。这不是「工具不够好」：
   这些缺陷的形态是「**公式本身被 OCR 打乱**」（多括号 `[\mathrm{L}}]`、
   缺下划线 `nH2O`、无效宏、串行 `\cdot L^{-1}H_3BO_3`），写不出可靠规则 ——
   强写规则就会重演「按形状猜」的错误（本工单的「可净化」列**已被这个错误坑过一次**）。
4. ⛔ **不许**为了「清零」把 `$` 删掉或把公式改成纯文本：那会把「渲染失败」
   变成「静默的内容丢失」，后者更坏。改不动就标存疑，不要假装修好。

## 五、本工单的边界（如实说明）

- 分组是**启发式**：依据是 texmath 报错片段（截断 90 字符），**不是**对源文件的二次解析；
  原因原文列保留在明细里，人工可复核/改判。
- **重型域默认不跑**：`04-题库/`（6020 份 ≈ 1 小时）未纳入本次体检；
  抽样 99 份（两轮随机）**0 失败**，且它经生成器进成书层、成书层实测全绿 ⇒
  需要时用 `--only 04-题库` 单独跑。
- 「实测可净化」= **真跑 `md_sanitize.sanitize()`、前后有变化**（实测而非形状推断）。
  即便可净化，也只保证「不再报错」，**不保证语义一定对** —— 落盘后仍须复跑闸门确认。

---

## 修复进展（2026-09-21 第二批 · 格式线）

> 本节原在文件中，工单重建时被覆盖，现补回（内容与当日提交一致）。
> 本批总量：全库 **173 → 122**；源层 106 按口径不修；**非源层 67 → 16**。

### 两处**根因**（改一处、多份转绿）

**根因 A · 生成器截断切断公式（新缺陷类 ⑩）**
`gen_weak_drill.ans_summary()` 原为 `seg[:limit]`：按固定长度硬切，公式被切一半 ⇒ 留未闭合 `$`
⇒ Word 出字面 `$`。新增 `safe_truncate()`（按**定界符配对**找安全边界）。
⚠️ **不能用「`$` 个数奇偶」当判据** —— `$$a` 恰好 2 个 `$` 却并未闭合（单测抓到）。
受害件：P0 交付物 `补弱卷-化学平衡-2026-09-02.md` 3 处 + 备课大纲 ×2。

**根因 B · `\ce{}` 转换器缺 mhchem 语法（新缺陷类 ⑪）**
`_preprocess_ce_in_math()` 不认三类记号，把源码原样带进 LaTeX ⇒ 整式渲染失败：
`^`（气体↑，11 处）/ `v`（沉淀↓，2 处）/ `\bond{->}`（配位键，4 份 5 处）。
**7 份 KP 页未改 md 一个字即转绿**。
边界：`^`/`v` 判据必须「前后皆空白或边界」（否则会毁掉 `Mn^{2+}` 等**全部**电荷上标）；
`\bond` 必须放在箭头替换**之前**。另补：箭头标注里 `$…$` 剥壳、标注已含 `\text{}` 时不再包一层。

### `md_sanitize.py` 新增 3 条规则（预防复发）

1. `\cdot` 紧跟字母 → 补空格。🔴 **必须排除 `\cdots`** —— 它是合法命令且全库 **296 处**。
2. `\hspace{2\mathrm{cm}}` → `\hspace{2cm}`（长度参数里不能套 `\mathrm`）。
3. `_RE_MATH_SPAN` 补 `\( … \)`：原只覆盖 `$…$` / `$$…$$`，漏掉 OCR 表格里大量使用的单反斜杠数学。
单测入库：`.workbuddy/scripts/test_md_sanitize.py`（11 项）/ `test_ce_converter.py`（13 项），
含**反向用例**（`\cdots` 不得被改、`Mn^{2+}` 上标不得被碰）与幂等用例。

---

## 修复进展（2026-09-21 第三批 · 格式线）

> 数字为重扫所得（同一闸门口径）。本批总失败 **122 → 107**；
> **非源层从 16 降到 1**（仅剩 1 份「标存疑」项，见下）；源层 106 按口径不动。

### 🔴 一处**管线根治**（改一处、多份转绿）

**关掉 pandoc 默认开启的 `superscript` / `subscript`**（`PANDOC_EXTENSIONS` 末尾加 `-superscript-subscript`）。

机制（实测）：pandoc 的 markdown **默认**把正文里的 `^…^` / `~…~` 配成一对，
而本库正文大量书写「漏了 `$` 的 LaTeX 碎片」（**数学域外 `^` 1111 处 / `~` 8100 处**），于是：

1. **`^` 跨 `$` 配对** ⇒ `$` 配对整体位移 ⇒ 数学域不再被识别 ⇒ Word 出字面 `$`。
   受害件 `03-知识点/分析化学/氧化还原滴定.md` —— **零改 md 一个字即转绿**。
2. **`~` 被吞**（静默内容损坏，闸门测不到）：`条件：70~80℃，0.5~1 M H+` 原先渲成
   `条件：7080℃，0.51 M H+`（区间号消失、"80℃，0.5" 变下标）。**对教学材料是误导性错误。**

安全性（逐条实测，非推断）：全库有意的 `^词^` 上标 **0** 处、`~下标~` **0** 处（全扫描）；
`-subscript` 影响**单调**（只可能把被吞文本还原，不会新增 `$` 失效）；
回归：`^` 风险集（= 全部 **214** 份含域外 `^` 的文件）**全量**跑闸门 **5 → 5 无新增**；
`~` 风险集抽样 40 份零回归。

顺带修一处**工具缺陷**：`render_gate.py` 原先**本地硬编码**方言串，与管线漂移后会让
「改前/改后同一结果」（本批实测白跑一轮）。改为用 **AST 从管线源码读取**（不 import、无副作用）。

### 逐份修复 **15 份**（每处先实测定位、再选修法）

| 类别 | 份数 | 代表处置 |
|:--|--:|:--|
| 花括号错位/缺失（`{\left\lbrack \mathrm{MnF}}_{6}…`、`\mathrm{` 未闭合） | 3 | 按**同表兄弟行 / 同文件既有写法**还原 |
| texmath 不支持的构造（array 列格式 `{c:ccccc}` 的 `:`、`\boxed` 不带括号、`:` 落在 `\mathrm{}` 内） | 3 | 逐条跑 pandoc **实测选型** |
| 转换器缺口（`\ce{}` **内嵌** `\ce{}`；单趟不递归） | 1 | 去内层 `\ce{}` |
| 跨域构造（`\left[…` 与 `\right.` 被拆在两个 `$` 里） | 1 | 把 `[` 移出数学域作普通字符 |
| 定界符误用（`\$` 当数学定界符 + `\_` 误转义下划线） | 1 | 改真 `$`、`\_`→`_` |
| OCR 垃圾字符（`\varphi^{\wedge}\circ`、`§`、`\$`、`\S`、英文直角引号当中文引号） | 3 | 按同文件同数值/同句式还原 |
| 结构缺失（array **从未闭合**：行内 `end{array}` 出现 0 次） | 1 | 补 `\end{array}`（**前缀二分**定位） |
| 散文里**描述 `$$` 字符**被闸门当公式 | 1 | 加反引号包成代码跨度（闸门本就排除代码跨度） |
| 表格单元格内 `\left( {\left( …` 嵌套 | 1 | 实测选型为同文件 L1039 的写法 |

### 仍未清零：1 份（**标存疑，非「修不动」**）

- `07-资料提炼/网课资料/…/笔记/09.气体与溶液_笔记.md`
  该页 `<details><summary>text_image</summary>` 里是**图片文字的 OCR 导出**，整块已错乱
  （「5555 5 3.5…」「凸起算」「Cu(80)L/C」等无意义串），**全库无干净副本**。
  已按工单口径在页内加 ⚠️ 存疑注，**未删除原文、未改动任何字符**。
  ⚠️ 注意：闸门对该文件报的「产物字面 `$`」此处属**误伤** —— 那些 `$` 是**扫描噪声字符**，
  不是公式定界符。**不做「为清零而改字符」的动作。**

### 另一处「内容丢失」登记（已可渲染，但内容待补）

- `高考化学/…/2024年高考化学试卷（湖南）（解析卷）.md`：某问「以 (　) 和 Cl₂CHOCH₃ 为原料」里
  **第一个原料式是空括号**，内容在扫描/转录中丢失（源里只剩 `\left(\right)`）。
  已修好分隔符与下标使其可渲染，页内加 ⚠️ 存疑注，**未臆造缺失的原料**，请对照原卷补录。

### 方法论（本批新增）

1. **先找根因，再修个案**：本批 16 份里，**1 份靠改管线一处修好**（那 md 一个字没动），
   且同时消掉一类**静默内容损坏**（`~` 区间被吞）—— 这是逐份改 md 永远修不到的地方。
2. **`:` 落在 `\mathrm{}` 内**是一类此前未记录的 texmath 限制；`\boxed` 必须带花括号同理。
   两者都靠「逐候选跑 pandoc」定，不靠直觉。
3. **定位大结构用「前缀二分」**：36 行的 array 逐行单独测都通过，全量失败 ⇒ 差异必在**闭合**上
   （果然该行 `end{array}` 出现 **0** 次）。
4. **先自校验再改数**：浙江式 `0.022.50` → `0.02250`，依据是该式自身
   `0.1000×0.02250÷0.025 = 0.09` 精确成立；另一处「7 个 9 vs 9 个 9」经逐句回核**证伪**为矛盾
   （实为不同工序）⇒ **不改数字**。

---

## 修复进展（2026-09-21 第四批 · 格式线）

### 又一处**根因**：`_parse_species()` 早退分支 —— 带电物种丢下标

这是本会话第三处「改一处、多份改善」的管线根因，也是**唯一一处改善量最多、却完全
不出现在闸门失败清单里**的缺陷（因为它只让公式**渲染得不对**，不让它**渲染不出来**）。

**病灶**（`build-all-handout-docx.py::_parse_species`）：

```python
if '^' in species or _has_explicit_subscript(species):
    s = _re.sub(r'\^' + _script_arg, r'^{\1}', species)
    s = _re.sub(r'_' + _script_arg, r'_{\1}', s)
    return s          # ← 规范化完 `^`/`_` 就返回，跳过了「裸数字补下标」
```

于是**凡是带电荷或带显式下标的物种，其余裸数字一律丢下标**：

| 源（md 里写的是 mhchem） | 转出 LaTeX | 应然 |
|:--|:--|:--|
| `\ce{MnO4^-}` | `MnO4^{-}` | `MnO_{4}^{-}` |
| `\ce{CO3^{2-}}` | `CO3^{2-}` | `CO_{3}^{2-}` |
| `\ce{[Cr(H2O)6]^{3+}}` | `[Cr(H2O)6]^{3+}` | `[Cr(H_{2}O)_{6}]^{3+}` |
| `\ce{Li_xC6}` | `Li_{x}C6` | `Li_{x}C_{6}` |

**库内自相矛盾**是判定它是缺陷、而非风格选择的铁证：同一个 `MnO4`，
`\ce{MnO4}`（无电荷）转出 `MnO_{4}`（有下标），`\ce{MnO4^-}`（带电荷）却转出 `MnO4`（无下标）；
而库内手写的 `\mathrm{MnO_4^{-}}` 一直是有下标的。

- **规模**：**455 处 / 101 份文件**（`03-知识点/有机化学` 43 份最多）。
- **修法**：早退分支末尾改 `_mask_cmds(s)`（补裸数字下标）。
- 连带修一处同族问题：**裸电荷正负号落在上标之外**（`Cr2O7^2-` → `Cr2O7^{2}-`，44 处）。
  🔴 该修法的负向先行断言不可省：全库另有 `R^1-X` / `R^2-CONHR^1` / `Ar^1-N=N-Ar^2`
  等**上标标号 + 连接号** 26 处，少了 `(?![A-Za-z0-9])`，`^1-` 会被并成 `^{1-}`、
  把连接号吞进上标。**这已是本会话第四次「按形状猜」被同类问题拦下**，故列为反向用例。
- 新增 `_mask_cmds()`：先掩码 LaTeX 命令再套下标规则，防 `\cdot5H2O` 的系数 5 被
  下标到命令上（`\cdot_{5}`）。实测全库「命令紧接数字」形态 = **0 处**，故纯防复发。

**自证中抓到并修掉一个我自己引入的回归**：`_mask_cmds` 初版在「无命令」时写成
`return s`，把常规分支的裸数字下标也一并跳过 ⇒ `PCl3` / `2H2O` / `H2O` 全丢下标。
**靠「实测校准」（先跑一遍真实输出、再写期望）发现**，已由单测的常规范式用例锁定。

**验证**：
- `test_ce_converter.py` 扩到 **28 项**（含 3 条 `R^n-` 反向用例、1 条命令尾数字反向用例、
  3 条常规范式防回归用例）→ 28/28 PASS。
- 受影响 **105 份**文件跑渲染闸门 → **PASS 105 / 失败 0**（无渲染回归）。
- 前后对照（HEAD 版管线 vs 本版）：`MnO4^{-}` sSub=1 → `MnO_{4}^{-}` sSub=2（OMML 里
  是 `sSubSup` 组合）；`[Cr(H2O)6]^{3+}` sSub 0 → 2；`R^1-CHO`、`PCl3` **逐字不变**。

### 📌 待办（不在本会话范围，登记备查）

受影响 md 中仅 **8 份**有 docx 产物（其中 4 份是 `06-学生侧材料/讲义/6-有机化学/*.docx`，
git 已跟踪）。这些 docx 经**排版后处理**（三线表 / 标题纯黑 / 孤立标点归并 / 选项间距），
**直接重出会丢掉后处理**，故应由讲义线按既定流程（重出 + 排版）处理，本会话不擅自重出。

---

## 修复进展（2026-09-21 第五批 · 格式线）

> 承接第四批。本批又修 `_parse_species` 的**两处既有缺陷**（均经 HEAD 版管线逐条对照
> 确认 `old == new`，不是第四批引入的）。

### ① 多位数下标只吃第一位

规则 `([A-Za-z\)}])(\d)` 用了**单字符** `\d`，`H12` 被切成 `H_{1}2`。
全库 **36 处 / 15 份**：`\ce{C6H12O}` → `C_{6}H_{1}2O`、`C10H8`、`P4O10`、
`C21H53NO10`、`C12H22O11`；同族还有 `^{288}115`（同位素记号）→ `^{288}_{1}15`。
修法：`\d` → `\d+`。改后 `C6H12O6` → `C_{6}H_{12}O_{6}`、`^{288}115` → `^{288}_{115}`。

### ② 无箭头分支里，单独的 `+` / `-` 被当成带电荷物种

无箭头的 `\ce{}` 走 `s.split()`，`+` 也被当物种送进 `_parse_species`；
其 `charge_match = ([+\-])$` 会命中，base 为空串 ⇒ 产出 **`^{+}`**。
全库：无箭头 `\ce{}` 里的 ` + ` **113 处 / 34 份**（`\ce{SiF4 + 2F-}` → `SiF_{4} ^{+} 2F^{-}`）；
单独 `-` **15 处 / 6 份**（`\ce{NH4+ - NH3}` 缓冲对、`\ce{R3P^+ - CHR2^-}` 键号 → `^{-}`）。
修法：`parsed = [p if p in ('+', '-') else _parse_species(p) for p in parts if p]`。
箭头分支本就用 `\s\+\s` 做分隔符，不受影响。
**判据确认**：单独 `-` 在库里**逐条看过上下文**，全部是键号 / 分隔符（不是电荷），
故与 `+` 同等对待 —— 这是本会话第五次「先看全部实例再定规则」。

### 明确**排除**的一处（不改）

`[A-Za-z]\d[+\-]$` 形态 131 处，抽样全是 `MnO4-` / `ClO4-` / `HSO4-` 这类**阴离子**
（数字是真下标、电荷只是 `-`），现有处理 `MnO_{4}^{-}` 本就正确 ⇒ **无第三处缺陷**。

### 验证

- `test_ce_converter.py` 扩到 **37 项**（新增 ⑤多位数下标 ×5、⑥分隔符 ×4）→ 37/37 PASS。
- 受影响 **321 份**文件跑渲染闸门 → **PASS 321 / 失败 0**。
- 第四批修好的 14 项（`MnO4^-`、`R^1-X`、`\cdot5H2O`、常规式…）本轮**逐条复测仍全对**。

### 本会话格式线最终成绩（非源层 15 域）

| 口径 | 起点 | 现在 |
|:--|--:|--:|
| 非源层失败 | 67（第一批前） | **1** |
| 非源层受检 | — | 2575 |

仅剩的 1 份是 `07-资料提炼/网课资料/…/09.气体与溶液_笔记.md` 里那一段
**图片文字 OCR 乱码块**（`<summary>text_image</summary>`，全库无干净副本），
已加显式存疑标记、**不臆造内容**。
