# 待处置事项评估报告（2026-09-20）

> 缘起：用户指令「仔细评估之后处理吧」。
> 本报告对 4 项待处置事项逐项评估，给出**取证 → 判断 → 建议**。
> 评估原则：影响面优先、确定性优先、不与并行会话冲突、不擅动红线区。

---

## §〇 评估总表

| # | 事项 | 评估结论 | 处置 |
|---|---|---|---|
| 1 | 红线区 3 份损坏 title | **不宜现在改**（口径未定 + 未来会重做） | 报告，待拍板 |
| 2 | 真题讲评 3 份空壳 | **确认是生成失败产物**，非我职责 | 已记录，待拍板 |
| 3 | 重跑习题书聚合 | **现在不宜重跑**（数据源含并行未提交态） | 待并行收敛后再跑 |
| 4 | push | **已完成**（并行会话已带走） | 无需操作 |
| 5 | vault 根 3 个垃圾文件（新发现） | 脚本 bug 产物，非本会话 | 报告，待拍板 |

---

## §一 红线区 3 份损坏 title —— 结论：不宜现在改

**文件**（`04-题库/元文件/教材习题/无机化学例题与习题/`）：

| 文件 | title / H1（损坏） | 正文原文（完好） |
|---|---|---|
| `Ch01.../习题1.37-137反应式2mathrmH.md` | `习题1.37 137反应式2mathrmH` | `1.37 反应式` + `$$2\mathrm{H}_2+\mathrm{O}_2=2\mathrm{H}_2\mathrm{O}$$` |
| `Ch02.../习题2.28-228由得mathrmH2m.md` | `习题2.28 228由得mathrmH2m` | `2.28 由 $\Delta_fH_m^\ominus(\mathrm{H}_2\mathrm{O},l)$ 得` |
| `Ch04.../习题4.15-415对于反应mathrmS.md` | `习题4.15 415对于反应mathrmS` | `4.15 对于反应` + `$$\mathrm{SO}_3=\mathrm{SO}_2+\frac12\mathrm{O}_2$$` |

**损坏机制**：旧脚本「题号 + 正文首句」拼接成标题时，**数学域被去符号压平**（`\mathrm{H}` → `mathrmH`），且**中英文无空格粘连**（`137反应式`）。

**范围**：该层 198 份中**仅此 3 份、6 行**损坏，其余 195 份完好。

### ⛔ 不宜现在改的三个理由

1. **这是未迁移的台账副本，无消费端**
   - `元文件/教材习题/`（195 份）在口径速查中标注为「OCR 台账副本，C 档，**副本层，无消费端**」，风险 **🔴 低**
   - 关键：正式层 `教材习题/无机化学例题与习题/` 的 **Ch01-Ch04 `习题/` 目录是空的**（Ch05 起才有内容）
   - ⇒ 这 3 份**从未正式入库**，属**待迁移状态**，不是「已交付内容损坏」

2. **命名口径未定，改了等于替未来下注**
   - 元文件层用「去符号压平」拼接式：`习题1.10-110在100g水中含45g`
   - 正式层用语义化式：`例5.1-用4个量子数描述n3所有电子`
   - **两套风格并存**。现在按元文件风格修，将来迁移若按正式层规范重做 → **白做**

3. **红线区 + 非「恢复原文」**
   - `04-题库/` 属禁触碰区
   - 该改动不是「还原被误改的原文」，而是**重新拟题名**（含主观判断），超出「缺陷修复」范畴

### ✅ 建议

**挂起**，随「Ch01-Ch04 正式迁移」一并处理。届时：
- 用**正式层命名规范**统一命名（而非修残片）
- 迁移时按正文原文重拟语义化题名

若您认为仍应现在修，**最小改动方案**为「只还原压平残片，不重新拟名」：

| 现 title | 最小修复 |
|---|---|
| `习题1.37 137反应式2mathrmH` | `习题1.37 反应式2H2+O2=2H2O` |
| `习题2.28 228由得mathrmH2m` | `习题2.28 由ΔfHm(H2O,l)得` |
| `习题4.15 415对于反应mathrmS` | `习题4.15 对于反应SO3=SO2+O2` |

---

## §二 真题讲评 3 份空壳 —— 结论：确认生成失败产物

**目录**：`00-首页/题组Word/真题讲评/`（38 份，**全部未入库** `??`，mtime 09-19 22:10-22:12）

**文本长度分布**（38 份）：

| 长度 | 份数 | 性质 |
|---|---|---|
| 67-68 字 | **3** | ⛔ 空壳 |
| 645 字以上 | 35 | ✅ 正常（含完整题目+解答） |

**3 份空壳内容**（仅标题 + 跳转占位）：
```
真题-热力学-RNA折叠Gibbs自由能-001.docx
  → RNA折叠-热力学参数的线性拟合与Gibbs自由能分析
    原题完整题干与解答请见：题-038-3-螺吡喃机械力变色
真题-结构-SrSb晶胞分数坐标-001.docx
真题-结构-WSe2键长计算-001.docx
  → 原题完整题干与解答请见：题-038-5-1-FeCr合金晶胞组成
```

**取证**：

| 判据 | 结果 |
|---|---|
| 被引用目标存在？ | ✅ 是（`题-038-5-1-FeCr合金晶胞组成`、`题-038-3-螺吡喃机械力变色` 均在库） |
| 引用恰当？ | ❌ **否** —— 晶胞题引「FeCr 合金晶胞」（勉强相关）；**RNA 热力学引「螺吡喃机械力变色」完全无关** |
| 声称的真题存在？ | ❌ **否** —— `RNA`/`SrSb`/`WSe2` 在 `04-题库/真题/` 各 **0 份** |

### 判断

这是并行会话「建真题讲评」时的**3 份生成失败产物**：
1. 讲评生成器**未取到原文**（三题在库中不存在）
2. 退化为「见 xxx」**跳转占位**
3. **兜底引用错配**到了无关题

### ✅ 建议

**不动**（属并行会话未跟踪产物，铁律）。交由该会话：
- 补齐三题原文后重出，或
- 移除这 3 份占位

---

## §三 重跑习题书聚合 —— 结论：现在不宜重跑

**dry-run 取证**：`build_module_book.py`（默认 dry-run，需 `--write` 才落盘）

| 项 | 数据 |
|---|---|
| 会重写 | **41 份教师版 md**（学生版需另跑 `--edition student`） |
| 真实内容变化 | **11 份、76 行**（标题从「会被切坏」→ 正确） |
| 变化分布 | 第一篇 18 行 / 第二篇 13 行 / 第三篇 29 行 / 第四篇 16 行 |

**76 行的性质**（以 `第二篇/4-配位化学.md` 为例）：

| 现役 md（好） | 修复前 old() 会切坏 | 修复后 new() |
|---|---|---|
| `(1) $[\mathrm{Co}(\mathrm{en})_3]^{3+}$ — **手性** ✅` | `1) $[\mathrm{Co}(\mathrm{en})-3]^{3+}$-手性` | `1) $[\mathrm{Co}(\mathrm{en})_3]^{3+}$-手性` |
| `1. $\sigma$ SALC 与能级图` | `sigma$ SALC 与能级图` | `$\sigma$ SALC 与能级图` |
| `1. d 轨道按 $D_{3h}$ 分组` | `d 轨道按 $D-{3h}$ 分组` | `d 轨道按 $D_{3h}$ 分组` |

⇒ 证实：**修复前这些 `###` 行在产物里会被切坏，修复后不会**。

### ⛔ 现在不宜重跑的理由

**数据源不干净** —— 重跑从**红线区 `04-题库/`** 读，而该区当前有 **62 份 M（未提交改动）**：

| 事实 | 数据 |
|---|---|
| 62 项 M 的 mtime | **09-19 16:46~16:51**（昨天下午） |
| 内容 | 主要为 `教材习题/高中化学竞赛教程第二分册/`（并行会话「第二分册逐讲落库」进行中） |
| 风险 | 重跑会**把并行会话未提交的中间状态固化进产物** → 可能产出带瑕疵的习题书 |

而**修复收益可延后** —— 76 行标题隐患**不重跑也不影响现役产物**（已验证 335 份 docx 内零命中）。

### ✅ 建议

**等并行会话（第二分册落库）收敛并提交后再重跑**，一次性拿全收益、风险最低。
符合「排队原则：卷已就绪未交付 > 新建内容 > 体系重构」与「不与并行会话抢数据源」。

重跑命令（待时机成熟）：
```bash
python 11-模板/scripts/build_module_book.py --write              # 教师版
python 11-模板/scripts/build_module_book.py --write --edition student  # 学生版
# 再重出 docx
python 11-模板/scripts/build-all-handout-docx.py --batch-root <目录>
```

---

## §四 push —— 结论：已完成，无需操作

| 项 | 值 |
|---|---|
| 本地 HEAD | `8a7a53608` |
| `git ls-remote origin master`（真值） | `8a7a53608`（**一致**） |
| 领先提交数 | **0** |
| 我的 `448221a45` | ✅ **在祖先链上**（已随并行提交推送） |

**⇒ 远端 = 本地，我的提交已落地。** 并行会话 `shikieiki692` 于 07:36 提交讲义修复并推送，带走了我的提交。

---

## §五 【新发现】vault 根目录 3 个垃圾文件

**文件**（vault 根，0 字节，mtime 09-20 00:08）：

```
作用域：无主区（
前置：、、
日期：2026-09-19
```

**取证**：

| 判据 | 结果 |
|---|---|
| 内容 | **0 字节**（完全空） |
| 是否被引用 | ❌ 无（正则命中的是 `09-审计报告/2026-09-19-*.md` **正文里的字段行**，非引用） |
| 命名 | 含**全角冒号**，是 markdown 字段行格式 |

**成因推断**：某脚本读 `09-审计报告/2026-09-19-*.md` 时，**把含全角冒号的字段行误当路径参数**，`open()` 出了 3 个空文件。

**风险**：**极低**（0 字节、无引用、仅根目录污染）。

### ✅ 建议

可删，但**非本会话产物**，**不擅自删**。若确认无脚本在用，可用：
```python
os.remove("作用域：无主区（")   # 走 safe-delete
os.remove("前置：、、")
os.remove("日期：2026-09-19")
```

同时**建议排查**产生它们的脚本（`open(..., 'w')` 前未做路径校验）。

---

## §六 本轮未执行任何写操作

除本报告外，**本轮未修改任何文件**。所有事项均为**评估 + 建议**，原因：

1. **红线区**（`04-题库/`）禁触碰
2. **并行会话产物**（真题讲评、二分册 M）不擅动
3. **重跑聚合**会固化他人中间状态
4. 评估后**核心结论恰是「多数项不宜现在动」**

> 本报告仅记录评估结论，供拍板。
