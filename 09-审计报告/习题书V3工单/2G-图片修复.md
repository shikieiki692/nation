# 工单 2G：习题书图片问题修复

只改 `04-题库/` 源文件与 `媒体仓库/` 引用关系，不改 `04-课件/` 产物。

## 1. 缺图补图

- 化学原理篇旧书 4.24："下图是一种正在投入生产的大型蓄电系统"但无图 → 在 `04-题库/` 找到该源文件（Grep 搜"蓄电系统"），到 `06-外部资料导入/` 找原书对应图（哈希文件），以 `![[64位哈希.jpg]]` 形式插入题目区；找不到就标注 `📌 图片待补：大型蓄电系统装置图`。
- 化学原理篇旧书 2.4：NaCl-H₂O 相图题错配了通用的 `water-phase-diagram.png` → 找源文件（Grep 搜"NaCl" + 相图），换成正确的 NaCl-H₂O 相图哈希图；找不到正确的就删除错配图并标注待补。

## 2. "📌 图片待补"占位清理

Grep `04-题库/` 中所有 "图片待补"，逐个到 `06-外部资料导入/`（Clayden OCR、真题 OCR 等）找回对应图片：
- 图片必须是 `媒体仓库/` 中实际存在的 64 位哈希文件名（用 Glob 验证 `媒体仓库/<hash>.*`）；
- 特别注意旧书有机篇 8-周环反应与自由基.md 第 702 行附近的待补图，其 hash 只有 36 位（`f01995e0c82b65bba799f6d4f7b92dc2268.jpg`），是被截断的坏 hash，需回源 OCR 重新定位正确图片；
- 实在找不到的，保留待补标注并在报告中列出。

## 3. 别名图改哈希原名（违反配图规范， Obsidian 断链风险）

Grep `04-题库/` 中的以下非哈希图片引用，逐一替换为 `媒体仓库/` 中同内容图片的 64 位哈希名（先在媒体仓库确认存在同名内容文件——可用文件大小/建立时间辅助比对，或直接把别名文件复制为哈希名后以哈希名引用）：

- `fe-pourbaix-diagram.jpg`、`water-phase-diagram.png`（化学原理）
- `13-8a-close-packed-…voids.jpg`、`octahedral_splitting.png`、`perovskite-ABX3-30th-final.jpg`、`八面体场d轨道分裂示意图.关系图.png`（结构化学）
- `base-catalysed-aldol.svg`、`carbene.png`、`diels-alder-butadiene-ethylene.svg`、`sn1-coordinate.png`、`sn2-reaction.svg`（有机化学）

特例：`04-题库/经典例题/有机化学/题-007-SN2vsE2竞争判断.md` 里的 `sn2-reaction.svg` 是错配装饰图（题目考 t-BuOK+2-溴丙烷的 E2），直接删除该图引用，不替换。

## 输出

逐项报告：处理结果 + 涉及的源文件 + 新引用的哈希名。
