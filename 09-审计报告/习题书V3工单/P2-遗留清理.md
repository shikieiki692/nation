# 工单 P2：2A 修复过程中发现的遗留内容问题

只改 `04-题库/` 源文件。逐项处理，拿不准的记入报告不要硬改。

## 结构化学基础系列（`04-题库/教材习题/结构化学基础/`）

1. **题-009-…-习题1.9.md**：答案区内有两个内容完全相同的 `## 解` 块（约 44–103 行整段重复）→ 删除其中一个。
2. **题-106-…-习题4.24.md**：答案中段 `\pmb{\sigma} _ {c c c c …}`（约 200 个重复 c）OCR 严重损坏 → 按上下文（相似变换/矩阵表示）修复为合理公式；无法确定原式的，把乱码段压缩为一处并加 `⚠️ OCR 损坏待核` 标注。
3. **题-176-…-习题6.26.md**：题干 (2)(3) 两行重复出现且磁矩数值与化合物对应关系被 OCR 打乱 → 按化学常识重排为一一对应列表（K₄[Mn(NCS)₆] 6.06 βe / K₄[Mn(CN)₆] 1.8 βe / [Cr(NH₃)₆]Cl₃ 3.9 βe 之类，请先按磁矩=√(n(n+2)) 自洽核算再改）。
4. **题-239-…-习题8.22.md**：`## 解` 起 (1)~(4) 完整解答 + 评注出现了两遍 → 删除重复的一份。
5. **题-155-…-习题6.5.md**：答案表格内 `$\text{LS(\( \Delta_o$ >P)} \)` 混合定界符 → 改为合法 math。
6. **题-234-…-习题8.17.md**：答案中部孤立 `(2)` 按上下文应为 `(4)` → 修正。
7. **题-247-…-习题9.5.md**：答案 `(4)…面间距为` 后紧跟孤立 `(5)` → 核查上下文修正编号。

## 其他

8. **容量分析串答案核查**（`04-题库/` 内 Grep "容量分析-习" 定位）：习2 / 习20 / 习22 的"提炼版"答案与题干数据不匹配（例：习2 题干 0.004000 mol/L KMnO₄/25.00 mL，答案用 0.02000/14.60 mL）→ 逐题核算，确实串答案的按题干数据重写答案，fidelity 改 `自编`。
9. **ABOC 题-078**（`04-题库/教材习题/ABOC/题-078-ABOC-Ch8-8.6-金属催化机理.md`）：题干图是 Grubbs 烯烃关环复分解（RCM），但参考答案写的是 Pd 偶联循环 → 按题干图重写答案（RCM 机理），fidelity 改 `自编`。
10. **ABOC 题-061**（`题-061-ABOC-Ch9-T3-杂环合成策略综合.md`）：查实为原创题且目标分子缺失、与原书 T3 完全不符 → 标 `status: deprecated`，正文顶部加一行弃用原因。

## 输出

逐项报告处理结果。
