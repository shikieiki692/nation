# 知识点系统审计报告 · 2026-09-03

> 审计对象：`03-知识点/`（920 个 `type: 知识点` 笔记 + 12 个 Excalidraw 绘图 + 若干系统/索引页）
> 审计方法：自写脚本 `audit_kp_system.py`（全库 basename + title/aliases/syllabus_code 别名索引，复刻 Obsidian/validate_kb 三级链接解析）＋ `validate_kb.py --full` / `--dir` 交叉验证
> 结论：**本轮仅审计，未修改任何文件。**

---

## 〇、最重要的口径说明（避免误读）

初次扫描曾报"正文断链 678 条"，经与 `validate_kb` 真实解析器逐条对账后，**绝大多数系误报**：

1. **第一层误报（缺别名解析）**：Obsidian 支持用 `title`/`aliases`/`syllabus_code` 链接（如 `[[氧化还原]]` 可解析到别名含"氧化还原"的笔记）。初版解析器只认 basename，虚报 ~621 条。
2. **第二层误报（索引范围偏窄）**：`06-外部资料导入/` 在 validate 中是合法链接目标，初版未纳入，漏掉 Arrow Pushing 总索引等 ~34 条。
3. **真值（用 validate 真实 `find_wikilink_target` 判定）**：正文真断链仅 **5 条**。

**正文链接健康度实际很好**，无需为"678"恐慌。下文所有数字均为别名感知 + 全库范围的最终真值。

---

## 一、结构层（frontmatter）

| 检查项 | 结果 |
|---|---|
| type 分布 | 知识点 920 / 系统 7 / guide 3 / 知识点索引 3 / 索引 2 / teaching_guide 1 |
| 必填字段缺失（title/type/subject/status/updated） | **0**（排除 12 个 Excalidraw 绘图后） |
| subject 值域 | 9 个值全部合法：有机化学 326 / 无机和结构化学 269 / 化学原理 174 / 决赛要求 58 / 分析化学 38 / 高中化学基础 32 / 初中化学基础 12 / 物理化学 7 / 综合 4 |
| status 值域 | 已填充 919 / 重定向 1（无 骨架/初稿/stub/deprecated 等） |
| 日期格式（updated/created/completed） | **0** 异常 |

**结构层整体健康**：必填字段、枚举、日期均零违规。

---

## 二、链接层

### 2.1 正文 wikilink 真断链：5 条（已与 validate 对账确认）

| 文件 | 断链目标 | 说明 |
|---|---|---|
| 无机和结构化学/单质制备方法.md | `[[还原剂]]` | 无对应页 |
| 有机化学/De Mayo反应.md | `[[烯酮]]` | 无对应页 |
| 有机化学/三元环开环.md | `[[碎片化]]` | 无对应页 |
| 有机化学/负氢转移.md | `[[Friedel-Crafts反应\]]` | **畸形 wikilink**：原文含字面反斜杠 `\]]`，应改为 `[[Friedel-Crafts反应]]` |
| 有机化学/非对映选择性.md | `[[04-题库]]` | 指向文件夹（非笔记），按需改显式路径或移除 |

### 2.2 frontmatter 链接字段悬空：13 条

集中在 `source_notes` / `prerequisite` 字段，指向不存在的**提炼/专题/课件**笔记（validate 仅校验 knowledge_points/depends_on/cross_references/related/superseded_by，**不查这两字段，故对网关完全隐形**）：

- 分析化学（8）：基准物质/容量分析计算/指示剂/标准溶液/滴定曲线/酸碱滴定 → `提炼-分析化学第六版-第X章…`；萃取 → `[[分配定律]]`
- 化学原理（4）：反应商 → `提炼-普化原理-第8章-化学平衡`；电化学 → `提炼-无机化学第五版-第10章-电化学基础`；缓冲溶液 → `质心…酸碱理论-L6…`；解离常数 → `04-课件/新授课/2026-06-03-酸碱平衡-基础班`
- 有机化学（1）：安息香缩合 → `提炼-学而思有机化学基础-批次F…`

> 性质：多为"溯源链接指向已重命名/移动的提炼笔记"，属来源可追溯性债务，非内容断裂。

### 2.3 媒体嵌入：0 缺失（全库媒体索引 50218 项，无引用落空）

---

## 三、覆盖层（深度缺口 · 重点发现）

题库 `knowledge_points` 与考纲条目 `related`/`cross_references` 引用了大量 **`03-知识点` 中不存在对应页** 的 KP 概念。别名感知去噪后仍有 **约 1037 个独立目标**（含部分噪音，需专项复核区分"应建页"vs"knowledge_points 字段填了非 KP 文本"）。高频明确应为 KP 的概念：

```
杂化轨道 91   氧化还原 59    分压定律 58   电极电势 53   有机反应机理 33
依数性 32     胶体 29       滴定 29       光度法 28     浓度 27
人名反应 24   超分子化学 21 分子构型 20   焓变 20       动力学 19
ph计算 18     芳香亲电取代 18 磁性 18      歧化反应 18   还原反应 16
胺 16         pKa 16       路易斯结构 16  点群 16       晶体点阵结构 16
亲电取代 15   聚合反应 15   分子间作用力 15 晶体衍射 15   滴定分析 14  对称性 14 …
```

**含义**：知识点层相对题库/考纲的"应覆盖概念"存在明显深度缺口——要么应补建这些 KP 页，要么 `knowledge_points` 字段混入了自由文本/非 KP token（如 `02-14`、`nmr`、`ir` 等题号/缩写）。建议作为独立专项处理（见第七节 R3）。

---

## 四、孤儿 KP：23 个（占总 KP 2.5%，已排除 12 个 Excalidraw 绘图）

无任何文件引用其 basename。多为：
- 初中/高中基础层（合理孤立）：分子原子与元素、水与水资源、化学反应速率与平衡、化学品使用与绿色化学、烃与烃的衍生物、物质的变化与性质、生物大分子与合成高分子、碳及其氧化物、空气与氧气、酸碱盐、金属材料
- 竞赛层未互链：NbO型结构、NO配体与亚硝酰配合物、SOCl₂氯化、单糖、无水无氧条件、消除反应的立体化学、芳香体系中吸电子基作用、轨道观点下的稳定性、逆羟醛缩合、钙、自发过程与热力学第二定律表述、钻穿效应与屏蔽效应

> 性质：孤儿不一定是错误（基础层/小众概念本就少被链接）；但 23 个竞赛层 KP 零入链提示"建而不链"或可被其他页 `related` 引用。Info 级。

---

## 五、分类一致性

### 5.1 决赛要求目录 subject 漂移（50 个文件）

`决赛要求/` 目录共 108 个 KP 笔记，subject 取值分裂：
- **58 个** `subject: 决赛要求`（金属有机与超分子 + 材料化学 + 综合）
- **50 个** `subject` = 基础模块：无机和结构化学 29 / 化学原理 18 / 有机化学 1 / 物理化学 2（均在 `物理化学深化/`、`结构与配位深化/` 子目录）

影响：`知识点.md` 的"按科目分布"dataview（`GROUP BY subject`）会把这 50 个深化 KP 归入基础模块，而非"决赛要求"组；而 `知识点总索引.md` 的决赛子模块表按**目录**计数，二者口径不一致。

> 疑似两套建库习惯（深化子目录早期用基础 subject，后期用 决赛要求）。**需确认是否设计意图**；若否，统一为 `subject: 决赛要求` 更利于"按科目"视图聚合。Info/P3。

### 5.2 重复 basename（链接歧义）：1 处

`晶体结构基础` 同时存在于 `无机和结构化学/` 与 `高中化学基础/`。`[[晶体结构基础]]` 解析到哪一个取决于 Obsidian 选取（validate 按 INCLUDE_DIRS 优先取一个），存在歧义风险。建议：互链处改显式路径，或其一改名（如 `高中化学基础/晶体结构基础` 保留、`无机和结构化学/晶体结构基础` 可加修饰）。**P1**。

### 5.3 重复 title（显示名撞车）：1 处

`逆羟醛缩合` 同时是 `有机化学/Aldol缩合.md` 与 `有机化学/逆羟醛缩合.md` 的 `title`。查 `Aldol缩合.md` frontmatter：`title: "逆羟醛缩合"` —— **主 Aldol 页 display title 写错**（应为"羟醛缩合 / Aldol缩合"），与专门的逆羟醛缩合页撞名。**P2**。

---

## 六、与 validate_kb 的交叉验证 & 两个操作发现

- `validate_kb.py --dir 03-知识点`：**Error 0 / Warning 6**（3 条正文断链 = 还原剂/烯酮/碎片化 + 3 条 stage-门禁），与本文 2.1 中 3 条一致（另 2 条 `Friedel-Crafts反应\]`、`04-题库` 因形态特殊，validate 未计入"断链"但属同类问题）。
- `validate_kb.py --full`：受检 6590 / **Error 1 / Warning 220**（基线 209，+11 为近期增量，含并行导入对话贡献）。
- **发现 A（网关盲区）**：validate 只校验 frontmatter 链接字段（knowledge_points 等），**不查 `source_notes`/`prerequisite`**，故 2.2 的 13 条悬空溯源链接对其隐形。
- **发现 B（报告竞态）**：`auto-validation/2026-09-03-validation.md` 为**共享日报路径**，本会话 `--full` 写入后，被并行导入对话的 `--changed` 运行覆盖（实测文件 mtime 11:09、内容回退为 18 文件报告）。建议：并行对话收敛后单独重跑 `--full` 取权威全量报告；或改为带时间戳的报告名避免互冲。

---

## 七、问题分级与修复建议（待决，未执行）

| 级 | 问题 | 数量 | 建议 |
|---|---|---|---|
| **P1** | 重复 basename 链接歧义（晶体结构基础） | 1 | 互链处改显式路径或其一改名 |
| **P1** | 覆盖层深度缺口（题库/考纲引用的缺失 KP） | ~1037 目标（高频数十个明确应建） | 专项：区分"应补建 KP"与"knowledge_points 噪声"，优先补建高频概念（杂化轨道/氧化还原/分压定律/电极电势/有机反应机理/依数性/胶体/滴定/光度法/人名反应/超分子化学/芳香亲电取代/歧化反应/点群/路易斯结构…） |
| **P2** | 畸形 wikilink（Friedel-Crafts反应\ 反斜杠） | 1 | 改 `[[Friedel-Crafts反应]]` |
| **P2** | 重复 title（Aldol缩合.md 显示名误写） | 1 | `title` 改"羟醛缩合" |
| **P2** | 索引文档陈旧（知识点.md 称 742，实测 920） | 1 处文本 | 更新为 920（或改 dataview 动态计数） |
| **P2** | frontmatter 悬空溯源链接（source_notes/prerequisite） | 13 | 修复/补全指向的提炼笔记，或改相对路径；建议扩展 validate 覆盖这两字段 |
| **P3/Info** | 决赛要求目录 subject 漂移 | 50 | 确认设计意图；若统一则改 `subject: 决赛要求` |
| **Info** | 孤儿 KP（竞赛层零入链） | 23 | 评估是否补 `related` 互链 |
| **Info** | 指向文件夹的链接（04-题库） | 1 | 按需改显式路径 |

### 建议处置顺序
1. **先修零成本硬伤**：Friedel-Crafts 反斜杠（P2）、Aldol 显示名（P2）、晶体结构基础歧义（P1）——3 处，影响直接。
2. **补覆盖缺口（P1，最大价值）**：独立专项盘点 ~1037 目标，产出"应建 KP 清单"，优先高频基础概念。
3. **文档与一致性**：更新知识点.md 计数（P2）、确认决赛 subject 约定（P3）。
4. **网关增强**：将 `source_notes`/`prerequisite` 纳入 validate 链接校验；报告路径改时间戳避免并行互冲。

---

## 八、附：审计脚本与明细

- 审计脚本：`11-模板/scripts/` 之外，本次用 `.workbuddy/tmp/audit_kp_system.py`（全库索引 + 别名感知解析）与 `reconcile_kp_links.py`（调用 validate 真实解析器对账）。
- 明细 JSON：`.workbuddy/tmp/audit_kp_detail.json`（含 broken_body / broken_fm / orphans / missing_kp_refs / subject_counter 等）。
- 链接真值已用 validate_kb `find_wikilink_target` 逐条确认，非推断。
