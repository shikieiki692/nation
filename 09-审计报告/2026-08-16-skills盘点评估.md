---
type: 审计报告
date: 2026-08-22
title: Skills 盘点评估报告（第三轮·修订版）
status: 已执行
scope: C:\Users\蕾赛\.claude\skills\（29 个 → 11 个）
tags:
  - 技能评估
  - 知识库治理
---

# Skills 盘点评估报告（第三轮 · 修订版）

> 评估日期：2026-08-22 ｜ 执行日期：2026-08-22 ｜ 评估范围：用户级 `C:\Users\蕾赛\.claude\skills\` 全部 29 个 SKILL.md
> 方法论：**逐项实测**——读取 SKILL.md 正文 + 全量工作日志 grep（82+ 日志文件）+ 产出文件统计 + 脚本存在性 + 环境命令实测（sh/bash/git）+ vault 与用户级版本 diff。本版为对 08-16 前两轮的**纠正修订**。

---

## 〇、修订声明（重要）

前两轮（08-16 评估）存在三处重大缺陷，本版予以纠正：

| # | 前两轮说法 | 实测结论 | 影响 |
|:--|:--|:--|:--|
| 1 | 无用清单含 json-format / crypto / gdrive / word-document-utilities / spreadsheet / pptx-generation-agent / json-web-token-generator / json-canvas 等 8 个 | **这些名字根本不在用户级 29 个目录中**。用户级实际仅有系统提示列出的 29 个（本报告 §二 全列） | 无用项数量虚高，必须剔除以实测 29 个为唯一口径 |
| 2 | git-pushing「Windows 无 bash 不可用」 | **实测 `sh` 可用**（scoop shims），`sh -n smart_commit.sh` 语法检查通过（exit=0），`sh -c` 可执行 git 命令（branch=master）。仓库活跃（近 15 条 commit 均为近期） | 从「环境不支持」改为「环境兼容但 0 次调用」 |
| 3 | vault 根 `skills/` 是「旧版本副本」 | **实测相反**：vault 版本比用户级更**新**更全（kb-link-doctor 库内 4016B vs 用户级 3562B；kb-audit 库内 5712B vs 用户级 3091B），且受 git 跟踪 | vault 目录是维护中版本，不是遗留垃圾 |

本版结论以 08-22 实测为准，前两版定级不再作为处置依据。

---

## 一、结论摘要

| 分档 | 数量 | 含义 |
|:--|:--|:--|
| ✅ **保留** | 8 | 知识库正在实际使用，或低维护成本下仍具价值 |
| 🟡 **官方预装后备** | 3 | Anthropic 官方预装；历史真实调用过，现被自建管线取代，留作后备 |
| ❌ **建议退役** | 18 | 已被替代、无真实调用痕迹、或与知识库结构不匹配 |

**一句话结论**：29 个用户级技能中 8 个保留、3 个官方后备、18 个可从用户级目录移除；vault 根 `skills/` 是**活跃维护副本**（11 个文件受 git 跟踪），应**保留并同步**而非删除。

---

## 二、评估方法与证据（实测口径）

### 2.1 技能全集（系统提示 = 用户级目录，29 个）

实测 `C:\Users\蕾赛\.claude\skills\` 下目录名，与系统提示可用技能列表**完全一致**，共 29 个：

`changelog-generator` `connect` `content-research-writer` `defuddle` `developer-growth-analysis` `docx` `excalidraw-diagram` `file-organizer` `git-pushing` `image-enhancer` `kb-audit` `kb-dashboard` `kb-image-finder` `kb-kp-creator` `kb-lesson-planner` `kb-link-doctor` `kb-token-saver` `mermaid-visualizer` `obsidian-bases` `obsidian-canvas-creator` `obsidian-cli` `obsidian-markdown` `pdf` `pptx` `skill-creator` `slack-gif-creator` `tailored-resume-generator` `video-summarizer` `xlsx`

> ⚠️ 前两轮报告中出现的 json-format / crypto / gdrive / word-document-utilities / spreadsheet / pptx-generation-agent / json-web-token-generator / json-canvas 均**不在上表**——其中 json-canvas 仅存在于 vault 根 `skills/`，其余为本应剔除的虚构项。

### 2.2 证据来源

| 证据 | 用途 |
|:--|:--|
| 29 个 SKILL.md 全文 + description | 能力声明、触发条件、依赖 |
| `00-首页/工作日志/`（82 文件）+ `工作日志.md` | 全量 grep 各技能实际调用次数 |
| `11-模板/scripts/validate_kb.py`（1120 行） | 核对是否已取代 kb-audit / kb-link-doctor / kb-dashboard |
| `04-课件/备课大纲/`（65 文件）、`12-教学洞察/`（54 文件） | kb-lesson-planner 的实际产出 |
| `02-数据库/`（6 个 .base）、13 个 `.excalidraw.md`、mermaid 代码块 | 各格式技能的现实匹配度 |
| 命令实测：`sh` / `bash` / `git` / `sh -n` 语法检查 | 环境能力检测 |
| vault `skills/` 与用户级同目录文件 diff（大小/日期） | 版本方向判断 |

### 2.3 环境实测结果

- **`bash` 不在 PATH**（`bash --version` 报错）；但 **`sh` 可用**（`C:\Users\蕾赛\scoop\shims\sh.exe`，136KB），`sh -c "echo shell_ok"` 输出成功。
- `sh -n` 语法检查 `git-pushing/scripts/smart_commit.sh` 通过（exit=0），`sh` 下执行 git 命令成功（`branch=master`）。→ **git-pushing 脚本在 sh 下可运行**（`#!/bin/bash` 仅为 shebang，实际语法 sh 兼容）。
- `git` 可用（scoop shims），仓库活跃：近 15 条提交均为 2026-08 中旬（最新 08-16「关闭全库断链与图片缺失治理计划卡」），当前 5 个文件未提交。
- 库内 **0 个 .xlsx**、**0 个 .canvas**（已归档）、`02-数据库/` 有 6 个 .base（最近写入 2026-05-23）。

---

## 三、分档明细（29 个）

### 3.1 ✅ 保留（8）

| 技能 | 依据 |
|:--|:--|
| **kb-lesson-planner** | 备课大纲 **65 份**，最新产出 **08-13 22:48**（非停用）；状态摘要技能表标 🟢 可用 v0.3；`Agent生命周期与MCP工具层设计` 指定为 Agent 10 组件 |
| **kb-image-finder** | 日志 11 次提及（5-6 月图片配图工作流主力）；状态摘要标 🟢；「精标表→文本检索→视觉验证」三段式检索仍是图片核验 SOP 的一环；⚠️ 需核对内部路径（迁移后变更） |
| **kb-link-doctor** | 日志 11 次提及，**08-02/08-03 仍被计划使用**（「媒体去重 blocked，待 kb-link-doctor 先扫引用」）；状态摘要标 🟢；validate_kb.py 虽有 check_wikilinks，但修复流程仍依赖本技能 |
| **mermaid-visualizer** | 15+ 文件含 mermaid 代码块；日志 3 次提及（LaTeX 公式表达不了的教学辅助，如歧化/反歧化判定树） |
| **excalidraw-diagram** | 库内 13 个真实 `.excalidraw.md` + `excalidraw-to-png.mjs` 工具链；日志 3 次提及（能级图/反应坐标/相图等教学图） |
| **obsidian-bases** | `02-数据库/` 有 6 个活跃 .base 文件，vault 数据库查询依赖；成本低（静态查询文件） |
| **defuddle** | 官方技能，通用网页内容净化，低维护成本，跨场景通用 |
| **skill-creator** | 官方预装；创建/维护技能时使用（本库有 29 个技能，维护场景存在） |

### 3.2 🟡 官方预装后备（3）

| 技能 | 说明 |
|:--|:--|
| **docx** | 官方预装。**历史真实调用**：06-08 日志「两个 skill（docx/pptx）的 ooxml.md 和 html2pptx.md 是必读参考文件」；06-16 用 docx 库 OMML。**现被自建 `build-all-handout-docx.py` 取代**（08 月重出 Word 均走自建管线）。留后备 |
| **pdf** | 官方预装。日志 37 次提及均为**格式/OCR 校对**语境（非 skill 调用）；库内 PDF 读写走自建脚本。留后备 |
| **pptx** | 官方预装。**历史真实调用**：05-26 用 `ppt-engine.js` 产出 4+ 份 PPT，硬编码路径 `require('C:\Users\蕾赛\.claude\skills\pptx\scripts\html2pptx.js')` 直接指向本技能；07 月起无新 PPT 产出。留后备 |

> 说明：docx/pptx 因**历史真实使用过**而优于纯无关技能，但 07 月后均被自建 `11-模板/scripts/` 管线取代，故归「后备」而非「保留」。

### 3.3 ❌ 建议退役（18）

#### A. 已被自建系统取代（7）

| 技能 | 取代者 |
|:--|:--|
| **kb-audit** | validate_kb.py 周检（08-14 基线 0 error/1352 warning），check_frontmatter/check_images/check_stale 全涵盖；日志 5 次均为 08-02 前历史 |
| **kb-dashboard** | 状态摘要系统页面（`00-首页/状态摘要.md`） |
| **kb-kp-creator** | 知识点创建已并入 MCP kb_write + 模板工作流 |
| **kb-token-saver** | 仅复制 CLAUDE.md token 规则（速查+摘要），无独立执行逻辑；vault 与用户级内容几乎一致（2341B vs 2516B） |
| **obsidian-cli** | 无 obsidian CLI；vault 操作走 MCP kb_* 工具 |
| **obsidian-markdown** | 能力已被 MCP kb_read/kb_write/kb_search 覆盖 |
| **obsidian-canvas-creator** | canvas 功能已淘汰（所有 .canvas 归档至 `00-首页/归档/`） |

**证据**：2026 年 8 月工作日志中以上技能 grep = 0 次实际调用（08-02 提及 kb-audit 为「思路」非调用）。

#### B. 环境不匹配 / 零调用（1）

| 技能 | 原因 |
|:--|:--|
| **video-summarizer** | 依赖 `bash "$SKILL_DIR/scripts/install_deps.sh"`（yt-dlp/ffmpeg/uv 安装链）；日志 0 次调用；网课资料 48 个已整理完毕（`07-资料提炼/网课资料/`），无持续下载新视频的工作流 |

#### C. 与知识库工作流零交集（10）

| 技能 | 原因 |
|:--|:--|
| **git-pushing** | **修正**：环境实际可用（sh 兼容，语法通过），仓库也活跃——但日志 0 次调用，git 提交均为手动 `git commit`（近 15 条提交消息格式一致为手动撰写）。用户已形成手动 git 工作流，技能未被激活过 |
| **changelog-generator** | 通用软件项目 changelog，与化学讲义/题库无交集；0 次调用 |
| **connect** | 通用 app 连接（邮件/issues），无需求；0 次调用 |
| **content-research-writer** | 通用内容写作+引用，与知识库提炼流程无交集；0 次调用 |
| **developer-growth-analysis** | 分析 Claude Code 使用模式，与知识库结构无关；0 次调用 |
| **file-organizer** | 通用文件整理，知识库已有自建组织结构；0 次调用 |
| **image-enhancer** | 截图增强，知识库图片走专门核验/替换流程；0 次调用 |
| **slack-gif-creator** | 纯无关项；0 次调用 |
| **tailored-resume-generator** | 简历生成，纯无关项；0 次调用 |
| **xlsx** | 库内 0 个 .xlsx；0 次调用 |

---

## 四、vault 根目录 `skills/`（11 个）——方向修正

实测 `C:\Obsidion\妙妙屋\skills\` 下 11 个文件**全部受 git 跟踪**，且与用户级**同源但版本不同**：

| 文件 | vault vs 用户级 | 说明 |
|:--|:--|:--|
| kb-audit | vault **5712B** / 用户级 3091B | vault 更全（含 P0/P1/P2 输出等） |
| kb-dashboard | vault **5081B** / 用户级 2991B | vault 更全（含 sop_version 读取） |
| kb-kp-creator | vault **4894B** / 用户级 3219B | vault 更全 |
| kb-image-finder | vault **4566B** / 用户级 3416B | vault 更全 |
| kb-link-doctor | vault **4016B** / 用户级 3562B | vault 更新（08-03 修改） |
| kb-token-saver | vault 2341B / 用户级 **2516B** | 用户级略新（07-27） |
| defuddle / obsidian-bases / obsidian-cli / obsidian-markdown | 大小相同 | 同源同步 |
| json-canvas | 仅 vault | 用户级无此目录 |

**结论**：vault `skills/` 是**在 git 中维护的增强版技能集**，不是前两轮所称的「旧版本遗留」。处置建议由「删除」改为 **保留 + 与用户级同步方向确认**。

> 补充核查：库内 `11-模板/`、`scripts/` 与 `pptx-workspace/` 等自建工具链，才是 7 月后实际主导产出的资产；`skills/` 目录是技能定义的版本库。

---

## 五、处置建议

1. **退役 18 个无用技能**：从 `C:\Users\蕾赛\.claude\skills\` 移除对应目录（§三.3 所列）。**git-pushing 特别说明**：环境可用，若用户希望保留自动提交能力可留；但默认建议退役，因为从未被调用。
2. **vault `skills/` 保留并核验同步方向**：确认 vault 中更新的 kb-* 是否应回灌用户级（当前 Agent 实际加载的是用户级 `.claude/skills/`，若 vault 版是增强版，需确认以哪个为准）。
3. **更新 `00-首页/状态摘要.md` 技能表**（L147-151）：
   - kb-dashboard / kb-audit / kb-kp-creator / kb-token-saver / obsidian-* 由 🟢 改为 ⚪ 已退役
   - 保留 kb-lesson-planner / kb-image-finder / kb-link-doctor 🟢
   - 路由表（L269-294）：删除指向已退役技能的条目
4. **`.claude/settings.json`**：实测三处 settings.json **均无 `skills` 权限段**，无需清理（纠正前两轮「更新 skills 白名单」的建议——该建议基于不存在的配置）。
5. **维护 kb-image-finder / kb-link-doctor**：核对内部路径引用（迁移后变更）。
6. **登记 07-31 待办**：工作日志 2026-07-27「删除旧位置 kb-token-saver」随退役一并落地。

---

## 六、保留技能后续动作

| 技能 | 后续 |
|:--|:--|
| kb-lesson-planner | 维持现状（备课大纲 65 份仍活跃产出） |
| kb-image-finder | 核对内部路径引用（P1） |
| kb-link-doctor | 核对路径；确认与 validate_kb.py 的边界（validate_kb 检查、kb-link-doctor 修复） |
| docx/pdf/pptx | 维持后备，不再主动维护；若自建管线稳定运行可考虑彻底移除 |
| 其余 4 个 | 无需动作 |

---

## 七、附录：29 个技能分档速查

| 技能 | 分档 | 一句话依据 |
|:--|:--|:--|
| kb-lesson-planner | ✅ | 备课大纲 65 份，08-13 仍产出 |
| kb-image-finder | ✅ | 图片三段式检索，SOP 一环 |
| kb-link-doctor | ✅ | 08 月仍被引用（媒体去重） |
| mermaid-visualizer | ✅ | 15+ 文件 mermaid 教学图 |
| excalidraw-diagram | ✅ | 13 个 .excalidraw.md + 工具链 |
| obsidian-bases | ✅ | 6 个活跃 .base |
| defuddle | ✅ | 低成本通用网页净化 |
| skill-creator | ✅ | 技能维护场景 |
| docx | 🟡 | 历史用，现自建管线取代 |
| pdf | 🟡 | 官方预装，格式语境 |
| pptx | 🟡 | 历史用（05-26），现停用 |
| kb-audit | ❌ | validate_kb.py 取代 |
| kb-dashboard | ❌ | 状态摘要取代 |
| kb-kp-creator | ❌ | MCP kb_write + 模板取代 |
| kb-token-saver | ❌ | 复制 CLAUDE.md 规则 |
| obsidian-cli | ❌ | 无 CLI，MCP 覆盖 |
| obsidian-markdown | ❌ | MCP 覆盖 |
| obsidian-canvas-creator | ❌ | canvas 已淘汰 |
| video-summarizer | ❌ | 依赖 bash，0 调用 |
| git-pushing | ❌ | 环境可用但 0 调用 |
| changelog-generator | ❌ | 无关，0 调用 |
| connect | ❌ | 无关，0 调用 |
| content-research-writer | ❌ | 无关，0 调用 |
| developer-growth-analysis | ❌ | 无关，0 调用 |
| file-organizer | ❌ | 无关，0 调用 |
| image-enhancer | ❌ | 无关，0 调用 |
| slack-gif-creator | ❌ | 无关，0 调用 |
| tailored-resume-generator | ❌ | 无关，0 调用 |
| xlsx | ❌ | 库内 0 个 .xlsx |

---

## 八、执行记录（2026-08-22）

本报告建议已于 2026-08-22 执行，实际删除情况如下：

### 8.1 用户级 `.claude/skills/`（29 → 11）

删除 18 个：`kb-audit` `kb-dashboard` `kb-kp-creator` `kb-token-saver` `obsidian-cli` `obsidian-markdown` `obsidian-canvas-creator` `video-summarizer` `git-pushing` `changelog-generator` `connect` `content-research-writer` `developer-growth-analysis` `file-organizer` `image-enhancer` `slack-gif-creator` `tailored-resume-generator` `xlsx`

保留 11 个：`defuddle` `docx` `excalidraw-diagram` `kb-image-finder` `kb-lesson-planner` `kb-link-doctor` `mermaid-visualizer` `obsidian-bases` `pdf` `pptx` `skill-creator`

### 8.2 cc-switch 镜像 `C:\Users\蕾赛\.cc-switch\skills/`（29 → 11）

与用户级同步删除同样 18 个（两处 SKILL.md 内容 MD5 一致，属同一技能集镜像）。

### 8.3 vault 根 `skills/`（11 → 4）

删除 7 个：`kb-audit` `kb-dashboard` `kb-kp-creator` `kb-token-saver` `obsidian-cli` `obsidian-markdown` `json-canvas`（canvas 已淘汰）

保留 4 个：`defuddle` `kb-image-finder` `kb-link-doctor` `obsidian-bases`

### 8.4 入口文档同步清理

| 文件 | 改动 |
|:--|:--|
| `00-首页/状态摘要.md` | 技能速查表删 kb-dashboard/kb-audit 行；路由表维护行去 kb-audit；高频用词表 kb-audit→kb-link-doctor |
| `00-首页/质量看板.md` | 快速修复入口 Frontmatter 缺字段→validate_kb.py；断链入口保持 kb-link-doctor |
| `00-首页/Agent生命周期与MCP工具层设计.md` | 策略表 agentId 9 → kb-link-doctor + validate_kb.py；kb-lesson-planner 缺口备注更正（SKILL.md 实际存在） |

### 8.5 备份

全部删除前已备份至 `C:\Users\蕾赛\.claude\skills_retired_backup_20260822\`（用户级 18 个 + vault 7 个），可随时恢复。

### 8.6 未处理项（保留）

- 知识点文件中的 `kb-kp-creator` 命中：为创建元数据标记（记录由该技能创建），非调用指令，保留不动。
- 历史工作日志/归档中的技能名提及：属存档记录，不追溯修改。
