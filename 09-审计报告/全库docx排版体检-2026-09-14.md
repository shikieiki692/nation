# 全库 docx 排版体检（2026-09-14 13:30）

> 触发：用户询问「其他文件也没有问题了吧」。范围＝全库 docx（`_归档`/`_archive` 与 `~$`、`*.tmp.docx` 除外），口径＝A 类（LaTeX 源码态）+ B 类（HTML 标签 / 花括号上下标残留）。

## 一、总结论

| 产线 | 份数 | A 类 | B 类 | 判定 |
|---|---|---|---|---|
| **习题书**（教师版+学生版） | 66 | **0** | **0** | ✅ 本轮已收口 |
| **习题书·黑白打印版** | 34 | **0**（原 5） | **0**（原 24） | ✅ 09-14 晚重出后归零 |
| **13-教案/必修一** | 52 | **0** | **0** | ✅ |
| **06-学生侧材料/讲义主体** | ~295 | **0** | **0** | ✅ |
| 学生讲义·数学工具 | 8 | **0** | **0** | ✅ 本轮修 3 处源 → 重导 |
| `_archive` / `_归档` 旧代 | — | 若干 | 若干 | ⚪ 已归档，不计 |

**活跃区仍有 22 份非零**，全部落在**上代产线**，与习题书/教案/讲义主线无关。

> **2026-09-14 晚更新：22 份已全部清零。** 清零靠的是**管线层三个修复**（texmath 兼容
> 归一化 / Obsidian `attachments/` 图片解析 / HTML 表放宽）+ **2 处源修复**，而不是
> 逐份改内容 —— 详见第十节。下文对根因的表述有一处**已被推翻**，已在原处标注。

## 二、当初的 22 份清单（按线分组）—— 现已清零

### A. exam 线（15 份）—— 泄漏出在**中间产物本身**

| 目录 | 份数 | 主要命中 |
|---|---|---|
| `00-首页/题组Word/阶段测试卷` | 2 | `$$`×3 + `brace-super`×3（化学原理-答案）；`mathrm`+`brace-sub`（元素与分析-教师版） |
| `00-首页/题组Word/第二轮习题集` | 7 | `dollar-dollar` / `mathrm-bare` / `brace-sub`（1–3 处/份） |
| `00-首页/题组Word/专项卷/二分册` | 4 | `text`×1（专项卷III）；`brace-sub`×1（专项卷V） |
| `00-首页/题组Word/综合模拟卷` | 2 | `brace-sub`×1 |

**根因已定**：`.workbuddy/tmp/exam_build/*.md` 中间产物里存在 OCR 畸形数学，例如

- `$\mathrm { { \bf S e C l } _ { 4 } }$`（命令与花括号间带空格 + `\bf`）
- `$\mathrm{H}_{2} \mathrm{OH} + \mathrm{H}_{2} \mathrm{OH} + \cdots$`（重复拼接）
- `$C_{6}H_{5}-C#CH$`（`#` 误当化学键）

**关键判据**：用 exam_build 中间产物**纯重导治不了**——只有中间产物本身干净的那份（阶段测试卷·结构化学）才从 A=6/B=2 归零。**必须先修内容**（逐题对照答案区/OCR 源），再重生成中间产物，最后重导。

> ⚠️ **此判断已被推翻（2026-09-14 晚）**。真实情况是：管线此时还缺三个修复 ——
> ① `\bf`/`\sf`/`\rm` 等**字体开关**、② `\text{}` 内的反斜杠命令，这两类会被 texmath
> **直接拒绝转换**（不是「中间产物脏」，而是「管线转不了」）；③ `attachments/` 图片目录
> 不在解析路径里。补上这三点后，**同一份中间产物纯重导即归零**。
> 当时之所以得出错误结论，是因为我上一轮把「泄漏未减」的 10 份当作「零收益重导」**回滚**了 ——
> 其实它们已经修好，只是被我回滚掉。**教训：把「重导后数字没变」直接推断成「源脏」是错的，
> 应先做最小复现**（本次用 `_preprocess_markdown` + `pandoc -t docx` 抓
> `Could not convert TeX math` 警告，一步定位）。

### B. `04-课件/导出版`（4 份）

`分子结构基础` / `晶体学与晶体结构` / `配位化合物基础` / `原子结构`（均 `-超级充实版（自学完整）`）——A/B 各 1–3 处。

### C. `10-导出/分子轨道真题单题Word`（3 份，2026-07-13 一次性产物）

`题-36决理-2-7`（A=5/B=6）、`题-38决理-9`（A=1/B=2）、`题-34决理-1-3-1`（A=1/B=1）。
**根因**：对应题库源里含 **raw HTML `<table>`**（`题-36决理-2-7.md` 有 3 处），pandoc 原样透传进 Word。

> ⚠️ **补充（2026-09-14 晚）**：真正的拦路虎是**图片**。题-36 的 13 张、题-38 的 21 张图
> 存放在 **`04-题库/真题/…/第二场/attachments/`**（Obsidian 默认附件目录），而管线只搜
> `<源目录>`、`<源目录>/media`、`Vault 根`、`媒体仓库` —— `--strict-images` 因此拒收，
> 这两份**根本无法重出**。补上 `attachments/` 候选后即通过；题-36 的两张四列表（含结构图）
> 另需放宽 `_convert_html_tables` 的「含 `<br>`/`![[]` 即跳过」条件才能落成真 Word 表。

## 三、本轮已修（可验证）

| 项 | 内容 | 验证 |
|---|---|---|
| 源侧 3 处 | 学生讲义·数学工具第 2/6 讲「反引号内混 `$`」（`` `k = A $e^{-E_a/RT}`$ ``）→ 改裸 `$...$` | 重导后 A/B 归零 |
| 产物 2 份 | `06-学生侧材料/讲义/数学工具/数学工具-第{2,6}讲*.docx` | A/B=0 |
| 产物 2 份 | `阶段测试卷/结构化学阶段测试卷-{学生版,教师版}.docx`（经中间产物纯重导） | A=6/B=2 → **0**；纯文本段数不变，逐段比对差异全为「公式由文本→Word 公式对象」 |
| 管线 1 处 | `build-all-handout-docx.py`：`--path` 在 `batch_root` 模式下被忽略 → 定向重建变全量重建（`a3be6c936`） | dry-run 已验「Selected 1」 |

## 四、本轮负面教训（须记）

1. **`--path` + `--batch-root` 同传** → `--path` 被忽略 → 一次「重导一份」把 exam_build 下 **109 份**全量导进 `阶段测试卷/`。已全部**隔离移出**（未直接删），目录复原为 11 份。
2. **重导产物名 = 中间产物 stem**：`专项卷III-学生版.docx` 的中间产物叫 `二分册专项卷III-学生版.md` → 重导会生成**新名字文件**而不覆盖旧名（已隔离 4 份）。
3. **「零收益重导」一律回滚**：泄漏未减、段数却从 410 改成 343 的那类，一律 `git checkout` 回滚（备份在 `.workbuddy/tmp/_reverted_bak/`），只保留能验证的改进。

## 五、另发现：51 份 md 有「转义符被吃」的源级损坏（docx A/B 查不到）

**机制**：某脚本用**非 raw 字符串**拼接内容 → Python 把 `\f` `\t` `\r` `\b` `\v` `\a` 当转义符吃掉：

| 控制字节 | 原命令 | 残留 |
|---|---|---|
| `\x0c`(FF) | `\frac` | `rac{` |
| `\t`(TAB) | `\text` / `\times` / `\theta` | `ext{` / `imes` / `heta` |
| `\r`(CR) | `\rightarrow` / `\rho` | `ightarrow` / `ho`（**并断行**） |
| `\b`(BS) | `\beta` / `\begin` | `eta` / `egin` |
| `\v`(VT) | `\vec` / `\varepsilon` | `ec` / `arepsilon` |
| `\a`(BEL) | `\alpha` / `\approx` | `lpha` / `pprox` |

**影响面（51 份）**：`04-题库/教材习题` 24、`04-题库/有机化学` 15（这两组是 **`\r\r\n` 双 CR 行尾**，非命令损坏）、`00-首页/工作日志` 5、`03-知识点/决赛要求` 3、`03-知识点/无机和结构化学` 1、`04-课件/新授课` 1、`07-资料提炼` 1、`00-首页/经验沉淀` 1。

**好消息（已实测）**：

- **习题书不受影响**。`build_module_book.py:215-220` 有「行首 `rac{` → 并回前行并补 `\frac`」的修复规则，而 `\x0c`/`\x0b` 在 `str.splitlines()` 里算**行分隔** → 残留正好落在行首 → 被自动还原。实测 `04-课件/习题集`（教师版+学生版 全部 md）控制字符 **0 命中**，且题面里是正确 `\frac{k_{1}}{k_{1}'}`。
- **A/B 扫描查不到这一层**——`rac{`/`ightarrow` 不在 A 类模式里。判断这类问题**必须单独扫控制字符**（`scan_ctrl_dir.py`）。

**已在同日后半程修完（见「八」）**。修法是规则化还原（`<CTRL>+remainder` → `\+首字母+remainder`，删孤立控制字节），逐条变更日志 + 逐文件断言；`00-首页/工作日志/*` 5 份历史记录也一并清掉了控制字节（只删字节、不改文义）。

## 六、建议

剩余 22 份 docx 的修法是**内容层**（逐题核对），不是重导；51 份 md 的转义损坏是**源级卫生**问题（习题书已自动免疫）。
两项都建议并入既有的「教学产物库盘点→分类→整理」任务统一处理；本轮不擅自动上代产线的题面内容与历史日志。

## 八、后半程已完成的源级治理（4 commit）

| # | 内容 | 规模 | commit |
|---|---|---|---|
| 1 | 转义损坏还原（`\frac`→FF+`rac`、`\text`→TAB+`ext`、`\rho`→换行+`ho`、`\chi`/`\end` 丢斜杠） | **14 份 / 78 处** | `d5048056d` |
| 2 | `\r\r\n`（双 CR）行尾归一 | **38 份 / 1185 处** | `119c96e18` |
| 3 | 提炼稿整份重建（反斜杠→换行） | 1 份 / 184→128 行 | `186fb4816` |
| 4 | 习题书有机化学 8 章 × 2 版重建 + 16 份 docx 重导 | 32 文件 | `f7ac9c756` |

**关键判据（都可复用）**

- TAB 类修复**必须要求「TAB 前是紧邻的非空白」**，否则会误伤正常缩进；
- 「反斜杠丢失」的补救**必须限定在行内 `$…$` 之间**——不限定会把 `- **φ (phi)**：…`、`(phi/V vs a)` 这类正文描述也改坏（实测候选从 30 文件降到 15 文件）；
- 双 CR 归一**只能改 `\r\r\n`**，绝不能碰普通 CRLF（全库 2030 份是正常的）。

**验证**：全库控制字符扫描 **0 命中**；`\r\r\n` **0 命中**；改动文件 `$` 奇偶全为偶；习题书 68 份 docx A=0/B=0 维持。

**残留（判定为误报 / 非受管内容）**：断行公式补扫剩 12 行 / 11 文件，全是正文纯文本 `ln p 对 t 线性`、英文 `to make …`；`Mathematics for Physical Chemistry/`、`06-外部资料导入/`、`clayden 有机化学/`、`mineru*/` 属源材料，未动。

## 九、复现命令

```bash
# 全库 A/B（带路径，自动排除 _归档/_archive）
python -X utf8 .workbuddy/tmp/scan_ab_paths.py "00-首页/题组Word" "06-学生侧材料" "13-教案" "04-课件" "10-导出"
# 命中文本
python -X utf8 .workbuddy/tmp/show_ab_samples.py <docx>…
# 重导安全性核对（HEAD 版 vs 当前版纯文本）
python -X utf8 .workbuddy/tmp/cmp_modified_docx.py <目录>…
# 控制字符破坏（非 raw 字符串把 \frac/\text/\rho 的转义符吃掉）
python -X utf8 .workbuddy/tmp/scan_ctrl_chars.py
python -X utf8 .workbuddy/tmp/scan_ctrl_dir.py <目录>…
# 真 code span 内混 $
python -X utf8 .workbuddy/tmp/scan_codespan_dollar2.py "04-课件" "13-教案"
```

---

# 十、收尾：22 份清零 —— 三个管线修复 + 两处源修（2026-09-14 晚）

## 1. 管线三修（commit `24dbb1b80`）

| # | 修复 | 症状 | 传播面 |
|---|---|---|---|
| **R6** | **texmath 兼容归一化**（新增 4b14） | `\bf`/`\sf`/`\rm`/`\it`/`\sl`/`\sc`/`\tt` 字体开关、以及 `\text{}` 内的反斜杠命令（`\sim`/`\cdot`/`\times`）→ texmath **直接拒绝转换**，pandoc 把整段 LaTeX 原样印进 Word | 全库 29 份 md |
| **R7** | **`attachments/` 图片解析** | Obsidian 默认附件目录不在 `_build_resource_path` / `_resolve_image_ref` 候选里 → `--strict-images` 判「unresolvable on disk」拒收，产物根本重出不了 | 题-36（13 图）、题-38（21 图） |
| **R8** | **HTML 表放宽** | `_convert_html_tables` 原先「含 `<br>` 或 `![[]` 即整块跳过」→ 表格被 pandoc 当 RawBlock 摊成流水段落 | 全库 6 处（此类仅 2 张表） |

**R6 的两个坑**（都实测踩过）：
- 换成 `\mathbf{X}` 时**必须保留外层花括号**：`\mathrm { \bf B }` 若抹掉花括号 → `\mathrm \mathbf{B}`，
  `\mathrm` 吃到命令作参数 → 仍然 FAIL。
- 只在内容**真含命令**时才改写 `\text{}`：否则 `\text {x}` 被归一成 `\text{x}`，
  无意义却把改动面从 29 份扩到 425 份。

**决定性诊断手法**（本轮转折点）：

```
pandoc -t native   → 只看「解析」，InlineMath=1 会骗人
pandoc -t docx     → 才暴露 texmath 的 "Could not convert TeX math"
```

失败写法速查（实测）：`$\mathrm { { \bf S e C l } _ { 4 } }$`、`$\boldsymbol{\rm B(SH)_3}$`、
`${ \sf C } _ { 1 1 }$`、`${\bf A}$`、`$\text{属9~\sim17电子构型}$`、`$\text{kJ\cdot mol}^{-1}$`、
`$\text{6.02\times10^{23}}$`。
对照**正常**：`$\mathbf{A}$`、`$\text{充电}$`、`$\text {属8电子构型}$`、`$\text{mol}^{-1}$`。

## 2. 源两修（commit `9588ee410`）

- `例18.3-Xe化合物推断与反应方程式.md`：清 3 段 OCR 幻觉（「華國 會議一章」+ 2192 字重复
  温度/百分比/等差整数 + 964 字 `\mathrm{H}_2\mathrm{O}` 残块），加校勘注。
- `化学原理阶段测试卷-答案.md`：3 处 `$$` 块内嵌 `$`（① CuI 歧化 / ② Cu⁺-硝酸 /
  ③ I⁻-硝酸）→ 去掉内层 `$`。全库扫出 273 处「疑似嵌套」，配对核验后**真问题仅此 3 处**，
  其余全是朴素配对的假阳性。

## 3. 产物重出（commit `2f7584ec7`，23 份）

| 组 | 份数 |
|---|---|
| 阶段测试卷 | 3 |
| 第二轮习题集 | 7 |
| 专项卷/二分册 | 4 |
| 综合模拟卷 | 2 |
| `04-课件/导出版` | 4 |
| `10-导出/分子轨道真题单题Word` | 3 |

## 4. 验收

| 项 | 结果 |
|---|---|
| 全库 A/B（00-首页/题组Word + 06-学生侧材料 + 13-教案 + 04-课件 + 10-导出） | **活跃区非零 = 0** |
| 6511 份 md 新旧预处理回归 | 变化 29 份；非数学区变化 6 处（全部 HTML 表→md 表） |
| 元素与分析阶段测试卷-教师版 vs HEAD 全文本字符级比对 | 差异 28 处 `-`→`−` + 4 零宽空格 + 1 处删除垃圾；内容 98.5% 逐字一致 |

## 5. 本轮新增可复用脚本

```bash
python -X utf8 .workbuddy/tmp/t_fontswitch.py       # texmath 失败写法单测（经预处理）
python -X utf8 .workbuddy/tmp/t_ce_docx.py          # pandoc→docx 决定性实验（抓 Could not convert）
python -X utf8 .workbuddy/tmp/classify_regress.py   # 全库回归：变化是否全在数学区 + 差异分类
python -X utf8 .workbuddy/tmp/find_math_culprits.py <md>…   # 逐公式找 texmath 元凶（预筛）
python -X utf8 .workbuddy/tmp/scan_nested_dollar.py # $$ 块内嵌 $
python -X utf8 .workbuddy/tmp/rebuild_misc.py [--apply]     # 单题Word + 导出版重出
python -X utf8 .workbuddy/tmp/rebuild_exam.py [--apply]     # exam 线 15 份重出（含映射）
```

---

# 十一、续查：打印版长期漏检 + 两处静默失效（2026-09-14 晚）

## 1. ⚠️ 口径错误：A/B 扫描一直用 `SKIP_PRINT` 排除打印版

10 节报「习题书 A=0/B=0」时，其实只覆盖 68 份；**打印版 34 份被 `SKIP_PRINT=1` 排除在外**，
而其中 **9 份带 A/B 命中（A=5 / B=24）**：

| 文件 | 泄漏 |
|---|---|
| `1-热力学-学生版-打印版.docx` | `Na(s)+(1/2)Cl_{2}}&\xri…`（玻恩-哈伯块） |
| `4-氧化还原与电化学-学生版-打印版.docx` | `$LiCoO_2+C_6\stackrel{…}$` |
| 另 7 份 | `mathrm` / `brace-sub` 等 |

**根因**：打印版是 `build_print_student.py` 的**确定性后处理**（源＝同章 `*-学生版.docx`，
去封面／黑白化／页眉页脚／图片灰度，不动公式与正文）。这批是 **09-12** 从**当时**的学生版做的，
学生版 09-14 才修好 → 打印版继承了旧泄漏。

**已修**（`750cbe774`）：重出 34/34。验收 —— 习题书全量 **102 份（含 34 打印版）A=0/B=0**；
`verify_print` 通过（公式 18,177 / 图片 1,531 / 彩字·高亮·底纹·彩色图残留全 0）；
抽验 1-热力学 打印版 vs 学生版**逐字一致**（28,928 字符，0 差异）。

> **教训**：扫描口径一旦排除某类产物，那类就永远"干净"。要么全覆盖，要么明确记下被排除的范围。
> 后续 A/B 扫描**一律含打印版**。

## 2. `verify_print.py` 静默失效（已修，`a9ae799de`）

它的 `DST` 仍指向 **09-08 拍平前**的 `习题书/学生版-打印版/`（该目录已不存在）→ 每次扫
**0 文件**却打印「✅ 无彩色残留」。改为 `DST=习题书根` + glob `*-学生版-打印版.docx`。

> **教训**：校验器"通过"前先看它**扫了多少文件** —— `文件数 0` 也是一次静默失效。

## 3. 另立一类：正文裸下标（A/B 查不到）

`p_SO₂=0.02`、`k_A`、`c_M`、`p_total`、`S_N2'`、`d_xy`、`²J_CF`、`H^A` —— Word 里**字面显示**下划线/尖号。
A/B 扫描只查带花括号的 `_{…}`/`^{…}`，**全漏**。

清单见 `09-审计报告/习题书裸下标清单-2026-09-14.md`：**248 处 / 146 行 / 20 章**，
分 A（`_`+Unicode 下标，去掉孤立 `_` 即可）/ B（单字母，可直接包）/ C（化学式，需判断）/
D（上标，需判断）四类，抽验 34 处全为真阳性。**按用户选择只出清单，未擅改。**
