---
title: 习题书 Word 排版污染治理（第二轮：内容层收口）
date: 2026-09-14
scope: 04-课件/习题集/习题书（教师版+学生版）→ 00-首页/题组Word/习题书
prev: 习题书Word排版污染治理-2026-09-13.md（第一轮：管线层三类根因）
status: 已收口（打印版未处理，按用户指示）
---

# 习题书 Word 排版污染治理 · 第二轮

继第一轮（`$$` 尺寸前缀 `\Big` 漏剥 / HTML 表格不落 Word 表 / texmath 不支持宏）之后，本轮把剩余的**内容层**问题全部收口。

## 一、两个新的规则级根因（管线层，比第一轮更隐蔽）

### R4：`$$` 与内容之间夹空行 → pandoc 整体放弃识别为 display math

pandoc `tex_math_dollars` 的硬规则（实测）：

| 写法 | 结果 |
|---|---|
| `$$\n\nx\n\n$$` | ❌ `$$` 沦为字面文本逐段印出 |
| `$$\nx\n$$` | ✅ `Math DisplayMath "\nx\n"` |

**关键：这类问题 pandoc 一声不吭**——不报 `Could not convert TeX math`，所以第一轮「只收 stderr 警告」的检测口径完全漏掉了它（这也是为什么第一轮扫描显示"只剩 7 处"、而 docx 里却有 52 处 `$$`）。

影响：4 个文件、20 个块（7-金属有机与偶联反应 11 处金属羰基反应式、2-分子结构与化学键 2、3-晶体结构 3、4-配位化学 4）。

**修法（管线级）**：`build-all-handout-docx.py` 新增步骤 4b12 `_normalize_display_math_blocks()` —— 状态机删除 `$$` 块内部及紧邻两侧的全部空行（display math 里空行没有任何合法用途）。
**必备守卫**：文件级 `$$` 计数为奇数（存在未闭合）时整体跳过，否则错误配对会把正文大段内容当成 math 吞掉。

### R5：`\overset`/`\underset` 展开时吞掉与前一个控制序列的分隔

源里存在 `\equiv\overset{+}{O}` 这类 **OCR 命令紧贴命令**的写法。4a/4b 展开后得到 `\equiv` + `O^{(+)}`，TeX 会把 `\equivO` 当成一个控制序列（非法命令）→ texmath 整体拒绝转换。

**修法**：展开结果前补 `{}`（不产生间距、不影响渲染）断开。
**判据必须向前扫到控制序列起点**：`\equiv\overset` 中 `\overset` 前面是字母 `v`，但再往前是 `\` → 确属控制序列，补；而 `\mathrm{CH_2=CH\overset{+}{C}H_2}` 中 `\overset` 前面的 `CH` 是普通原子符号（再往前是 `=`）→ 不补。**只用 `prev.isalpha()` 会误伤 55~77 处**（本轮实测踩过，见 §3）。

## 二、本轮踩的 3 个坑（复用必读）

1. **内嵌替换函数的快照必须与 `m.start()` 所指字符串一致**。4a 替换后 `text` 已变，却让 `_underset_repl` 继续用 overset 替换**前**的快照做位置判断 → 索引错位 → 凭空多出 55 处 `{}`。改法：overset 替换后重新取快照 `_cur_after_overset`。
2. **新增步骤的执行顺序要按「产物还需被谁消费」排**。4b8 生成的 `\overset/\underset` 必须由 4a/4b 继续展开，因此 4b8 必须排在 4a **之前**；放在后面会触发 `precheck compat_macro_residual` ERROR（12-高分子化学 报错）。改法：把 4b7/4b8/4b8b 整块前移。
3. **`\{([^{}]*)\}` 一遇嵌套花括号就静默失配**——4b8 的参数含 `\mathrm{p}K_\mathrm{a1}` 时整体不匹配、不报错。凡 LaTeX 宏参数一律用平衡括号扫描。

## 三、内容层修复（改 04-题库，9 个文件）

| # | 根因 | 修法 | 文件（04-题库/） |
|---|---|---|---|
| 1 | `\$` 转义残留，`\cdots` 落到 math 外被丢弃 | `C\$\cdots\$C` → `C⋯C` | `教材习题/赵鑫光/题-赵鑫光-晶体-例9.md` |
| 2 | closing `$` 右侧紧跟数字（pandoc 禁止） | `乙烷($\sim$50)` → `乙烷(∼50)` | `有机化学/羰基化学与缩合反应/题-XES-033-环己酮酸性比较.md` |
| 3 | 同上 | `[$\sigma$2s + $\pi$2s]` → `[$\sigma 2s + \pi 2s$]` | `有机化学/周环反应/题-有机-周环-WoodwardHoffmann规则电环化.md` |
| 4 | 括号不平衡：`\mathrm {` 跨物种包裹整行 + 单元格少一个 `}` | 第 1 段重写为规范 LaTeX | `化学原理/Ch06-化学平衡/06-22.md` |
| 5 | OCR 把 `(` 认成 `\(` | `\(90278` → `(90278` | `教材习题/化学能力测试/题-360-…HgO分解热力学.md` |
| 6 | OCR 漏写换行符 | `\K _` → `\\ K _` | `化学原理/Ch08-酸碱平衡/08-07.md` |
| 7 | texmath 不支持 `{c:c}` 列格式 | `\begin{array}{c:c}` → `{cc}` | `教材习题/高中化学竞赛教程第二分册/题-1128-…高碘酸氧化葡萄糖.md` |
| 8 | `\text{}` 内使用数学命令 `\sim` | `\text {属9~\sim17电子构型}` → `\text{属9} \sim \text{17电子构型}`（2 处） | `化学原理/Ch12-化学键与分子结构/12-01.md` |
| 9 | OCR 漏写换行符 | `N-N\N-N` → `N-N\\ N-N` | `教材习题/赵鑫光/题-赵鑫光-分子-习60.md`、`题-赵鑫光-分子-例12.md` |

### 玻恩-哈伯循环图复原（用户最初报的问题 1 的内容层部分）

源 `教材习题/一分册能力测试/题-068-一分册测试-Ch4A-10-NaCl生成热玻恩哈伯循环.md` 的循环图被 OCR **拆成两个 `$$` 块，且丢掉底边那一行**——只有「顶边横箭头 + 左右两个竖箭头」，`Na(g)+Cl(g) →ΔH₂→ Na⁺(g)+Cl⁻(g) →ΔH₃→ NaCl(g)` 被孤立在第二个块里。

按答案给出的 `ΔfH = ΔH₁+ΔH₂+ΔH₃+ΔH₄` 关系复原为**单个三行 5 列 array**，箭头列对齐；顺带 `NaCl(S)` → `NaCl(s)`、`(1 / 2)Cl₂` → `\frac{1}{2}`；末尾校勘注同步说明复原依据（原注"按原书结构照录"已失效）。

## 四、验证

| 指标 | 初始 | 本轮后 |
|---|---|---|
| texmath 转换失败（74 个 md 全量） | 20 → 7（第一轮） | **0** |
| docx 文本层 `$$` 泄漏 | 52（第 1 轮重导后） | **0** |
| 全库残留宏（`\overset`/`\underset`/`\textcircled`/`\atop`/`\xrightleftharpoons`） | — | **0** |
| 回归 diff（162 源文件逐文件） | — | 42 处，**全部**为 `$$` 块内空行删除（逐文件比对**非空行序列完全一致**） |
| docx 重导 | — | 教师版 **33/33**、学生版 32/33（1 个待查，见 §5） |

## 五、遗留

1. **`1-热力学-学生版.docx` 重导失败 1 例**（留 0 字节 tmp），教师版同章成功——待定位（疑似 strict-images 图片缺失）。
2. **打印版 34 份**仍是 9-12 旧产物，未享本轮修复；脚本中已无打印版生成入口（仅 `validate_docx_structure.py` 有文件名映射）。
3. **检测口径修正**（本轮最重要的方法论收获）：只收 pandoc stderr 警告会漏掉「没被识别为 math」类问题；而 `native` 的 `Str "$$"` 精确匹配又会漏掉「`$` + RawInline + `$`」形态（如 `C\$\cdots\$C` 在 docx 里显示为 `C$$C`）。**最终以 docx 文本层扫描为准，双口径交叉验证。**

## 六、复现命令

```bash
# 双口径综合扫描（字面 $$ + 转换失败）
python -X utf8 .workbuddy/tmp/scan_all.py
# docx 产物污染扫描
SKIP_PRINT=1 python -X utf8 .workbuddy/tmp/scan_docx_pollution.py "00-首页/题组Word/习题书"
# 回归 diff（需先 git show HEAD:脚本 > .workbuddy/tmp/_old_build_docx.py）
python -X utf8 .workbuddy/tmp/regress_unknown.py
# 重建 md + 重导 docx
CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= python -X utf8 11-模板/scripts/build_module_book.py --strict --write --clean
CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= python -X utf8 11-模板/scripts/build-all-handout-docx.py \
  --batch-root "04-课件/习题集/习题书-教师版" --output-dir "00-首页/题组Word/习题书" \
  --filename-suffix=-教师版 --strict-images --parallel 4
```

---

# 第三轮（同日）：内容层收口

## 一、本轮修的三类问题

| # | 类别 | 规模 | 表现 |
|---|---|---|---|
| **P1** | 公式片段被整段删除 | **7 题** | Word 里出现 `{3}H_{8}$`（应为 `$\mathrm{CsB}_{3}\mathrm{H}_{8}$`） |
| **P2** | 闭合 `$` 后紧跟数字 | 1 处 | `$\mathbf{B}^{\bullet*}$2.` → docx 残留 `$^{}$2.` |
| **P3** | 正文裸 LaTeX 下标/上标 | **10 文件 / 14 行** | Word 里出现 `M_{M}`、`cm^{-1}`、`C_{4v}`、`H[AuCl_{4}]` |

## 二、P1 的关键事实（先证伪再动手）

- **规律**：`$` + 化学式主体 + 可选 `_` 被整段删除。`$CsB_{3}H_{8}$`→`{3}H_{8}$`；题-014 连不带 `_` 的 `$\mathrm{Fe}` 都被删。
- **精确规模 7 题**：题-008/012/014/017/020/024/027，**全部位于 `04-题库/教材习题/化学竞赛初赛讲义/` 第 28 行**。
- **成因不在现有脚本**：6 个嫌疑脚本正则逐一比对全无匹配；全库不存在「含 `\$` 与 `_` 且替换为空串」的 `re.sub`。
- **不会再犯**：`git log -S'CsB_{3}H_{8}'` 返回空（正确文本从未进 git），最早入库版本即已破坏。
- ⚠️ **我上一轮用宽松正则报出的「5,281 行 / 1,805 文件」是严重误报**——同位素 `$^{35}\mathrm{Cl}$`、`\frac{1}{2}$`、中文下标 `$E_{电池}$`、OCR 空格式 `\mathrm{H} _ {2}`、AI 空占位 `$ 和 $` 全被误命中。**真实规模就是 7 题**。

### P1 恢复依据（用户确认「残留形态 + 答案区优先」）

| 题 | 残留 | 恢复 | 依据 |
|---|---|---|---|
| 1.3 | `分解 {3}H_{8}$ ，可得 {2}B_{9}H_{9}$ …` | `$\mathrm{CsB}_{3}\mathrm{H}_{8}$` 等 6 式 | 残留+OCR+答案三方吻合 |
| 1.11 | `用 {3}$ 与 {3}$ 直接反应` | `$\mathrm{SO}_{3}$ 与 $\mathrm{NH}_{3}$` | **OCR 版语序相反**（`NH₃ 与 SO₃`），以答案 `SO₃+NH₃` 为准 |
| 1.13 | `向 ^{2+}$ 溶液…滴入 ^{2+}$` | `$\mathrm{Co}^{2+}$`、`$\mathrm{Hg}^{2+}$` | 答案 `Co²⁺ + [Hg(SCN)₄]²⁻` |
| 1.20 | `Pb(OAc) {2}$ … R {1}$ NHC(Se)NR {2}$ R {3}$` | `$\mathrm{Pb(OAc)}_{2}$`、`$\mathrm{R}^{1}\mathrm{NHC(Se)NR}^{2}\mathrm{R}^{3}$` | 答案 |
| 1.24 | `{4}C_{3}$ 与 {2}$ 的盐酸` | `$\mathrm{Al}_{4}\mathrm{C}_{3}$`、`$\mathrm{HgCl}_{2}$` | 残留+OCR+答案 |
| 1.36 | `置于 {2}$ … 适量 {4}$` | `$\mathrm{NiCl}_{2}$`、`$\mathrm{NaBH}_{4}$` | 答案 |
| 1.41 | `用 {2}$ 还原 … {4}Cl$` | `$\mathrm{FeCl}_{2}$`、`$\mathrm{NH}_{4}\mathrm{Cl}$` | OCR |

## 三、P2/P3 的真因（推翻我原先的猜测）

**`^{}` 空上标不是预处理清空 `^\circ` 造成的。** 实测 `0g`（`build-all-handout-docx.py:1636-1644`）只做 `\circ`→`\theta` 替换，**内容永不清空**；4a/4b 展开也不产 `^{}`。两库正文空 `^{}`/`_{}` 实测出现 **0 次**。

真因是 **pandoc `tex_math_dollars` 规则「闭合 `$` 不得后跟数字」**（与第一轮 `$\sim$50` 同一个坑）。实测 AST：

```
IN : 上的 H → $\mathbf{B}^{\bullet*}$2. 对炔键加成
AST: Str"$" RawInline(tex,"\mathbf{B}") Str"^{" RawInline(tex,"\bullet*") Str"}$2."
plain: '上的 H → $^{}$2. 对炔键加成'     ← 与 docx 逐字符一致
```

P3 则是纯粹源写法问题：题库有 59 次无 `$` 包裹的 `X_{…}`/`X^{…}`（另 4 次转义形态 `\_{…}`）。

## 四、附带修复：precheck 三个盲区

`generic_bare_subscript`（`:407`）要求 `_` **紧跟字母**，导致 `X_{…}`、`X^{…}`、`\_{…}` 三类全成盲区——**`^` 从未纳入检测**。实测出问题的源行 `err=0 warn=0`。

新增 `braced_bare_script` 规则（仅 WARN），正例验证**零误报零漏报**：

| 输入 | 是否报 |
|---|---|
| `C_{4v}`（裸） | ✅ |
| `M\_{M}`（转义） | ✅ |
| `A^{1,3}`（裸上标） | ✅ |
| `$C_{4v}$`（math 内） | ❌ 不报 |
| `\_\_\_\_`（填空占位） | ❌ 不报 |

## 五、另一项健壮性修复

`docx_utils._atomic_replace()`：预览面板/杀毒/Windows Search 对 `.docx` 持**长时**句柄时，`os.replace` 只返回 WinError 32，重试再多也无效。现改为重试耗尽后退回 `shutil.copyfile` + `os.unlink`（已写单测验证两条路径）。

## 六、本轮验收

| 项 | 结果 |
|---|---|
| 七题原残骸串 | **7/7 全部消失** |
| P3 裸下标扫描 | **归零**（`scan_bare_sub2.py` 12 文件 → 0） |
| 本轮改动文件 `$` 偶数 | 全部 OK |
| precheck 新规则正例 | 8/8 判定正确 |
| docx B 类残留 | 见下节（重导后复测） |

## 七、检测口径的三次修正（方法论）

裸下标扫描从 1080 行 → 54 → 36 → 17 → 16 行，逐步排除四类假阳性，值得记录：

1. **多行 `$$…$$` 块内部**未遮蔽 → 1080 行假阳性（首版只做了行内遮蔽）
2. **`$$\begin{aligned}` 这种 `$$` 非独占行**未被识别为块边界（判据改为「行内 `$$` 计数为奇数即切换」）
3. **`\(…\)` 行内 math**（pandoc `tex_math_single_backslash` 支持）被当裸下标
4. **HTML 注释行 / 标题行**噪音

另有两处**已知误报**（不必处理）：`题-039-5` 的 `$ $\mathrm{…}$ $`（OCR 把 `$$` 拆成 `$ $`，属另一类问题）、以及各类判据把 `^{N}` 上标算进 `{N}$` 的情况。

## 八、复现命令

```bash
# 裸下标全量扫描（跨行遮蔽版）
python -X utf8 .workbuddy/tmp/scan_bare_sub2.py
# 七题残骸验收
python -X utf8 .workbuddy/tmp/verify_p1c.py
# precheck 新规则正例测试
python -X utf8 .workbuddy/tmp/test_precheck.py
# 占用 fallback 单测
python -X utf8 .workbuddy/tmp/test_atomic_fallback.py
```

---

# 第四轮（同日）：`$` 壳类 + 标题行盲区

## 一、本轮新增两类

| # | 类别 | 规模 | 表现 |
|---|---|---|---|
| **P5** | `$` 壳 / 内容缺失 | 7 行 / 5 文件 | `$ 和 $`（内容被吃）、`$ $\mathrm{…}$ $`（`$$` 被 OCR 拆开） |
| **P6** | 标题行内裸上下标 | 影响 docx 1 处 | `### 4. 24000 cm^{-1} 谱带的归属` |

## 二、P5 明细与恢复依据

| 文件 | 形态 | 处理 |
|---|---|---|
| 题-026（习题1.39） | `$ 和 $` | → `$\mathrm{NH}_{3}$ 和 $\mathrm{N}_{2}$`（OCR 原文 + 答案 `…+2NH₃+N₂` 三方吻合） |
| 题-039-5（2 处） | `$ $\frac{…}$ $` | → `$$…$$`（pandoc 实测解析为 DisplayMath） |
| 题-039-7 | `$ $-\![…]_n\!-$` | → `$$…$$` |
| 题-039-6 | `$ $m_{…}$$` | → `$$…$$` |
| 题-091（3 处） | `$(D_{\infty h}$ 点群$)$` | 括号被错包进 math → 移到 math 外：`($D_{\infty h}$ 点群)` |
| XES-008 / XES-010 | `$ 和 $` / `$ 或 $` | 按上文语义补回 |

## 三、检测口径：`$` 壳必须「按配对扫描」，不能正则

正则 `\$([\s，。…]{1,4})\$` 会把「**上一对的闭合 `$` + 下一对的开启 `$`**」误当一对；
空内容 `{0,4}` 还会命中 `$$`。正确做法：逐字符按配对顺序前进，**`$$` 先整体跳过**，
命中后**整对跳过**。

**严禁**全局替换 `$ $`→`$$` —— 会毁掉正常的 `$a$ $b$`。

（首版正则报 10 行、其中 1 行是文档示例；配对版收敛到 7 行 / 5 文件。）

## 四、P6：扫描脚本自身的盲区

`scan_bare_sub2.py` 最初把**标题行**当噪音跳过 → `### 4. 24000 cm^{-1}` 漏检，
它正是 docx 里最后 1 处 B 类（`brace-super: 1`）的来源。补扫标题行后全库命中 4 处：
3 处在 `04-题库/元文件/`（未进习题书，非阻塞），1 处为本次修复。

## 五、另一条校验误报：字面 `**`

`_validate_docx` 原判据是朴素的 `"**" in document_xml`，命中 `【习题 11.131**】`
（原书**难度星号**，源与 OCR 均为转义 `\*\*`）。已在源里排除：
只有 docx 的 `**` **多于**源的 `\*\*` 时才报警（对照测试 3/3 通过）。

## 六、`_atomic_replace` fallback 再加固

首版 fallback 用 `shutil.copyfile` **仍会失败**（copyfile 内部以 `wb` 打开，被同样规则挡掉
→ 学生版实测仍报 1 error）。改为：

```python
with open(dst, "r+b") as fh:
    fh.seek(0); fh.write(src.read_bytes()); fh.truncate()
```

（沿用 KB 已验证的「最能穿透 Word 占用」写法。）加固定后学生版重跑 **33/33，0 errors**。

## 七、发现（未修，待定夺）：题-006 与 题-026 同题

- `题-006-…-习题1.38.md` 的**文件名/别名标为 1.38**，但正文实为 **1.39**（与 题-026 同题）。
- 题库中**不存在**真正的 1.38 内容（`grep '习题 1.38'` 仅命中 cross-ref 索引与旁及）。
- 后果：习题书里 1.39 出现两次；本轮修好损坏份后两份都完整。
- 属红区的库级数据问题，**未擅自改动**。

## 八、本轮验收

| 项 | 结果 |
|---|---|
| docx A 类（LaTeX 源码态） | 63 → **0** |
| docx B 类（HTML/花括号） | 93 → 11 → 1 → **0**（68 份，排除打印版） |
| `$` 壳扫描（题库 + 习题书 md 两侧） | **归零** |
| 正文裸下标扫描（题库 + 习题书） | **归零** |
| 标题行裸上下标 | 习题书侧归零；题库剩 3 处（`元文件/`，非阻塞） |
| 两版重导 | 各 **33/33，0 errors** |
| tmp 残留 | 0 |

## 九、复现命令

```bash
# $ 壳（配对式，可传目录）
python -X utf8 .workbuddy/tmp/scan_shell2.py [目录…]
# 标题行裸上下标
python -X utf8 .workbuddy/tmp/scan_heading_bare.py [目录…]
# 正文裸下标（跨行遮蔽）
python -X utf8 .workbuddy/tmp/scan_bare_sub2.py [目录…]
# docx 字面 **
python -X utf8 .workbuddy/tmp/scan_docx_strong.py
# `**` 校验判据对照测试
python -X utf8 .workbuddy/tmp/test_strong_warn.py
```
