---
title: 讲义等宽段治理-口径与清单
type: 审计报告
status: 已完成
created: 2026-09-11
scope: 06-学生侧材料/讲义（81 份活跃产物）+ 04-课件/学生讲义（活跃源 md）
related:
  - 00-首页/活跃任务/任务卡-2026-09-11-讲义等宽段治理.md
  - 09-审计报告/Word讲义产物排版验收报告-2026-09-11
tags:
  - 审计报告
  - Word产物
  - 排版治理
---

# 讲义等宽段治理 · 口径重建与逐块清单（阶段 1 产出）

> 结论先行：**原任务卡的三处前提经实测不成立**；真实构成已查清，源 md 与 docx 双侧对账已精确到「159 块 = 158 段 + 1 处未闭合围栏」。

---

## 一、口径纠正（旧 → 新）

| 核查项 | 任务卡口径 | 实测 |
|:--|:--|:--|
| "818 段是 Courier 等宽" | 成立 | **不成立**。81 份产物 `styles.xml` **无任何等宽字体**；`VerbatimChar`/`SourceCode` 字体均为 `FangSong + Times New Roman`。带等宽的只有 `_archive/` 3 份旧产物 |
| 818 段的组成 | 视为同质（"代码块残留"） | **两类混装**：真代码块 **158** + 正文夹行内反引号 **660** |
| 多行代码块 → 多段 | 是 | **否**。pandoc 把整个代码块放进**一个** `<w:p>`（块内换行用 `<w:br/>`）→ **1 代码块 = 1 段** |
| 判据"流程/排序 108 → ≤ 40" | 可达 | **不可达**（只治真代码块后剩 44） |

### 818 段真实构成

| 任务卡类别 | 合计 | 真代码块（`pStyle=SourceCode`） | 行内反引号（正文类 pStyle） |
|:--|--:|--:|--:|
| 结构图 | 74 | 66 | 8 |
| 流程排序 | 108 | 64 | 44 |
| 纯文本 | 636 | 28 | 608 |
| **合计** | **818** | **158** | **660** |

行内 660 段的 pStyle 分布：Compact 290 / BodyText 104 / FirstParagraph 104 / 无样式 47 /
BlockText 30 / SourceCode 28 / MistakeBlock 13 / MethodologyBlock 8 / Heading3 8 / Heading2 3 / Heading1 1
（注：`SourceCode=28` 出现在"纯文本"类，属真代码块，计入左列）。

---

## 二、双侧对账（源 md ↔ docx）

| 项 | 数值 |
|:--|:--|
| 活跃候选源 md | **71** 份（`04-课件/学生讲义/**/*.md`，排除 `_归档`、首层 `_` 目录、`README`、`讲义升级模式-` 前缀） |
| 源 md 围栏块 | **159** |
| docx `SourceCode` 段 | **158** |
| 差值 | **1**，唯一来源：`有机化学/醇醚胺酚.md`（见 §四） |

> **重要口径修正**：活跃集合**不能**施加 `超级充实/基础版/复习/-新课` 前缀过滤。
> 该过滤只在批量默认路径生效；81 份产物中大量 stem（如 `自由基反应`、`醛酮羧酸`、`重油/波谱分析` 等）
> 是显式 `--path` 构建的，必须纳入 `candidates`。

三分类双侧一致：md 结构图 67 / 流程排序 64 / 纯文本 28 ↔ docx 66 / 64 / 28（差 1 = 上述缺陷块）。

围栏语言标注：**裸 ``` 157 块 / ```text 2 块**；无 ```mermaid。

**含 `$` 的块：0 块。** → 本次出块**不会搬迁任何数学**，`$` 奇偶风险显著低于预期。

---

## 三、逐块建议分型（启发式，待阶段 2 人工核验）

| 建议分型 | 块数 | 建议处置 |
|:--|--:|:--|
| 反应式 | 45 | 提正文（保留 Unicode 箭头） |
| 几何/结构式 | 31 | 转图片（先查图谱库，未命中再评估重画/降级） |
| 判断树 | 28 | 转嵌套列表 |
| 文字型 | 25 | 提正文/公式 |
| 结构图/连线 | 15 | 评估转图片，否则留等宽 |
| 数据/对齐表 | 10 | 转 md 表格或正文 |
| 流程步骤 | 5 | 转有序/嵌套列表（列表前空一行） |
| **合计** | **159** | |

> **注意量级变化**：任务卡估计"A/B 必修 30–40 段、C 档 60 段左右"。真实分型下，
> "需出块"（反应式+判断树+文字型+流程步骤+数据表）≈ **113** 块，"转图片/留等宽" ≈ **46** 块。
> 上表为**正则启发式建议**，阶段 2 必须逐块人工核验，禁止直接照单执行。

### 真代码块（`SourceCode`）按文件分布 — 25 个文件

| 段数 | 文件 |
|--:|:--|
| 26 | `有机化学/自由基反应` |
| 19 | `结构化学/结构化学专题课-学生用合集（完整版）` |
| 15 | `有机化学/醛酮羧酸` |
| 11 | `有机化学/活性中间体与反应机理基础` |
| 11 | `有机化学/重排反应` |
| 10 | `有机化学/过渡金属催化` |
| 8 | `有机化学/亲核取代与消除反应` |
| 8 | `有机化学/有机波谱分析` |
| 6 | `元素与分析/元素化学深度-超级充实版（自学完整）` |
| 6 | `有机化学/立体化学与区域选择性` |

---

## 四、数据质量发现（新增）

### 4.1 未闭合围栏：`04-课件/学生讲义/有机化学/醇醚胺酚.md`

全文只有 **1 个** ``` 行（L396，开启后无关闭），且该文件为 CRLF 行尾。
pandoc **不识别**这个未闭合围栏 → 该段内容被当作普通段落，**``` 字面量直接印进 Word**：

```
初赛讲义提供的规律总结——"羟/卤/氧/氮的相互转化"： ``` R-OH ⇌ R-X ⇌ R-NH₂ (通过取代) R-OH → R-CHO/R₂CO (通过氧化) …
```

成因：围栏从未闭合（其余 3 处"奇数围栏"经复核为 ``` ```` ```text ```` 带信息串造成，属误报，已修正判据）。
处置：阶段 3/4 修源（补闭合围栏后立刻出块为正文/列表），并重导产物。

### 4.2 行内反引号高度集中（供"另开一卡"排期依据）

660 段行内反引号中，**430 段（65%）集中在数学工具区**：

| 段数 | 文件 |
|--:|:--|
| 95 | `数学工具/数学工具-第1讲-函数图像与线性化` |
| 94 | `数学工具/数学工具-第4讲-积分与分离变量` |
| 88 | `数学工具/数学工具-第5讲-偏导与全微分` |
| 76 | `数学工具/数学工具-第6讲-量纲数量级与估算` |
| 45 | `化学原理/酸碱理论-超级充实版（自学完整）` |
| 33 | `数学工具/数学工具-附录-数学符号速查卡` |
| 30 | `化学原理/电化学基础-超级充实版（自学完整）` |
| 28 | `结构化学/晶体学/晶体结构深化-超级充实版（自学完整）` |
| 23 | `数学工具/数学工具-第3讲-导数与微分` |
| 21 | `数学工具/数学专题-第2讲-对数与指数` |

→ 后续单独治理时应**优先攻数学工具区**（与「行内数学禁反引号」铁律同源）。

---

## 五、复现与产出物

```bash
PY="C:/Users/蕾赛/.workbuddy/binaries/python/versions/3.13.12/python.exe"
cd "C:/Obsidion/妙妙屋"
"$PY" -X utf8 .workbuddy/tmp/verbatim_report.py    # 产物侧：818 段分类 + 交叉表
"$PY" -X utf8 .workbuddy/tmp/verbatim_worklist.py  # 源侧：159 块逐块清单 + 对账 + 数据质量
```

| 产出 | 路径 |
|:--|:--|
| 产物侧全量明细（基线，供治理后 diff） | `.workbuddy/tmp/verbatim_baseline.json` |
| 源侧逐块清单（CSV，含起止行号/分型/建议/引导语/活性字符） | `.workbuddy/tmp/verbatim_worklist.csv` |

> 旧脚本 `scan_verbatim.py` **保持原样未改动**，以便随时复现旧口径（818 / 108）；
> 新增 `verbatim_report.py` 为其超集（增加 `w:pStyle` 维度）。

---

## 六、阶段 1 附带产出：单文件试点（已验证）

试点文件：`元素与分析/元素推断综合训练-超级充实版（自学完整）.md` —— 该文件 3 个真代码块**全部出块**。

| 原位置 | 原形态 | 治理后 | 依据 |
|:--|:--|:--|:--|
| L112-120 | 竖向流程（7 行，`↓` 串成一条线） | 有序列表（4 项） | 非对齐的线性管道 |
| L326-336 | 判断树（`├─ └─`，9 行） | 嵌套列表（3 层） | 判断树 → 嵌套列表 |
| L405-410 | 「三步翻译法」被压平的有序步骤 | 有序列表（3 项） | B 档 |

### 验证结果

| 检查项 | 结果 |
|:--|:--|
| 该产物 `SourceCode` 段 | **3 → 0** |
| pandoc 保真度 | 判断树 3 层缩进完整（`ilvl` 0/1/2）；有序列表 `numFmt=decimal`、无序 `numFmt=bullet` **正确区分** |
| 全库等宽段 | 818 → **815**（真代码块 158 → **155**；行内 660 **不变**） |
| 守门 | A=0 / B=0 未破坏；OMML 7,868 不变；`qa_comprehensive` 汇总为空；缺失图片 0 |
| 双闸门 | js-yaml 1/1 通过；validate_kb **0 Error / 0 Warning** |
| 行尾 | 零抖动（CRLF 计数与行数同步变化，未出现整文件行尾改写） |

**结论**：「判断树 → 嵌套列表」「压平步骤 → 有序列表」两条路径已跑通并可铺开。
该文件已重导产物；改前快照在 `.workbuddy/tmp/bak_verbatim/`。

---

## 七、下一步（阶段 2 起）

1. **阶段 2** 逐块人工核验 `verbatim_worklist.csv`（159 行）：确认引导语、`$`、markdown 活性字符、判断树缩进风格、几何图是否可从图谱库命中。
2. **阶段 3** 试点：先取 1 个判断树块转嵌套列表 → 单文件重导 → 读 `document.xml` 验证 pandoc 的列表缩进/编号保真度，通过后再铺开。
3. 阶段 4 反应式出块 → 阶段 5 几何图分型 → 阶段 6 字体修复 → 阶段 7 验收 → 阶段 8 双闸门 + 提交 + 回写。

---

## 八、批次执行日志

### 批次 1（2026-09-11 晚）· 判断树 / 压平步骤 → 列表

| 文件 | 块数 | 类型 |
|:--|--:|:--|
| `有机化学/亲核取代与消除反应` | 2 | 判断树 1 + 步骤 1 |
| `有机化学/立体化学与区域选择性` | 2 | 判断树 2 |
| `有机化学/有机立体化学` | 1 | 判断树 |
| `有机化学/加成反应` | 1 | 判断树（4 层嵌套） |
| `有机化学/自由基反应` | 1 | 判断树 |
| `有机化学/羰基化学与缩合反应` | 1 | 判断树 |
| `有机化学/高等有机机理` | 1 | 判断树 |
| `元素与分析/元素化学深度-超级充实版（自学完整）` | 2 | 步骤 2 |
| `元素与分析/氧化还原滴定与沉淀滴定-超级充实版（自学完整）` | 1 | 判断树 |
| `结构化学/结构化学专题课-学生用合集（完整版）` | 1 | 判断树 |
| **合计** | **13** | 判断树 10 + 流程步骤 3 |

**实现方式**：规则化转换器 `.workbuddy/tmp/convert_batch.py`。它不是一次性脚本而是**可复用工具**：
内置自测（用已手工转换的试点块反推校验）、严格准入门槛（含 `$`/反引号/`*`/`_`/`[]`/`\`/`<`/`|` 或列对齐空格即整块放弃）、
`--only <子串>` 限定范围、`--apply` 才落盘、逐文件断言 行数/CRLF 变化量、幂等（重复运行报 0 块）。

**踩到并修掉的 3 个 bug**（都很有代表性，后续批次复用须注意）：
1. **层级取错符号**：`│   └── x` 的层级我一开始用行首 `│` 的列号 → 嵌套整片压平（加成反应块塌成一层）。
   正确做法是取前缀里**最后**一个分支符（`└`）的列号。
2. **只认裸围栏**：断言写死 `== "\`\`\`"`，遇 `\`\`\`text` 直接崩。改为用开栏正则匹配（允许信息串）。
3. **行尾一刀切**：给新行统一补 `\r`，把**纯 LF** 文件（`有机化学/有机立体化学.md`）写成混合行尾并被断言拦下。
   改为**跟随该块自身**的行尾。

**收尾修正**：
- `有机化学/醇醚胺酚.md` **未闭合围栏缺陷已修复**（删围栏 + 5 行互变关系改无序列表 + 列表前补空行）。
- 试点文件的冗余序号「1. 第一步 …」已与新转换器的风格对齐（剥掉纯序号前缀）。

**批次结果**：

| 指标 | 批次前 | 批次后 |
|:--|--:|--:|
| 全库等宽段 | 818 | **802** |
| 其中真代码块（SourceCode） | 158 | **142** |
| 其中行内反引号 | 660 | 660（本卡不治，未恶化） |
| 源 md 围栏块 ↔ 产物 SourceCode | 155 ↔ 158（陈旧） | **142 ↔ 142，0 不匹配** |
| 未闭合围栏 | 1 | **0** |

守门：A=0 / B=0、OMML 7,868 不变、`qa_comprehensive` 汇总空、缺失图片 0、
`$` 奇偶仅剩 `_归档` 既有存量；双闸门 js-yaml 12/12 + validate_kb 12 文件 0 Error / 0 Warning。

### 剩余真代码块 142 段的构成

| 建议分型 | 块数 | 后续处置 |
|:--|--:|:--|
| 反应式 | 45 | 阶段 4：提正文（逐条核验 markdown 活性字符） |
| 几何/结构式 | 27 | 阶段 5：转图片（查图谱库优先）或降级 |
| 文字型 | 24 | 提正文/公式 |
| 判断树 | 17 | 余下多含活性字符或列对齐空格，**须人工**逐块处理 |
| 结构图/连线 | 15 | 评估转图片，否则留等宽（阶段 6 配 SimSun 字体） |
| 数据/对齐表 | 10 | 转 md 表格或正文 |
| 流程步骤 | 4 | 同批次 1 规则，但含活性字符，须人工 |

---

## 九、批次 2 + 阶段 6

### 批次 2 · 去围栏出块（38 块 / 13 文件）

给转换器扩了第三条规则 `try_inline`：**只处理"无缩进、无列对齐、无 markdown 活性字符"的块**。
凡"多行块里存在缩进行 / 列对齐"一律判为**对齐示意**（真结构式竖排骨架、坐标轴、注记行）→ 跳过交人工。
另补 `步骤 N` 序号规则，使"步骤 1–5"落成真有序列表。

跳过原因统计：`行数过多 26` / `多行块含缩进行 52` / `多行块含列对齐 17` / `含活性字符 8` / `行首块级 1`。

> 口径提醒：`文字型` / `反应式` 这两个**启发式分型桶并不纯** —— 实测里面混着
> `Br` 竖排骨架、`H—OH` 投影、`4000/3000/2000` 坐标轴这类**真示意图**。
> 所以不能按桶批处理，必须靠"缩进 / 列对齐"守卫把它们挡在门外。

### 阶段 6 · 字体修复（ASCII 等宽网格恢复）

**根因是两处归一化，缺一不可**：
1. **样式层**：`docx_utils.postprocess_pandoc_docx()` 的 `for style in doc.styles` 把 `VerbatimChar` 的 `SimSun` 换成 `FangSong`；
2. **run 层**（同函数 1139–1148 行）：把每个 run 的 `eastAsia` 重置为 仿宋/TNR。**run 级直格式会覆盖样式**，所以只改样式完全无效。

**修法**：新增常量 `MONO_FONT = "SimSun"`、`MONO_STYLES = {"VerbatimChar", "SourceCode"}`；
样式层豁免这两个样式，且 `pStyle = SourceCode` 的段落内 run 层强制 `ascii/hAnsi/eastAsia/cs = SimSun`。
选 SimSun 的理由：西文字形半角等宽、汉字全角，天然成网格，字形仍是衬线、不刺眼。

**验证**：`VerbatimChar` / `SourceCode` = SimSun；`Normal` / `BodyText` / `FirstParagraph` / `Table`
仍为 `FangSong + Times New Roman`、`Heading1` 仍为 `SimHei + TNR` —— 正文与标题**零误伤**。

**历史产物**：新增 `patch_mono_font.py`，直接给已存在的产物打补丁（改 styles.xml 两个样式 +
`SourceCode` 段落内 rFonts），**81/81 份已生效、0 损坏、幂等**（复跑 0 改动）。
踩坑：同一份 `styles.xml` 的前缀是**混合**的（`VerbatimChar` 用 `ns0:`、`SourceCode` 用 `w:`），
正则不能假定统一前缀，且 rFonts 的前缀要取自实际命中的元素。

### 阶段 3 累计结果

| 指标 | 起点 | 现在 |
|:--|--:|--:|
| 全库等宽段 | 818 | **764** |
| 其中真代码块（SourceCode） | 158 | **104** |
| 其中行内反引号 | 660 | 660（本卡不动） |
| 源 md 围栏块 ↔ 产物 SourceCode | 不对齐 | **104 ↔ 104，0 偏差** |
| 未闭合围栏 | 1 | 0 |

守门：A=0 / B=0、OMML 7,868 不变、`qa_comprehensive` 汇总空、缺失图片 0、`$` 奇偶仅剩 `_归档` 存量；
双闸门 js-yaml 13/13 + validate_kb 13 文件 0 Error / 0 Warning。


---

## 十、批次 3–8 执行日志（2026-09-11 深夜）

### 批次 3 · 放宽守卫后新增出块（16 块 / 9 文件）
- `RE_ALIGN` 从"任何 `\S␣␣\S`"改为**行尾注记型对齐**（更精确），恢复了一批"反应式 + 行尾注记"块；
- 新增「根 + 缩进树」规则、「①序号 + 缩进子项」规则、「`标签␣␣数值` 两列表格」规则；
- 一处**误判被守卫拦掉**：`自由基反应.md` L550 那块的缩进行其实是跨行折行的括号补充说明，不是子项 ——
  新增守卫「子项行首不得为块级结构 / 不得以 `(` `（` 起头」。

### 批次 4 · 再补两条规则（5 块 / 4 文件）
「序号主项 + 缩进子项 → 嵌套列表」、「竖向 `↓` 流程 → 有序列表」。

### 批次 5 · 修两处解析缺陷（2 块 / 2 文件）
- **纯 `│` 空行被误拒**：`│`、`│   │` 这类"空树形符行"是间隔符，原实现按"树形符后无文字"整块放弃，
  导致多棵大判断树（亲核取代五步选路、活性中间体三步判据）全部被挡。改为跳过而非报错。
- **折行续写误判**：`│   │      口诀："…"` 这类行的最后一个分支符后面不接 `─`，
  原实现把它当独立节点 → 层级错乱。新增守卫：**前缀里没有拐角符（`├└┌┐…`）即判为续写，整块放弃**。
  （注意不能判"最后一个树形符后是否接 `─`"——`─` 本身也在 `TREE_CHARS` 里，会把所有正常节点误杀。）

### 批次 6 · 活性字符守卫改精确模式（13 块 / 8 文件）
旧版字符类 `[$`*\[\]\<|]` 过严：化学里的 `[Ru(EDTA)…]²⁻`、`π*`、`-->`、`|` 都是**普通文本**，
一刀切把大量可转块挡在门外。改为**精确模式**判定：

| 模式 | 含义 |
|:--|:--|
| `` ` `` | 行内代码 |
| `$` | 数学（出块会改变 `$` 奇偶口径，必须放弃） |
| `**` / `__` | 加粗 |
| `\*[^*]*\*` | 成对单星号 → 会被解析为强调 |
| `!?\[\[` / `\[[^\]]*\]\s*[(\[]` | wikilink / md 链接 / 图片 |
| `<[!a-zA-Z?/]` / `</` | 残留 HTML 标签 |
| `\` | 反斜杠 |
| 表格分隔行 + 含竖线 | 会被解析为表格 |
| 词边界下划线 | 会被解析为强调 |

### 批次 7 · 人工定型（8 块 / 5 文件）
无法规则化的块逐块人工替换（`manual_batch7.py`，含"围栏位置 + 块内原文"双重断言）：

| 文件 · 位置 | 原形态 | 治理后 |
|:--|:--|:--|
| 分光光度法 L396-399 | 流程 + 下挂注记 | 有序列表（6 步，注记并入各项） |
| 有机波谱分析 L295-298 | 裂分实例 + `↑` 注记 | 项目符号列表 |
| 自由基反应 L84-87 | 稳定性序列 + 原因标注 | **两列表格**（顺序 / 类型 / 稳定化来源） |
| 自由基反应 L507-511 | 3 个链终止反应式 | 项目符号列表 |
| 自由基反应 L544-548 | 反应式 + 括号续行 | 正文段 + 引用块说明 |
| 重排反应 L147-150 | 反应链 + 下挂注记 | 有序列表（3 步，注记并入） |
| 重排反应 L630-633 | 跨 2 行的反应链 | 合并为单行正文 |
| 高等有机机理 L139-143 | ρ 三档 + 双行注记 | **三列表格** |

> 主动跳过 1 块（`自由基反应.md` L718-721）：其注记 `(与碳正离子最大区别)` 的对齐位置
> 指向哪个节点有歧义，**不猜**，保留原代码块。

### 批次 8 · 人工定型（3 块 / 3 文件）
| 文件 · 位置 | 原形态 | 治理后 |
|:--|:--|:--|
| 亲核取代 L383-387 | 横向分叉 `┌─┼─└─`（一个中间体三出路） | 有序列表（3 项） |
| 有机化学基础 L575-582 | **引用块内**的判断树 | 引用块内嵌套列表（保留 `> ` 前缀，2 层） |
| 有机立体化学 L594-621 | 折行口诀树 | 嵌套列表（**5 层**，口诀降为父节点子项） |

验证：`ilvl` 0–4 全部保真、`numFmt=bullet`；

### 批次 3–8 累计结果

| 指标 | 起点（本卡开工） | 现在 |
|:--|--:|--:|
| 全库等宽段 | 818 | **717** |
| 其中真代码块（SourceCode） | 158 | **57** |
| 其中行内反引号 | 660 | 660（本卡不动） |
| 源 md 围栏块 ↔ 产物 SourceCode | 不对齐 | **57 ↔ 57，0 偏差** |
| 未闭合围栏 | 1 | 0 |

守门：A=0 / B=0、OMML 7,868 不变、`qa_comprehensive` 汇总空、缺失图片 0、
`$` 奇偶仅剩 `_归档` 存量；双闸门 js-yaml + validate_kb 全批次 0 Error / 0 Warning；**行尾零抖动**。

---

## 十一、阶段 5 前置：图谱库命中率普查（结论：低产，需人工）

对剩余 57 块（绝大部分是真图）做了配图可行性普查。工具
`.workbuddy/tmp/img_hit_survey.py` / `img_hit_detail.py`。

**主题级命中率 87%（23 个主题中 20 个在三个学科图谱索引中有命中）**，但
**精确匹配率远低于此** —— 逐条目检后：

| 主题 | 主题级命中 | 精确匹配实况 |
|:--|:--|:--|
| 八面体场 d 轨道分裂 | ✅ 多处 | ✅ **命中** `5f17019e…jpg`（自由离子→球形场→eg/t₂g，Δ₀=10Dq，已视觉核验） |
| d⁶ / d⁵ 具体排布 | ✅ | ❌ 库图是简并轨道排布，非 eg/t₂g 分裂后的排布 |
| Newman 投影式 | ❌ 0 命中 | —— |
| 卡宾 | ✅ | ❌ 只有"三重态卡宾与烯烃加成"机理图，非"单线态 sp² 键角 102°"几何示意 |
| 多碘阴离子 | ✅ | ❌ 只有 I₇⁻，非 I₃⁻ / I₅⁻ |
| 催化循环 | ✅ | ❌ 只有 Monsanto 羰基化循环，非本讲义对应反应 |
| 胶团 / 双电层、IR 区段轴、NMR 谱图 | ❌ 0 命中 | —— |

**结论**：这些 ASCII 图是**教学专用图**（骨架式、能级图、投影式、决策框图），
图谱库只有**部分主题级**命中，逐块精确匹配率实测约 **2/57**。因此
「ASCII 图 → 图片」不适合批处理，须逐图人工决策（找图或重画），建议**单独立项**。

**缓解**：阶段 6 字体修复后，剩余 57 段全部为 `SimSun` 等宽网格（已逐段核验，
如结构化学 14 段 `SourceCode` run 级字体 100% 为 `SimSun`），ASCII 图在 Word 中**已能正确对齐**，
不再是"错位"问题，仅剩"ASCII 观感 vs 真图片"的美观取舍。

---

## 十二、批次 9–10（深挖「结构图/连线」桶）

批次 8 后剩余 57 块，逐块人眼复核（而非只看启发式分型）后又取出 13 块。
**关键教训：`结构图/连线`、`几何/结构式` 这两个桶同样不纯** —— 里面藏着大量
「①②③ 步骤 + `│ ▼` 连接符」的**催化循环步骤**（本质是列表）和「框线流程图」（本质是决策树）。

### 批次 9 · 人工定型 10 块 / 4 文件

| 文件 · 位置 | 原形态 | 治理后 |
|:--|:--|:--|
| 周环反应 L222-230 | 4n / 4n+2 电环化选择规则 | 嵌套列表（2 组 × 2 项） |
| 周环反应 L393-407 | 周环反应判断树（已是列表形态，被围栏包住） | 去围栏 → 嵌套列表（3 组，含 2 层子项） |
| 有机波谱分析 L403-406 | 结构解析流程（横向 `──→` 串联） | 有序列表（5 步） |
| 有机波谱分析 L465-478 | **¹H NMR 化学位移速查**（12 行 `标签␣␣数值`） | **真表格**（13 行） |
| 活性中间体 L164-172 | 碳正离子共振三例 | 项目符号列表 |
| 过渡金属 L131-142 | Suzuki 循环（│▼ + ①②③） | 有序列表（3 步） |
| 过渡金属 L170-178 | Heck 循环 | 有序列表（4 步） |
| 过渡金属 L291-302 | Wacker 循环 | 有序列表（5 步） |
| 过渡金属 L369-381 | Tsuji-Trost 循环 | 有序列表（2 步） |
| 过渡金属 L405-415 | Buchwald-Hartwig 循环 | 有序列表（5 步） |

**⚠️ 一块错误转换被拦下并回退**：`有机化学基础.md` L160-165 转成了「表示方式」两列表格，
但**该表在 L152-157 已存在**（且是三列、带 `\ce{}` LaTeX）。我的转换既**重复**又**丢掉了 `\ce{}`**，
已 `git checkout` 回退并重导。**教训：转表格前必须先查上方是否已有同内容表格。**

**主动跳过 1 块**：`自由基反应` L499-508 —— `CH₃ / |` 标注的对齐位置指向哪个碳有歧义，**不猜**。

### 批次 10 · 结构化学 3 块

| 位置 | 原形态 | 治理后 |
|:--|:--|:--|
| L2868-2888 | 框线「读题分模块」流程图（19 行） | 嵌套列表（3 分支 × 子项）→ **−1 个代码块** |
| L2674-2685 | DTBSQ 钴(III) 配合物：骨架 + 4 行文字说明 | 骨架**保留**代码块；4 行说明**拆出等宽**成列表 |
| L2693-2703 | DTBSQ 钴(II) 配合物：骨架 + 3 行文字说明 | 同上（3 行说明拆出） |

> 后两处**块数不变**但文字不再用等宽渲染 —— 这是「分型处置」的另一种形态：
> 结构图留等宽，文字提正文。

### 最终结果

| 指标 | 本卡开工 | 现在 |
|:--|--:|--:|
| 全库等宽段 | 818 | **706** |
| 其中真代码块（SourceCode） | 158 | **46**（−71%） |
| 其中行内反引号 | 660 | 660（本卡不动） |
| 源 md 围栏块 ↔ 产物 SourceCode | 不对齐 | **46 ↔ 46，0 偏差** |
| 未闭合围栏 | 1 | 0 |

守门：A=0 / B=0、OMML 7,868 不变、`qa_comprehensive` 汇总空、缺失图片 0、
`$` 奇偶仅剩 `_归档` 存量；双闸门 js-yaml + validate_kb 逐批 0 Error / 0 Warning；行尾零抖动。

**剩余 46 段全部是真图**（骨架式 / 投影式 / 能级图 / 配合物结构 / 催化循环框图 / 坐标轴），
机械可转空间已尽。阶段 5（→ 图片）另立卡，见
`00-首页/活跃任务/任务卡-2026-09-11-讲义ASCII图转图片.md`。
