# 7076 项「已删除」变更评估报告

> 生成时间：2026-09-12 21:05
> 评估对象：`git status` 中 7076 个 `D`（deleted）条目
> 结论：**未发生数据丢失。7072/7076 是目录重构（迁移/改名）在 git 侧的投影，4/7076 是标题清洗后的旧版本。**

---

## 一、一句话结论

这 7076 项**不是删除**，而是**一次跨 2 周进行的目录重构（reorganization）**：
文件被**移到新位置**或**改名**，git 只看到"旧路径消失 + 新路径未跟踪"，于是标成 `D`。
**关键证据：7072 个被删文件的"原文件名"在磁盘其他位置依然存在**（脚本逐一比对）。

| 判定 | 数量 | 占比 |
|:--|--:|--:|
| 文件名在磁盘他处存在（迁移/改名） | **7072** | 99.94% |
| 文件名不存在（旧版本被新版取代） | **4** | 0.06% |
| **真丢失** | **0** | **0%** |

---

## 二、逐项迁移映射

| 被删（旧位置） | 数量 | 现状（新位置） | 判定 |
|:--|--:|:--|:--|
| `一化 高中化学/` | 2564 | `06-外部资料导入/一化 高中化学/`（2564 文件） | ✅ 数量**完全吻合** |
| `（教师用）人教版高中化学课本/` | 1575 | `06-外部资料导入/人教版教材/` | ✅ 共 2889 = 1314+1575，**完全吻合** |
| `人教版高中化学课本/` | 1314 | ↑ 同上 | ✅ |
| `结构化学基础/` | 1551 | `06-外部资料导入/结构化学基础/`（1555 文件） | ✅ **+4**（含新增） |
| `09-审计报告/历史任务卡/`（18 个具名卡） | 18 | `00-首页/归档/` 同名文件 | ✅ 已定位 |
| `09-审计报告/归档/结构化学基础题库-20260827/`（7） | 7 | `09-审计报告/` 根下同名（移出归档层级） | ✅ 已定位 |
| `09-审计报告/归档首页/`（4） | 4 | `00-首页/` + `00-首页/归档/` | ✅ 已定位 |
| `13-教案/必修一/*.md`（11 个旧名） | 11 | 重命名为 `*（合订版）.md` + 拆分为 50 文件 | ✅ 见 §三 |
| `media/*.md`（9） | 9 | `06-学生侧材料/讲义/media/` | ✅ 已定位 |
| `00-首页/题组Word/习题书/…`（6） | 6 | 标题清洗（去「第三篇/第四篇-…-」前缀） | ✅ 见 §三 |
| `无机化学下册/`、`高中化学竞赛笔记/` 等尾部若干 | ~14 | 迁至 `06-外部资料导入/` 对应子目录 | ✅ 已定位 |

**迁移目标目录的 mtime**：`06-外部资料导入/人教版教材` = 09-06 11:11、
`06-外部资料导入/一化 高中化学` = 08-16、`00-首页/归档` = 09-07 22:43
→ **全部早于本会话（09-12 18:27 起）**，非本会话所为。

---

## 三、两类特殊变更（非搬运，需注意）

### 3.1 `13-教案/必修一/`：重命名 + 拆分（11 → 50）

旧名与新名的对应关系：

| 旧名（git 中已删） | 新名（磁盘上，未跟踪） |
|:--|:--|
| `1.1 物质的分类及转化.md` | `1.1 物质的分类.md` + `1.1 物质的分类及转化（合订版）.md` |
| `2.5 氯气的实验室制法、氯离子检验与卤素.md` | `2.5 氯气的实验室制法.md` + `2.5 …（合订版）.md` |
| `2.10 物质的量在化学计算中的应用.md` | `2.10 一定物质的量浓度溶液的配制.md` + `…（合订版）.md` |
| 其余 8 个 | 对应 `*（合订版）.md` |

**且大幅扩充**：新增 `2.1 钠`、`2.2 钠的氧化物` … `3.1 铁的单质` … `4.8 分子间作用力与氢键`
及 `必修一 教案总览.md`、`第一章 学情分析.md` 等，**11 → 50 文件**。
这与 `lesson-plan-split` 技能（合订版按主题边界拆分）的设计一致，属**既定工作流**。

### 3.2 习题书 docx：标题清洗（6 个）

| 旧名 | 新名 |
|:--|:--|
| `第三篇-有机化学-1-结构基础与波谱分析-学生版.docx` | `1-结构基础与波谱分析-学生版.docx` |
| `第三篇-有机化学-1-结构基础与波谱分析-教师版.docx` | `1-结构基础与波谱分析-教师版.docx` |
| `第三篇-有机化学-1-…-学生版-打印版.docx` | `1-…-学生版-打印版.docx` |
| `第四篇-元素与分析-1-结构基础与波谱分析-*.docx`（3） | **该章已从第四篇移除** |

**第四篇那 3 个是"误放归位"**：`1-结构基础与波谱分析` 属第三篇（有机化学），
却曾出现在第四篇（元素与分析）目录下；第四篇现行目录从 `1-化学基础与计量` 起，无此章。
新版 docx **已于今天 20:37-20:38 重新生成**（mtime 可证）。

---

## 四、风险与后续处理建议

### 4.1 风险评估

| 风险 | 级别 | 说明 |
|:--|:--:|:--|
| 数据丢失 | **无** | 0 个真丢失，全部可定位 |
| 仓库状态混乱 | **高** | 7076 D + 490 M + 66 ?? 长期挂在工作区，`git status` 失去可用性，且**极易误提交**（如 `git add -A` 会一次性把 7076 删除固化） |
| 迁移未完成 | **低** | 根目录 `结构化学基础/` 残留 **4 个文件**未迁移 |
| 并入并行会话冲突 | **中** | 若有其他会话基于旧路径工作，会找不到文件 |

### 4.2 后续处理（建议按序）

**P0 — 冻结风险（立即）**
- **禁止使用 `git add -A` / `git add .` / `git commit -a`**。只 add 明确列出的文件（本会话已遵守此铁律）。
- 建议在 `~/.workbuddy/MEMORY.md` 固化该条，防止其他会话误操作。

**P1 — 把重构正式入账（需你拍板）**
这批重构是**真实、合理、已完成**的整理工作，只差一次提交让它从"工作区噪声"变成"版本历史"。
建议**单独一次提交**（不与其他内容混）：

```
docs: 目录重构入账 —— 外部资料归入 06-外部资料导入、任务卡归档、教案重命名拆分
```

选项：
- **(A) 全量入账**：`git add -A`（会把 7076 D + 490 M + 66 ?? 一次性固化）——最干净，但需先确认 490 M 里无并行会话的半成品。
- **(B) 只入账删除与迁移**：用 `git add -u` 处理 D/M，未跟踪的 66 项另行确认——更稳。
- **(C) 暂不入账**：维持现状，但必须严守 P0。

**我不自行执行**——这涉及 7000+ 文件的批量变更，且可能包含并行会话未完成的工作，属高风险操作，需你明确授权。

**P2 — 清理尾巴（可立即做，低风险）**
1. 根目录 `结构化学基础/` 残留 4 个文件 → 确认是遗漏还是有意保留；
2. `.gitignore` 建议补入 `06-外部资料导入/` 中体积巨大的 OCR 中间产物（现行 4744 文件）；
3. `媒体仓库/` 已在 `.gitignore`（第 9 行），但 `06-学生侧材料/讲义/media/` 未忽略 → 确认是否该入库。

---

## 五、验证方法（可复现）

```python
# 逐一取 D 路径的 basename，在磁盘全库（排除 .git）中查找
# 7072/7076 命中 → 迁移；4/7076 缺失 → 标题清洗
# 见 .workbuddy/tmp/assess_deletions.py
```

关键交叉验证：
- `06-外部资料导入/一化 高中化学/` 实测 **2564** 文件 = 删除数 **2564**
- `06-外部资料导入/人教版教材/` 实测 **2889** 文件 = 1575 + 1314
- 具名任务卡 `find` 命中 `00-首页/归档/`
- 迁移目标目录 mtime 均 **早于本会话起始时间**

---

## 六、附：490 个 M 与 66 个 ??

### 6.1 四百九十个 M 的构成

| 扩展名 | 数量 | 说明 |
|:--|--:|:--|
| `.md` | 284 | **习题书源文件**（`04-课件/习题集/习题书-学生版|教师版/` 各篇各章） |
| `.docx` | 97 | 习题书导出产物（`00-首页/题组Word/习题书/`） |
| `.jsonl` | 5 | `02-数据库/quality-metrics.jsonl` 等 |
| `.py` | 4 | `build_exam_paper.py`、`build_print_student.py`、`build_module_book.py`、`gate_exercise_books.py` |

**⚠ 关键判断：这 490 个 M 来自"习题书重出"产线，本会话未触碰。**

- 本会话（09-12 19:24 起）只改动了：5 个讲义 md、1 个索引、2 张任务卡、5 个讲义 docx。
- 而 490 个 M 里含 284 个习题书 md + 97 个习题书 docx + 4 个组卷脚本，
  **正是本会话之外的产线**（记忆中的"两条消费端产线：习题书自动聚合"）。
- 其 mtime 集中在 **09-12 20:09-20:38**，与本会话时间**重叠** → 判定为**并行会话同时在跑**。

→ **这 490 个 M 绝不可由本会话提交**，否则会裹走并行会话未完成的半成品（违反既定铁律：
"只 add 本会话改动文件、不动并行会话未跟踪文件"）。

### 6.2 六十六个 ?? 的构成

| 分组 | 数量 | 说明 |
|:--|--:|:--|
| 目录重构新位置 | 61 | `06-外部资料导入/*`（4）、`00-首页/归档/`、`13-教案/必修一/*`、各 README |
| 本会话/近期 memory | 6 | `.workbuddy/memory/2026-09-07..12.md` |
| auto-validation 报告 | 6 | `09-审计报告/auto-validation/2026-09-06..12-validation.md` |

→ 前 61 项**必须与 7076 删除项成对处理**（只删不增 = 实际丢失）；后 12 项为日志类，可独立入账。

---

## 七、最终处置清单

| 优先级 | 动作 | 责任方 |
|:--:|:--|:--|
| **P0** | 禁用 `git add -A` / `git add .` / `git commit -a`（工作区含 7000+ 未定变更） | 全部会话 |
| **P0** | 本会话（讲义产线）**不提交** 490 M + 61 ?? | 本会话 |
| **P1** | 目录重构入账：需先确认**并行会话（习题书产线）已收工**，再由**单一会话**统一提交迁移（D+?? 成对） | 待你调度 |
| **P2** | 根目录 `结构化学基础/` 残留 4 文件 → 确认补迁或有意保留 | 你 |
| **P2** | `.gitignore` 评估补入 `06-外部资料导入/*/OCR 中间产物`（体积 4744 文件） | 你 |
| **P2** | 确认 `06-学生侧材料/讲义/media/`（9 个 md）是否该入库 | 你 |

**再次强调：本次评估只读，未对任何文件做增删改；未执行任何 git add/commit/stash。**
