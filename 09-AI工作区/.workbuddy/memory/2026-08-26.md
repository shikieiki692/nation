# 2026-08-26

## 学生讲义Word 排版美化（化学综合测试卷）
- 源文件：`00-首页/学生讲义Word/第一轮-4.5小时综合测试（A卷高考预赛+B卷竞赛）.docx`
- 交付：`..._打印版.docx`（同目录，原文件未改动）。
- 做法：tencent-docx 的 `beautify_only` 流水线在本环境不可用（format-extract 的 `dist/cli.cjs` 缺失，html-to-docx 需联网装 venv）。改走**内容保全式直接重排**（python-docx，托管 venv：`~/.workbuddy/binaries/python/envs/default`）。
- 改动点：A4+干净页边距；标题/卷头/大题三级层级（黑体+底纹+分隔线）；题号与题源标签加粗；表框+表头底纹；页眉标题+页脚"第X页/共Y页"域；图注灰字。
- 校验：183 非空段落文本完全一致，28 图 / 5 表均保留。
- 复现脚本留档：`09-AI工作区/restyle_doc.py`。

## 字体与第二轮微调（_打印版.docx 继续修改）
- 需求：除中文和中文符号外统一 Times New Roman；化学式/公式写法标准。
- 关键发现：原文公式已用 `vertAlign` 正确上下标（1119 个 run 带 vertAlign，0 个用 Unicode 上下标字符）；非标准之处仅在于上一轮把全部 Latin/数字设成了宋体，导致公式数字像中文数字。
- 做法（脚本 `09-AI工作区/apply_fonts.py`，仅改字体不碰文字/上下标）：遍历全部 run（正文/表格/页眉/页脚），保留各 run 原有 eastAsia（宋体/黑体），把 ascii/hAnsi/cs 设为 Times New Roman。
- 校验：5229 个 run 全部 ascii=Times New Roman，0 个异常；eastAsia 仅 宋体(5039)/黑体(54)；vertAlign 仍 1119。元素符号大小写、⇌/↑/↓ 箭头均本就标准。
- 说明：反应式现用 "=" 而非 "→"（中文卷常见写法），未强行替换以免误伤数学等号；如需统一成 "→" 可再处理。

## 第三轮：原文件也应用同样处理（in-place）
- 用户要求把源文件 `第一轮-4.5小时...docx` 也改成同款（美观 + 非中文 TNR）。
- 先备份为 `...（原版备份）.docx`（同目录）；改 `restyle_doc.py`：`set_run_font` 改为 split 字体（ascii/hAnsi/cs=Times New Roman，eastAsia 保留宋体/黑体），OUT 改回源路径覆盖写入；补 `fix_fields.py` 把页脚域代码(PAGE/NUMPAGES) run 也设 TNR。

## PPTX 模板换皮引擎升级（物质的分类）
- 需求：把已有课件套上统一模板，**美观 + 不改动原内容 + 自有设计**，且能套到其它不同 PPT。
- 关键评估：标准模板 `高中化学课件模板.pptx` 只有 1 母版/1 版式、主题色还是 Office 默认，真正"好看"靠每页直接画上的装饰形状；母版挂接路线走不通，只能走**规格驱动、逐页重绘皮肤**。
- 交付产物（均在 `09-AI工作区/pptx-化学模板/review-gai/`）：
  - `template.yaml` —— 墨绿+米白编辑式设计规范（颜色/字体/外壳几何/标题页规则）。
  - `reskin2.py` —— 规格驱动引擎，相比旧 `reskin.py` 的改进：
    1. 读 `sldSz` 动态算坐标，任意画布尺寸通用；
    2. 遍历全部母版/版式；
    3. 语义清理源母版顶部横幅图（`cx>0.8W & cy<0.15H`），不崩；
    4. 字体映射表：ea→微软雅黑，latin 仅替换衬线/旧字体为 Arial，跳过数学/符号字体；
    5. 外壳插在"背景之后、内容之前"（全屏背景图之后），不遮挡；
    6. 标题/封面/末页：删全屏背景图→满版墨绿+文字转米白；
    7. 清理母版/版式里的 `file:///` 孤儿外链和悬空图片 rel。
- 验收（《物质的分类》）：
  - 校验通过，无 relationship CRITICAL；
  - 30 页文字 0 丢失，每页仅新增 1 个页码文本；
  - 封面、末页满版墨绿，内容页统一米白底+左侧墨绿竖条+琥珀进度条+标题区琥珀细线+页脚页码。
- 泛化测试：用 `高中化学课件模板.pptx`（1 母版/1 版式，结构完全不同）跑通，20 页均正确应用。
- 输出文件：`1.1.1 物质的分类-【模板版v2】.pptx`（review-gai 目录 + 已复制到桌面源目录）。
- 校验：与备份全文 275 段完全一致；7852 个 run 全部 ascii=Times New Roman；vertAlign 上下标 1156 处保留；唯一非宋/黑 eastAsia 是空格 run，无影响。
- 现在源文件、_打印版 两份均为同款美化+字体；原版备份保留可回退。

## 高中化学 PPT 换皮（物质的分类）
- 源文件：`桌面/高中化学课件/必修一（2）/3536352/1.1.1 物质的分类-...pptx`
- 交付：`.../1.1.1 物质的分类-【模板版】.pptx`（同目录，原文件未改动）。
- 做法：严格按 `pptx-化学模板/review-gai/README.md` Mode A（已有课件换皮）；用 `.venv/Scripts/python.exe` 跑 `unpack.py → reskin.py → validate.py → pack.py`。
- 改动点：背景米白、顶部横幅墨绿、主题色板、字体统一为微软雅黑/Times New Roman、每页左侧墨绿竖条+橙黄进度段+底部分隔线+右下页码。
- 修复源文件缺陷：删除 `slideMaster1/2.xml.rels` 中两个指向不存在 `file:///D:\qq文件\...png` 的孤儿外链，避免 Office 报文件损坏。
- 校验：30 页完整，0 处内容文字丢失，每页仅增加 1 个页码文本。

## Desktop 卷：删★ + 横线转下划线 + 非中文 TNR
- 文件：`桌面/第一轮-4.5小时综合测试（A卷+B卷）.docx`，先备份为 `...（备份）.docx`。
- 需求：删除 ★；把 ＿＿（U+FF3F 全角低线填空）统一转成"真实下划线格式"；除中文外字体全 Times New Roman（含"少数中文字符"——实为原文件已有 1 个 run 中文用 TNR，保留不动）。
- 做法（脚本 `09-AI工作区/process_desktop.py`）：遍历所有 run（正文/表格/页眉/页脚），① 删 ★；② 把连续 ＿ 串替换成等量空格并设 run underline=single（中段 ＿ 拆分出新 run）；③ 每个 run 的 ascii/hAnsi/cs 设 Times New Roman，eastAsia 不动。
- 校验：★ 剩余 0（原 180）、＿ 剩余 0（原 136）、下划线空白 run 48、ascii 非 TNR 0、eastAsia 非中文/异常 0；与备份逐段对比 215 段 0 处差异（仅 ★ 删、＿→空格+下划线）。
- 该文件原本 ascii 已是 Times New Roman，第 3 项基本已满足，仍强制统一收口。

## Desktop 卷 第二轮：综合精修（最终版）
- 同一桌面文件，从 `（备份）` 纯净版重跑，一次性应用：删 ★、填空横线加长（每 ＿→3 个下划线空格，3×）、去题目来源、去答案、修 μ 宋体、转 A3 双栏。脚本 `09-AI工作区/finalize_desktop.py`（取代旧 process_desktop.py）。
- **关键坑1（μ 仍宋体）**：根因是源 run 的 `<w:rFonts w:hint="eastAsia">`——hint 强制把希腊/符号也按 eastAsia(宋体) 渲染，覆盖已设好的 cs=Times New Roman。修复=全局删除 w:hint。希腊μ/π/Δ/α/β/ρ 等现全 TNR（hint=0, cs=TNR 校验通过）。
- **关键坑2（来源去不干净）**：来源括号 `（2024 山东卷·题4，★★）` 在真实文档里被拆到多个 run（"（2024 山东卷·题4，" 与 "★★）" 分属两 run），逐 run 正则匹配不到完整括号。修复=在**段落级**拼接全文做 SRC_PAT 匹配再跨 run 裁掉字符；空 run 直接删除。结果来源 0 残留。
- **关键坑3（图片丢失）**：process_run 曾把"无 w:t 的 run"当空文本删掉，误删了图片 `<w:drawing>` run。修复=无 w:t 的 run 直接跳过不删。27 图→剩 26（1 张在答案块内随答案删除，正确），超大图(168/127mm)缩到 118mm 适配栏宽。
- 去答案：删两段"参考答案与解析"块（A卷块[A卷答案头..B卷头前]、B卷块[B卷答案头..文末]），共删 57 段；A卷/B卷 标题保留，题目 1–56 完整。
- 转 A3 双栏：3 个 section 全设 297×420mm、左右18mm、栏距 680twip(≈12mm)；3 个表格设 tblW pct=5000 适配栏宽。
- 校验：★0、＿0、答案0、来源0、hint0、cs≠TNR0；符号 run 10 全部正常；图26(≤118mm)；题号1–56；段落122(原179)。

## Desktop 卷 第三轮：A3 试卷结构精修（当前版）
- 同一桌面文件（已是 A3 双栏+去★/来源/答案+TNR），进一步"像试卷"：脚本 `09-AI工作区/beautify_exam.py`（在已清理文件上就地改，未改文字）。
- 结构：拆成 4 个 section——S1 单栏(标题+「A卷」卷头，占满整幅 A3 宽)、S2 双栏(A卷题目)、S3 单栏(「B卷」卷头，满宽)、S4 双栏(B卷题目)。卷头/标题不再挤在栏内，真正"满宽试卷头"。连续分节符(continuous)不强制换页。
- 间距：全局行距 1.4；题号段落前距 8pt；选项行后距 3pt；大题标题(Heading2)前14后6、卷头(Heading1卷头)前后8、主标题后10。
- 美化：主标题 20pt 加粗+底部蓝线(2.25pt)；「A卷/B卷」卷头 15pt 加粗+浅蓝底(DEEAF6)+蓝线；大题标题 13pt 加粗+浅灰底(F2F2F2)+灰线。共 12 个标题带底纹/线。
- **坑(已修)**：build_sectPr 初版把 pgSz/pgMar/栏距写成 EMU 而非 twips，导致页面尺寸被放大到天文数字。修复=用 mm_tw(mm)=round(mm*1440/25.4) 转 twips 重写。现校验：4 节全 16838×23811tw(=297×420mm)、边距18mm、栏距12mm、文字0改动(对比 `（结构优化前）` 副本)。
- 桌面现有 3 份：`.docx`(当前版)、`（备份）`(原始含★/来源/答案)、`（结构优化前）`(本轮前已清理版，可回退)。

## Desktop 卷 第四轮：B卷另起页 + 页脚页码（当前版）
- 同一桌面文件，脚本 `09-AI工作区/add_footer_break.py`（就地改，未动文字/栏布局）。
- B卷另起一页：定位「B卷」卷头前的 section break(在最后 A卷题目段)，把其 `<w:sectPr><w:type>` 由 `continuous` 改成 `nextPage`。校验 S1→S2 断点=nextPage。
- 页脚页码：4 个 section 各加 footer，内容 `第 ` + PAGE域 + ` 页 / 共 ` + NUMPAGES域 + ` 页`，居中、9pt；run 字体 ascii/hAnsi/cs=Times New Roman、eastAsia=宋体，并删 w:hint。PAGE/NUMPAGES 用 begin/instrText/end 标准域写法，Word 打开自动更新。
- 校验：4 section 各自 footerReference→footer4..7（均含正确域与"第 页 / 共 页"文字）、数字字体 0 异常；与 `（加页脚前）` 副本逐段对比 150 段 / 36 表格单元格 0 处差异。
- 注：包里有 3 个空 footer 孤儿部件(footer1-3)，未被任何 sectPr 引用，无害，未强行删除以免损坏包。

## Desktop 卷 第五轮：卷头去特殊格式 + B卷题距加大（当前版）
- 同一桌面文件，脚本 `09-AI工作区/restyle_headers.py`（就地改，保留页脚与 B卷 另起页）。
- A卷/B卷 卷头：删 `<w:shd>`(浅蓝底) 与 `<w:pBdr>`(蓝底线)，保留 加粗+居中+15pt。校验 shd=False pBdr=False bold=True align=CENTER。
- B卷 题目（idx_bvol 之后、匹配 `^\d+[.．]` 的 20 道题）：space_before 8pt→14pt，题间距更大。
- 校验：A卷→B卷 断点仍 nextPage、4 节页脚域 intact、段落 150 不变（仅改格式/间距）。

## Desktop 卷 第六轮：修复「卷头与题目间大间距」（当前版）
- 同一桌面文件，脚本 `09-AI工作区/fix_gap.py`（就地改，留 `（修间距前）` 副本可回退）。
- **根因**：原结构把"标题+A卷"单独放在一个 **1 栏 section**（PARA1 的 sectPr `cols=1`），题目在 2 栏 section，二者用 **continuous 连续分节符**连接——单栏→双栏的连续断点会在 Word 里产生一大段纵向空白。B卷 同理（PARA112 sectPr `cols=1`）。所以 A卷、B卷 卷头下面都空一大块。
- **修复**：① 把承载 sectPr 的卷头段落所在节由 1 栏改为 **2 栏**（PARA1、PARA112 的 sectPr `cols=2`）；② 给标题/PARA1「A卷」/PARA112「B卷」加 `w:pPr/w:cols w:span="2"`，让它们仍**跨双栏满宽**显示；③ 顺手把卷头 after(160→120)、紧跟卷头的大题标题 before(280→160) 收一点。
- 关键点：插入 `w:cols` 必须放在 `w:sectPr` **之前**以满足 OOXML schema 顺序（用 addprevious）。
- 校验：4 节全部 `cols=2`（无残留 1 栏→消除单→双过渡间隙）；标题/A卷/B卷 span=2；B卷 nextPage 仍在(PARA111)；footer 部件仍在；与 `（修间距前）` 逐段对比 150 段 / 11 表 0 差异。

## Desktop 卷 第七轮：二次修复卷头大间距（当前版，仍未彻底）
- 同一桌面文件，脚本 `09-AI工作区/fix_gap2.py`（就地改，留 `（二次修间距前）` 副本）。
- **真正根因（第六轮判断错了）**：卷头（A卷 PARA1 / B卷 PARA112）各自带着 sectPr，等于把"标题+A卷"和"B卷"分别隔离成**只含满宽(span=2)内容的独立小 section**。即使已改 2 栏，Word 在"前一段落是 span=2 且 section 结束"的连续断点处仍会注入一大段纵向空白——单→双只是表象，本质是**卷头独占一个 section**才会空一大块。
- **修复**：删除 PARA1、PARA112 的 sectPr，把卷头合并进题目所在的 section；只保留 PARA111 的 `nextPage` 2 栏 sectPr（B卷 另起页）+ body sectPr（B卷 题目段）。结果：**全文仅 2 个 section**，A卷/B卷 不再是独立节，后面直接接双栏题目，无断点空隙。
- 卷头仍 `span=2` 满宽；两 section 均 2 栏、各带 footer（rId38 / rId40）；B卷 nextPage 保留。题距收一点：卷头 after=80、紧跟大题标题 before=120（合计≈3.5mm，正常试卷间距）。
- 校验：section 数=2（PARA111 + body）、标题/A卷/B卷 span=2、无残留 sectPr 在卷头、footer 在、与 `（二次修间距前）` 逐段 150 段/11 表 0 差异。
- 注：若打开仍见空隙，则可能是 Word 渲染缓存，按 Ctrl+A 全选→Ctrl+Q 清除段落格式、或打印预览刷新；结构层面空隙已消除。

## Desktop 卷 第八轮：处理「最终」版（用户新给的文件）
- 文件：`Desktop/第一轮-4.5小时综合测试（A卷+B卷）最终.docx`（先备份为 `（备份）`，脚本 `09-AI工作区/finalize_final.py`，从备份重跑）。
- 该「最终」版与之前工作版不同：中文已是仿宋、非中文已是 TNR，但仍有隐患（4 个 run 带 `w:hint="eastAsia"` 会让 μ/π 等又错渲成宋体；1 个 run 无 ascii 字体）。
- **重新编号**：原编号有缺号(缺2/5/21/22/34/35…)且重复"38"(两段均标38)，还有"52."被拆成 "5""2""." 三个 run（单 run 正则匹配不到）。改用**跨 run 累积**算法：定位首个含终止点的 run(k)，清空 0..k-1，把 k 改写为"新号."+后缀。结果整卷连续 **1..43**（A卷1–28、B卷29–43），无缺号无重复。
- **字体（彻底）**：遍历 package 所有 part 的全部 `<w:r>`（正文/表格/页眉/页脚/文本框），设 eastAsia=仿宋、ascii/hAnsi/cs=Times New Roman，删除 `w:hint`；另设 docDefaults 兜底。校验 bad_eastAsia=0 / bad_ascii=0 / hint_left=0。
- **B卷**：去掉 nextPage（已连续），在 B卷 卷头前插 **2 个空段落（隔两行）**。
- 校验：题目序列 1..43 连续=True；B卷 前 2 空行=True、nextPage=0；与备份**逐段对比 43 道题干（去掉题号后）完全一致**——只改了题号与字体，文字/化学式/表格/图未动。
- 提醒点：本用户偏好"仔细"，逐 run 处理符号字体 + 跨 run 编号是这套化学卷排版的可靠做法，已固化进 finalize_final.py。
