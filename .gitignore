.obsidian/
.trash/
.claude/
.claudian/
kb-vault-mcp/node_modules/
pptx-workspace/node_modules/
.kb/
09-审计报告/备份/
媒体仓库/

.DS_Store
Thumbs.db

__pycache__/
*.pyc

# PDF 不同步（不提交到远程）
*.pdf
*.PDF

# 图片检索/下载脚本临时产物
search_results.txt
mismatches.json
md_images*.txt
mapping_flat.txt
download_results*.json
all_results.json

# 扫描书页图（只同步 OCR 文本，不同步书页图）
/无机化学下册/**/*.jpg
/中级无机化学/**/*.jpg
/各省预赛题目/**/*.jpg

# 构建依赖与虚拟环境（2026-08-25 审计补充）
node_modules/
.venv/

# 与知识库无关的个人目录
eiki/
copilot/
.agents/

# 构建/工具工作区（2026-09-06 审计补充）
pptx-workspace/
scripts/
skills/
# 例外：11-模板/scripts/qa/ 是本库正式的 QA 脚本目录（2026-09-15 固化）
# 注意 git 规则：父目录被排除时无法用 ! 反向包含子文件，故须先放行父目录、再逐层收窄
!11-模板/scripts/
11-模板/scripts/*
!11-模板/scripts/qa/
!11-模板/scripts/qa/**
# 格式线·闸门工具箱（2026-09-21 固化）：这些是**正式工具**，须逐条反向放行。
# ⚠️ 不写反向规则时，`git add` 会**静默跳过**该目录下的新文件（只提示被 ignore）——
#    曾因此让一次提交漏掉 render_gate.py / md_sanitize.py，须用 git add -f 补提。
#    判据：`git check-ignore -v --no-index <路径>`（不带 --no-index 对已跟踪文件不报，会误判为「OK」）。
!11-模板/scripts/render_gate.py
!11-模板/scripts/md_sanitize.py
!11-模板/scripts/import_gate.py
!11-模板/scripts/audit_deprecated.py
!11-模板/scripts/render_gate_allowlist.txt
!11-模板/scripts/hooks/
!11-模板/scripts/hooks/**
# 回堵：上面的 ! 会越过第 14 行的 __pycache__/ 规则，这里再排除一次
11-模板/scripts/qa/__pycache__/

# 临时构建产物（2026-09-01）
.tmp-*/
.preview_build*/
_tmp_*
suspects.txt
mis_split_suspects.txt
images_in_q.txt

# 批量修改前的 md 备份快照（2026-09-01）
.workbuddy/backups/
# 体检/恢复脚本的运行时中间产物（2026-09-01，allobj.txt 18MB+ 仍会膨胀）
.workbuddy/tmp/

# 校验器每次全量重生成的依赖图（6.5 MB，每次提交 churn 约 20 万行）
02-数据库/dependency-map.json

# Word / Office 打开文档时生成的临时锁文件（~$xxx.docx），非真实产物
~$*.docx
~$*.doc
