完整流程现在就是：

1. 讲义源文件写在 `04-课件/学生讲义/`，图片统一写成 `![[media/xxx.jpg]]`
2. 先跑预检：`pdf_preflight.py`
3. 再跑主编译：`convert_handout_to_pdf.py`
4. 脚本会自动做 `preprint()`、同步 `media/` 到 `11-模板/scripts/.chem_media/`、调用 Pandoc + Lua filter、再走 XeLaTeX 两遍
5. PDF 输出到 `04-课件/学生讲义/同名.pdf`

- 图片源文件放在：`C:\Obsidion\妙妙屋\media\`
- Markdown 里统一这样写：`![[media/xxx.jpg]]`
- 编译时脚本会自动复制到：`C:\Obsidion\妙妙屋\11-模板\scripts\.chem_media\`
- PDF 里的图片不是“外部地址”，而是从 `.chem_media` 读入后直接嵌进 PDF 里



![[NO3-共振结构.lewis.png]]`