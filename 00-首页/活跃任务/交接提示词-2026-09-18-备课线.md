# 新会话交接提示词 · 备课线下一阶段（2026-09-18）

> **用法**：把本文件「§一 提示词正文」整段复制到新对话首条消息即可。
> **本文件仅作留档**，放于 `00-首页/活跃任务/`，不参与闸门校验。

---

## §一 提示词正文（复制以下全部内容）

```
你接手「妙妙屋化竞知识库」备课线的一条续接工作。请严格按下面的事实基线、
任务清单、纪律铁律执行。**先读 §二 事实基线核对现状，再动手；不许凭记忆假设文件存在。**

工作目录：C:\Obsidion\妙妙屋（Obsidian 库，git 仓库 master 分支）
身份：备课线执行助手（本库另有题库线会话在并行跑，边界见 §四）

---

【一、本线当前状态（2026-09-18 实测，HEAD 7750da0d9）】

已闭环（勿重做）：
1. 《未央化学竞赛课程计划（初二至高二）》§三 章后课 **12/12 齐备**（6 大章 × 复习课+习题课）
   - `04-课件/复习课/` 六章复习课：`第一轮化学原理复习` / `第一轮结构化学初步复习`
     （另有 `-v2` 版并存）/ `第二轮元素化学复习` / `第二轮分析化学复习` /
     `第三轮有机化学复习` / `第三轮结构物化深化复习`
     （目录内另有 `README.md`、`2026-05-23-酸碱理论复习.md`、`第一轮化学原理总览与导航.md`
     三份非章后课文件，共 10 个 .md）
   - `04-课件/习题课/` 六章习题课（同名六章）+ 另有 `2026-06-06-真题模拟拆解-冲刺班.md`、
     `README.md`（共 8 个 .md）
2. 课程计划「教材来源」列 → 提炼页对标 **99/103 命中**（未命中 4 条属正常的合并页/口径差异）
3. 四大分册提炼可回溯：第一分册 10/10、第二 10/10、第三 10/10（第六~十讲合并为一份
   `提炼-上海中学竞赛课程-第三分册-过渡金属`）、第四 13/13
4. Zchem 5 份网课资料索引页「三、提炼状态」8 处 ⏳ 已于本日回填为实际产物 wikilink
   （commit 22d940bf3），并新建了 `07-资料提炼/网课资料/README.md` 导航页
5. 《课程计划配套产物齐全性核查》报告已完成 → `09-审计报告/2026-09-18-课程计划配套产物齐全性核查.md`
6. 《课程计划下一步规划》已产出 → `00-首页/活跃任务/计划-2026-09-18-课程计划下一步规划.md`（**先读这份**）

---

【二、事实基线（动手前必须自己复核一遍）】

必读文件（按顺序）：
1. `00-首页/活跃任务/计划-2026-09-18-课程计划下一步规划.md`（本线主计划，缺口清单在此）
2. `09-审计报告/2026-09-18-课程计划配套产物齐全性核查.md`（齐全性结论与证据）
3. `备课思路/未央化学竞赛课程计划（初二至高二）.md`（课程计划正本，§二 各模块表 + §三 章后课）
4. `备课思路/六大新课大章节总表.md`（**§4.1 是缺口权威基准**，§11 是章后层标准）
5. `备课思路/备课与教学思路待办.md`（**执行真相源**，§五 是真实待办；注意它的状态栏须与实际一致）
6. `00-首页/活跃任务/计划-2026-09-16-备课线下一阶段.md`（§十.2 待拍板项、§十.3 可自主推进项）

目录结构事实（勿臆测）：
- 学生讲义现役 **77 份**（`find "04-课件/学生讲义" -name "*.md" -not -path "*_归档*"`）
  子目录：`化学原理/`（12 份）`有机化学/`（20 份）`结构化学/`（含 `分子结构/` `晶体学/`
  `配位化学/` 子目录）`元素与分析/` `数学工具/` `_综合/`；另有 `_归档/`（弃用区，勿引用）、
  `_待升级/`（勿动）
- 教学逻辑提炼 **62 份**（`07-资料提炼/教学逻辑提炼/`，13 个课程源子目录）
- 网课资料：`07-资料提炼/网课资料/`，含 README.md（本日新建）+ 10 个子目录

---

【三、任务清单（按优先级；P1/P2 需先请示拍板）】

▍P1 章级缺口（内容层，需创作——**开工前先请示用户是否本轮做**）

P1-1 【第三轮有机化学】`芳香反应` 缺独立学生讲义
  - 现状实测：`04-课件/学生讲义/有机化学/` 下**无** `芳香反应*.md`；
    最接近的是 `芳香烃与卤代烃.md`（基础班口径，含卤代烃，非竞赛专题讲义）
  - 但 `04-专题与题型/专题/专题-芳香反应.md` 已达「已审校」→ **讲义侧是短板**
  - 依据：`六大新课大章节总表.md` §4.1 表 + 课程计划 §二.6

P1-2 【第三轮结构物化深化】`结构表征与波谱桥` 待建
  - 现状实测：`04-课件/学生讲义/结构化学/` 下无「表征/波谱桥」类文件；
    有机侧有 `有机化学/有机波谱分析.md`，但**结构线（XRD/磁性/光谱）到有机波谱之间无桥接章**
  - 依据：`六大新课大章节总表.md` §4.1 末行

P1-3 【第三轮结构物化深化】`配位光谱与对称性桥` 待建
  - 现状实测：`find "04-课件/学生讲义" -iname "*对称*"` **零命中**；
    `结构化学/配位化学/` 下仅 3 份（配位化合物基础-第一轮基础版 / 配位化合物基础-超级充实版 /
    配合物深化-超级充实版），**无光谱桥章**
  - 依据：同上

▍P2 工具课/桥课缺口

P2-1 【第一轮化学原理】`化学计量与计算规范` 现役无独立讲义
  - 实测：现役区无同名页，仅 `_归档/2026-06-23-化学计量与计算规范.md`（**弃用区，不可作现役引用**）
  - 邻近现役落点：`化学原理/气体基础-超级充实版（自学完整）.md`

P2-2 【第一轮化学原理】`沉淀溶解平衡` 现役无独立讲义
  - 实测：仅 `_归档/2026-06-23-沉淀溶解平衡.md`；邻近为 `化学原理/酸碱理论-超级充实版（自学完整）.md`（多平衡段）

P2-3 【第一轮化学原理】`离子反应` 现役无独立讲义
  - 实测：仅 `_归档/2026-06-23-离子反应.md`；邻近为 `酸碱理论-超级充实版` +
    `04-课件/习题集/四·成书层（习题书）/习题书-学生版/第四篇-元素与分析/2-离子反应与方程式`

P2-4 【第三轮有机化学】`有机合成与逆合成` 缺主讲义半边
  - 金属有机侧已有 `有机化学/过渡金属催化.md`；逆合成半边仍散

> **P2 三项的共同形态**：`04-课件/学生讲义/_归档/` 下确实存有 4 份同名弃用页
> （`2026-06-23-化学计量与计算规范.md`、`2026-06-23-沉淀溶解平衡.md`、
> `2026-06-23-离子反应.md`、`2026-06-23-氧化还原反应.md`）
> → **弃用区不可作现役引用**，故这三项在「现役区」等于缺位。
> ⚠️ 其中 `氧化还原反应` 疑已被 `化学原理/电化学基础-超级充实版（自学完整）.md` 实质吸收
> → 建议优先「标注缺口」而非新建（**待用户拍板**）

▍P3 维护（低风险，可自主推进，无需拍板）

P3-1 `07-资料提炼/网课资料/网课资料总索引.md` §二（课程包概览）16 行「当前状态」列普遍滞后
  —— 多写「待索引提炼」，但多数已有产物。本日只修了 5 份子索引页，**总索引未动**
P3-2 `备课思路/备课与教学思路待办.md` §二.3/§二.4 本就写「全部完成」，与本日回填一致，**无需改**

▍已登记但仍待用户拍板（勿自行决定）
- 09-16 计划 §十.2 六项：中段课 4 节是否建 / 傅献彩口径 / 裸下标 130 处是否并入本线 /
  7 份提炼页行数是否裁剪 / 7 个「现役无独立讲义」是否新建 / `备课思路/` 是否纳入 validate_kb 受检
- 本计划 §二 P2「现役无独立页」三项：**新建讲义 vs 明确标注缺口**（涉创作量，须请示）

---

【四、纪律铁律（每条都踩过坑，必须遵守）】

▍边界（最高优先级）
- 🔴 **红区禁碰**：`04-题库/`、`05-真题库/`、`_归档/`（除显式授权）。**另有题库线会话在并行跑**
- 🔴 **不碰他会话未跟踪文件**：`.workbuddy/memory/2026-09-17.md`、`2026-09-18.md`
  （**共享日志，多会话并发追加**）、`.workbuddy/memory/工程细则速查.md`、`题库选题口径速查.md`、
  `02-数据库/quality-metrics.jsonl`（M）、`00-首页/题组Word/**`（64 项 docx/tmp，属 Word 导出会话）
- 🔴 **`.workbuddy/memory/*` 一律不提交**（`.gitignore` 外但仍按惯例不提交）
- 判「文件是否他会话占用」用 `git status --porcelain -- <路径>` + mtime

▍写文件纪律
- **改 md 后必跑双闸门**：
  ① `jsyaml_verify.js`：`NODE_PATH="C:/Users/蕾赛/.workbuddy/binaries/node/workspace/node_modules"
     node .workbuddy/scripts/jsyaml_verify.js`
     ⚠️ **该脚本不解析 `--list`**（跑的是固定 4 项硬编码检查）→ 要验自定义清单须另写 node 脚本
     （可参考已写好的 `.workbuddy/tmp/zchem_jsyaml_check.js` 口径）
  ② `validate_kb.py`：`"C:/Users/蕾赛/.workbuddy/binaries/python/versions/3.13.12/python.exe" -X utf8
     11-模板/scripts/validate_kb.py --changed <文件1> <文件2> ...`
     ⚠️ `--changed` 是 **nargs="*"（路径列表）**，传「清单文件路径」只会受检 1 个（看着像通过）
     → **用 Python subprocess 传列表**，并**核「受检数 == 清单行数」**
     ⚠️ 汇总行在 **stderr**，`> out 2>&1` 会顺序错乱 → 分开重定向
     ⚠️ **`validate_kb` 受检域**：01/02/03/04-课件/04-专题与题型/04-题库/06-学生侧材料/
        07-资料提炼/12-教学洞察。`00-首页/`、`06-外部资料导入/`、`09-审计报告/`、`11-模板/`、
        `备课思路/` **域外**（受检 0 ≠ 通过，是**空跑**）
- **跑完 validate_kb 必删副产物** `09-审计报告/auto-validation/<日期>-validation.md`
  —— 该文件**未被 git 跟踪**，`git checkout --` 会报 `pathspec did not match`
  → 必须用 Python `os.remove()`
- **含正则/反斜杠/非 ASCII 的脚本必须 Write 成 `.py` 再跑**（bash 内联会吞 `\$`、`\s`、`\r`；
  heredoc 非 ASCII 会 mojibake）
- **批量改 md**：`open(newline="")`、断言行数守恒、快照＋逐行 diff、**比对行尾类型（CR 与否）与 HEAD 一致**
- ⚠️ **同一文件的多个 Edit 绝不能并行**（实测同发 2 个都回 Successfully 却各静默丢 1 处）
  → 串行 + 改完 Read 复核

▍引用纪律
- **引用前必须验存**：目标页存在（`find`/`ls`）且 `status` 非 `deprecated`；wikilink 目标**唯一**
  （`find . -name "<名字>.md" -not -path "./.git/*"` 应命中 1）
- `_归档/` 路径**不可作现役落点引用**（弃用区）
- **配图铁律：只用现有教材图，禁 AI 自绘**。未命中标 `⛔ 待补教材图` 留白

▍提交纪律
- 只 `git add` 本会话改动文件；**禁 add→commit 一条龙**
  → commit 前必核 `git diff --cached --name-only | wc -l` 与预期一致
- 统计一律 `git status --porcelain -uall`（不加 `-uall` 会把子目录折叠成一行）
- 中文路径 git 输出是八进制转义 → 加 `-c core.quotepath=false`，或用 `-z` + `.decode("utf-8")`
- **本会话不 push**：本地已领先远程 71 笔（含题库线提交），**等各线收敛后统一推**

---

【五、推荐起手动作（Step 0）】

1. 跑 `git log --oneline -3` + `git status -sb` → 记录当前 HEAD 与领先笔数
2. 读 §二 的 6 份必读文件（尤其「计划-2026-09-18-课程计划下一步规划.md」）
3. **复现缺口数字**：用 `find`/`ls` 逐个核实 §三 的 P1-1~P2-4，确认与提示词一致
   （若不一致 → 以实测为准，并在回复中说明差异，**绝不照抄本提示词**）
4. 输出一份 **checkpoint 报告**：现状 + 缺口复核结果 + 建议执行序 → 交用户拍板
5. 得到拍板后再动手；每完成一项跑双闸门并 commit

---

【六、必须避免的错误（本库历史踩坑）】

- ❌ 拿「清单文件路径」直接传给 `validate_kb --changed`（受检 1 个，假通过）
- ❌ 以为 `jsyaml_verify.js` 支持 `--list`（不支持，会跑无关的硬编码检查）
- ❌ 用 `git checkout --` 还原 auto-validation 副产物（未跟踪，必报 pathspec 错误）
- ❌ 对未跟踪的共享 memory 文件用 `git show HEAD:<file>` 做基准重建
  （HEAD 无此文件 → 返回空 → **会误删他会话内容**，本库曾因此丢 363 行，靠暂存区救回）
- ❌ 在 `00-首页/`、`备课思路/` 文件上跑 validate_kb 后当成「通过」（域外空跑）
- ❌ 把 `.workbuddy/memory/*` 提交进 git
- ❌ 凭印象写 wikilink（本日初稿引了不存在的 `[[教学逻辑提炼-补遗总表]]`，`find` 一查即否）
```

---

## §二 事实基线速查（供你自己核对，勿写进提示词）

| 项 | 实测值 | 核实命令 |
|:---|:---|:---|
| HEAD | `7750da0d9` | `git log --oneline -1` |
| 领先远程 | `[ahead 71]` | `git status -sb` |
| 工作区（-uall） | 74 项 | `git status --porcelain -uall \| wc -l` |
| 现役学生讲义 | 77 份 | `find "04-课件/学生讲义" -name "*.md" -not -path "*_归档*" \| wc -l` |
| ├ 各子目录 | 化学原理 12 / 有机化学 20 / 结构化学(顶层) 5 / 元素与分析 14 | `ls "…/<子目录>/"*.md \| wc -l` |
| 教学逻辑提炼 | 62 份 | `find "07-资料提炼/教学逻辑提炼" -name "*.md" \| wc -l` |
| 复习课 / 习题课 | 各 10 / 8 个 .md（**章后课 12 节 = 6 章 × 2 齐备**；余为 README/v2/导航等） | `ls "04-课件/复习课/"` |
| 本会话两笔提交 | `22d940bf3`（索引页回填）、`7750da0d9`（下一步规划） | `git log --oneline -2` |

**工作区 74 项归属**（接管时勿误动）：
- `00-首页/题组Word/**` 64 项 → Word 导出会话
- `.workbuddy/memory/*` 7 项（4 M + 2 ?? + 2 ??）→ 共享日志，含他会话
- `02-数据库/quality-metrics.jsonl` 1 M → 题库线
- 我本会话 0 项（已全部提交）

---

*本提示词基于 2026-09-18 实测（HEAD `7750da0d9`）撰写。所有数字均经命令核实，未采信报告转述。*
