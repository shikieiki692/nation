---
title: 任务卡-讲义等宽段治理
type: 任务卡
status: 进行中
priority: P2
created: 2026-09-11
updated: 2026-09-11
owner: Agent
scope: 04-课件/学生讲义
related:
  - 00-首页/工作日志/2026-09-11
  - 09-审计报告/讲义等宽段治理-口径与清单-2026-09-11
  - 09-审计报告/Word讲义产物排版验收报告-2026-09-11
tags:
  - 任务卡
  - Word产物
  - 排版治理
  - 讲义
---

# 任务卡：讲义等宽段治理（Word 代码块残留）

> **本卡已按实测口径重写**。原版有三处前提经复核不成立（见 §二），若你有旧版印象，请以本版为准。
> 进度、工具、复现命令都在下面，**下个会话可直接接着做**。

---

## 一、目标

把源 md 里**不该写在代码块**里的内容提到正文/列表/表格，只保留真正靠空格对齐的 ASCII 图/表；
并让保留的 ASCII 图在 Word 里**真正对齐**（等宽网格）。

---

## 二、口径纠正（原版三处硬伤 · 已实测）

| # | 原版说法 | 实测事实 |
|:--|:--|:--|
| 1 | 818 段在 Word 里是 **Courier 等宽**，与正文割裂 | **不成立**。81 份活跃产物 `styles.xml` **无任何等宽字体**；"等宽"只是 pandoc 样式名 `Verbatim Char` 的字面直译 |
| 2 | "多行代码块展开成多个 SourceCode 段" | **错**。pandoc 把整个代码块放进**一个** `<w:p>`（块内换行用 `<w:br/>`）→ **1 代码块 = 1 段** |
| 3 | 判据"流程/排序 108 → ≤40" | **不可达**（只治真代码块时剩 44） |

### 818 段的真实构成

| 类别 | 合计 | 真代码块（`pStyle=SourceCode`） | 正文夹行内反引号 |
|:--|--:|--:|--:|
| 结构图 | 74 | 66 | 8 |
| 流程/排序 | 108 | 64 | 44 |
| 纯文本 | 636 | 28 | 608 |
| **合计** | **818** | **158** | **660** |

**两类根因完全不同**：真代码块来自 ``` 围栏；行内 660 段是正文段落里夹了 `` `…` ``，
**它们本来就是正文，没有围栏可拆**。→ **本卡只治真代码块；行内 660 段另开一卡**
（其中 430 段集中在数学工具区，与「行内数学禁反引号」铁律同源，优先攻那里）。

### 另一处原版遗漏：保留段的对齐本来就是坏的

ASCII 图靠半角空格对位，但字体是比例字体 → 必然错位。根因：`docx_utils.postprocess_pandoc_docx()`
**两处**归一化把 SimSun 抹掉了（样式层 + run 层；run 级直格式会覆盖样式，故只改样式无效）。**已修**，见 §三。

---

## 三、已完成（截至 2026-09-11 深夜，最新推送 `57e505457`）

**批次 1–11 累计：真代码块 158 → 45（−71.5%）；全库等宽段 818 → 705；行内 660 未动。**

| 批次 | 内容 | 块 / 文件 |
|:--|:--|:--|
| 1 | 判断树→嵌套列表、压平步骤→有序列表 | 13 / 10 |
| 2 | 无缩进/无列对齐/无活性字符的块去围栏提正文 | 38 / 13 |
| 3 | 放宽 `RE_ALIGN` 为"行尾注记型"；新增根+缩进树、①序号+子项、`标签␣␣数值`两列表格 | 16 / 9 |
| 4 | 新增「序号主项+缩进子项」「竖向 ↓ 流程」两条规则 | 5 / 4 |
| 5 | 修两处解析缺陷：纯 `│` 空行被误拒 / 折行续写被误判为节点 | 2 / 2 |
| 6 | 活性字符守卫从字符类一刀切改为**精确模式**（`[配合物]`、`π*`、`-->` 不再误杀） | 13 / 8 |
| 7 | **人工定型**：稳定性顺序与 Hammett ρ 转真表格、标准曲线法转有序列表、反应链合并 | 8 / 5 |
| 8 | **人工定型**：横向分叉→有序列表、引用块内决策树→嵌套列表、折行口诀树→5 层嵌套列表 | 3 / 3 |
| 9 | **深挖「结构图/连线」桶**：¹H NMR 化学位移转真表格、电环化/周环判断树、Suzuki·Heck·Wacker·Tsuji-Trost·Buchwald 五个催化循环 | 10 / 4 |
| 10 | 结构化学：框线流程图→嵌套列表；两处配合物**文字说明拆出等宽**（结构骨架留块） | 3 / 1 |
| 11 | **阶段5 试点**：八面体场 d 轨道分裂图换成图谱库已核验图片（已确认嵌入 docx）；另 3 处混装块文字拆出等宽 | 4 / 1 |

| 其他 | 结果 |
|:--|:--|
| **顺带修缺陷** | `有机化学/醇醚胺酚.md` L396 未闭合围栏（pandoc 不识别，围栏字面量被印进 Word） |
| **阶段 6 字体修复** | `docx_utils.py` 新增 `MONO_FONT="SimSun"` / `MONO_STYLES={"VerbatimChar","SourceCode"}` 两层豁免；`patch_mono_font.py` 给历史产物打补丁 **81/81 生效、0 损坏、幂等** |
| **对账** | 源 md 围栏块 ↔ 产物 `SourceCode` 段 **45 ↔ 45，0 偏差** |
| **回写** | 任务卡按实测口径整份重写；`docx-math-leak-qa` 技能 §六点五 已纠正（旧版"树形块不转"是错的）；新建技能 `md-codeblock-to-list`；**阶段 5 已拆分出独立卡** `任务卡-2026-09-11-讲义ASCII图转图片.md` |

### 判据（新口径，替代原版）

- [x] 真代码块 158 → **45**
- [x] 行内 660 段未被恶化
- [x] `verify_active_v2`：A=0 / B=0；OMML 7,868 不变
- [x] `qa_comprehensive`：汇总为空；缺失图片 0
- [x] `$` 奇偶：仅剩 `_归档` 既有存量
- [x] 双闸门：js-yaml + validate_kb **逐批** 0 Error / 0 Warning
- [x] 技能回写完成
- [ ] 真代码块 → ≤25（**未达成 = 45**；原因见 §四，非"漏做"而是**剩余主体是真图**）

---

## 四、剩余（真代码块 45 段 —— 全是真图，已另立卡）

**→ 已全部移交给独立卡：`任务卡-2026-09-11-讲义ASCII图转图片.md`**（含 46 块逐块清单）。

| 分型 | 量 | 处置 |
|:--|--:|:--|
| 几何/结构式 | 13 | **真图**：Newman 投影、竖排骨架、Cram/Felkin-Anh、周环轨道、卡宾 |
| 数据/对齐表 | 9 | **真图**：d 轨道能级方块图、坐标轴、配位结构对照 |
| 判断树（剩余） | 8 | 全部是**图**（势能曲线、催化循环框图、骨架式、配位结构） |
| 结构图/连线 | 8 | **真图**：SN2 过渡态、邻基参与、多核配合物、复分解 |
| 文字型 | 6 | **真图**：骨架式（`CHO / H—OH / CH₂OH` 之类） |

### 阶段 5 普查结论：**低产，须人工**

- **主题级命中率 87%**（23 主题中 20 个在学科图谱索引有命中），但**精确匹配率实测约 2/45**：
  这些是**教学专用图**，库图多数只是同主题的别的反应/别的构型（卡宾只有"三重态与烯烃加成"、
  多碘只有 I₇⁻、催化循环只有 Monsanto）。**唯一确认的精确匹配**：八面体场 d 轨道分裂图
  → `5f17019ed…jpg`（**已换上**，见批次 11）。
- **缓解**：阶段 6 字体修复后剩余 45 段全为 `SimSun` 等宽网格（已逐段核验）→ ASCII 图在 Word 里
  **已能正确对齐**，不再是"错位"问题，只剩"ASCII 观感 vs 真图片"的美观取舍 → 本卡**不紧急**。

> **口径提醒（本轮最重要的教训）**：**所有**启发式分型桶都不纯 ——
> `文字型`/`反应式` 里混着骨架式；`结构图/连线`/`几何/结构式` 里藏着
> 「①②③ + `│ ▼`」的**催化循环步骤**和**框线决策流程图**（本质都是列表）。
> **必须逐块人眼复核**，不能按桶批处理（批次 9 就是靠逐个看又取出 10 块）。

---

## 五、工具与复现（`.workbuddy/tmp/`，已被 gitignore）

```bash
PY="C:/Users/蕾赛/.workbuddy/binaries/python/versions/3.13.12/python.exe"
cd "C:/Obsidion/妙妙屋"
"$PY" -X utf8 .workbuddy/tmp/verbatim_report.py     # 产物侧口径（分 SourceCode / 行内）
"$PY" -X utf8 .workbuddy/tmp/verbatim_worklist.py   # 源侧逐块清单 + 双向对账 + 未闭合围栏检测
"$PY" -X utf8 .workbuddy/tmp/convert_batch.py       # 转换器（--selftest / dry-run / --apply / --only）
"$PY" -X utf8 .workbuddy/tmp/dump_blocks.py <分型>   # 按分型导出块内容，供人工定规则
"$PY" -X utf8 .workbuddy/tmp/patch_mono_font.py     # 阶段6 字体补丁（--apply）
"$PY" -X utf8 .workbuddy/tmp/push_batches.py        # 网络抖动时按 ~5MB 分批推送
```

转换器与技能副本在 `~/.workbuddy/skills/md-codeblock-to-list/`（`.workbuddy/tmp/` 会被清理，技能里的是长期副本）。

**重导**：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= "$PY" -X utf8 11-模板/scripts/build-all-handout-docx.py --path "<源 md>" --output-dir "06-学生侧材料/讲义"`
（「（Word清稿）」7 份不参与普通批量导出，涉改须单独 `--word-clean`）

---

## 六、踩过的坑（照做可省数小时）

1. **判断树层级要取前缀里最后一个分支符的列号** —— 取行首 `│` 会把 `│ └──` 的嵌套整片压平。
2. **围栏有两种**（裸 ``` 与 ```text），断言别写死 `== "```"`。
3. **行尾不能一刀切** —— 给新行统一补 `\r` 会把**纯 LF** 文件写成混合行尾（项目里有纯 LF 文件）。
4. **活跃源集合口径**：`04-课件/学生讲义/**/*.md` 只排除 `_归档`、首层 `_` 目录、`README`、`讲义升级模式-` 前缀；
   **绝不能**再加 `超级充实/基础版/复习/-新课` 过滤（该过滤只在批量路径生效，大量产物是 `--path` 显式构建的）。
5. **HTML 注释里的代码块不会进产物** → 对账要排除。
6. **导出报 WinError 5**（文件被占用）→ 重试；仍失败则把 `_<stem>.<pid>-<tid>.tmp.docx` read+write 回目标（`force_replace_tmp.py`）。
7. **大推送**：单次 >15MB 极易被网络打断（`curl 55` / `HTTP 408` / `remote end hung up`）。
   先 `env | grep -i proxy`（本机 `http_proxy` 指向本地代理 `127.0.0.1:62853`，需 `-c http.proxy= -c https.proxy=` 绕开），
   再用 `push_batches.py` 按 ~5MB 分批推。**核对必须用 `git ls-remote origin master` 比对哈希**（`git log origin/master..HEAD` 会静默输出 0）。
8. **`styles.xml` 的 XML 前缀是混合的**（同一份文件里 `VerbatimChar` 用 `ns0:`、`SourceCode` 用 `w:`），
   写补丁正则时不能假定统一前缀；`rFonts` 的前缀要取自实际命中的元素。
9. **折行续写别当节点**：`│   │       口诀："…"` 这种行前缀里只有竖线 `│`、没有拐角符，
   是上一节点的折行续写。判据要用「前缀里是否含拐角符 `├└┌┐…`」，
   **不能**判"最后一个树形符后面是否接 `─`" —— `─` 本身也在 `TREE_CHARS` 里，会把所有正常节点误杀。
10. **纯 `│` 的空行是间隔符**，不是"树形符后无文字"的坏块。按报错处理会把大判断树整片拒掉。
11. **工作副本是 CRLF、git 里是 LF**：`.gitattributes` 声明 `*.md text eol=lf`，所以
    `git diff` **看不见**行尾变化（提交时会被归一）。判行尾安全只能靠脚本内断言
    （`c1 == c0 + want_crlf`），或直接 `open(..., newline="")` 数 `\r\n`。另外仓库里确实有**纯 LF** 的 md。
12. **`cat >> 文件 << 'EOF'` 写含反引号/`$`/`\` 的 md 时用单引号定界**，否则会被 shell 展开。
    同理：bash 内联 python 里写 `"\\n"` 会被吃成真实换行 —— 含转义的脚本一律先 Write 成 `.py` 再跑。
13. **转表格前先查上方是否已有同内容表格。** 实测 `有机化学基础.md` L160-165 的 ASCII 块
    在 L152-157 **已有一张三列表格**（且带 `\ce{}`），盲转会造成重复且**丢掉 LaTeX 化学式**。
    已 `git checkout` 回退。教训：**「去围栏」不等于「转表格」，先看上下文已有的呈现形式。**
14. **分型桶不可信，只能用来排序，不能用来批处理。** 每一轮"以为转完了"之后，
    **把剩余块 dump 出来逐块人眼再过一遍**，都还能再取出 10 块左右（批次 9 即为如此）。

---

## 七、参考

- 详细清单与批次日志：[[09-审计报告/讲义等宽段治理-口径与清单-2026-09-11]]
- 后续卡（剩余 46 张真图）：[[00-首页/活跃任务/任务卡-2026-09-11-讲义ASCII图转图片]]
- 技能：`~/.workbuddy/skills/md-codeblock-to-list/SKILL.md`、`~/.workbuddy/skills/docx-math-leak-qa/SKILL.md`
