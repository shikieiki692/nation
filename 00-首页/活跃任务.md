---
title: 活跃任务
type: 系统
role: 执行聚合
status: active
updated: 2026-06-24
task_system_version: v2.1
---

# 活跃任务 · 结构化任务总览

> **用途**：本页是活跃任务系统入口，只做聚合、筛选和规则说明，不再手写整份任务清单。
> **质量看板**：知识库健康检查指标见 → [[质量看板]]
> **分工**：
> - `00-首页/活跃任务/`：短期执行真相源，每个任务一张卡
> - `[[活跃任务]]`：聚合总览、筛选入口、健康检查、状态规则
> - `[[工作日志]]`：记录过程、依据、验证结果、证据链接
> - `[[状态摘要]]`：读取任务卡聚合结果，生成 AI 启动快照
> - `[[Agent战略方向]]`：只保留长期方向，不承载短周期任务清单
> - `[[自动汇总-任务卡健康检查]]`：任务卡自动事实中间层，便于排查聚合异常
> - 入口顺序与职责边界：见 [[Agent入口文件关系说明]]

> ✅ **当前判断（2026-06-25 更新）**：
> - **原子结构收官内容全面升级**：Slater规则定量计算(Na+Ti)、深度边界从☆→实做、image_count 5→10、学习目标9条、Cotton图+4s/3d径向分布+周期律趋势三图、深化方向区分第一轮/第三轮 ✅
> - **Word生成管线修复**：![[img.png|alt]]管道语法正则修复，SRC_GLOB从日期锁定→*.md(覆盖过渡-化学键衔接)，新增post-generation验证(图片计数+断链检查) ✅
> - **Word文件**：`06-学生侧材料/学生讲义/第9课-原子结构收官.docx` 从40KB(无图)→287KB(10张图全部嵌入) ✅
> - **6项优化建议已落实**：残留计划清理(6文件删除) / 存档checklist(加入最小执行协议) / 孤儿图片检测(validate_kb.py新增) / SRC_GLOB修复 / Word后验证 / 深度边界自动查阅(加入协议) ✅
> - **讲义升级模式样板**：创建 [[04-课件/学生讲义/讲义升级模式-原子结构样板]]，提炼8维升级模式+P1/P2/P3升级清单 ✅
> - **图片引用全面修复**：11-8氢光谱图标题/11-13角度分布图换完整版(原仅Y²局部)+图注调校/11-15电子云图号/11-21径向分布图由3片mineru碎片拼接完整(原仅3d部分)/Slater屏蔽小标题统一#### ✅
> - **公式字号 11.5pt→14pt（四号）**：docx_utils.py新增OMML公式字号处理——6个显示公式全部14pt、长行内公式(>5字符)自动14pt ✅
> - **原子结构讲义微调+标准化序列模板**：前置知识→前置要求统一；升级模式样板新增12段标准化内容序列模板（7段必修+5段建议）✅
> - **模板-学生讲义 v2.0/v2.1**：v2.0双模式 → v2.1拆分为自学完整版+课堂填空版独立模板；原文件改为索引页 ✅
> - **过渡-化学键衔接 大幅扩充**：359行→517行(+46%)，引用7个KP+4份资料提炼；新增§4形式电荷·共振·VSEPR、竞赛级例题2道、σ-给予/π-反馈协同、Fajans规则系统展开 ✅
> - **原版三讲答案速查页**：第八讲(130填空答案+排布表)、第六七讲(22填空答案+价电子轨道式+OCR校正) ✅
> - **原子结构讲义超级充实升级版（06-25）**：创建课堂填空版+自学完整版双版本优秀范例。四步教学循环全覆盖，图片25+张，嵌入式计算4处，空白互动表5+张，例题11道，3张Excalidraw/Mermaid配图。关联任务卡[[任务卡-原子结构讲义超级充实升级]] ✅
> - **超级充实版KB深度集成（06-25 二轮）**：两份讲义新增6×🧠教学洞察callout(认知冲突/类比/错误模式)、5×🗣️教学金句(质心+周坤)、5×🔗KP章节引用、📅课时关联、📋专题页引用。讲义从"纯内容"升级为"教学内容+教学策略+知识库联动"三位一体 ✅
> - **备课思路深度边界对齐（06-25 二轮）**：备课大纲depth_boundary扩展至Z≤54+Slater📝掌握+Cotton/钻穿🌟了解；新增depth_boundary_note；新建[[备课思路/深度边界调整说明-原子结构超级充实版]]，确立🌟了解性 vs 📝掌握性区分标准 ✅
> - **教学过程总表·结构化学模块（06-25 二轮）**：新建[[备课思路/教学过程总表-结构化学模块.md]]（~450行），覆盖第9-22课14课时完整KP级映射，含总览表+逐课展开(KP/教学策略/认知冲突/类比)+4附录 ✅
> - **待处理**：P1化学键衔接讲义配图&升级(可套用升级模式) / 晶体结构基础加图 / PPT样板(VSEPR) / 分子结构基础+元素周期表按升级模式升级 / 按要求模板补齐分子结构基础/晶体结构基础/配位化合物基础等讲义缺失段
- **新增4张任务卡（06-25）**：①[[任务卡-超级充实版模式推广]]（P1）将原子结构超级充实版模式推广到分子结构/晶体结构/配合物基础 ②[[任务卡-备课大纲前置检查清单标准化]]（P2）为所有备课大纲新增§0.0前置知识检查清单 ③[[任务卡-习题配套建设]]（P2）三级习题体系（基础→竞赛→真题） ④[[任务卡-教学逻辑链路一致性审计]]（P2）全链路审计含教学洞察/教学逻辑/考纲对齐
- **答案格式修复（06-25 五轮）**：原子结构课堂填空版删除14处inline `（答案：...）`和2个答案速查表，纯净空白。元素周期律和分子结构课堂填空版已无inline答案问题 ✅
- **Word模板创建（06-25）**：新建 `custom-reference.docx`（SimSun/SimHei/TNR字体，A4页面），待集成到管线 ✅
- **新增2张任务卡（06-25）**：①[[任务卡-结构部分超级充实讲义收官]]（P1）晶体结构+配位化合物超级充实升级 ②[[任务卡-PPT制作方法探究与试点]]（P2）先探究PPT方法，再以原子结构试点

**读完本页后通常下一跳**

- 想看当前全局状态：[[状态摘要]]
- 想看默认推进方向：[[Agent战略方向]]
- 想看三份入口怎么配合：[[Agent入口文件关系说明]]
- 想排查任务卡聚合异常：[[自动汇总-任务卡健康检查]]

## 使用规则

1. 新任务优先新建一张任务卡到 `00-首页/活跃任务/`
2. 每张任务卡必须维护 frontmatter，至少包含：`status`、`priority`、`area`、`source_notes`、`related_notes`、`evidence`
3. 任务完成后，不删除卡片；改为 `status: completed` 并补 `completed`
4. 任务卡记录"还有什么没做"；任务经过与验证细节写入 `[[工作日志]]`
5. `[[状态摘要]]` 和数据库看板只从任务卡聚合，不再从日志正文扫描短期 follow-up
6. 若某任务已被审计结论吸收（如复用已有专题页、接受历史命名、整体闭环已覆盖），应直接关卡，不必再补冗余产物
7. 若某任务明确等待用户决策、实体打印验证、真实课堂反馈或外部资料到位，应改为 `blocked`，不要长期停留在 `active`
8. 若任务属于资料导入 / 资料提炼 / 网课提炼，关闭前必须确认相关提炼页已补“备课抽料资产标记”；否则只能记为“已入库待接入”，不应直接 `completed`

## 状态快照

> 让 Agent 一眼看清"现在到底有多少还在做、多少已完成、多少正在等外部条件"，避免 `blocked` 只写进 frontmatter 却在入口层隐身。

```dataview
TABLE
  length(rows) as 总数,
  length(filter(rows, (r) => r.status = "active")) as Active,
  length(filter(rows, (r) => r.status = "blocked")) as Blocked,
  length(filter(rows, (r) => r.status = "completed")) as Completed,
  length(filter(rows, (r) => r.status = "active" AND r.priority = "P1")) as "P1 Active"
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
```

## 健康检查

> 用于快速发现 frontmatter 漏填、完成态漏日期、以及证据字段为空的任务卡，避免入口页统计失真。

```dataviewjs
const pages = dv.pages('"00-首页/活跃任务"')
  .where(p => p.type === "活跃任务卡");

const issues = [];

for (const p of pages) {
  const missing = [];

  if (!p.status) missing.push("缺 status");
  if (!p.priority) missing.push("缺 priority");
  if (!p.area) missing.push("缺 area");
  if (!p.source_notes || (Array.isArray(p.source_notes) && p.source_notes.length === 0)) missing.push("缺 source_notes");
  if (!p.related_notes || (Array.isArray(p.related_notes) && p.related_notes.length === 0)) missing.push("缺 related_notes");
  if (!p.evidence || (Array.isArray(p.evidence) && p.evidence.length === 0)) missing.push("缺 evidence");
  if (p.status === "completed" && !p.completed) missing.push("completed 缺完成日期");

  if (missing.length > 0) {
    issues.push([p.file.link, missing.join("；"), p.updated ?? "-"]);
  }
}

if (issues.length === 0) {
  dv.paragraph("✅ 当前任务卡 frontmatter 关键字段完整，未发现 completed 漏日期问题。");
} else {
  dv.table(["任务卡", "异常", "updated"], issues);
}
```

## 状态维护速查

| 状态 | 何时使用 | 必填要求 |
|:---|:---|:---|
| `active` | 仍需继续推进，且当前不依赖外部决策/资料/反馈 | `status`、`priority`、`area`、`source_notes`、`related_notes`、`evidence` |
| `blocked` | 明确等待用户决策、打印验证、课堂反馈、外部资料 | 保留原有证据，并在正文写清阻塞原因 |
| `completed` | 已有明确产出或审计结论吸收，不再继续执行 | 除基础字段外，**必须补 `completed: YYYY-MM-DD`** |

## 任务面板

### 🔴 P1 高优

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "active"
  AND priority = "P1"
SORT file.name ASC
```

### 🟡 P2 中优

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "active"
  AND priority = "P2"
SORT file.name ASC
```

### 🟢 P3 低优 / 长期

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "active"
  AND priority = "P3"
SORT file.name ASC
```

### ⛔ Blocked / 等待外部条件

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  priority as 优先级,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "blocked"
SORT updated DESC, file.name ASC
```

### ✅ 近期完成（历史任务卡）

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  completed as 完成日期,
  join(evidence, " · ") as 证据
FROM "09-审计报告/历史任务卡"
WHERE type = "活跃任务卡"
  AND status = "completed"
SORT completed DESC, file.name ASC
LIMIT 10
```

## 快速入口

- 结构化数据库视图：[[02-数据库/活跃任务.base|活跃任务.base]]
- 当日过程归档：[[工作日志]]
- 启动快照：[[状态摘要]]
- 自动事实中间层：[[自动汇总-任务卡健康检查]]
- 入口职责边界：[[Agent入口文件关系说明]]
- 默认执行路径：[[Agent最小执行协议]]

*本页只做聚合，不再手写维护任务明细。任务新增、推进、关闭请直接更新任务卡。*
