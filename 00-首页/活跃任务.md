---
title: 活跃任务
type: 系统
role: 执行聚合
tags: [系统, 任务, AI入口]
status: active
updated: 2026-09-15
task_system_version: v2.1
---

# 活跃任务 · 结构化任务总览

> ✅ **当前判断（2026-09-15 · 第40届真题入库 + 章后层收官）**：
> - **第40届初赛真题已入库并登记**：10 大题 / 83 子问 / 88 图零断链，索引 + 提炼页 + 4 处题库索引同步；题库真题 636 → **646**。修掉 3 题「待分类」静默掉书隐患；修 37 文件 `subject → source_subject` 并统一初赛题目数口径。
> - **物化补强线**：P0-A（化学动力学计算建模，新建讲义 763 行 / 25 题全实题）、P0-C（电化学 §九 超电势，3 道真题例题）**完成**；P0-B 活度部分完成（题源不足，如实降规格）；**P1 判定题源不足不做**；新讲义《二氧化碳化学》1 份。
> - **章后层**：六大章后课考试维度注入 **5/5 收官**（10 份课件，复习课 §一点五 + 习题课 §真题实战与命题趋势专项）。
> - ⚠️ **并行会话仍在 `40届初赛/` 与 `04-题库` 活跃**（工作区约 1,670 项改动）；**本会话改动全部未提交**（用户指示）。
> - ⚠️ **存量待决**：5 个 `教学洞察-大类名` 短链无对应页（`12-教学洞察/` 全为细分主题页）；`04-题库/README` 与 `题库架构总览` 部分总量数与实测有差、无法归因。

> 🟢 **讲义线·精读 批13~15 + 格式修复批16（2026-09-20，本会话）**：
> - **精读推进**：本会话今日 4 笔提交 —— 批13 `055b5635d`（非结构线：物化「化学动力学计算建模」全题重写 + 有机「二氧化碳化学」）／批14 `db7a27856` ＋ 批15 `f6774a046`（2-结构化学线 5 份：结构表征与波谱桥 R1 自相矛盾订正、超分子化学增量数据订正等）／**批16 `af891ae26`（格式修复）**。
> - **格式修复已清零**：修掉「**块级 HTML 表格内公式不渲染**」——讲义线 `04-课件/学生讲义/` 的「含公式块级 HTML 表」**剩余 0**。修法＝转 pipe table（衰变表含中间重复表头 → 拆两张背靠背表）。
> - ⭐⭐ **两条长效结论**：(a) `html_tables_to_markdown.py` 的「行尾保持」判据（`CRLF数==LF数`）对**末尾混 1 个裸 LF** 的文件**恒 False**（本仓常见）→ 该脚本不可直 `--apply`，须**自写转换脚本**；**判据报 ❌ 先对「未改对象」自证**。(b) **HTML 表问题只影响 Obsidian 渲染，不影响 Word**（Pandoc 本就转 OMML，`oMath` 守恒）→ 修复价值在 Obsidian 侧。
> - **待续（格式修复线）**：`07-资料提炼/网课资料` 12 处同类表（最接近生产链的剩余项）；讲义线**待精读 38 份**（`3-物理化学 9` 须避让并行会话）。
> - 📎 [[00-首页/工作日志/2026-09-20]]

> 📦 **历史状态块（2026-08-24 ~ 09-04，共 6 块）已归档** → [[00-首页/归档/活跃任务-历史状态块-2026-08-24至09-04]]


**读完本页后通常下一跳**

- 想看当前全局状态：[[状态摘要]]
- 想看默认推进方向：[[Agent战略方向]]
- 想看三份入口怎么配合：[[Agent入口文件关系说明]]
- 想排查任务卡聚合异常：[[自动汇总-任务卡健康检查]]

## 使用规则

1. 新任务优先新建一张任务卡到 `00-首页/活跃任务/`
2. 每张任务卡必须维护 frontmatter，至少包含：`status`、`priority`、`area`、`source_notes`、`related_notes`、`evidence`
3. 任务完成后，不删除卡片；改为 `status: completed` 并补 `completed`
4. 任务卡记录"还有什么没做"；任务经过与验证细节写入 `[[工作日志]]`
5. `[[状态摘要]]` 和数据库看板只从任务卡聚合，不再从日志正文扫描短期 follow-up
6. 若某任务已被审计结论吸收（如复用已有专题页、接受历史命名、整体闭环已覆盖），应直接关卡，不必再补冗余产物
7. 若某任务明确等待用户决策、实体打印验证、真实课堂反馈或外部资料到位，应改为 `blocked`，不要长期停留在 `active`
8. 若任务属于资料导入 / 资料提炼 / 网课提炼，关闭前必须确认相关提炼页已补“备课抽料资产标记”；否则只能记为“已入库待接入”，不应直接 `completed`

## 状态快照

> 让 Agent 一眼看清"现在到底有多少还在做、多少已完成、多少正在等外部条件"，避免 `blocked` 只写进 frontmatter 却在入口层隐身。

```dataview
TABLE
  length(rows) as 总数,
  length(filter(rows, (r) => r.status = "active")) as Active,
  length(filter(rows, (r) => r.status = "blocked")) as Blocked,
  length(filter(rows, (r) => r.status = "completed")) as Completed,
  length(filter(rows, (r) => r.status = "active" AND r.priority = "P1")) as "P1 Active"
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
```

## 健康检查

> 用于快速发现 frontmatter 漏填、完成态漏日期、以及证据字段为空的任务卡，避免入口页统计失真。

```dataviewjs
const pages = dv.pages('"00-首页/活跃任务"')
  .where(p => p.type === "活跃任务卡");

const issues = [];

for (const p of pages) {
  const missing = [];

  if (!p.status) missing.push("缺 status");
  if (!p.priority) missing.push("缺 priority");
  if (!p.area) missing.push("缺 area");
  if (!p.source_notes || (Array.isArray(p.source_notes) && p.source_notes.length === 0)) missing.push("缺 source_notes");
  if (!p.related_notes || (Array.isArray(p.related_notes) && p.related_notes.length === 0)) missing.push("缺 related_notes");
  if (!p.evidence || (Array.isArray(p.evidence) && p.evidence.length === 0)) missing.push("缺 evidence");
  if (p.status === "completed" && !p.completed) missing.push("completed 缺完成日期");

  if (missing.length > 0) {
    issues.push([p.file.link, missing.join("；"), p.updated ?? "-"]);
  }
}

if (issues.length === 0) {
  dv.paragraph("✅ 当前任务卡 frontmatter 关键字段完整，未发现 completed 漏日期问题。");
} else {
  dv.table(["任务卡", "异常", "updated"], issues);
}
```

## 状态维护速查

| 状态 | 何时使用 | 必填要求 |
|:---|:---|:---|
| `active` | 仍需继续推进，且当前不依赖外部决策/资料/反馈 | `status`、`priority`、`area`、`source_notes`、`related_notes`、`evidence` |
| `blocked` | 明确等待用户决策、打印验证、课堂反馈、外部资料 | 保留原有证据，并在正文写清阻塞原因 |
| `completed` | 已有明确产出或审计结论吸收，不再继续执行 | 除基础字段外，**必须补 `completed: YYYY-MM-DD`** |

## 任务面板

### 🔴 P1 高优

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "active"
  AND priority = "P1"
SORT file.name ASC
```

### 🟡 P2 中优

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "active"
  AND priority = "P2"
SORT file.name ASC
```

### 🟢 P3 低优 / 长期

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "active"
  AND priority = "P3"
SORT file.name ASC
```

### ⛔ Blocked / 等待外部条件

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  priority as 优先级,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "blocked"
SORT updated DESC, file.name ASC
```

### ✅ 近期完成

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  completed as 完成日期,
  join(evidence, " · ") as 证据
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "completed"
SORT completed DESC, file.name ASC
LIMIT 10
```

## 快速入口

- 结构化数据库视图：[[02-数据库/活跃任务.base|活跃任务.base]]
- 当日过程归档：[[工作日志]]
- 启动快照：[[状态摘要]]
- 自动事实中间层：[[自动汇总-任务卡健康检查]]
- 入口职责边界：[[Agent入口文件关系说明]]
- 默认执行路径：[[Agent最小执行协议]]

*本页只做聚合，不再手写维护任务明细。任务新增、推进、关闭请直接更新任务卡。*
