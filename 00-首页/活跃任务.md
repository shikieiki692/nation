---
title: 活跃任务
type: 系统
role: 执行聚合
tags: [系统, 任务, AI入口]
status: active
updated: 2026-08-06
task_system_version: v2.1
---

# 活跃任务 · 结构化任务总览

> **用途**：本页是活跃任务系统入口，只做聚合、筛选和规则说明，不再手写整份任务清单。
> **分工**：
> - `00-首页/活跃任务/`：短期执行真相源，每个任务一张卡
> - `[[活跃任务]]`：聚合总览、筛选入口、健康检查、状态规则
> - `[[工作日志]]`：记录过程、依据、验证结果、证据链接
> - `[[状态摘要]]`：读取任务卡聚合结果，生成 AI 启动快照
> - `[[Agent战略方向]]`：只保留长期方向，不承载短周期任务清单
> - `[[自动汇总-任务卡健康检查]]`：任务卡自动事实中间层，便于排查聚合异常
> - 入口顺序与职责边界：见 [[Agent入口文件关系说明]]

> ✅ **当前判断（2026-08-06 补充更新）**：
> - **学生讲义 L1+L2 全量迁移收官（08-06）**：38 份讲义教师向内容→HTML注释 + 易错清单→核心公式速查 18 份 + emoji 清理 + blockquote 34→0；precheck 38/38 0 error；Word 回归 0 教师词残留（详见 [[00-首页/工作日志/2026-08-06]]）
> - **物化+元素自建题库建成（08-04）**：新建 `04-题库/物理化学/`（6子目录18题）+ `04-题库/元素化学/`（7子目录21题）；遗留任务建 3 张卡（图片补图130处/分析化学方法卡/有机升级），详见 [[00-首页/工作日志/2026-08-04]]续27。
> - **validate_kb 全量验证+降噪+KP覆盖评估完成（08-03）**：48 error→0（48 个 frontmatter 补齐）；Warning 5251→2179（-59%，脚本降噪+别名补全+引用修复）；KP 覆盖评估结论——1161 REDLINK 去重后 ~525 真缺口、6 候选全部被现有近义 KP 覆盖，**0 新建 KP**（详见 [[09-审计报告/KP覆盖评估报告-2026-08-03]]）；周巡检基线 0 error / 2179 warning / 7293 info ✅
> - **全库frontmatter一致性治理完成（07-31）**：全身扫描10模块全覆盖，修复177文件——49个`type:knowledge_point`→`type:知识点`升级+76个`template_version:v1.3`补全+4个旧模板字段升级+10个11-模板quoted type修复+33个临时文件清理+质量看板P0修复。验证：907/907 KP type标准、0残留旧格式 ✅
> - **KP待完善项非文字任务清零收口（07-30）**：Phase 2扩展94个KP嵌入赵鑫光HIGH图片+D类7项+题库联动4项完成。剩余21项全为ChemDraw(8)/计算化学(5)/依赖KP(4)/录课(2)/长期(2)，Agent均不可做。**Agent可做结构性任务已清零。** ✅
> - **讲义生产链路全量检查与Agent阅读链路建立完成（07-27）**：7个模板/SOP/规范文件v5.3一致性全量检查与修复（排版规范6处+内容结构规划+SOP验收清单扩展+S2 checklist+模板🌟措辞+2个废弃模板标识）+ 4项管线冲突修复（标准态θ统一·图注FangSong·图注格式无来源·输出路径更新）+ Agent 10讲义阅读链路建立（开工前必读4文件条件列表+SOP路由表+检查项闭合）+ P2废弃模板处理决策（不归档防断裂wikilinks）。讲义生产全链路：模板→MD规范→SOP→排版规范→Agent提示词，现已闭合 ✅
> - **四轮基石体系升级完成（07-27）**：R1-R5全量审计（断链30+处修复）+ R2资源对接（上海中学+赵鑫光）+ 4个跨轮讲义补轮次划分表 + 10个R1讲义补深度分层📝🌟🔒 + R5骨架创建 + R3深度检查（KP零缺失+16专题结构统一）✅
> - **题库检索优化完成（07-26）**：汇智竞赛题目259文件补全aliases（`汇智XX结构题N` + `汇智XX结构-N`双别名），上海中学14个deprecated文件确认状态正确（已拆分+superseded_by完整），教学改编题source_file确认不适用（教师自编题无源文件）。剩余：媒体去重（1,093文件/43.4MB，需kb-link-doctor确认无引用断裂后再执行）、答案库建设（赵鑫光483题+ABOC 224题，高token消耗暂缓）✅
> - **知识库治理六轮收口完成（07-23~24）**：28项治理待办闭环——KP填实100%（911个KP零骨架零待填充）+°→⊖全库清理（约197处）+汇智3合集deprecated+初赛讲义题号唯一化+ABOC 30对拆页合并（225→195题）+图片路径核实+CRLF/BOM全库统一（3226个md转LF）+讲义生产SOP审查修补（6处）+上海中学14个多题文件拆分（125→127个单题文件）。剩余：学生材料清理、clayden去重（暂缓）、ΔᵣH°散文口径规范 ✅
> - **validate_kb验证+修复完成（07-22）**：两轮扫描修复→0 error / 14 warning（从492降97%），修复1个error（分子识别.md缺updated）+8个warning（status=active→已填充）+13个空difficulty+5个空importance ✅
> - **KP difficulty全库补全完成（07-22）**：176个KP文件补全difficulty评级（化学原理54+有机化学52+无机和结构化学44+决赛要求23+分析化学3），difficulty覆盖率80%→100%（887/887）；新增经验沉淀7-6（批量清理灾难教训） ✅
> - **07-20清理破坏修复完成（07-22）**：发现07-20 [待补充]清理操作误将5个核心备课思路文件覆盖为占位符（第一轮/第二轮总体备课框架、轮次深度切分总表、近五年真题命题拆解分析、备课与教学思路待办），全部从git历史成功恢复 ✅
> - **全库系统审计完成（07-22）**：扫描910个KP+316个提炼+2484个题库+179个专题与题型，发现并修复：33个KP文件importance字段缺失（906/910=99.6%）、1个重复Le Chatelier原理文件标记为已合并、系统页面数据校准（专题68+题型110、题库knowledge_points98.6%、连接工程总表补入分析化学模块）✅
> - **[待补充]残余100%清除完成（07-22）**：117个KP文件/323处[待补充]全部清理 ✅
> - **教学视角全覆盖完成（07-22）**：910/910 KP文件教学视角100%覆盖 ✅
> - **target_kp全覆盖完成（07-22）**：需补文件100%覆盖（272/316，缺失44个均为索引/笔记类）✅
> - **5份超级充实版讲义终验修复完成（07-02）**：全量验证5份讲义（原子结构/周期律/分子结构/晶体学/配合物），发现并修复：原子结构3条孤立答案+章节编号缺失、晶体学6张乱序图+子节号错误+image_count偏差。配合物/周期律/分子结构验证通过 ✅
> - **六大章节章后复习课/习题课全部落地（06-30）**：12份文件（6章×复习+习题），标准化结构（Mermaid知识网络+统一框架+高频陷阱+分层习题）。结构化学复习课v2结合KB深度增强（三步法/σπ配位场/CFSE/TS图/Miller指数）。首页MOC+复习课/习题课README已同步 ✅
> - **25-32届初赛+决赛真题提炼完成（06-30）**：从OCR源文件提取第25-27届初赛（107个题目文件）和第25-32届决赛（75个题目文件），共182个题目文件+11份提炼索引。决赛题使用扩展模板含考点抽象和变式拓展。知识库真题总量从~593增至~775 ✅
> - **结构化学讲义低质手绘图替换与图片提质 已完成（06-30）**：全面核查确认——7份核心讲义均有 `has_images: true` + 稳定 `image_count`；图片引用全部为 `![[media/...]]` 标准格式；无残留 bare ref；图片索引 v1.7 零缺失 ✅
> - **分子结构基础-超级充实版重构收官（06-30）**：新增 §一 化学键基础（电负性/三大键型/Fajans规则/H₂势能曲线/σπ键/配位键），12→13节，练习题 24→29（🌱14+🔥9+🏆6），图片 14→16引用（清理1张重复+新增1张网络资源），全文件图注1-13连续编号，preflight 0 error ✅
> - **过渡-化学键衔接.md 已退役（06-30）**：核心内容合并入分子结构基础 §一，标记 stage:deprecated + superseded_by ✅
> - **工作流新增：讲义重组标准流程 + 图片替换决策树（06-30）**：基于分子结构重构实战经验提炼，落地为 `11-模板/` 下两份标准文档 ✅
> - **超级充实版讲义全线收官**：原子结构·元素周期律·分子结构·晶体结构·配位化合物，5本×2版本(自学完整+课堂填空)=10份讲义文件，~150张配图(含~20张Excalidraw原创) ✅
> - **Phase 2 Batch A 晶体学基础收官（06-26）**：上海中学第六讲晶体学基础超级充实升级——自学完整版+课堂填空版双版本，6张Excalidraw配图(七大晶系决策树·14种布拉维格子·晶胞计数·分数坐标·对称性限制定律·点阵-基元-晶胞关系图)，image_count 15→22 ✅
> - **第八讲原子结构（三）教师增强版+融合版（06-26）**：教师增强版493KB docx(深蓝色注解着色·33张图) · 超级充实融合版502KB(第八讲+KB深度·14处🧠⚠️💡教学callout) ✅
> - **Slater规则深度边界修复（06-26）**：4处修复(超级充实融合版+教师增强版+对应docx) ✅
> - **元素周期律Excalidraw配图修复+学习目标格式统一（06-26）**：JSON转义+教材原图替换·表格→bullet list对齐原子结构样板 ✅
> - **docx管线图片约束居中+全量重新生成（06-26）**：docx_utils.py新增_resize_and_center_images() · 78份docx全量重新生成 ✅
> - **全库图片清理（06-26）**：148 folders / 205.9MB网课资料图片去重，保留书籍+真题图片 ✅
> - **P0 LaTeX管线规范化收口（06-28）**：图片索引 `v1.7` 核验通过（0缺失）· 原子结构规范化样板 v1 完成 · `pdf_preflight.py` / `audit_image_index.py` / `handout_audit_utils.py` 落地 · bare image ref 兼容层清理 · 文档口径统一（使用手册 / Agent一键说明）✅
> - **P0 LaTeX管线稳健性修复 + 完整回归（06-28 → 06-29 收口）**：编译脚本新增 stale 产物清理、ASCII build key、缓存缺图回源补拷、ASCII 状态输出；完整回归现已收口为 **6 本全图 OK**，且 PDF 成品目录已统一到 [[00-首页/学生讲义PDF/README]]。详见 [[09-审计报告/2026-06-28-PDF回归]] ✅
> - **知识库状态同步 + validate_kb 首轮降噪（06-30）**：`13` 条状态异常收口；`validate_kb.py` 新增 `title / aliases / syllabus_code / 06-外部资料导入` 链接解析与旧命名兼容；quick validate 已压到 `0 error / 492 warning / 920 info`，且 `日期格式` warning 清零。详见 [[09-审计报告/auto-validation/2026-06-29-validation]] 与 [[00-首页/工作日志/2026-06-30]] ✅
> - **习题配套建设扩展完成（06-30）**：为4份缺习题讲义补充三级习题（超分子化学16题+过渡金属催化16题+周环反应15题+有机波谱分析15题=62题）；创建 [[11-模板/模板-习题三级分类]] v1.0；更新自学完整版模板和讲义升级样板纳入3级标准。8份讲义共160题 ✅
> - **教学逻辑链路首轮审计完成（06-30）**：7层全链路审计（班级授课计划→教学过程总表→备课大纲→讲义→KP→专题页→教学洞察→教学逻辑提炼→考纲），发现文件名引用不匹配（7处）+KP/专题页目录为空（2处严重）；教学洞察16/16+教学逻辑提炼6/6全部存在；考纲20/20考点全覆盖。详见 [[00-首页/审计报告-教学逻辑链路一致性审计-2026-06-30]] ✅
> - **结构化学教学过程总表完成**：覆盖第9-24课16课时，含KP级映射+认知冲突+教学策略+教学洞察引用 ✅
> - **Marp PPT管线试点成功**：原子结构§1发展史20页PPT→PPTX(4.1MB) ✅
> - **新增 2 张后续任务卡（06-30）**：`validate_kb 与 published 断链目录治理`（P2）· `结构化学讲义低质手绘图替换与图片提质`（P1）
> - **入口文件已同步**：状态摘要·活跃任务·工作日志已更新 ✅
> - **2026-06-30 状态收口**：结构部分收官 / PDF 管线规范化收口 / 入口文件与工作流同步已完成；当前维护主矛盾已切到 `published` 断链目录治理与结构化学图片提质 ✅
> - **当前活跃任务（2张 active + 10张 blocked）**：
>   - 🔴 P1: [[00-首页/活跃任务/任务卡-KP待完善项非文字任务清零]] — 21项全为外部工具/真人依赖（ChemDraw 8/计算化学5/依赖KP 4/录课2/长期2），Agent可做=0
>   - 🟢 P3: [[00-首页/活跃任务/任务卡-validate_kb每周运行]] — 每周一 08:23 定时巡检，基线 0 error / 2179 warning（2026-08-03 确认），目录重组后立即补跑
>   - ⛔ blocked: [[00-首页/活跃任务/任务卡-PPT制作方法探究与试点]] — 降级为后续研究项，待用户重启
>   - ⛔ blocked: [[00-首页/活跃任务/任务卡-非正文剩余工作落地]] — PPT/教具/工具卡/评分表/纸质试印
>   - ⛔ blocked: [[00-首页/活跃任务/任务卡-实验模块决策]] — 等待用户决定是否纳入竞赛教学主线
>   - ⛔ blocked: [[00-首页/活跃任务/任务卡-学生反馈闭环建立]] — 等待真实学生使用反馈
>   - ⛔ blocked: [[00-首页/活跃任务/任务卡-纸质版排版测试]] — 等待真实预览/打印反馈
>   - ✅ completed: [[00-首页/活跃任务/任务卡-媒体去重]] — 双轨落地+孤儿隔离+zip备份完成，2026-08-06 用户确认关闭（残留挂账转图片治理）
>   - ⛔ blocked: [[00-首页/活跃任务/任务卡-答案库建设]] — 赵鑫光483题+ABOC 224题，高token暂缓
>   - ⛔ blocked: [[00-首页/活跃任务/任务卡-学生材料清理]] — 需用户决策试印版保留
>   - ⛔ blocked: [[00-首页/活跃任务/任务卡-clayden去重]] — 134文件比对，暂缓
>   - ⛔ blocked: [[00-首页/活跃任务/任务卡-ΔrH散文口径规范]] — 需确认规范口径
> - **真题链：25-39届初赛全覆盖 + 25-32届决赛已入库** ✅

**读完本页后通常下一跳**

- 想看当前全局状态：[[状态摘要]]
- 想看默认推进方向：[[Agent战略方向]]
- 想看三份入口怎么配合：[[Agent入口文件关系说明]]
- 想排查任务卡聚合异常：[[自动汇总-任务卡健康检查]]

## 使用规则

1. 新任务优先新建一张任务卡到 `00-首页/活跃任务/`
2. 每张任务卡必须维护 frontmatter，至少包含：`status`、`priority`、`area`、`source_notes`、`related_notes`、`evidence`
3. 任务完成后，不删除卡片；改为 `status: completed` 并补 `completed`
4. 任务卡记录"还有什么没做"；任务经过与验证细节写入 `[[工作日志]]`
5. `[[状态摘要]]` 和数据库看板只从任务卡聚合，不再从日志正文扫描短期 follow-up
6. 若某任务已被审计结论吸收（如复用已有专题页、接受历史命名、整体闭环已覆盖），应直接关卡，不必再补冗余产物
7. 若某任务明确等待用户决策、实体打印验证、真实课堂反馈或外部资料到位，应改为 `blocked`，不要长期停留在 `active`
8. 若任务属于资料导入 / 资料提炼 / 网课提炼，关闭前必须确认相关提炼页已补“备课抽料资产标记”；否则只能记为“已入库待接入”，不应直接 `completed`

## 状态快照

> 让 Agent 一眼看清"现在到底有多少还在做、多少已完成、多少正在等外部条件"，避免 `blocked` 只写进 frontmatter 却在入口层隐身。

```dataview
TABLE
  length(rows) as 总数,
  length(filter(rows, (r) => r.status = "active")) as Active,
  length(filter(rows, (r) => r.status = "blocked")) as Blocked,
  length(filter(rows, (r) => r.status = "completed")) as Completed,
  length(filter(rows, (r) => r.status = "active" AND r.priority = "P1")) as "P1 Active"
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
```

## 健康检查

> 用于快速发现 frontmatter 漏填、完成态漏日期、以及证据字段为空的任务卡，避免入口页统计失真。

```dataviewjs
const pages = dv.pages('"00-首页/活跃任务"')
  .where(p => p.type === "活跃任务卡");

const issues = [];

for (const p of pages) {
  const missing = [];

  if (!p.status) missing.push("缺 status");
  if (!p.priority) missing.push("缺 priority");
  if (!p.area) missing.push("缺 area");
  if (!p.source_notes || (Array.isArray(p.source_notes) && p.source_notes.length === 0)) missing.push("缺 source_notes");
  if (!p.related_notes || (Array.isArray(p.related_notes) && p.related_notes.length === 0)) missing.push("缺 related_notes");
  if (!p.evidence || (Array.isArray(p.evidence) && p.evidence.length === 0)) missing.push("缺 evidence");
  if (p.status === "completed" && !p.completed) missing.push("completed 缺完成日期");

  if (missing.length > 0) {
    issues.push([p.file.link, missing.join("；"), p.updated ?? "-"]);
  }
}

if (issues.length === 0) {
  dv.paragraph("✅ 当前任务卡 frontmatter 关键字段完整，未发现 completed 漏日期问题。");
} else {
  dv.table(["任务卡", "异常", "updated"], issues);
}
```

## 状态维护速查

| 状态 | 何时使用 | 必填要求 |
|:---|:---|:---|
| `active` | 仍需继续推进，且当前不依赖外部决策/资料/反馈 | `status`、`priority`、`area`、`source_notes`、`related_notes`、`evidence` |
| `blocked` | 明确等待用户决策、打印验证、课堂反馈、外部资料 | 保留原有证据，并在正文写清阻塞原因 |
| `completed` | 已有明确产出或审计结论吸收，不再继续执行 | 除基础字段外，**必须补 `completed: YYYY-MM-DD`** |

## 任务面板

### 🔴 P1 高优

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "active"
  AND priority = "P1"
SORT file.name ASC
```

### 🟡 P2 中优

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "active"
  AND priority = "P2"
SORT file.name ASC
```

### 🟢 P3 低优 / 长期

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "active"
  AND priority = "P3"
SORT file.name ASC
```

### ⛔ Blocked / 等待外部条件

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  priority as 优先级,
  area as 领域,
  join(related_notes, " · ") as 关联,
  join(evidence, " · ") as 证据,
  updated as 更新
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "blocked"
SORT updated DESC, file.name ASC
```

### ✅ 近期完成

```dataview
TABLE WITHOUT ID
  file.link as 任务,
  completed as 完成日期,
  join(evidence, " · ") as 证据
FROM "00-首页/活跃任务"
WHERE type = "活跃任务卡"
  AND status = "completed"
SORT completed DESC, file.name ASC
LIMIT 10
```

## 快速入口

- 结构化数据库视图：[[02-数据库/活跃任务.base|活跃任务.base]]
- 当日过程归档：[[工作日志]]
- 启动快照：[[状态摘要]]
- 自动事实中间层：[[自动汇总-任务卡健康检查]]
- 入口职责边界：[[Agent入口文件关系说明]]
- 默认执行路径：[[Agent最小执行协议]]

*本页只做聚合，不再手写维护任务明细。任务新增、推进、关闭请直接更新任务卡。*
