# 题组 Word 成品索引

> 全部题库类 Word 成品的导航与命名规范。2026-09-08 重组：后缀式统一命名 + 按册/模块/篇细分 + 旧版统一收编 `_archive/`（全程零删除，docx 守恒 819）。

## 目录结构（2026-09-08 重组后）

| 目录 | 内容 | docx 数 |
|:--|:--|--:|
| `习题书/` | 四篇 54 章＋来源索引＋附录；`第X篇-…/章名-教师版\|-学生版\|-学生版-打印版.docx` | 108 |
| `专项卷/` | `一分册/`（I~XII 双版 24）＋`二分册/`（I~XII 双版 24）＋结构化学专项卷、省预赛专项卷I（双版） | 52 |
| `第一轮习题集/` | 随堂层：`化学原理/`（8 习题集双版＋方程式专项＋总索引双版）＋`结构化学/`（5 题组双版＋复习讲义） | 29 |
| `第一轮·竞赛教材版/` | 第一轮**跟课专题卷**（竞赛导向题源）：结构 6＋原理 9 共 15 专题×25 题 ＋ 总索引 | 31 |
| `第一轮·综合套卷/` | 第一轮**综合测**：12 套×30 题（结构化学 15＋原理 15）＋ 总索引 | 25 |
| `第二轮习题集/` | 随堂基础·第二轮（章节练习 d2~d3，8 卷×20 题双版） | 16 |
| `综合模拟卷/` | 竞赛导向四子类混编（I~VII 双版） | 14 |
| `阶段测试卷/` | 原理 I/II、有机（卷＋答案册）、结构、元素（教师版/学生版＋答案册） | 11 |
| `预赛基础卷/` | 各省预赛 d2~d3（3 卷×25 题双版） | 6 |
| `_archive/` | 旧版收编（见 `_archive/README.md`） | ~300 |

## 2026-09-18 整理记录（第一轮两系列）

> 触发：源题版式治本（commit `0742a0c42`）后重出 docx，顺带清出两类错位。

1. **`04-课件/习题集/` 下 56 份未跟踪 docx 已移入回收站**
   —— `第一轮·竞赛教材版/`（31 份）与 `第一轮·综合套卷/`（25 份）曾把 docx 直接落在 **md 源目录**，
   与「出口双轨（题库产物 → `00-首页/题组Word/`）」冲突。
   移前已全量核验：56/56 在 `00-首页/题组Word/` 均有同名产物，且**图数 / OMML 公式数完全一致**
   （仅文本差 1~5 字符＝本轮版式修复的段落分隔差异）→ 确认均为修复前旧版。
   备份：`.workbuddy/tmp/_docx_cleanup_bak/04课件docx_56.tar`（46 MB）。
2. **3 份旧命名 docx 已归档**
   —— `第一轮结构-4-晶体结构-教师版.docx`、`总索引.docx`、`第一轮综合卷01-教师版.docx`
   （旧命名：半角连字符 + 无括号 + 无前导下划线），已复制入各自 `_archive/`。
3. **根目录残留 2 份旧命名 docx（09-18 复核：仍被锁，非待办动作）**
   —— `总索引.docx`、`第一轮综合卷01-教师版.docx` 仍留在 `第一轮·综合套卷/` 根目录
   （`第一轮结构-4-晶体结构-教师版.docx` 已在 09-18 重出时被新命名版取代）。
   ⚠️ **不是「待 WPS 关闭后删除」的挂起动作，而是确认无法删除**：
   经 09-18 复核，句柄由 **WPS 后台服务**（`wps.exe` ＋ `wpscloudsvr.exe`，**非用户打开预览**）持有，
   `os.remove` / `shutil.move` 均报 `PermissionError 13 / WinError 32`（绕过 safe-delete 后仍失败）。
   `_archive/` 副本已齐（**md5 已核**），根目录残留**不影响功能**（**均未被 git 跟踪**，不产生 diff）。
   → **处置结论：不再尝试删除**，任其自然失效；若日后 WPS 服务释放句柄可随手清理。


## 统一命名规范（成品输出规则收拢）

1. **版本后缀词表**（全局唯一，禁再发明）：
   - `-教师版`＝题面＋参考答案区；`-学生版`＝纯题面；`-学生版-打印版`＝黑白灰度打印（仅习题书族）
   - `-答案`＝答案单独册（仅阶段测试卷族历史惯例保留）
2. **分隔符**：一律连字符 `-`（文件名禁中点 `·`、空格混排）
3. **卷号**：罗马数字 I~XII 保留（卷族既有谱系）；章号阿拉伯数字
4. **组卷规则**：每卷 ≤15 题；同一来源 ≤2 题（模拟卷）；教师版/学生版双版成对
5. **出口双轨**：讲义类产物 → `06-学生侧材料/讲义/`；题库类产物 → `00-首页/题组Word/`；旧版一律进各自 `_archive/`，禁止在产物目录自建归档夹

## 同步管线（防复发）

- **习题书**：`gate_exercise_books.py --rebuild` → `build_module_book.py`（md 源）→ `build-all-handout-docx.py --batch-root … --output-dir 00-首页/题组Word/习题书 --filename-suffix -教师版|-学生版`（拍平后缀式直出）
- **打印版**：`.workbuddy/scripts/build_print_student.py` 从学生版后处理生成
- **试卷族**：`.workbuddy/scripts/build_exam_paper.py`（md 已带 -教师版/-学生版 后缀）→ `build-all-handout-docx` 转 docx
- 结构校验：`validate_docx_structure.py`（已适配后缀式 rglob；打印版按学生版源核对）
- 闸门：`validate_kb.py`（0 Error 出卷）＋`jsyaml_verify.js`＋`--strict-images`

## 已知待办

- ✅ **嵌图不匹配已清零（2026-09-09）**：曾 9 份 docx 内嵌图数 < md 嵌图数，根因三类并全部修复——①源 md 游离 `$$`（1-化学基础与计量，径改删 3 行）②源 md 行内公式被断行腰斩 `\frac`→`rac{`（3-化学动力学，径改合并 4 处）③OCR 残行 `/>…\( \begin{array}…` 未闭合致 pandoc raw-TeX 吞图（2-分子结构与化学键等，管线级修复：`_preprocess_markdown` 新增行级数学平衡守卫）——重建教师/学生/打印版 4 章 ×3 版本后 `validate_docx_structure` 全绿（docx=105，mismatch=0）
- ⚠️ 红区挂账：H1 混入的**源头**在 `04-题库/教材习题/化学竞赛初赛讲义/题-047-…习题8.49.md` 等题库文件的答案块（下一讲导语随转录带入）——题库侧未动（红区），习题书 md 已在下游径改清除；若题库线日后修题-047，习题书无需再动

## 数量

- 习题书 docx×105 = 35 份/版本 ×3 版本（31 章＋来源索引＋附录＋2 个跨篇重名章以消歧前缀名并立）
- 第一轮两系列 docx×**56**（2026-09-17 新增）＝「第一轮·竞赛教材版」**31**（15 专题×2 版＋总索引）＋「第一轮·综合套卷」**25**（12 套×2 版＋总索引）。源 md 在 `04-课件/习题集/`，由 `build-all-handout-docx.py --batch-root … --output-dir …` 批量转换；产物名按本 README 规范重命名（`（教师版）`→`-教师版`、去中点 `·`）。

## 相关审计与工具

- 来源构成与指向性分级：`09-审计报告/2026-09-06-题库质量与考试指向性评估.md`、`-习题书来源构成报表.md`
- 习题书源侧说明：`04-课件/习题集/README.md`
- ⚠️ C14005：带内嵌图 docx 在腾讯文档预览器打不开属预览器缺陷，文件本身合法
