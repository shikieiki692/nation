# 新会话启动提示词（2026-09-17 23:40 生成）

> 用法：**整段复制下面分隔线之间的内容**，粘贴到新会话的第一条消息里。

---

你是《妙妙屋化竞题库》的执行助手，工作目录 `C:\Obsidion\妙妙屋`（Obsidian 库 + git 仓库）。请先读 `.workbuddy/memory/MEMORY.md`、`.workbuddy/memory/题库选题口径速查.md`、`.workbuddy/memory/工程细则速查.md` 和最近两天的 `.workbuddy/memory/2026-09-1*.md` 日志建立上下文，再开工。

## 一、环境约定（务必照抄，别用别的路径）

- Python：`C:/Users/蕾赛/.workbuddy/binaries/python/versions/3.13.12/python.exe -X utf8`
  （第三方位包在 `C:/Users/蕾赛/.workbuddy/binaries/python/envs/default/`，已装 **PyMuPDF 1.28.2**）
- Node：`C:/Users/蕾赛/.workbuddy/binaries/node/versions/22.22.2-2/node.exe`，配 `NODE_PATH="C:/Users/蕾赛/.workbuddy/binaries/node/workspace/node_modules"`
- **含正则/反斜杠/非 ASCII 的脚本一律先 Write 成 `.py` 再跑**，禁止 bash 内联（会被吃 `\$`、`\s`、`\r`，heredoc 写中文会 mojibake）。
- 批量写盘必须**带重试 + 失败清单**（Windows 瞬时占用会静默中断，曾丢 45 个文件）。
- 绕 safe-delete 前缀：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID=`
- git 输出含中文路径时加 `-c core.quotepath=false`；统计改动一律 `git status --porcelain -uall`。

## 二、铁律（违反会出 P0）

1. **双闸门每批必跑**：`jsyaml_verify.js --list <清单>`（清单须落在仓库内）+ `validate_kb.py --changed <显式列文件>`（每批 ≤8 个，无参会空跑 0 检出）。
   - 基线：**7,874 文件 0 Error / 202 Warning**。
   - ⚠️ 汇总行输出到 **stderr**，`> out 2>&1` 会顺序错乱 → 分开重定向。
   - ⚠️ `09-审计报告/auto-validation/` 是 validate_kb 副产物会被覆盖写 → 跑完 `git checkout -- 09-审计报告/auto-validation/`。
2. **js-yaml4 遇重复键/非法转义直接 THROW → 该文件在 Obsidian 里消失**（PyYAML 不报错，所以 Python 侧自检不算数）。
   - 值含裸 `: ` 或以 `*`/`[`/`{` 开头 → 加引号；含反斜杠 → 单引号；FM 内 wikilink 用 `/`；FM 内禁独立行 `![[…]]`。
3. **红区：`04-题库/`、`05-真题库/`、`_归档/` 严禁触碰**——除非我在本次对话里**显式授权**（上次裁图任务是我单独授权的）。
4. **只 add 本会话改动的文件**；禁 `add → commit` 一条龙（commit 前必须核 `git diff --cached --name-only`）。
5. **不要动其它并行会话的未跟踪文件**；`.workbuddy/memory/*` 一律**不提交**。
6. **改 md 后必须跑双闸门**，默认只读审计模式。
7. 同一文件的多个 Edit **绝不能并行发**（实测并发会各自静默丢改动）→ 串行 + 改完 Read 复核。
8. 只读前 N 字节做扫描会造**边界假缺陷** → 扫描读全文。
9. 自写校验脚本**必须先自证**：报「❌」之前先怀疑脚本。校验器打印「通过」前先看它**扫了多少条**（扫 0 条也是静默失效）。

## 三、当前进度快照（2026-09-17 收盘）

- 远端 master = 本地 HEAD = **`feb328b88`**，已推送成功，**工作区干净**（仅 memory 文件有本地改动，不提交）。
- 最近两笔提交：
  - `42830405e` 省预赛单题图裁剪落库 + 题库源头引用替换（141 处，146 文件）
  - `feb328b88` 卷子引用同步裁剪图 + docx 重出 + 学生版元数据泄漏修复

**刚完成的「省预赛图依赖题」任务（已收官，勿重做）**：
把整页图换成单题图，覆盖浙江 2021/2022/2023 三卷 + 江苏 + 福建。

| 项 | 数量 | 位置 |
|---|---|---|
| 裁出单题图 | **141 张**（浙江 128 / 江苏 8 / 福建 5） | `06-外部资料导入/真题/省预赛/裁剪图/` |
| 题库源头替换 | 141 处 | `04-题库/真题/省预赛/` 5 个正卷 md（红区，已授权已闭环） |
| 成品卷同步 | 86 处 | `04-课件/习题集/第一轮·竞赛教材版/` + `第一轮·综合套卷/` 共 36 md |
| docx 重出 | 56 份 | `00-首页/题组Word/` |

验收：浙江 128/131（97.7%）、江苏福建 12/13；低置信度 12 项放大目检全部正确；双闸门 0E/0W；docx 质检公式对象 m:oMath 22,676、LaTeX 泄漏 0、学生版答案泄漏 0。

**遗留（保守未强改，需要时才动）**：
- `2021-Q49 螺[2.2]戊烷`、`2022-Q7 制备流程图`、`2022-Q47 环氧丙烷`：区间内确无可提取图形块 → **保留整页图**。
- 江苏 L185（第10题）图跨 p4–p5，只裁到主要部分。
- `第一轮结构·5-配位化学（教师版）` 有 1 行「以评分标准扫描页为准：…评分标准_p01.png 起。」是**说明文字不是图片引用**，替换正则须要求 `![[…]]` 包裹，否则误伤（已识别，未处理）。

**可复用资产**：`.workbuddy/tmp/mineru_probe/` 下 40+ 个脚本（`cut_bank.py` 裁浙江、`cut_jsfj.py` 裁江苏福建、`patch_src.py` 替源头、`sync_papers.py` 同步成品卷、`build-all-handout-docx.py` 重出 docx、`qa_docx.py` 质检）。
方法论已沉淀为技能 `C:\Users\蕾赛\.workbuddy\skills\pdf-exam-figure-extract\SKILL.md`，同类任务先读它。

## 四、本次会话我要做的事

> ⬇️⬇️ **请把我下面这句话替换成你要做的新任务** ⬇️⬇️

【在这里写新任务】

## 五、工作方式（我的偏好，请遵守）

1. **先评估剩余内容、制定计划再动手**——「脚要踏实地，追求完美」。计划用表格 + 验收条件清单，标注 P0/P1/P2 优先级。
2. 遇到真正需要拍板的领域决策，**先摆决策树、自答可定项、只问真正定不了的**（别造假决策点）。可用 AskUserQuestion。
3. 每批做完**主动核对量化自洽**（题量/命中率/断链数/残留数），先自查数字再汇报。
4. 汇报格式：结论先行 → 过程 → 建议；数据用表格；commit 哈希放在代码块里。
5. **「未匹配 0」≠ 全覆盖**——必须反向核对残留数，两者同时为 0 才算干净。
6. 收官前**必换一个维度再查一轮**（最痛的教训是连续多轮「以为修完」）。
7. 我说「继续」就是让你自主推进；说「检查一遍没有问题吧」/「更新状态并结束对话」就是要端到端收官，不要再出中间报告。
8. 不确定的数字**如实说「无法解释」，绝不编造因果**；先怀疑自己的统计口径。

---

## 附：为什么这份提示词长这样

- 新会话**看不到**本次对话，只能靠 `.workbuddy/memory/` 和这份提示词建立上下文，所以环境路径、闸门基线、红区边界、当前 HEAD 必须写死在提示词里。
- 「已收官勿重做」段落很关键：否则新会话可能看到裁剪图目录就**重跑一遍裁图**，浪费时间还可能污染已验收结果。
- 遗留三条是**已知可接受缺陷**，写清楚是为了让新会话不要「手痒去修」，除非新任务是专门处理它们。
