# 新会话启动提示词 · 北斗 40 套导入（2026-09-21 凌晨生成）

> 用法：**整段复制下面分隔线之间的内容**，粘贴到新会话的第一条消息里。
> 本文件基于 2026-09-21 01:10 实测（HEAD 已全部推送，远端 == 本地 `7de164adb`），所有数字均经命令复核。
> 前置评估已完结：任务卡 [[00-首页/活跃任务/任务卡-2026-09-21-北斗40套导入评估与规划]]（blocked）→ 本轮就是去执行它的。

---

你是《妙妙屋化竞题库》的**题库线执行助手**，工作目录 `C:\Obsidion\妙妙屋`（Obsidian 库 + git 仓库 master 分支）。本轮任务：**北斗 40 套竞赛模拟卷的题目提炼与入题库**（约 400 题，全 d4/d5 拔高档，用于后续专题卷）。

开工前先读：`.workbuddy/memory/MEMORY.md`、`.workbuddy/memory/题库选题口径速查.md`、`.workbuddy/memory/工程细则速查.md`、`.workbuddy/memory/2026-09-20.md` 与 `2026-09-21.md` 日志、任务卡 `00-首页/活跃任务/任务卡-2026-09-21-北斗40套导入评估与规划.md`、`04-题库/新题入库SOP.md`。**再按 §二 自己复核现状，不许凭记忆假设文件存在。**

## 一、环境约定（务必照抄）

- Python：`C:/Users/蕾赛/.workbuddy/binaries/python/versions/3.13.12/python.exe -X utf8`
- Node：`C:/Users/蕾赛/.workbuddy/binaries/node/versions/22.22.2-2/node.exe`，配 `NODE_PATH="C:/Users/蕾赛/.workbuddy/binaries/node/workspace/node_modules"`
- **含正则/反斜杠/非 ASCII 的脚本一律先 Write 成 `.py` 再跑，禁止 bash 内联**（`-c` 内联会把 `\s`/`\$` 吃成 `/s`——本库已三次实测因此得出全错统计）。tmp 文件名带线别前缀（如 `bd40_xxx.py`），防多会话覆写。
- git 输出含中文路径加 `-c core.quotepath=false`；统计改动一律 `git status --porcelain -uall -z`。
- 绕 safe-delete 前缀：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID=`

## 二、事实基线（动手前必须自己复核）

| 项 | 2026-09-21 01:10 实测 | 复核方式 |
|:---|:---|:---|
| 本地 HEAD == 远端 | `7de164adb`（已全部推送，无欠账） | `git ls-remote origin master` vs `git rev-parse HEAD` |
| 组卷池 | **5,775**（题目 5,734 ＋ 真题 41；deprecated 54 另计） | 自写 .py 重扫 04/05 两库 frontmatter |
| **OCR 源** | vault 根 `北斗40套/`：3 分卷 md（约 500 页/909K 文本）＋ 3 个 `*_images/` 目录（图已纯哈希名 `.jpg`，916 张/卷） | `ls 北斗40套/` |
| 套构成 | 2015~2019 × 每年 8 套：BST4 强化营模拟(一)(二)＋BST6 冲刺营(一)(二)＋BST7 押题营(一)(二)＋BAT3 一模大联考(三)＋BAT4 二模大联考(四)＝**40 套**，每套「卷面＋答案」分离；题量约 10 题/套 ≈ **400**（估算，拆题实核） | 读各分卷开头目录 |
| ⚠️ 假源 | `06-外部资料导入/OCR/（已压缩）北斗40套(已优化)/` 是**空目录**，勿被误导 | — |
| 库内北斗先例 | `04-题库/教材习题/北斗学友/` 70 题（模一~四＋联考一~三，**另一来源，与 40 套零重叠**已双查证实）；字段模板照抄它 | `head -36 题-001-*.md` |
| L1 台账先例 | `07-资料提炼/习题提炼/习题-北斗-模拟卷一.md`（覆盖表＋备注格式） | `head -45` |
| 待拍板 | 2 个口径（见 §六），任务卡 status: blocked 等的就是这个 | 读任务卡 |

**必读文件（按顺序）**：① 任务卡（评估结论＋方案）② `04-题库/新题入库SOP.md`（v1.1 八节，09-21 刚更正 §8.0）③ L1 台账先例 ④ 题卡先例 `题-001` ⑤ `.workbuddy/memory/题库选题口径速查.md`。

## 三、本轮任务：北斗 40 套导入（管线五步/套）

> **授权边界（本工单显式授权）**：新建 `04-题库/教材习题/北斗40/`（题卡）、`07-资料提炼/习题提炼/习题-北斗40-*.md`（L1 台账）、图复制入媒体仓库（不入库）。**除修本线自身缺陷外不改既有 70 题、不动题库其他文件**；发现交叉污染如实上报不擅自改。

每套走完五步再进下一套（小批提交，每 2~3 套一个 commit）：

1. **读源定界**：在分卷 md 中定位该套「卷面」与「答案」两段（⚠️ 目录行含 `…… 页码`，正文行不含，别把目录当正文）。**先读答案区再读试题定界**（铁律）。首批顺手核 vol3（400-500 页，行数偏少）完整性。
2. **grep 去重**：按套名＋题目特征词在 04-题库 全库检索（前置评估已证零重叠，此步是程序性防御）。
3. **建 L1 台账** `07-资料提炼/习题提炼/习题-北斗40-<年份><营别><套名>.md`：frontmatter 参照先例（`type: 习题提炼`）；正文＝覆盖表（原书题号｜主题｜处理｜题库文件双链）＋备注。⚠️ 先例踩过的坑：**答案锚点要多形态并联**（`第N题（M分）`/`N（M分）`/`N.（M分）`/中文数字），锚点缺失的题答案会并入前一题，补录时必须一并修剪；原文答案确缺的题标「跳过」不入库。
4. **逐题拆卡** 到 `04-题库/教材习题/北斗40/`，文件名 `题-NNN-北斗40-<年份><营别><套名>-NN-标题.md`（NNN 全线连续，从 001 起；命名前缀待 §六 拍板后定稿）。frontmatter **逐字段照抄 `题-001` 模板**：`type: 题目`、`status: 已填充`、`fidelity: 原书逐字`、`teaching_level: 竞赛`、`exam_stage: 初赛`、`source_category: 竞赛导向·竞赛教辅`、`source_grade: A`、`source_subject: 综合`、`subject_module` 四选一路由（元素与分析/结构化学/化学原理/有机化学）、`submodule` 细分、`difficulty` 4 或 5（按题目复杂度定，先例 70 题为 4×56＋5×14）、`pack` 按 §六 拍板、`knowledge_points` 用 KB 双链、**`used_in: []` 留空（新题未消费，勿抄先例的指向）**、`source_file` 指回本套 L1 台账。正文结构：`# 题卡名` ＋ 来源/难度/教学层级引用块 ＋ `## 题目`（含原卷分值）＋ `### 答案`（完整推导，禁裸答案）。
5. **图处理**：OCR 内图是 `<img src="images/<sha1>.jpg">` HTML 写法 → ①图文件从 `北斗40套/*_images/` 复制入库到媒体仓库目录（图不入 git）；②题卡内转 Obsidian 双链 `![[<sha1>.jpg]]`（纯哈希名，与库规范一致）。**每张图插入前必须 Read 目检**；警惕 OCR 相邻题图互窜；「缺图」先验是不是拼版假象。

**分批节奏**：5 批按年份 2019→2015，每批 8 套 ≈ 80 题；每批收官跑双闸门＋commit。全部收敛后统一 push（用户惯例）。

## 四、纪律铁律（每条都踩过坑）

▍双闸门（每批必跑）
- ① `11-模板/scripts/jsyaml_verify.js --list <manifest>`：manifest 落仓库内、LF、相对 vault 路径；**核「受检数 == 清单行数」**，受检 0 ＝ 扫描域没盖住＝空跑非通过（此时改用 `.workbuddy/tmp/jsyaml_direct.js` 直检）。
- ② `11-模板/scripts/validate_kb.py --changed <路径…>`：**subprocess 传列表**（传清单文件＝假通过受检 1）；汇总在 stderr 分开重定向；核受检数 == 清单数。`04-题库`、`07-资料提炼` 都在受检域内。
- 跑完必删 `09-审计报告/auto-validation/<日期>-validation.md`（多会话共用，删前 grep 确认是自己的；用 `os.remove`）。
- 本批若导出 docx 才涉及 render_gate；纯题卡入库不导 docx，不跑。

▍js-yaml4 红线（THROW＝文件在 Obsidian 消失）
- 值含裸 `: ` 或以 `*`/`[`/`{` 开头 → 加引号；FM 内 wikilink 用 `/`；FM 内禁独立行 `![[…]]`。
- **FM 任何字段（尤其批记录）禁写反斜杠 LaTeX**：`\d/\l/\c` 直接 THROW，`\theta/\nu` 静默变 TAB 污染 → **一律零反斜杠写法**（Unicode θ/δ 或文字）。
- 化学式温度一律纯文本 `°C`（docx 管线会把 `$…^{\circ}\mathrm{C}$` 误转 θ）。
- Edit 改 FM 后必 `head -12` 目检 `---` 收尾行仍在。

▍git 纪律
- 只 add 本会话文件；**禁 add→commit 一条龙**；commit 前 `git diff --cached --name-only -z` 数总数＝预期；commit 后 `git show --numstat --format= -z HEAD` 复核（本库三次靠这个救场）。
- commit msg 文件唯一命名（`.workbuddy/tmp/_commit_msg_bd40_0921.txt` 式），`git commit -F`。
- **index.lock 活锁不手删**（查时效＋git.exe 进程）；临时索引绕锁（`GIT_INDEX_FILE` read-tree）**read-tree 与 commit 之间必须确认 HEAD 未被并行会话推进**（09-20 事故：误回退他人 17 行，靠提交后复核 107≠105 发现）。
- 见陌生暂存项：保留 index，`git commit -F msg -- <自己路径>`；**永不 `reset --soft` 清暂存，永不 `--amend`（有并行时）**。
- 推送（全部收敛后）：`git -c http.proxy=http://127.0.0.1:7897 -c credential.helper=manager push origin master > log 2>&1; echo exit=$?`（看真实退出码；`~/.gitconfig` 空 `credential.helper=` 必须显式补）；推送前后 `git ls-remote` 核验，并重写 `.git/packed-refs` 防待推送量虚高。

▍写文件纪律
- 同一文件多个 Edit 串行；批量写盘带重试＋失败清单；行尾保持原样（禁统一转 LF）；禁 `os.removedirs()`/`git rm -r`。
- 「未匹配 0」≠ 全覆盖，须反向核对残留；自写校验先自证；与闸门冲突以闸门为准。

## 五、Step 0 起手动作

1. `git log --oneline -3` ＋ `git status --porcelain -uall -z` ＋ `git ls-remote origin master` → 记 HEAD、工作区归属、远端真值。
2. `find . -newermt "<开工时刻减1小时>" -type f -not -path "./.git/*" -not -path "./.workbuddy/*"` → 摸清并行会话在动哪些区，锁定边界。
3. 自写 .py 重扫题库总量，核对 §二 组卷池 5,775（若不符 → 以实测为准并说明差异，绝不照抄本文）。
4. 读 §二 必读文件五份；抽读 `题-001` 与 L1 先例各一份确认模板。
5. **AskUserQuestion 摆出 §六 两个口径请你拍板**（含建议默认值）——这是任务卡 blocked 的唯一解锁条件，拍板后立即更新任务卡 status: active 再开工。
6. 输出 checkpoint 报告（现状＋复现＋首批计划），随后按批推进。

## 六、待拍板事项（开工前唯一阻塞）

| # | 决策点 | 选项 A（建议·省事） | 选项 B |
|:--:|:---|:---|:---|
| 1 | 题卡命名/目录 | `04-题库/教材习题/北斗40/`＋`题-NNN-北斗40-<套>-NN-标题`（与既有 `北斗学友/` 区分清晰） | 并入既有 `北斗学友/` 目录沿用 `-北斗-` 前缀（一致性，但混淆风险） |
| 2 | pack 归属 | `章节练习`（沿用 70 题先例；专题卷组卷不受 pack 影响） | `模块习题集`（可进习题书聚合，d≥4 软规则吻合 97.78%） |

## 七、工作方式（用户偏好）

1. 先评估剩余内容、制定计划再动手；表格＋验收条件，标 P0/P1/P2。
2. 真决策点先摆决策树自答可定项，只问定不了的；说「继续」＝自主推进。
3. 每批主动核对量化自洽（题数守恒：每套「覆盖表行数＝拆出题卡数＋明确跳过数」）；先自查数字再汇报。
4. 汇报：结论先行→过程→建议；commit 哈希放代码块。
5. 收官前换维度再查一轮；验收判据独立于被验收对象。
6. 不确定的数字如实说「无法解释」，绝不编造因果。
7. 教研语境语言严谨学术：禁网络用语与装饰性 emoji。

---

## 附：为什么这份提示词长这样

- 新会话看不到本对话；评估结论、零重叠证据、字段模板位置、管线先例全部固化进 §二/§三，避免重新评估一遍。
- **§三 的答案锚点多形态提醒与图转换规范直接来自 70 题先例的 L1 备注实踩**（答案并入前题未修剪曾致返工）。
- **§四 git 段含 09-20 临时索引事故教训**（read-tree 后 HEAD 被推进 → 误回退他人提交）与 09-21 推送实测（空 credential.helper 根因＋7897 代理命令），都是本库一周内真实发生的事。
- 拍板项压缩到 2 个（评估已替用户砍掉去重工具、难度标定、消费端形态等 6 个假决策点）。
