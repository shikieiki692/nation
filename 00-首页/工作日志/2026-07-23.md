---
title: 工作日志-2026-07-23
type: 系统
role: 工作日志
date: 2026-07-23
updated: 2026-07-23
---

# 2026-07-23 工作日志

## 核心成果

### 断链修复工程（承接 07-22 全库深度检查报告）

**背景**：用自研脚本 `11-模板/scripts/build_link_map.py` 全库扫描（5350 文件 / 85648 链接），基线断链 **6494 处 / 3734 个不重名**。按「改名修复（Track A）→ 高频别名挂接（Track B）→ 待建 KP 登记（Track C）」三轨推进。

**成果总览**：

| 检查点 | broken 处数 | 不重名断链名 | 说明 |
|:---|:---:|:---:|:---|
| 基线 | 6494 | 3734 | 修复前 |
| M1 | 6398 | 3682 | Track A + 脚本修正后 |
| M2 | 5587 | 3510 | Track B/C 完成后 |
| M3 | 5587 | 3510 | 两项挂起决策闭环（质量改进，断链数不变属预期） |

**累计净修复 907 处 / 224 名**。

### Track A：改名类修复 72 处 / 41 个文件

- 应用 `02-数据库/link_map_rewrite_plan.json` 改写计划，76 处中 72 处成功。
- 4 处跳过：09-审计报告与工作日志中的历史记录行，按规则保持原样。

### Track B：高频断链别名挂接 35 项全落地

- **26 个文件实改**：在目标 KP frontmatter `aliases` 追加断链名（如 光度法→分光光度法、能斯特方程→Nernst方程、CFSE→晶体场理论、KP-铁/钴/铝/锡/镓/镍→对应元素 KP、3 个质心教学逻辑提炼文件挂带括号变体名）。
- **8 项免改**：别名早已存在，断链纯因脚本 BOM bug（见下）。
- **特例改写**：`[[​"化学式推断"]]`（YAML 三重引号残留）改 7 处/7 文件→`[[化学式推断]]`；路径式 `[[03-知识点/无机和结构化学/配位场理论]]` 实改 5 处/3 文件→`[[配位场理论]]`（化学原理版既有别名，唯一解析且保持原显示文本；09-审计报告 3 处历史行未动）。

### Track C：待建 KP 优先级清单

- 新建 `00-首页/待建KP优先级清单.md`，登记 20 个高频零匹配名（分析化学 35 → 三相点 11），**只登记不创建**（避免再造薄壳）。

### 脚本修正（build_link_map.py）

1. `ASSET_EXT` 加入 `.base`（消除 6 个 .base 文件假阳性）。
2. **关键 bug**：`parse_aliases` 正则 `^---` 不匹配带 BOM（`\ufeff`）的文件，导致一大批文件的 aliases 完全没被索引——M2 修复量约一半来自此修正。已改 `^\ufeff?---` 兼容。

### 两项挂起决策（已闭环）

1. `亲核加成反应` 别名从 `羰基亲核加成.md` 挪至 `亲核加成.md`（通用术语指向通用 KP，消除跨文件重复别名歧义）。
2. Track B 改出的 5 处 `[[晶体场理论]]`（库中有化学原理/无机两个同名文件，属歧义链接）改指 `[[配位场理论]]`（化学原理版既有别名）。

## 遗留与说明

- 剩余 5587 处断链的主体是**待建 KP 长尾**（数千个低频未建知识点名），本质是"内容缺口"而非"链接错误"，后续按 `待建KP优先级清单.md` 逐批建设。
- 模板占位符类断链名（专题-XX、KP-1、<KP>、Note Name 等）按规则不修。
- 09-审计报告、00-首页/工作日志中的断链属历史记录，一律保持原样。
- 反向引用索引已重建（5351 文件 / 85648 链接）。

---

## 第二步：口径与结构清理（同日，Task task-1784779528867-p2b1ppy）

依据 07-22 全库深度检查报告第十/十一/十二节，分四批次推进：

### 批次1 结构卫生
- 8 个 json 中间产物归档至 `09-审计报告/中间产物/`；错放的 `审计报告-教学逻辑链路一致性审计-2026-06-30.md` 归位 09-审计报告（4 处引用自动修复）；`add-frontmatter.ps1` 收编至 `11-模板/scripts/`。
- `media/决赛提取图片/`（201 张零引用 jpeg）隔离至 `10-附件/待清理/`；`clayden 有机习题/` 移入 06 管线（均经引用核查后受控 mv）。
- 纠错：A 组 8 个删除目标实测已不存在；.gitignore 已合规；`OCR待整理/` 实为独有 PDF 原件非重复占位（审计误判，未删）；mineru02/、高中化学竞赛笔记/、无机化学 习题集/ 三目录被大量相对路径图片嵌入引用，移动需批量改链，另立专项。

### 批次2 重复 KP 合并（18 对全成功）
- 同名 11 对（晶体场理论/螯合物/软硬酸碱/高低自旋/原子半径/过渡金属通性[留无机450行版]/Arrhenius/化学热力学/半衰期/相变热力学[留决赛版]/晶体结构基础）+ 异名 5 组（Le Chatelier 双文件、Simmon(s)-Smith 误拼正名、Meisenheimer、Robinson、Maxwell 坏公式版）+ 教学洞察 2 对。
- 每对均：独有内容并入正典 + 正典 aliases 挂旧名 + 壳文件软标记。与审计出入：无机版晶体场理论非纯空壳（独有例题已并入）；过渡金属通性未见 d¹³ 错字。

### 批次3 重复标注降级（36 文件）
- 16 个注水专题页 status 精品→已填充 + 重复候选注记（2 页无正典可指：波谱分析综合、胶体与表面化学）。
- 题库：ABOC 7 组 14 文件重复对标注 duplicate_of（题-197~204）；汇智题-001 内部 题4≡题10、题20≡题23 加注记；有机加成 2 文件实质同题标注；初赛讲义 4 个"题-001"题号不唯一仅列报。
- 有机疑似对 6 组核查：5 组确认真重复并完成合并软标记（E1cb/反应机理/位阻/氧化/Prevost-Woodward 合页），亲核加成对按既定结论不合并。另发现 ABOC 约 20 对"题面/答案拆页"口径问题（未动，另立专项）。

### 批次4 状态口径校准（实测驱动）
- 新跑统计脚本（`11-模板/scripts/kp_stats_snapshot*.py`）：有效 KP 907（磁盘 931−已合并 24）；difficulty 96.8%/importance 97.0%/source_extracts 97.4%/教学视角 97.8%；题库 type=题目 2381 分项闭合；专题 68 页（精品 28）。
- 校准 5 个系统页：状态摘要（注水"100%"全部改实测+口径注）、题库总索引（10 处，分项加总闭合）、质量看板（加口径注）、模块连接覆盖表（20 处，分析化学 84.6% 与决赛物化 87.8% 两短板现形）、教材提炼状态盘点；首页 sop_version v1.5.8→v1.5.7 回退对齐 SOP 本体。
- 备课思路 3 处：高低自旋显式锁正典链接；第三轮 48 课时口径统一（32 孤例改写）；轮次切分总表 13 处编号对齐 v2.0。
- 全角（待填充）核查：frontmatter 零例，61 处全是正文占位（属内容填充，另立任务）。

### 工具修复与挂起
- **kb_delete bug 修复**：manager.ts 对磁盘恢复 Task 的 `decisions`/`children` 未做空值防御致 INTERNAL_ERROR，src+dist 已加 `??=`；运行中 server 需重启加载。
- **挂起**：24 个已合并壳文件物理删除（Bash 审批三次超时），清单已落盘 `02-数据库/待物理删除清单.md`（重启后走 kb_delete 或批准一次 rm 均可）。
