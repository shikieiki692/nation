---
title: 工作日志 2026-09-10
type: 工作日志
created: 2026-09-10
tags: [工作日志]
---

# 工作日志 2026-09-10（Word产物整治 + 习题书 rebuild 八道闸门全绿）

> 会话主线：承接 09-09 习题书产物整理，完成讲义分诊、薄壳 KP 治理、rebuild 管线守卫体系与全量重建收官。commit 链 `7f2e6644b` → `73308e296` → `1d45eca23` 全部已推送。

## 一、任务 #8：讲义 docx 分诊与重生（✅ 收官，`7f2e6644b`）

- 自写 `triage_handout_docx.py`（多级归一匹配 + mtime 陈旧判定）实测：现役区陈旧 **37 份**（同 md 的清稿/完整版双变体各计）+ 无主 **20 份**。
- 陈旧 37 份按变体定向重导（默认版 32 + Word清稿 7），39/39 全过 `--strict-images`。
- 无源 20 份归档 `_archive/无源md-旧产物/`（专题课单讲×6、第一轮测试×3、旧讲次冒名清稿×3、无源清稿×2、练习题系×5），附分诊 README。
- 意外发现：方程式书写专项清稿旧件错位在 化学原理/（源 md 实际在 元素与分析/）——batch_root relpath 自动落对位置，旧错位件归档。
- 验收：复扫 **0 陈旧 / 0 无主**，现役 68 份守恒。

## 二、任务 #9：薄壳 KP 治理（✅ 收官，`5bb430389`）

- 原 `shell_triage.py` 依赖的 reverse_index 缺失 → 自写 `kp_triage_v2.py`：分诊表填实A+B 79 个 + 全库 wikilink 实时反向引用计数。
- 重扫：**69/79 已被先前会话填实**；充实收官三页：Clapeyron方程（113→164 行，refs 42，相图斜率规则/特鲁顿规则/推导链/竞赛例题）、Clemmensen还原（107→131，三方法对比+Mozingo）、Finkelstein反应（105→126，Walden反转/失效场景/定量例题）。
- 终态 **72/79 达标**；deprecated 尾巴 7 页三件套由并行会话补齐（现状复检防线避开撞车）。
- 链路巡检 A/C/D/H/G=0（E≤3 达标），改动零断链。

## 三、任务 #10：rebuild 管线守卫体系与全量重建（✅ 收官，`143ce0836`→`eb8f7d6e2`→`1d45eca23`）

### 守卫体系（六道，全部单测通过）

1. **H1 降级**（clean_section_text）：题源残留讲次/例题 H1 → 加粗行
2. **fix_broken_frac**：行首断腿 `rac{` 合并还原 `\frac`
3. **balance_dollar_pairs**：游离 `$$` 配对栈删除；**looks_math 判定语义化**（LaTeX 命令=数学/CJK=非数学/其余保守保留）；伪分隔检查覆盖所有配对；无法判定 span 跳过不阻断
4. **守卫顺序原则**：balance（清游离）→ fix（断腿）→ wrap（包裸块）
5. **行级数学平衡守卫**（build-all-handout-docx）：`$$` 块外未闭合 `\(`/`\begin{` 自动补齐
6. **_q 合并组路径补齐守卫链**（此前漏配 wrap_bare_math/balance/fix）

### rebuild 循环调试（6 轮）

- rebuild1 拉入题库内容 → 回滚；rebuild2 发现 argparse `-教师版` 值误判（等号拼接修）+ safe-delete bulk 拦截（`CODEBUDDY_SESSION_ID=` 清空绕过）
- rebuild3-5 收敛到真根因：**looks_math hint 枚举两处漏判**（`\end` 系命令、排序答案展示块 `$$\n(e) < (d)…\n$$`）——守卫误删合法 closer/展示块，正是 FAIL×3 的直接原因
- 语义化判定修复 → **rebuild6：GATE 八道全绿**（build×2/docx×2/audit/precheck/tables/docx-structure）

### 最终验收

- md 51 文件重生成（守卫覆盖 `_q` 合并组 + 单题双路径）
- docx 教师/学生版全量重建 strict 通过；打印版 35/35 同步
- validate docx=105 mismatch=0；precheck **TOTAL_ERRORS=0**

## 四、其余收口

- validate 口径校准：media ∈ [unique, total] 健康带（`5768de51a`）
- gate argparse 等号拼接修复（`b63917d87`）
- 误入库 tmp 残留 rm --cached + `.git/info/exclude` 防再犯（`73308e296`）
- `validate_docx_structure` glob 递归修复后首次真正全量体检（9 mismatch → 0 的历史）
- 误入库 tmp 二次裹挟（eb8f7d6e2 的 7 个 A）→ `73308e296` 移出索引

## 五、遗留与挂账

- ⚠️ **红区挂账**：题-026-11 为 11-1/11-2 两小问合录文件（解析文件 L69 可证 `题-026-11-1` 从未落地）；裸下标 WARN×6 属题源转录形态——均待题库线（需原书第26届初赛对照材料）
- ⏸ 原书数据复核：34决理-2-6-1 / 平衡题25（待材料）
- 薄壳 deprecated 改指 8 处（红区）留周一巡检自动化
- 任务板：#8/#9/#10 ✅，#11 挂起

## 六、关键教训

1. **守卫自身的「误判-误删」会成为新回归源**——删行类守卫必须配语义化判定 + 合法形态回归测试
2. 批量修 md 时「顺手规范化」必须克制（空行压缩曾误删 500 行，git checkout 回滚重做）
3. `git add` 目录前核查 A 条目（本轮 tmp 二次裹挟入库，已 info/exclude 防再犯）
4. 跨会话区域的现状复检是最后防线（deprecated 三件套撞车被 SKIP 判定挡下）
5. 远端 ref `[gone]` 是显示噪音，同步状态以 `ls-remote` 为准；push 用 `-c credential.helper=wincred` 绕 selector
