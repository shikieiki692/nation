---
title: 工作日志 · 2026-08-26
type: 工作日志
created: 2026-08-26
updated: 2026-08-26
---

# 工作日志 · 2026-08-26


## 📌 本日总览（Word 正文宋体→仿宋改活日）

> **一句话**：把 Word 管线正文字体从宋体 `SimSun` 改成仿宋 `FangSong`，并在验证时发现并修复一个「正文换不回仿宋」的后处理 bug，最终重新生成并用解包计数确认宋体残留清零。

**核心成果**：
1. **管线字体改活**：`docx_utils.py` 的 `CN_BODY_FONT` 由 `SimSun` → `FangSong`，`build-all-handout-docx.py` docstring 同步，`Word管线总览.md` 字体口径区同步为「正文`FangSong`（原`SimSun`按需求改仿宋）」。
2. **bug 修复**：后处理段落 run 循环里有一处「已有 eastAsia 字体就跳过」的守卫，而 pandoc 默认把 CJK run 的 `eastAsia` 写成 `SimSun`，导致正文 run 直接跳过、根本换不成仿宋（只有表格 cell 被正确替换）。已改为——已有 `eastAsia` 且 **非 `SimSun`**（如标题 `SimHei`、其它既有 CJK 字体）才保留；`SimSun` 一律落到目标正文字体。
3. **验证（解包计数，真实而非手记）**：重新生成「结构化学第一轮复习」`0 errors`；`styles.xml + document.xml` 解包检查——`document.xml` `eastAsia="SimSun"` **0**、`eastAsia="FangSong"` **4407**；`styles.xml` `SimSun` **0**、`FangSong` **31**。`py_compile` 通过。

**说明**：题库 KP 全覆盖、首份试卷出卷、习题书第一篇等今日组卷/习题集工作已记录在 [[00-首页/工作日志/2026-08-25]]（该日志承接 08-25→08-26 的跨夜工作），本日志只登记本次独立的「正文字体改仿宋」改活，避免重复记账。

---

## 📋 今日任务概览

- [x] **Word 管线正文字体 SimSun → FangSong** · `docx_utils.py` 字体常量 + 后处理逻辑（含 bug 修复）+ `Word管线总览.md` 口径同步
- [x] **验证** · 重新生成「结构化学第一轮复习」docx，解包确认正文 SimSun=0 / FangSong=4407

---

## 📝 详细工作记录

### Word 管线正文字体改仿宋 ✅

- **任务类型**：🧱 BUILD · 按用户对 Word 成品正文的要求，将中文正文字体由宋体改为仿宋。
- **改动文件**：
  - `11-模板/scripts/docx_utils.py`：`CN_BODY_FONT = "SimSun"` → `"FangSong"`；样式遍历改为「若 eastAsia 缺失 **或为 SimSun** 则强制替换为目标字体」；新增段落 run 守卫修正（见下）。
  - `11-模板/scripts/build-all-handout-docx.py`：docstring「正文 SimSun」→「正文 仿宋 FangSong」。
  - `00-首页/Word管线总览.md` line 130：「中文正文：`SimSun`」→「中文正文：`FangSong`（原为 `SimSun`，按需求改为仿宋）」。
- **字体口径（最终）**：正文/表格 `FangSong`（仿宋）；标题/表头 `SimHei`（黑体）；英文/数字/变量 `Times New Roman`；图注 `FangSong`。

### 后处理 bug 修复：正文换不回仿宋 ⚠️ → ✅

- **现象**：最初验证只跑了一次解包，报告 `SimSun=0`；但实际对已生成的「第一轮-4.5小时综合测试」/「结构化学第一轮复习」再解包，发现 `document.xml` 里 **大量 `eastAsia="SimSun"`（3894 处）**、`FangSong` 仅 513 处——正文字体根本没被替换，只有表格 cell 变了。
- **根因**：`postprocess_pandoc_docx` 的段落 run 循环里，守卫逻辑为
  `if not has_theme and rFonts.get(eastAsia) and _has_cjk(run_text): continue`
  pandoc 默认会把 CJK run 的 `eastAsia` 写成 `SimSun`，于是这些正文 run 全被 `continue` 跳过，永远留在宋体。
- **修复**：把守卫改成「已有 eastAsia 且 **非 SimSun**」才保留——这样标题 `SimHei`、其它既有 CJK 字体不受影响，而 `SimSun` 残留下落到 `body_font`（`FangSong`）。
- **再验证**：重新生成「结构化学第一轮复习」docx，`0 errors`；解包确认 `document.xml` `eastAsia="SimSun"=0`、`eastAsia="FangSong"=4407`；`styles.xml` `SimSun=0`、`FangSong=31`。**宋体残留清零**。

