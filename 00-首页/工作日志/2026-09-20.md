---
title: 工作日志 2026-09-20
type: 工作日志
created: 2026-09-20
tags: [工作日志]
---

# 工作日志 2026-09-20

> **本页是 09-20 多会话共享日志。** 各会话请各自追加自己章节；本会话（**讲义线·正文精读 ＋ 格式修复**）只写下面「二/三/四」三节。
> ⚠️ **批次编号撞车提醒**：同日有**两条讲义线**在跑且编号重叠——① **2-结构化学线**（批12~批16）；② **非结构线（物化/有机/分析）**（批11~批13）。两边文件零重叠但编号重叠，故下文一律**标线别**。

---

## 一、讲义线·正文精读（物化线会话序列）—— 批16~27，`3-物理化学` 12/12 收官

> 本会话＝**物化线**（对象 `04-课件/学生讲义/3-物理化学/`），与下方「二」节的 **2-结构化学线**文件零重叠、编号区间也不同（本线今日批16~27）。
> 本会话今日 **5 笔提交**：批26 `c3147608c` / 批27 `f34666779` / 批24 补遗 `7b9b6a3d2` / 收官报告 `d2c987354` / 报告补记 `1b6c26437`（推送状态见本节末）。

**收官口径**：12 份均走完「分段精读 → 修复脚本（--check/--apply）→ FM 更新 → 双闸门 → docx 重导复核 → pathspec 原子提交」。
批次：批16 物化综合计算｜批17 溶液与相图｜批18 胶体与表面物理化学｜批19 热力学初步｜批20 酸碱理论｜批21 化学平衡｜批22 电化学基础｜批23 沉淀溶解平衡〔独立复读〕｜批24 气体基础｜批25 化学动力学〔独立复读〕｜批26 化学动力学计算建模〔独立复读〕｜批27 统计热力学与Maxwell关系〔首次全量〕。

### 本会话两批要点（细目见收官报告）
- **批26 化学动力学计算建模**（52,220→54,318 B / 805→807 行，独立复读；10 处）。最有价值的一条：**题9 校勘的「自洽性论证」被自己的引文错误反向证伪** —— 校勘把题库 k 写成 3.26×10⁻⁵，却又断言「0.693/k=2.13×10⁵ s ≠ 21260 s」，而按 10⁻⁵ 计算**恰为** 21260 s、论证自证失效；正解是 3.26×10⁻⁶（源答案误把题面速率值当 k）。另：题15 校勘口径归属写反（回查源文件**全部 git 历史**确认题库三小问从未乘 2）；**题2 补回题源 07-02 被漏抄的第(3)问**（题面与答案一起漏）；题22 补官方 ΔH_D 值 +24 与「符号相反扣 1 分」；例3 温度依赖解说订正。
- **批27 统计热力学与Maxwell关系**（35,706→36,891 B / 589→591 行，**首次全量**；10 处）：§四编号顺延（4.3→4.2）｜3 处半截 math｜学习目标记法统一为 q^T｜中文入 math 下标补 text｜§5.3 补离域 Helmholtz 式｜弯引号→直引号。15 题（全自编）逐题独立验算通过。

### ⭐⭐ 本会话新踩并已固化的坑（详目已入 [[经验沉淀]] 1-13 / 3-11 / 6-6 / 6-7）
1. **FM `last_audit` 里写 LaTeX 反斜杠 → js-yaml4 直接 THROW（P0）**：给批27 追加记录时写了反斜杠 LaTeX 命令；YAML 双引号串里 `\d`/`\l`/`\c` 属**非法转义** → 闸门①当场抓到 `unknown escape sequence (21:564)`（该 P0 会让文件在 Obsidian 消失）。改**零反斜杠写法**后复验通过。⚠️ 既有 `\theta`/`\nu`/`\alpha` 属「首字母恰好合法」——不报错，但**解析后变真 TAB/控制符**（即已知 TAB 污染的根因）。
2. **暂存区被并行会话污染**：`add` 后 `commit` 前，对方把**故意做坏的钩子测试夹具** `04-课件/新授课/_hooktest_bad.md` add 进同一 index。**正解＝临时索引**：`GIT_INDEX_FILE=<tmp> git read-tree HEAD` → `add -- <我的文件>` → 核数量 → `commit`；既不碰他人 index、也不违反本库铁律「禁 `git commit -- <pathspec>`」。提交后须 re-add 真实 index（否则显示 `MM`）并清掉临时索引文件。
3. **格式线新装的 pre-commit 门禁 `render_gate`（回归模式）**：收官报告因正文含数学记号示例被报「产物字面美元符 1→3」而拦下；**未用 `--no-verify`**，改为文字描述后回落基线通过。格式线随后也修了该假阳性（`ce59b3b8c` 起统计排除 SourceCode 段落）。

### 收官 QA 与库级上报
- **六项全绿**：12 份一并过双闸门（js-yaml 受检 12 / 失败 0；validate_handout 受检 12 / 0E / 0W）＋ docx `media == FM image_count` **12/12 相符** ＋ 批前/批后行尾比对 ＋ 五模式扫描全 0。报告：[[09-审计报告/2026-09-20-物化线讲义正文精读收官QA]]。
- **批24 补遗**：`气体基础` 编号族 44 处收敛 → validate_handout 由「题0/答0」变「**题22/答22**」。
- 📊 **库级 P0（不在本线，已上报）**：math 域外**纯文本 Unicode 上/下标 45,636 处 / 137 份讲义**经 pandoc→docx 降级（md 源 `H₂O`(108)→docx `H2O`、`TiO₂`(23)→`TiO2`、`Fe³⁺`→`Fe3+`、`MnO₄⁻`→`MnO4-`）。两条修复路径 —— 改源统一 math（45k 处跨线改写）／管线加 Unicode→OMML 预处理（改格式线正在改的 build-all-handout-docx.py）—— **均不在物化讲义线**，故只记录不动手。

### 工作区隔离与推送
- 只提交本会话 5 笔；其余工作区改动全部属并行会话（`00-首页/题组Word/` docx、`04-题库/`、`11-模板/scripts/` 等）**零触碰**。`.workbuddy/memory/*` 不提交。
- **推送状态**：批26 / 批27 **已推送**；`d2c987354` / `7b9b6a3d2` / `1b6c26437` **未推送** —— 收盘时本地 HEAD 领先远端 8 笔，其中 5 笔属**并行会话在途提交**（`bc92ab295` 题库线、`ce59b3b8c`/`e0160ea02` 格式线等），避免代为公开其半成品。


---

## 二、讲义线·正文精读（本会话）

> 全天 HEAD 起点 `d9b6ea0c1` → 收官 `af891ae26`（**已推送，远端 == 本地**）。
> 本会话今日共 **4 笔提交**：批13 `055b5635d` / 批14 `db7a27856` / 批15 `f6774a046` / 批16 `af891ae26`。

### 批13（`055b5635d`，6 files / +107 / −51）—— **非结构线**：物化 2 ＋ 有机 1

| 文件 | 行数变化 | 主战场 |
|:---|:---|:---|
| `3-物理化学/化学动力学计算建模-超级充实版（自学完整）.md` | 764→805（+41） | ★ |
| `6-有机化学/二氧化碳化学-超级充实版（自学完整）.md` | 1029→1043（+14） | 补 3b |
| `3-物理化学/沉淀溶解平衡-超级充实版（自学完整）.md` | 977→978（+1） | 补 3b |

**化学动力学计算建模 的实质订正**：
- **题24 1-1** 气压高度公式补负号（原 `+Mgh/RT` 会得 p₂=2.74p₁，与紧随的 0.365 自相矛盾）→ 径改 + 校勘。
- **题24 1-2** 删公式多余 `^{-1}`，补 Clausius-Clapeyron 通式与 `348.5 K = 75.3 °C`。
- **题12 全题重写（最大头）**：四组数据是「不同初浓度、各自一个时间点」→ **必须走积分式**。① C/D 组同浓度加倍、半衰期减半 ⇒ 总级数 n=2；② A/B 组用准一级反解 k′、k′∝[B]₀ ⇒ β=+1、α=1；③ 二级积分式定 k=0.0100 dm³mol⁻¹h⁻¹；**四组回代全部复现（误差<1%）**。原题库 β=−1 使四组互不自洽 → 径改 + 校勘。
- **题15(2)** 删多余因子 2，统一三小问 `v_AB = d[AB]/dt` 口径 → 径改 + 校勘。
- **例4 判据升级为定量**：对 1/[NAD⁺] 回归得 `截距=0.0508x+0.196`（r=0.99999）、`斜率=0.000877x+0.00370` → 联立给 r_max≈5.1、K_M12≈0.019、K_M1≈0.24、K_M21≈0.26 mmol·L⁻¹。
- **例3 补量纲自检**；**题21** K_D 气/液 vs 液/气 两种约定互为倒数且与题22 指向相反 → **标存疑未径改**。
- 其余：题2(2) 补逐级比值 + 全程复算；题10 标注缺 V/A 不可复算；题11 澄清 p 为醚分压；题20 补 nm⁻² 与每分子占面积 1.27 nm²；表观活化能改 `k_表观=Πk_i^{ν_i} ⇒ E_a=Σν_iE_{a,i}`。
- ⭐ **又一次「先怀疑自己」**：精读时凭印象怀疑 32 届初赛 7-3（`[V₂(edt)₄]²⁻`）的「V-V **双键**」应为三键 → **查原卷解析 L541 确认原答案就是「金属-金属双键」** → 我的怀疑为误判，**未改动**。

### 批14（`db7a27856`，6 files / +5 / −3）—— **2-结构化学线** 3 份

- `晶体结构深化-超级充实版（自学完整）.md`（660 行）：**零问题**，仅追加 FM `last_audit`。
- `结构表征与波谱桥-超级充实版（自学完整）.md`（637 行）：
  - **R1 硬伤订正（L593 §11 答案 25）**：原「最可能是**八面体**，含对称中心，**低自旋**…$t_{2g}^6$，全配对」→ 与题面「UV-Vis 有一条弱带」**自相矛盾**（$t_{2g}^6$ 闭壳层无 d-d 跃迁）→ 改「**高自旋**」+ `$t_{2g}^4 e_g^2$` + 补足推理链。
  - **R2**：FM 原缺 `last_audit` → 插在 `updated:` 行后。
- `配位光谱与对称性桥-超级充实版（自学完整）.md`（620 行）：
  - **R3 笔误订正（L294 §5.1 表）**：`平面四方 | $D_{4h}$ | $b_{1g} > a_{1g} > b_{2g} > e_g$` → `$b_{1g} > b_{2g} > e_g > a_{1g}$`（与全讲其余 7 处一致；$a_{1g}=d_{z^2}$ 应最低）。
  - **R3'**：FM 原缺 `last_audit` → 插入。

### 批15（`f6774a046`，4 files / +4 / −4）—— **2-结构化学线** 2 份

- `配合物深化-超级充实版（自学完整）.md`（691 行）：
  - **R4a 图注残渣清理（L310）**：`*图 6 …成键机制* 反馈*` → 删尾部 ` 反馈*`。
  - **R4b FM 数字订正**：`image_count: 11` 但 `last_audit` 写「image_count 7→12」→ 实际引用 11，订正并追加批15 标记。
  - 图注编号本身混乱（图 6 出现两次、L398 无编号、L406 标图 5）——**体例问题，按最小侵入未动**。
- `超分子化学.md`（512 行）：
  - **R5 数据订正（§2.3 表）**：大二环效应增量 `$10^4$–$10^6$` → `$10^2$–$10^4$`（与 §8 公式、§10 答案 3、答案 15(c)、`03-知识点/决赛要求/金属有机与超分子/主客体化学.md` L114 一致）。**辨析**：「穴醚绝对结合常数 $10^6$–$10^{10}$」≠「相对单环的增量 $10^2$–$10^4$」。
  - **R5'** FM 追加（沿用原**裸值无引号**风格）。

### ⭐⭐ 批15 实证：闸门拦住我自己引入的 P0

给 FM `last_audit` 追加内容时，在**双引号包裹**的值内写入**英文双引号** `"反馈*"` → 字符串提前闭合 → js-yaml 报 `bad indentation of a mapping entry`。**这正是「js-yaml 遇非法转义 → 文件在 Obsidian 消失」的 P0**，被第一道闸门**精准拦下**（只报该份、另一份正常通过）。
→ 修正为中文书名号 `『反馈*』`，复检 OK 2 / 失败 0。
→ **固化教训：给 FM 值追加内容时，禁写入英文双引号 `"`、裸 `: `、或以 `*`/`[`/`{` 开头的内容。**

---

## 三、讲义线·格式修复 批16（`af891ae26`，4 files / +36 / −4）

> 本轮起用户把本会话职责**收窄为「格式修复」**（其他任务由别的会话做）。目标：「表格内公式渲染不出来」。

### 根因与对象
- **根因**：`<table>` **独占一行** → Obsidian 判为 `html_block` → 块内 `$...$` **完全不经 inline 解析** → 显示为 LaTeX 源码。
- **对象**：`第八讲原子结构（三）-教师增强版.md` / `-超级充实融合版.md`，各 9 个单行 HTML 表，**仅 2 个含公式**（L527/L564 与 L698/L735，两版逐字相同）：
  - 表1 = 碳同位素丰度表（3 列）×2 版
  - 表2 = X 同位素衰变表（3 列，**含中间重复表头**＝原卷分页表头重复）×2 版
- **修法**：复用官方 `html_tables_to_markdown.py::convert_table` 做单元格规整；表2 **按「中间重复表头」拆为两张背靠背 pipe table**（263-266 / 267-269）；其余 7 个无公式空表（`<td></td>` 待填练习表）**保持原样**。

### ⭐⭐ 关键实证（推翻先前假设）
1. **`html_tables_to_markdown.py` 的「行尾保持」判据对「末尾裸 LF」有固有盲区**：判据 = `CRLF数 == LF数`；本批两文件是 **777 CRLF / 778 LF**（末尾混 1 个裸 LF）→ **对未改的原始文件跑同一判据就是 False**（已自证）→ 该脚本 `--apply` 会 skipped → **改自写转换脚本**。
   → **教训：判据报 ❌ 前先对「未改的原始文件」跑同一判据自证**，区分「存量形态盲区」vs「本次引入的缺陷」。
2. **HTML 表格问题只影响 Obsidian 渲染，不影响 Word**：实测旧 docx 的 HTML 表内公式 **`oMath` 数已是 4/5**（Pandoc 对 `<table>` 内 `$...$` 本就转 OMML）。→ 本次修复**价值在 Obsidian 侧**；Word 侧 `oMath` 209/272 **新旧守恒**。

### 行尾与 docx 核验
- 转换**严格保持 CRLF**：777→792（+15）、940→955（+15），**裸 LF 差值恒为 1**（既存裸 LF 未被顺手改）；`git diff --numstat` == `--ignore-cr-at-eol --numstat` = **17/2**（真改动，无 CRLF 膨胀）。
- docx：`w:tbl` 21→22 / 25→26（拆表 +1）；`oMath` 209/272（旧=新）；总 oMath 152 新旧一致；`media` 33/33 不变；`literal $` / `literal alpha` = 0（无源码泄漏）。

### 双闸门
- js-yaml 受检 **2 / OK 2 / 失败 0**；validate_handout 受检 **2 / 🔴0 / 🟡0**（FM 追加后**复跑**双闸门均过）→ 暂存守卫 == 4 → `af891ae26` → push `f6774a046..af891ae26` → `ls-remote` == 本地。

---

## 四、格式问题全库盘点（本会话复核）

「含公式的块级 HTML 表」全库 **686 个 / 171 文件**，分布：

| 区域 | 表数 | 处置 |
|:---|:--:|:---|
| **`04-课件/学生讲义/`** | **0** ✅ | **批16 清零** |
| `04-题库/` | 2 | 🔴 红线区，禁碰 |
| `07-资料提炼/网课资料`（周坤） | 12 | 域外，下一批可做 |
| `09-AI工作区/一次性产物归档` | 10 | 归档，废弃 |
| `09-审计报告` | 5 | 报告区 |
| `06-外部资料导入` ＋ `mineru*` ＋ 教材 OCR 等 | 425+96+… | **域外 E 项，一律不动** |

→ **结论：讲义线格式问题已清零**；剩余全在域外／归档／报告／红线区。

---

## 五、遗留（下一会话待办）

| # | 项 | 现状 | 优先级 |
|:--:|:---|:---|:---|
| **F1** | **`07-资料提炼/网课资料` 12 处含公式块级表** | 域外提炼区，最接近生产链的剩余项 | 🟠 P1 |
| **F2** | 讲义线精读**待精读 38 份** | 分布：`3-物理化学 9`（🔴 并行会话在飞，须避让）／`_数学工具 8`／`2-结构化学 8`／`5-无机化学 7`／`4-分析化学 5`／`6-有机化学 2`（🟡 `二氧化碳化学` 被并行会话碰）／`_综合 1` | 🟢 常规 |
| **F3** | 巨文件 4 份（须拆批） | `结构化学专题课-学生用合集`(3364)／`晶体学与晶体结构-超级充实版`(2434)／`配位化合物基础-超级充实版`(1625)／`电化学基础`(1569) | 🟢 常规 |
| **F4** | 生成器修复 `09572605e` 是否重跑（习题书 41 份） | 待用户拍板 | ⏸ |
| **F5** | 同型错字「钢系」全库仍 6 处 | 未擅动 | ⏸ |
| **F6** | P1：`used_in` 记账缺陷标注落到具体文档 | 三铁律仅在速查文件 | 🔴 P0 |

> ⚠️ **F2 现状更新（09-20 物化线会话）**：`3-物理化学` 的 9 份已由**物化线批16~27 全部收官（12/12）** → F2 剩余应为 **29 份**（`_数学工具 8`／`2-结构化学 8`／`5-无机化学 7`／`4-分析化学 5`／`6-有机化学 2`／`_综合 1`），且 `3-物理化学` 自即日起**不再是避让对象**。

---

## 六、工作区隔离说明

本会话**只提交本会话 4 笔**（批13/14/15/16），其余工作区改动**全部属并行会话**（`3-物理化学` 2 份 + `6-有机化学/二氧化碳化学` 备份、`00-首页/题组Word/真题讲评/` 若干 docx、`04-题库/` 等），**零遗漏、零触碰**。
`.workbuddy/memory/*`、`02-数据库/quality-metrics.jsonl` **一律不提交**。

**提交链**：`db7a27856` → `f6774a046` → `055b5635d` → `af891ae26`（远端 == 本地）。
