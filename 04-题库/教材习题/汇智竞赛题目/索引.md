---
title: "索引"
aliases: [汇智竞赛题目, 汇智竞赛索引]
type: 索引
tags: [化竞, 汇智, 题库, 结构化学, Dataview]
created: 2026-07-18
updated: 2026-08-25
---

---

> [!info] 保真性声明（2026-08-25 重录完成）
> 本子库已于 2026-08-25 完成**替换式忠实重录**：全部 136 题（原子 23 + 分子 37 + 晶体 76）的题干与答案已逐字转录自教师版题库，此前为自编替换题的文件均已覆盖（重录前未覆盖比例：分子 85% / 晶体 75% / 原子 42%）。重录后逐串覆盖率 **99.2%**（1327 中文串仅 2 题残留，已人工修复）。
> 原 125 个重复/旧拆分文件已标记 deprecated + superseded_by。引用时以 `题-汇智-{类别}-{N}.md` 为正典。

---

## 全部题目

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    (contains(file.name, "原子结构") ? "原子结构" : contains(file.name, "分子结构") ? "分子结构" : "晶体结构") AS 模块,
    difficulty AS 难度,
    question_type AS 题型,
    exam_stage AS 阶段,
    teaching_level AS 教学层级
FROM "04-题库/教材习题/汇智竞赛题目"
WHERE type = "题目"
SORT file.name ASC
```

---

## 按模块分组浏览

### 原子结构

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/汇智竞赛题目"
WHERE type = "题目" AND contains(file.name, "原子结构")
SORT file.name ASC
```

### 分子结构

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/汇智竞赛题目"
WHERE type = "题目" AND contains(file.name, "分子结构")
SORT file.name ASC
```

### 晶体结构

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/汇智竞赛题目"
WHERE type = "题目" AND contains(file.name, "晶体结构")
SORT file.name ASC
```

---

## 按难度分层浏览

### 难度 5（决赛级）

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    (contains(file.name, "原子结构") ? "原子结构" : contains(file.name, "分子结构") ? "分子结构" : "晶体结构") AS 模块,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/汇智竞赛题目"
WHERE type = "题目" AND difficulty = 5
SORT file.name ASC
```

### 难度 4（竞赛拔高）

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    (contains(file.name, "原子结构") ? "原子结构" : contains(file.name, "分子结构") ? "分子结构" : "晶体结构") AS 模块,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/汇智竞赛题目"
WHERE type = "题目" AND difficulty = 4
SORT file.name ASC
```

### 难度 3（拓展）

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    (contains(file.name, "原子结构") ? "原子结构" : contains(file.name, "分子结构") ? "分子结构" : "晶体结构") AS 模块,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/汇智竞赛题目"
WHERE type = "题目" AND difficulty = 3
SORT file.name ASC
```

---

## 题库说明

- **来源**：汇智竞赛培训题目
- **覆盖范围**：结构化学专题（原子结构、分子结构、晶体结构）
- **难度定位**：初赛至决赛级，整体偏竞赛拔高
- **题型**：综合题
- **特点**：涵盖结构化学核心知识模块，适合竞赛备考强化训练

---

*本索引由 Claudian 自动生成。如需更新，请运行 Dataview 查询。*
