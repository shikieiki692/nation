---
title: 化学竞赛初赛讲义题库索引
aliases: [初赛讲义题库, 初赛讲义题目索引]
type: 索引
tags: [化竞, 初赛讲义, 题库, Dataview]
updated: 2026-07-13
---

# 化学竞赛初赛讲义题库索引

> 本索引动态聚合 `04-题库/教材习题/化学竞赛初赛讲义/` 下的全部题目，支持按难度、知识点、题型、子模块等维度检索。
> 
> 当前收录：**530 道题**（含拆分后的独立题目，2026-07-13 更新）

---

## 一、总览统计

```dataviewjs
const pages = dv.pages('"04-题库/教材习题/化学竞赛初赛讲义"').where(p => p.type == "题目");
const byDiff = (d) => pages.where(p => p.difficulty == d).length;
const byStage = (s) => pages.where(p => p.exam_stage == s).length;
const byType = (t) => pages.where(p => p.question_type == t).length;
const byLevel = (l) => pages.where(p => p.teaching_level == l).length;

dv.table(["指标", "数值"], [
    ["总题数", pages.length],
    ["---", "---"],
    ["初赛难度", byStage("初赛")],
    ["---", "---"],
    ["难度 5（最高）", byDiff(5)],
    ["难度 4", byDiff(4)],
    ["难度 3", byDiff(3)],
    ["难度 2", byDiff(2)],
    ["难度 1", byDiff(1)],
    ["---", "---"],
    ["竞赛拔高", byLevel("竞赛拔高")],
    ["拓展", byLevel("拓展")],
    ["巩固", byLevel("巩固")],
    ["入门", byLevel("入门")],
]);
```

---

## 二、全部题目总表

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    submodule AS 章节,
    difficulty AS 难度,
    question_type AS 题型,
    exam_stage AS 阶段,
    teaching_level AS 教学层级
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目"
SORT difficulty DESC, file.name ASC
```

---

## 三、按章节分组浏览

### 反应方程式

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "反应方程式"
SORT file.name ASC
```

### 原子结构

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "原子结构"
SORT file.name ASC
```

### 分子结构

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "分子结构"
SORT file.name ASC
```

### 配合物

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "配合物"
SORT file.name ASC
```

### 金属有机化学

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "金属有机化学"
SORT file.name ASC
```

### 推断技术

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "推断技术"
SORT file.name ASC
```

### 晶体结构

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "晶体结构"
SORT file.name ASC
```

### 热力学和动力学初步

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "热力学和动力学初步"
SORT file.name ASC
```

### 溶液与化学分析

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "溶液与化学分析"
SORT file.name ASC
```

### 有机化学基本原理

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "有机化学基本原理"
SORT file.name ASC
```

### 人名反应与机理推断

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "人名反应与机理推断"
SORT file.name ASC
```

### 有机波谱学初步

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "有机波谱学初步"
SORT file.name ASC
```

### 高分子化学简介

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    difficulty AS 难度,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND submodule = "高分子化学简介"
SORT file.name ASC
```

---

## 四、按难度分层浏览

### 难度 4（竞赛拔高）

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    submodule AS 章节,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND difficulty = 4
SORT file.name ASC
```

### 难度 3（拓展 / 竞赛级）

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    submodule AS 章节,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND difficulty = 3
SORT file.name ASC
```

### 难度 2（巩固 / 基础）

```dataview
TABLE WITHOUT ID
    link(file.path, title) AS 题目,
    submodule AS 章节,
    question_type AS 题型,
    knowledge_points AS 知识点
FROM "04-题库/教材习题/化学竞赛初赛讲义"
WHERE type = "题目" AND difficulty = 2
SORT file.name ASC
```

---

## 五、题库说明

- **来源**：化学竞赛初赛讲义（13讲 + 2个附录）
- **难度定位**：初赛实用层，覆盖化学原理、无机化学、有机化学等
- **题型**：以计算题和推断题为主
- **特点**：覆盖面广，适合第一轮训练

---

*本索引由 Claudian 自动生成。如需更新，请运行 Dataview 查询。*
