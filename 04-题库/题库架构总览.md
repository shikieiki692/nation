---
title: "题库架构总览（单一事实源）"
type: 索引
source_subject: 题库治理
updated: 2026-09-01
version: v1.0
question_count: 3904
---

# 题库架构总览

> 本文是题库体系的**单一事实源**：目录地图、字段规范、五载体分工、数据流、维护脚本。
> **新题导入标准流程**见 [[04-题库/新题入库SOP]]（v1.2，含 09-01 增补：字段门槛/反模式清单/构建预演）。
> 数字为 2026-09-01 全库实测（架构排查日）。其他文档与本页冲突时，以本页为准。

## 一、一张图看懂：题目的一生

```
外部资料(06-外部资料导入/教材OCR)
    │  人工转录/导入（10-待审核区，P1 流程）
    ▼
04-题库【仓储层】  ←── pack 字段决定命运 ──→  每题一个 .md，frontmatter 完整
    │                              │
    │ pack=模块习题集                │ pack=章节练习（默认层）
    │ + type=题目 + subject_module  │ （随时堂习题集/练习，不入书）
    ▼                              │
build_module_book.py 生成            │
    ▼                              ▼
04-课件/习题集/习题书-{学生版,教师版}   04-课件/习题集/第一轮化学原理-*习题集.md
【成书层】31章/1,283题·双版本        【随堂层】8卷407题·题答同卷·⭐分级
    │
    ├─ build_source_map.py → 溯源映射.json（成书题↔源文件）
    ├─ validate_module_book.py（题头数/答案块/图片校验）
    ▼
build-all-handout-docx.py → 00-首页/题组Word/习题书/{教师版,学生版}/
build_print_student.py    → …/学生版-打印版/          【成品层】docx
```

**评测层与真题策略层**（平行于上述主干）：
- 化学原理阶段测试卷（`04-课件/习题集/化学原理阶段测试卷.md`）：唯一合成试卷，50 小问/100 分/150 分钟 + 独立答案卷。
- 05-真题库（63 题）：按**题型**加工讲评的真题策略层，与 04-题库/真题（按届次的**原题仓储**）分工不同。
- 综合模拟卷（pack 值，214 题）：**决赛真题**统一用此 pack——初赛小问题拆散入模块，决赛整卷保持卷形。

## 二、目录地图（磁盘实测）

| 区域 | 路径 | md 数 | 内容 |
|---|---|---|---|
| 题库仓储 | `04-题库/` | 4,258 | 全部题目唯一权威源（另有 2 个 md 无 frontmatter） |
| ├ 教材习题 | `教材习题/`（12 套书） | 2,847 | ABOC/Clayden/上海中学/中级无机/Weller/结构化学基础/赵鑫光… |
| ├ 真题 | `真题/`（25-39届+省预赛） | 687 | 按届次原题；决赛题 pack=综合模拟卷 |
| ├ 学科模块 | `化学原理/有机化学/物理化学/分析化学/元素化学` | 668 | 按知识点自组织 |
| ├ 其他 | `教学改编题/经典例题/真题（无答案）` | 42 | 改编 28 / 经典 12 / 无答案区仅 README+索引 |
| 真题讲评 | `05-真题库/` | 64 | 按题型加工（63 题 + README） |

> 数字校准于 2026-09-02（磁盘实测，含省预赛 260 题入库后的变化）。

### 真题双仓分工（2026-09-02 定）

真题**故意存两份、不合并**，两者是「筛子」的两端而非重复收录：

| 仓 | 路径 | 角色 | 格式特征 |
|---|---|---|---|
| 仓储层 | `04-题库/真题/`（687） | 按届次归档的**原始入库**，追求全 | 与普通题同构：答案折叠在 `<details>` 内，六字段齐全 |
| 交付层 | `05-真题库/`（63） | 已精做过解析的**讲评用题**，追求深 | 一题一文件；答案**不折叠**，正文为 `## 题目` + `## 解析`（含 分析/解答/反思 三段）；独有 `year` / `type_tag` / `related_notes` 字段 |

- **检索时合并看**：`02-数据库/题库.base` 的「真题（双仓合并）」视图同时拉两边。
- **上浮通道**：`04-题库/真题/` 里写好三段解析的题，可上浮到 `05-真题库/`，用 `cross_references` 双向互链。
- ⚠️ 不要为了「统一格式」把 687 个批量迁到 05-真题库——大部分题没有解析可写，会产出 600+ 空壳。
| 习题书源稿 | `04-课件/习题集/习题书-{学生版,教师版}/` | 68 | 构建产物，勿手改 |
| 随堂习题集 | `04-课件/习题集/第一轮化学原理-*.md` | 9 | 8 卷 407 题 + 总索引（手编） |
| Word 成品 | `00-首页/题组Word/习题书/` | docx×99 | 教师版33/学生版33/打印版33 |
| 审核区 | `10-待审核区/` | 1 | 仅 README，当前无积压 |

### 三模块视图与成品（2026-09-02 定）

题库按 `teaching_level` 四档切成三个**使用视图**（一库三视图：文件不搬家，归属由字段唯一决定），
对应 `02-数据库/` 三个 Bases（`习题集.base` / `习题书.base` / `测试题.base`，口径已用
`sim_module_pools.py` 离线验证：1,769 / 1,525 / 888，两两交集 0，并集 4,182）：

| 视图 | filter | 题量 | 用途 |
|---|---|---|---|
| 习题集 | 基础＋巩固 | 1,769 | 课内即时检测：小批量、答案随卷 |
| 习题书 | 拓展 | 1,525 | 考纲专项＋课后拔高（作业/阶段练习难度） |
| 测试题 | 竞赛 | 888 | 综合摸底、限时测评；真题整卷模考是它的一个模式 |

- **来源 ≠ 难度**：`教材习题/` 横跨四档（基础 205 / 巩固 827 / 拓展 1,179 / 竞赛 598），
  真题小问也跨四档（05-真题库 12/33/14/4）→ **禁止按目录划分模块归属**，
  新题标对 `teaching_level` 即自动入池。
- **池 vs 成品**：上表三个是「池」（视图）；`04-课件/习题集/` 习题书 31 章 1,283 题
  （docx 三版本）与 4 份阶段测试卷是历史**成品**（池的某次输出），不随视图重切、互不混同。
- 出卷后照常跑 `mark_used.py` 回填；测试卷卷尾写「失分复盘」（见 `模板-专题练习卷` §7.1），
  按失分 KP 从习题集/习题书池抽未用题组补弱卷（最小闭环，不做逐题对错记录）。
- 已知薄点：基础档全库仅 452 题，习题集视图上线后按模块看缺哪补哪。

## 三、frontmatter 字段规范（题目文件必读）

| 字段 | 取值规范 | 说明 |
|---|---|---|
| `type` | **题目**（唯一合法题目类型） | ⚠️ 例题也写 `type: 题目`，用 `question_type: 例题` 区分——`type: 例题` 不被任何管线识别（例9.5/Ch09 批次踩坑） |
| `pack` | `模块习题集` / `章节练习` / `综合模拟卷`(决赛真题) / `预赛专项` | 入书门槛：模块习题集 + subject_module 齐备 |
| `subject_module` | `化学原理` / `结构化学` / `有机化学` / `元素与分析`（四篇之一，**单行标量**） | 构建路由的第一道闸，缺了或写成 YAML 列表都进不了 pool。**校验器 QB_ENUM 四个受约束字段之一** |
| `source_subject` | **来源教材科目**，16 值（如 `无机和结构化学`/`物理化学`/`综合`） | 2026-09-02 由 `subject` 改名消歧。**不校验收敛**，与 `subject_module` 分工并存——两者值不同是设计意图（全库 1,906 处），勿“修正”成一致 |
| `teaching_level` | `基础` / `巩固` / `拓展` / `竞赛`（**4 档**） | 2026-09-02 由 14 种收敛而来（`入门`→基础、`强化`/`挑战`→拓展、`拔高`/`提高`/`进阶`/`决赛`/`高级`/`竞赛决赛`/`竞赛深化`→竞赛、`竞赛拔高`→竞赛）。**不校验**，按 4 档写 |
| `module` | 章/主题级中文值 | 分类线索之一，但以 subject_module + 文件路径优先 |
| `submodule` | 主题词（标量） | 分类精确层依据；⚠️ 勿从文件名机械截取（“螺吡喃机械力变色”曾错标“机械力化学”） |
| `status` | `已补全答案` / `已填充` / `deprecated` | **deprecated=已废弃**，构建跳过（现已有日志），须写 `superseded_by` 指明取代文件 |
| `fidelity` | `原书逐字` / `原书改写` / `自编` | 成书章内排序依据（逐字>改写>自编） |
| `syllabus_codes` | 整数数组，码表 = `02-考纲条目` 各条目 frontmatter 的 `syllabus_code`（基础要求 1–56） | 质量审计“考纲相关”判定依据 |
| `question_type` | 列表，**10 种原子题型** `选择`/`填空`/`简答`/`计算`/`推断`/`作图`/`机理`/`方程式书写`/`推导`/`合成` + 角色值 `例题`/`综合` | 2026-09-02 B2b 补标 4.7% → 29.0%；同日 B6 二轮补标 → **42.0%**（1,757/4,182）。仍缺 2,425 条，见下方「题型补标」说明 |
| `knowledge_points` | **只放能解析到 `03-知识点/` 的 wikilink** | 六字段之一，**不允许为空**；断链会被 `validate_kb` 判 warning、`audit_question_bank` 判 P1 |
| `concepts` | 纯文本数组（**不写 `[[ ]]`**） | 2026-09-02 新增：比知识点更细的概念标签（如「极弱酸强化」「计量点pH计算」）。**不参与断链校验**，只用于检索。与 `knowledge_points` 的分工见下方说明 |
| `used_in` | 文本数组，记录该题被哪些卷/讲义用过 | 组卷脚本自动回填；`题库.base` 的「未用过的题」视图依赖它 |

> **`knowledge_points` vs `concepts`（2026-09-02 拆分）**：
> 原先题目把「气体体积」「离子对效应」「玻尔兹曼常数」这类**比知识点更细的概念**也写成 `［［双链］］`，
> 而 `03-知识点/` 只有 948 个文件，导致 1,590 条红链——不是写错，是**粒度错配**。
> 现按能否解析自动拆分：解析得到的留在 `knowledge_points`（继续可校验、维系知识图谱）；
> 解析不到的剥离 `[[]]` 移入 `concepts`（纯标签，可检索、不告警）。信息零丢失。
> 拆分脚本：`.workbuddy/scripts/split_kp_concepts.py`。

### `question_type` 的两个维度（2026-09-02 B2b 定）

| 维度 | 取值 | 判定依据 |
|---|---|---|
| **作答形式** | `选择` / `填空` | 客观信号：选项行 `A. B. C. D.`、下划线/空格占位 `____` |
| **内容题型** | `计算` / `推断` / `作图` / `机理` / `方程式书写` / `推导` / `合成` / `简答` | 题干动词 + 宾语 |

两者**并存不互斥**，组卷时按哪个维度筛都能命中：

```
求 pH 的选择题          → [选择, 计算]
「配平下列方程式 (1)____」 → [填空, 方程式书写]
「画出 A 的结构简式____」 → [填空, 作图]
```

**三条语义裁定**（用户 2026-09-02 拍板，勿再改）：

1. **「画出 A 的结构式 / 结构简式 / 构型」统称 `作图`**，不拆给 `推断`。
   理由：从作答动作看「要画图」就是一类。实测作图档 164 条里 114 条是画结构、
   只有 10 条是画装置图/相图/能级图，拆开会让 `作图` 缩到 26 条失去意义。
2. **`简答` 只在「强信号」下自动写入**（2026-09-02 B6 修订，取代原先的「一律不写」）。
   原规矩是"简答 = 没识别出题型的委婉说法，只统计不写"。B6 实测后放宽为两档：
   - **T3S 写**：命中 `为什么/为何(?!值)/解释/简述/说明理由/比较/讨论/鉴别`，
     **且整题没有任何 T1/T2 命中**，**且**题干不含竞争动词（写出/画出/配平/计算/求/合成…）。
     最后一道闸门是关键——「解释…并写出方程式」是混合题，单标简答会把另一半吞掉。
   - **T3 不写**：只命中 `说明/怎样/如何/指出/描述/分析` 这类插入语动词。
   实测 T3S 抽样 15 条，明确正确 13 条（87%）。
3. **一题多问的真题大题留空不猜**。含 `7-1/7-2/7-3`、`(1)(2)(3)`、`10.1/10.2`
   等 ≥3 个小问的干，单一标签必然失真（738 条）。例外：若各小问题型**同质**
   （如「计算下列溶液的 pH：(1)…(2)…(3)…」三问全是计算），则按该题型写（43 条）。

**B6 二轮补标（2026-09-02）**：覆盖率 29.0% → **42.0%**，写入 545 条。
本轮修掉四个具体缺陷，每个都配了反例：

| 缺陷 | 反例 | 修法 |
|---|---|---|
| 选项正则的负向断言 `(?<![\w.。（(])` 把 `(A) 2;` 全排除 | 习题1.1 `…(A) 2; (B) 4; (C) 8; (D) 16` | 去掉 `（` `(` → 全库 394→482 |
| 「判断下列」误命中（中文分词陷阱） | 「判断**下列**说法是否正确」含子串「判断下列」 | 删掉该分支 |
| 「推断」作名词 | 「全合成**推断题目**的分析方法」「评论这**一推断**的有效性」 | 加 `(?!题目\|的\|有效性\|…)` |
| 计算词表缺项 + 窗口窄 + 只认动宾顺序 | 「生成焓**为多少**」「浓度**为何值**」「求…计量点**pH**」（隔 40 字） | 补 生成焓/ΔH 等、窗口 24→60、加宾语在前分支 |

**写入-校验流程**（两步，勿省）：
`infer_question_type.py --pending-out <清单>` → `qt_write_guard.py snapshot`
→ `infer_question_type.py --write` → `qt_write_guard.py verify`。
verify 断言改动只含 `question_type` 的单行插入且**行尾风格零变化**
（`.gitattributes` 有 `*.md text eol=lf`，git diff 看不出行尾污染，只能逐字节比快照）。

**补标入口**：`02-数据库/题库.base` → 「题型待标注（按目录批量补）」：
按 `所在目录` 排序，同目录的题高度同质，可一次标几十道。
「题型待标注·真题优先」单独列出真题缺口。

**剩余 2,425 条的三档构成**（2026-09-02 B6 实测，按目录统计）：

| 档 | 条数 | 说明 |
|---|---|---|
| 无信号 | 1,325 | 正则完全没命中。多为 OCR 不全、纯数据表格题、或题干只有图 |
| 多小问跳过 | 738 | 一题多问且各问不同质，单一标签必然失真 |
| T3 弱信号 | 362 | 只命中「说明/怎样/如何」等插入语，不足以认定题型 |

按目录：教材习题 1,729（71%，其中 Clayden / 结构化学基础 / ABOC 三家最多）、
真题 232、化学原理 192、有机化学 156。同目录高度同质，适合按目录分批人工补。

推断脚本：`.workbuddy/scripts/infer_question_type.py`（`--write` 才落盘，幂等）。
诊断脚本：`.workbuddy/scripts/qt_gap_diagnose.py`（只读，输出动词频率/目录分布/多问切段/按目录抽样）。
写入校验：`.workbuddy/scripts/qt_write_guard.py`（snapshot / verify 两步）。

## 四、pack 分层现状（4,016 md 实测分布）

> 数字校准于 2026-09-02 磁盘实测。

| pack | 数量 | 说明 |
|---|---|---|
| 章节练习 | 2,346 | 默认层：随堂练习、教材习题普通题 |
| 模块习题集 | 1,303 | 入书候选层（1,283 在书 + 18 gap 排除 + 2 deprecated，**零余量**） |
| 综合模拟卷 | 214 | 决赛真题整卷层 |
| 预赛专项 | 260 | 省预赛精选（江苏 10 + 福建 10 + 浙江 240，2026-09-02 入库） |
| （空） | 135 | 索引 55 / 系统 33 / 答案 19 / 题组 7 / 真题卷 5 / 真题答案 5 / 其他 11——非题目文件，正常 |

> ⚠️ 上表是「04-题库 含非题目文件」口径（成书对账沿用此口径，勿改）。
> 按 **type ∈ {题目, 真题} 且含 05-真题库** 的题目口径（B4，4,182 题）则为：
> 章节练习 2,343 ／ 模块习题集 1,319 ／ 预赛专项 306 ／ 综合模拟卷 214。
> 差异来源：预赛专项 +46（05-真题库 的省预赛题）、模块习题集 +17（05-真题库 初赛题）。

### 四b、pack 准入规则（2026-09-02 B4 定，校验脚本 `check_pack_rule.py`）

**硬规则（实测 100% 成立，违反必须改）**

| 条件 | pack |
|:--|:--|
| `exam_stage: 省预赛` | `预赛专项` |
| `exam_stage: 决赛` 且属真题 | `综合模拟卷` |

「属真题」= 在 `05-真题库`，或路径以 `真题/` 开头，或文件名以 `真题-` 开头。

**软规则（按难度的默认值，实测吻合 97.78% = 4,089/4,182，可偏离）**

`difficulty ≤ 3` → `章节练习`；`difficulty ≥ 4` → `模块习题集`。

> 93 条例外散布在 43 个目录（最大聚集仅 10 条），不是「整本书被统一归类」的可解释模式，是真实噪声，
> 故只作新题建议不做强制。编者因成书/组卷需要整章归入某档时，**以编者判断为准**。

**模块习题集 1,303 → 成书 1,283 的对账（2026-09-01 终态，零余量闭合）**：
- 18 道 gap 排除（题面缺图/答案待补/需外部资料，构建日志 `[gap-excluded]` 逐题列出；含 WCh21.23 提图无图+原书无答案）
- 2 道 deprecated 排除：ABOC061（2026-08-30 核实为自编冒充原书转录且目标分子缺失无法作答，质量剔除合理，无取代文件故 superseded_by 留空）、上海中学049（已被题-赵鑫光-晶体-习6 取代）；构建日志 `[deprecated-excluded]` 逐条可查。全库 deprecated 共 5 个，另 3 个为无机第5版题-008/009/011（pack=章节练习，被例12.6/12.19/12.9 取代，不入书，正常）
- 2 道余量为 Chem09 例题批次字段差异（pack=章节练习，见五、遗留）

## 五、成书现状与闭环校验

| 篇 | 章 | 题数 | 09-01 变化 |
|---|---|---|---|
| 化学原理 | 6 | 128 | +8（Ch09 沉淀溶解例题批次 promote） |
| 结构化学 | 7 | 564 | +19（Ch21 配位反应机理 18 题恢复入书 + 习6 promote） |
| 有机化学 | 12 | 363 | +1（38 届初赛螺吡喃题 submodule 修正入书） |
| 元素与分析 | 6 | 228 | +2（例13.4/例14.8 恢复） |
| **合计** | **31** | **1,283** | 本轮 +25 |

四重校验全部通过：① `validate_module_book.py` 双版本绿 ② 溯源映射 1,283 行逐题对应 ③ `probe_lost24.py` 对账闭合（pack 总数 − 成书 − gap − deprecated = 0）④ docx 三版本 0 errors。

## 六、维护脚本清单（都在 `11-模板/scripts/`，用系统 Python 3.12）

| 脚本 | 用途 | 跑法 |
|---|---|---|
| `build_module_book.py` | 生成习题书双版本源稿 | `--edition student/teacher --clean --write`；**先 rm 输出目录再跑**（Obsidian 文件锁坑） |
| `build_source_map.py` | 溯源映射 | 构建后必跑 |
| `validate_module_book.py` | 成书校验 | `--root 04-课件/习题集/习题书-教师版 --edition teacher`（学生版同理） |
| `build-all-handout-docx.py` | 源稿→docx | `--batch-root … --output-dir … --strict-images --cover --parallel 4` |
| `build_print_student.py` | 打印版 docx（`.workbuddy/scripts/`） | 直接跑 |
| `audit_book_classification.py` | 分类审计（晶体误入化学键等） | 定期/大改后 |
| `audit_book_quality.py` | 质量四维审计 | 定期（硬指标：难度过低/考纲无关/篇幅过短/缺乏解析） |
| `audit_book_coverage.py` | 覆盖率核查 | 定期 |
| `promote_questions.py` / `demote_questions.py` | 升/降 pack | --write 前先 dry-run |

**Python 调用统一加**：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID=` 前缀（绕 safe-delete shim）+ `-X utf8`。

## 七、架构规则（踩坑沉淀）

1. **一个题目 = 一个 md = type:题目**。合集/题组文件（type:题组）只做浏览整理，不进习题书；若要入书必须拆成单题文件。
2. **deprecated 必须写 superseded_by**，且取代文件必须真实存在、pack=模块习题集——否则取代链断裂=题目静默消失（例6.5/13.4/14.8 事故）。构建现已对 deprecated 打日志。
3. **subject_module 是路由第一闸**：必须是四篇之一的单行标量。submodule 不得从文件名机械截取。
4. **同一道题可出现在多载体**（习题书整大题 vs 习题集取小问 vs 测试卷）：设计互补，不是重复收录。
5. **数字以本页为准**；构建脚本自动回写 `04-课件/习题集/README.md`，手工改动其他文档数字前先跑构建对账。

## 八、遗留待办（按优先级）

> 📋 全量待办已整理为 [[00-首页/活跃任务/待办清单-2026-09-01]]（分类：需你亲手 4 项 / 待需求 5 项 / 例行 4 项 / 明确不做 6 项），本节保留题库域明细。

1. ~~Ch21 配位反应机理 16 题~~ ✅ 2026-09-01 已恢复入书（18 题，结构化学 547→564）
2. ~~Ch09 例题 9 题~~ ✅ 2026-09-01 已 promote 入书（8 题，化学原理 120→128；例9.5 此前已在书）
3. ~~真题（无答案）空壳目录~~ ✅ 已在索引.md 补状态说明（保留目录占位，原图在 06-外部资料导入 未题目化）
4. ~~ABOC061 待裁决~~ ✅ 调查结论（2026-09-01）：文件内 2026-08-30 warning 载明——非原书 T3 转录而是自编冒充、目标分子缺失无法作答，**维持 deprecated**（superseded_by 留空正确：无取代文件）。如未来按原书重录 T3，重录后去标即可入书。
5. **ABOC 解题思路占位（2026-09-01 大幅推进）**：原结论「无源可补」有误——原书 OCR 含「习题解析」节（`mineru/03-教材书籍/ABOC有机化学/ABOC202505_200-397.md`）。三轮共逐字回填 **49 条**（v1 行首 36 + v2/v3 行中 9 + v4 超长块截断 4；fidelity=原书逐字，尾部注明 OCR 来源；护栏：行中切分/同码取最长/遇章节标题截断/3.10 排除）。剩 **84 条**定性为原书侧无可用解析（77 条作者未做 + 5 条空块 + 7.6.2-2/3.10 仅一句附图或混思考题），**永久无源，除非获得纸质书**。成书管线会过滤解题思路教学块，回填不影响成书。
6. **打印版低 DPI 图（已调查闭合）**：学生版 505 张引用图中 437 张有效 DPI<150，全库 34,143 个图片 basename 中**无一存在更大副本**——源头即 72~150 DPI 的 OCR 原图，无源可换；除非重新扫描纸质书（外部输入）。此项关闭。
7. 题-033 需查纸质原书——维持待办（需外部输入）。
8. **40 道题的 `knowledge_points` 全部无法解析**（B1 批次产出，2026-09-02）：
   集中在 无机化学例题与习题 Ch14/Ch15/Ch18/Ch22（22 道）、第 34~39 届决赛（10 道）、结构化学基础（3 道）、赵鑫光（3 道）、有机化学（2 道）。
   **特征**：每题挂的都是细粒度概念（如「CO还原性」「赤血盐黄血盐制备」「单原子催化」），一个都匹配不上 `03-知识点/`。
   **处置**：拆分脚本已**刻意跳过、保持原样**——因为 `knowledge_points` 不允许为空，脚本不凭空造 KP，也不该用空列表把缺口掩盖成另一种告警。
   **需人工**：为每题指派一个真实存在的 `03-知识点/`，或新建对应知识点文件。完整清单在 `.workbuddy/backups/kp_empty_list_files.txt`。
   这 40 题贡献了当前 `断链-frontmatter` 158 条中的绝大部分，处置后告警可再降约 116 条。
