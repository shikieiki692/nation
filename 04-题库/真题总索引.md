---
title: 真题总索引
type: 索引
module: 全科
subject: 系统
tags: [化竞, 真题, 索引, Dataview]
updated: 2026-05-08
---

# 真题总索引

> 本文件通过 Dataview 动态聚合所有已入库的真题，支持按年份、模块、题型、难度等多维度筛选。

---

## 一、按届次汇总（全部）

```dataview
TABLE WITHOUT ID
  link(file.name, title) as 题目,
  year as 年份,
  exam_stage as 赛段,
  module as 模块,
  question_type as 题型,
  difficulty as 难度,
  teaching_level as 层次,
  join(knowledge_points, "、") as 核心知识点
FROM "04-题库/真题"
SORT year DESC, file.name ASC
```

## 一（附）决赛专题汇总

```dataview
TABLE WITHOUT ID
  link(file.name, title) as 题目,
  year as 年份,
  exam_session as 场次,
  module as 模块,
  difficulty as 难度,
  join(knowledge_points, "、") as 核心知识点
FROM "04-题库/真题"
WHERE exam_stage = "决赛"
SORT year DESC, file.name ASC
```

## 二、按模块统计

```dataview
TABLE WITHOUT ID
  count(file.name) as 题数,
  average(difficulty) as 平均难度,
  min(difficulty) as 最低难度,
  max(difficulty) as 最高难度
FROM "04-题库/真题"
GROUP BY module
SORT count(file.name) DESC
```

## 三、按题型统计

```dataview
TABLE WITHOUT ID
  count(file.name) as 题数,
  average(difficulty) as 平均难度
FROM "04-题库/真题"
GROUP BY question_type
SORT count(file.name) DESC
```

## 四、按难度分布

```dataview
TABLE WITHOUT ID
  count(file.name) as 题数,
  join(map(rows, (r) => link(r.file.name, r.title)), "、") as 题目列表
FROM "04-题库/真题"
GROUP BY difficulty
SORT difficulty ASC
```

## 五、按知识点交叉筛选

> 点击下方任意知识点链接，即可跳转到该知识点页面查看其关联真题。

```dataview
TABLE WITHOUT ID
  knowledge_points as 知识点,
  count(file.name) as 出现次数,
  join(map(rows, (r) => link(r.file.name, r.title)), "、") as 关联题目
FROM "04-题库/真题"
FLATTEN knowledge_points
GROUP BY knowledge_points
SORT count(file.name) DESC
LIMIT 30
```

## 六、最近更新

```dataview
TABLE WITHOUT ID
  link(file.name, title) as 题目,
  year as 年份,
  module as 模块,
  difficulty as 难度
FROM "04-题库/真题"
WHERE updated = date("2026-05-08")
SORT file.name ASC
```

---

## 图例说明

| 图标 | 含义 |
|:---:|:---|
| ⭐ | 竞赛拔高题（difficulty = 5） |
| 📐 | 计算题 |
| 🔬 | 推断题 |
| 🧪 | 方程式题 |
| 📝 | 机理题 |

---

*索引自动生成时间：2026-05-08*
*题目总数：`= length(filter(file.inlinks, (f) => contains(string(f.folder), "04-题库/真题")))` 道*
