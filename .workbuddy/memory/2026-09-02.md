# 2026-09-02 工作日志

## 省预赛图依赖题 · 批次1-2（07:35–08:00）

- **批次1 江苏 2021 卷 ✅**：24 张裁剪图逐张看图核验（碱基 A/G/T/C、三单体、七聚钼酸根、三元氮化物、菲啶、题8 四图、题9 四图、题10 四图），嵌入 8 个题目文件替换整页占位；手册 10 题状态→已核对+补图。μ₃/μ₄ OCR 修正确认上轮已落库。
- **批次2 福建 2021 卷 ✅**：10 张图核验（单胞投影、光刻胶路线、题8 酸碱排序 2 图、Pelitinib 路线 2 图+中间体、炔烃烯化 3 图），嵌入 5 个题目文件；手册 10 题状态→已核对+补图。
- 每批后 validate_kb --full：0 error / 1,641 warning / 图片缺失 0 保持。
- 提交：ffb8a8b7（江苏）、1334376d（福建）。
- **待办**：批次3-5 浙江三卷 60 题（含 A/B 双正确存疑悬案）。
- **推送阻塞**：GitHub 直连超时，后台重试中 + 12:00 定时任务兜底（automation 04e33ac7）。
- **手册结构注意**：处理手册每卷有"总览+详细"两段、详细段内还有 `## 题N` 子标题，按标题定范围时必须用行号/下一卷标题，别用"下一个 ## "。

## 省预赛批次3-5：浙江三卷收官（10:27–10:45）

- **关键发现**：浙江三卷主卷 md（08-09 更新）早已内嵌原卷整页图 42/39/48 处，全部 60 道图依赖题被覆盖——手册里 60 个"待处理"是过时遗留，非真实缺口。
- **抽查证实**：手册浙江段裁剪图存在跨题归档错位（题52 候选实为题51 的吡啶选项、题4 候选之一为题3 的 EMIM⁺），机械嵌入会放错图 → 决策：浙江不逐题嵌裁剪图，以主卷整页图为准。
- **题4 悬案裁决**（看图 fe6b0ee0 + 整卷选项）：结构=三氯异氰尿酸 C₃N₃O₃Cl₃（X=C/Y=N/Z=O/W=Cl）。A 必正确；B 简单阴离子口径亦成立（Cl⁻ 181 > N³⁻ 146 > O²⁻ 140 pm），与官方 AB 吻合 = 单选卷双解瑕疵。C/D 排除理由已落库。主卷 + 核对清单已加注记。
- 手册 80 题状态全部关闭（0 待处理）；清单浙江节加补图方式说明。
- 验证：0 error / 1,641 warning / 图片缺失 0。提交 efb820bc。
- **推送仍阻塞**（本地积压 4 commit：ffb8a8b7/1334376d/4826ec89/efb820bc），后台任务 + 12:00 automation 兜底。

## 2026-09-02 自动化：git push 积压提交
- 第 1 次默认 helper 链推送挂死（10m38s 零输出），根因=系统级 gitconfig 的 credential.helper=helper-selector 非交互挂起。
- 第 2 次用 `-c credential.helper= -c credential.helper='!GCM路径'` 直连成功（4 秒），944b89d0..efb820bc 共 4 笔上远端，ls-remote 验证 head=efb820bc。
- 教训：push 前直接带上 helper 重置参数，不要先裸试（第 1 次必挂）。

## 2026-09-02 续：MEMORY.md 精简
- MEMORY.md 从 ~9,000 字符压缩至 ~3,100：合并环境/基线/铁律，删除历史流水（当日整备、口径变迁明细），保留硬规则、命令、防坑要点与遗留待办。

## 浙江三卷 240 题按 SOP 拆题入库（07:35–12:00）

- **执行方式**：zj_extract.py 解析三卷整卷 md（80×3 题块 + 答案映射；答案正则需容忍 `(图)` 后缀、只解析 ``` 围栏防年份污染）→ 3 个并行分类代理逐题出分类 JSON（subject_module/submodule/difficulty/teaching_level/kps/desc，**KP 白名单硬约束**）→ zj_generate.py 按 SOP v1.1 生成 240 个单题文件。
- **产物**：`04-题库/真题/省预赛/浙江卷2021|2022|2023/` 各 80 文件，`题-浙江{yy}-{单选|多选}-{NN}-{描述}.md`；六字段齐全（pack=预赛专项，不入习题书故免构建预演），cross_references 回链整卷，答案折叠块标注 AI 非官方。
- **质量**：validate_kb 6,407 文件 0 error / 1,641 warning **纹丝不动**（240 新题 0 断链 0 缺图——白名单硬约束生效）；diag 题目计数 4,119+63=4,182。
- **索引同步**：题库总索引（省预赛桶 20→260，总量 3,946→4,186）、习题集体系总纲、省预赛索引（浙江逐题拆解节）、状态摘要、交接清单、任务卡周检阈值 4,182。
- **提交推送**：8204a280 已推 origin/master ✓（网络恢复）。三个分类代理经 TaskCreate 团队协作（SendMessage shutdown 收尾）。
- **坑**：bash -c 传含反引号的字符串会被命令替换吞掉（`no_answer` 被执行）——含反引号内容的批量文本替换一律走脚本文件，不走内联 python -c。

## 集成收尾（12:30）

- 体系总纲 pack 覆盖矩阵同步（预赛专项列按 240 文件 frontmatter 实数：元素 51/原理 62/有机 79/结构 48，合计 4,478→4,718）。
- 提交 38be4340 已推送。工作树干净，全部同步。
- 下一批候选（待用户触发）：高考化学 1,482 文件题目化（同浙江管线可复用）/ 结构·有机·元素三篇阶段测试卷 / IMA 规模化导出。

## 三篇阶段测试卷生成（12:35–13:10）

- 产出：`04-题库/{结构化学,有机化学,元素与分析}阶段测试卷.md`，各 50 题链接清单（复刻化学原理卷模式，答案在题文件 details 块）。
- 选题：模块习题集题池（结构 579/有机 363/元素 226），used_in 排除 + 答案状态优先 + d3→d4→d5 梯度 + 跨子模块配额（最大余数法锁 Σ=50）；used_in 回填 150 处。
- 验证：三新卷 0 死链 0 重复；validate_kb 6,410 文件 0 error / 1,641 warning 不变。提交 aa629df1（本地，不推送——用户指示）。
- **过程坑（三个连环）**：① os.path.splitext 把"习题1.1"的 .1 当扩展名 → 文件名反查失败；② 子模块组数×下限 > 50 时最大余数循环死循环（有机 25 组×3=75>50）→ 加 K 上限与迭代上限；③ 每选题全树 walk 超时 → 单次遍历+内存索引；stdout 重定向被 kill 时缓冲丢失，调试必须 -u。
- **误伤与恢复**：回滚 used_in 时误删原理卷既有 43 处标记 → 从原理卷链接反查恢复 45/50（1 个无 status 行单独补，4 个已有变体保留）。
- **新发现待办**：化学原理阶段测试卷本身有 4 条重复链接（历史文件为"### 汇总 + - d4 视图"双布局，磷脂双层膜出现 3 次）——双视图去重需专项处理，本轮 git 还原不动。
- 待办清单条目 5（三篇测试卷）与 7（高考化学题目化）已关闭/修正（7 为过时待办：高考赛道 L1/L2/L3 早已完成）。

## 首页入口同步 + 推送（14:15）

- 同步 6 个入口文件：状态摘要（顶部置顶 09-02 进展 + 关键状态表新增"阶段测试卷"行 + 健康基线 6,410）、工作日志总索引（补 09-02 行）、新建 `00-首页/工作日志/2026-09-02.md`（全天五项工作 + 教训沉淀）、待办交接清单（受检 6,410 + 最近已完成 09-02 条目）、任务卡（run 记录 6410）、活跃任务（当前判断置顶）。
- 推送积压 2 commit（aa629df1 三篇测试卷 / 4dd54872 待办关闭）+ 本次文档同步 commit，用户明确要求推送。
- 教训补一条：多文件批量替换脚本里 anchor 含换行时必须用文件自身的 eol 变量拼（首个 anchor 硬编码 \n 在 CRLF 文件上 assert 失败）。

## 推送落地（14:37）

- 后台重推 4m12s 后成功：`8560b6c0..cebe2d7a master -> master`，PUSH_EXIT=0。3 个积压提交（aa629df1 三篇测试卷 / 4dd54872 待办关闭 / cebe2d7a 首页同步）全部落库，工作树干净。
- 15:30 兜底定时任务（d2e3ceb9）已删除（不再需要）。
- 今日推送观察：GitHub 直连间歇性阻塞，后台挂起式重试 + 定时兜底的双保险模式两次奏效，可沿用。

## 题库升级 B1：knowledge_points 拆两级（提交 2a3a4256）

- 根因：1,590 条红链不是写错，是**粒度错配**——题目引用「气体体积」「离子对效应」等比 948 个知识点文件更细的概念，被当成"必须存在的页面链接"校验。
- 方案：能解析到 `03-知识点/` 的留 `knowledge_points`，解析不到的剥离 `[[]]` 移入新字段 `concepts`（纯文本，不参与断链校验，仅检索）。723 文件 / 1,696 项降级。
- Warning 1,641 → 209（断链-frontmatter 1,590 → 158）。40 题知识点全部解析不出 → **刻意跳过保持原样**（`knowledge_points` 不允许为空，见 fix_kb_phase2_apply.py:215），清单在 `.workbuddy/backups/kp_empty_list_files.txt`。
- 新建 `02-数据库/题库.base`（5 视图）。顺手校正总览过时数字：4,016→4,258、真题 447→687、预赛专项 20→260。

## 题库升级 B2a：字段地基（提交 43b4b14d，4,195 文件）

1. **subject → source_subject**（4,184 处）：消除与 subject_module 的同名歧义。前者=来源教材科目（16 值，不收敛），后者=四大模块（组卷筛选，受 QB_ENUM 约束）。**1,906 处值不同是设计意图，全部保留**。锚定行首 `^subject:` 改名，subject_module 零误伤。
2. **teaching_level 14 种 → 4 档**（基础/巩固/拓展/竞赛）。长尾落点有硬信号：`拔高/提高/进阶/决赛/高级` **100% 落在真题目录**→竞赛；`强化/挑战` 全在教材习题→拓展。结果：452/1,352/1,534/888。缺失 257 题按 difficulty 推断并紧贴 difficulty 行插入；difficulty 也缺的 96 题不推断。
3. **补 year 280 题**：路径「第{N}届」→ `N + 1986`（11 组配对 100% 验证，全量 356 条已有 year 反查 0 冲突）。
4. 修两个 base 的双仓合并（原 真题.base 漏 05-真题库、「有机专项」用的 module 字段在真题库 100% 缺失→改 subject_module）；真题收口到 真题.base，题库.base 移除重复视图。
5. 同步 SOP（其枚举与实测脱节：SOP 写「入门」，实测主流是「基础」380 vs 62）。
- 校验：Error 0 / Warning **209**（与 B1 持平）。

### 本批踩坑（重要，跨批次通用）

- **bash heredoc 里 `:\` 会被吃掉**：`teaching_level:\s*` 变成 `teaching_level:/s*`，正则静默失效。但 `\s` 前面是中文/括号/行首时没事（`求\s`、`（\s{2,}`、`^\s*[ABCD]` 均正常）。→ **含冒号+反斜杠的正则一律用 Write 写成 .py 文件，不要走 heredoc**。
- **本库 `.gitattributes` 有 `*.md text eol=lf`，git diff 会做 CRLF 归一化** → 之前用「`git diff --stat` vs `--ignore-cr-at-eol --stat`」检测行尾污染**在本库是无效手段**。改为与 zip 快照逐行比对（`verify_b2a.py`）。
- **本库是混合行尾**：4,189 个待改文件里有 134,919 个 CRLF。用 `split("\n")` 后 CRLF 行的 `\r` 留在行尾——原地改值不受影响（`\r` 落在正则 `\s*$` 里），但**插入新行会丢 `\r`**，必须按邻居行尾风格补（487 次插入，241 次落在 CRLF 上下文）。
- `Path.read_text()` 不接受 `newline=`；且默认 `newline=None` 会折叠 CRLF→LF，**检测行尾时正好毁掉要测的东西**，必须用 `open(..., newline="")`。
- **git add 会刷出 ~150KB CRLF 警告**（`.gitattributes` 归一化所致），不 `2>/dev/null` 会被 SIGTERM 打断并留下 `.git/index.lock`（0 字节残留），后续 git 全报 exit=128，需 PowerShell `Remove-Item` 清锁。
- **改完文档要重跑校验再提交**：B1 在 题库架构总览.md 写了 `[[wikilink]]` 示例被当真实链接，Warning 209→210，本批才发现并改全角修回。这正是 SOP §7 明令禁止的反模式。
- git status 报 `M` 但 `git diff` 为空 = stat 缓存过期（内容字节相同），无害；`git update-index --refresh` 有时清不掉，提交时按显式 pathspec 暂存即可。

## 题库升级 B2b：补 question_type（提交 0fd95a64，1,193 文件）

覆盖 **4.7% → 29.4%**（4,119 个题目文件里 1,209 条有题型）。
- 归一化已有 190 条的 24 种写法 → 列表 + 8 种原子题型（162 改写；11 条「综合题」无法拆分进 backlog）。
- 推断写入 1,041 条。**只写可信的**（用户决策），用户拍板的三个语义：
  1. 「画出 A 的结构式」**统称「作图」**（实测作图档 164 条里 114 条画结构、10 条画装置图/相图，拆开会让「作图」缩到 26 条）
  2. `选择/填空`（作答形式）与 `计算/推断/机理`（内容题型）是**两个维度，并存**——求 pH 的选择题 → `[选择, 计算]`
  3. 多小问真题大题**留空 + 建待标注视图**，不猜

### 分档规则（宁可漏不可错）
| 档 | 信号 | 处理 |
|---|---|---|
| T1 | 选项行 `A. B. C. D.` / `____` 占位 | 写 |
| T2 | 内容动词 + 宾语双条件 | 写 |
| T3 | 「简述/为什么/说明」→ 简答 | **只统计不写**（542 条）——它是「没识别出题型」的委婉说法 |
| — | 一题多问且各小问题型不一致 | 留空（701 条）；同质多问（各问都是计算）才写（25 条） |

### 抽样核验抓出的 6 类误判（脚本注释里每条都有证据）
- **P1 标题行混入题干**：`题-浙江23-单选-24-丙烷氧化反应机理` 被判机理题，实为选择题。文件名带题型词是常态 → `clean_stem()` 剥首个 `# ` 标题行（有 `## 题目` 小节则取其后）。
- **P2 答案区泄漏**：OCR 逐字重录题（赵鑫光/无机化学例题）无 `## 答案`，`解：` 直接跟题干 → 拿答案去推断。ANS_CUT 扩到 `^[解答][：:]`/`【解】` 等。
- **P3 bare「求」**：`求[：:]?\s*(?:该|下列|各|其)?` 量词全可选 → 「不要**求**立体化学」命中计算。改成**动词 + 必须出现宾语**（CALC_OBJ 词表）。
- **P4 名词用法**：「由晶格匹配**计算**结果，I 层中…」→ 同 P3 修复。
- **P5 行内选项**：浙江预赛 `- A. x　B. y　C. z　D. w` 挤一行，逐行检测全漏（选择档 44→438 条）。统一跨行/行内检测，并要求**相邻选项间隔 ≥2 字符**排除「化合物 A、B、C、D」字母代号。
- **P6 括号代号**：「碘代吖丙啶 **(B)**, 氮杂联烯阳离子 **(C)**」→ 选项正则加 `(?<![（(])` 排除紧跟开括号的字母。

### 本批新踩的坑
- **脚本漏 type 白名单**：`infer_question_type.py` 初版没检查 type，误写 19 个非题目文件（索引/README/真题整卷/图片索引，`04-题库` 下混着 140 个这类文件）。已用 `rollback_b2b_misfire.py` 按 `git show HEAD:` 恢复 → **批量改字段的脚本第一件事就是加 type 白名单**（`{题目, 真题}`，注意 05-真题库 的 type 值是「真题」不是「题目」）。
- **多小问一刀切会误伤同质题**：「计算下列溶液的 pH：(1)…(2)…(3)…」三问全是计算。解法 `infer_multi()` 按小问切段逐段推断，**各段都有 T1/T2 信号且并集 ≤2 才写**（25 条）。切段时**必须把编号前的总起句拼进每段**——否则每段只剩化学式、没有动词，全段落空（首次只命中 1 条，加上 prefix 后 25 条）。
- **小问编号要覆盖三种写法**：`7-1` / `5-1-1`（三段式，简单 `\d-\d` 会因前后断言失败而完全漏掉）/ `10.1` / `**1.1.1**`。并要求同组值**连续**（`_has_run`），否则 `0.10/0.20/0.30 mol/L` 会被当成三个小问。
- **校验脚本口径 bug**：用 `re.match(r"^---\n", t)` 找 frontmatter 会把 **CRLF 文件全漏**（统计成 2,386 题，实际 4,119）。老老实实 `split("\n")` 后找第二个 `---`。（继 B2a 的 `git diff` 之后，第二次栽在「行尾影响匹配」上。）

### 校验
Error 0 / Warning **209**（与 B2a 持平）。逐行 diff 0 异常：1,037 插入 + 162 归一化替换，无删除、行尾无污染。快照 `pre_b2b_20260902_161927.zip`。

### 剩余 2,970 条待标注的构成
71% 在 `04-题库/教材习题`（结构化学 972 / 有机 962 / 元素与分析 597 / 化学原理 439），真题 254。
→ 按**所在目录**排序补（同目录同质，可批量），`题库.base` 已加 `file.folder` 列 + 「题型待标注（按目录批量补）」「·真题优先」两个视图。

---

## B3 组卷器升级（进行中）

### 摸底：字段底数（4,182 题）
fidelity / difficulty / teaching_level / source = 100%；`used_in` 344（8.2%）；`depends_on` **仅 4**（承接题功能只能降级为人工看题面 ⚠️）。

### 三个静默失效的 dataview 坑（实测抓出的，不是猜的）
1. **`used_in` 是 wikilink 不是纯文本标签**。全库 344 条统一写法 `used_in: "[[结构化学阶段测试卷]]"`（指向 4 份阶段测试卷，文件都真实存在）。
   → dataview 解析成 **Link 对象，没有 `.length`**，所以 `p.used_in && p.used_in.length` 对全部 344 条**恒为假**，`EXCLUDE_USED` 完全失效且不报错。
   → 统一改 `hasUsed = u => u != null && (Array.isArray(u) ? u.length > 0 : true)`。
2. **`Number(p.difficulty)` 遇 9 条例外写法**（`3-5`×5、`4-5`×2、`3 # 1-5`、`1-5 # 1=入门`）得 NaN，被 `>= / <=` 静默丢弃 → `diffNum()` 取第一个整数（区间下界）。
3. **`exam_stage` 有 `/` 多值**（`初赛/决赛`），精确 `!==` 会漏 → `stageHit()` 用 split 后 some。
> 新增 dataviewjs 查询一律照抄 §一 的三个工具函数（`hasUsed` / `diffNum` / `stageHit`）；
> **dataviewjs 代码块之间不共享作用域，每块都要单独定义一遍。**

### 来源归一化（限流的前提）
`source` 有 1,228 个去重值，1,083 个只出现一次——同一本书被拆成多种写法。归一化后 1,087 个，
TOP：化学竞赛初赛讲义 496 ／ 赵鑫光《…学习笔记》482 ／ 普通化学原理（第4版）习题解析 306 ／ 周公度结构化学 285。
不归一化「同来源限流 ≤3」形同虚设。**JS 版 `srcKey()` 与 Python 版已逐条比对一致**（脚本 `check_workbench_js.py`）。

### 新脚本
- `mark_used.py` —— 出卷后回填 `used_in`，闭环关键一步（不回填 `EXCLUDE_USED` 就是摆设）。
  默认 dry-run，`--write` 落盘；`--tag` 可省略（取试卷文件名）。**单值写标量、多值写数组**，沿用 wikilink 约定。
  已实测：LF / CRLF 两条分支都正确（题库 1,326 CRLF + 2,557 LF 混合）、幂等（重跑全跳过）、非题目文件跳过。
- `check_workbench_js.py` —— 抽 dataviewjs 给 `node --check` 做语法检查 + 跨语言比对 `srcKey`。

### 本批新踩的坑
- **`grep -c $'\r'` 在 Git Bash 下是假阳性**：把纯 LF 文件报成「51/51 行含 CR」。
  查行尾只能用 Python 数 `b.count(b'\r\n')` vs `b.count(b'\n')`。
- **bash 双引号里 inline `python -c` 写含 `$` 的正则会被转义搞坏**：`\s*$` 直接失配（统计全变「无 type」）。含 `$` 的正则一律写成 .py 文件。
- **文档里的示例 wikilink 会被校验器当成断链**：`used_in: ["[[A卷]]"]` 让 Warning 209→212。
  语法示例一律用**全角方括号** `【A卷】`（与既有规范一致）。
- `mark_used.py` 第一版又把 `04-题库/README.md` 写进去了（链接以 `04-题库` 开头过了目录过滤，但 type=系统）。
  → **type 白名单这个坑第三次出现**，脚本里补 `fm_type() in {题目, 真题}`。

---

## B4 生命周期治理（下午，Task #8/#9/#10/#11 全部完成）

### Task #9 status 收敛 + 题库.base 口径对齐（`ba03bb56`）
- 补 30 条缺失 status → `已填充`；`status` 覆盖率达成 **4,182/4,182 = 100%**。
- 5 条「待填充」查实是**部分填充**（第37届决赛第一场，只录了靠后的小问），状态正确，不动，交给视图追踪。
- **发现 `题库.base` 是唯一停留在 4,119 口径的界面**：顶层只有 `inFolder("04-题库")`，漏了 05-真题库 的 63 条；
  而 题库架构总览、组卷工作台 `SCOPE="both"`、`mark_used.py` 全都按 4,182。补 `or` 后对齐。
- 视图改造：「待治理」拆成**按动作类型互斥**的两个——「待治理（废弃待决）」（决策：恢复还是归档）
  与「部分填充待补全」（干活：补录小问）。加 `deprecation_reason` / `superseded_by` 两列。
- 新增 `sim_base_views.py`（离线模拟各视图命中行数）、`status_census.py`、`cmp_zhenti_fields.py`。
- 「层次待定」视图现在是 **0 行**（B2a 已把 teaching_level 收敛到 100%），保留作兜底网。

### Task #10 废弃机制收敛（`06c37ac0`）——本轮最有价值的发现
- **全库有两套并存的废弃机制**：A `status: deprecated`（5 条）；B `deprecated: true` +
  `deprecatedDate` + `sunsetDate`（3 条，status 仍是「已补全答案」）。
  组卷工作台第 71 行与 `build_module_book.py` 第 618 行**都只认 A**，
  → B 那 3 条虽已标记废弃、日落日期 2026-09-06 近在眼前，仍长期出现在组卷池和习题书里。
- 用户决策：**并入 `status: deprecated`**（一套机制，工作台不用改即自动排除）。
- 动手前先证伪：3 个父文件的小问 **100% 已独立成题**（12.35-12.45 / 19.57-19.63 / 19.64+19.68-19.77），
  标废弃不会丢内容；7 个 superseded_by 目标全部可解析。
- 结果：命名冲突全消（`supersededBy`→`superseded_by` 7 条、`superseded_note`→`deprecation_reason` 11 条），
  status 分布 已补全答案 795→792、deprecated 5→8。
- **差点误判**：`demoted`/`promoted`/`depends_on` 看着像僵尸字段，grep 后发现分别被
  `demote_questions.py` / `promote_questions.py` / `audit_question_bank.py` 读写，是承重字段。
  → 立规矩：**判僵尸字段前先 grep 脚本**，真野字段只有 `big_question` 16 / `source_author` 8 / `quality_tier` 5。
- 顺带三修：SOP §8.0 放宽取代文件 pack 要求；「精选池」加 `status != deprecated`（原本虚高 5 条，1,319→1,314）；
  `validate_kb.QB_LINK_FIELDS` 加 `superseded_by`（防 SOP §七 记载的取代链断裂事故重演，反向测试确认会真触发）。

### Task #11 讲义 problems 映射（`a3c6f501`）
- 新脚本 `check_lecture_problems.py`。**按 `created` 切新旧**而非 `stage`：
  按 stage=published 强制会让 84 份已定稿历史讲义一次性变红，与「存量不强制回填」冲突。
  当前新讲义为 0 → 绿灯；反向测试 `--since 2026-06-01` 得 ERROR 209、退出码 1，证明规则真触发。
- **现状：217 份讲义里 22 份有 problems，148 条引用中 38 条（25.7%）解析不到。**
  根因是引用粒度与文件命名不一致（讲义写小问级 `题-039-1-1-X`，文件是大题级 `题-039-1-X`），
  另有 31 条是讲次内部自造编号。属规范 §五.3 同一笔欠账，不自动修。
- 格式口径统一为 **wikilink**：规范 §五 承诺的「反查这道题被哪些讲义用过」只有反链能实现。
  修掉 `11-模板/模板-习题组.md` 的 `题-NNN` 占位符源头（已泄漏进一份讲义）。

### 本批新踩的坑
- **`git show HEAD:<file>` 不能当行尾基线**：`.gitattributes` 有 `*.md text eol=lf`，
  git 存的是归一化后的 LF，磁盘可能是 CRLF。只能拿**改前 zip 快照**逐字节对比
  （`12.35-12.45-简答题.md` 的 2 个纯 LF 行就是这么确认是改前就有的混合，非本次引入）。
- **`.gitattributes` 加 `*.json text eol=lf` 是灾难**：74 个 json 是 CRLF，且 `dependency-map.json`
  每次跑校验都重生成 → 会持续刷 diff。已撤回，只加 `*.base` 与自引用的 `.gitattributes`。
- 四份关键校验结果自始至终 **Error 0 / Warning 209**，与基线持平。


## B6 开题：question_type 缺口诊断（推翻原预期）

- **原计划**：补 2,970 条 question_type，目标覆盖率 29.4% → 80%。
- **实测结论**：现有规则下 T1/T2 命中数为 **0**，一条都补不了。2,970 条全是被规则**故意跳过**的三档：
  - 推断-无信号 **1,727**（58.1%）／推断-多小问跳过 **701**（23.6%）／推断-T3 简答 **542**（18.2%）
- **诊断脚本** `.workbuddy/scripts/qt_gap_diagnose.py`（只读，四张表：动词频率/目录分布/多问切段/按目录抽样）。缺口 70% 集中在 `04-题库/教材习题/*`（无机化学例题与习题 376、初赛讲义 370、Clayden 310、结构化学基础 276、赵鑫光 228、ABOC 167）。
- **两个已量化的可捡漏点（都做了抽样核验）**：
  1. **选择题检测是 bug**：`infer_question_type.OPT_ANY` 的负向断言 `(?<![\w.。（(])` 把 `(A) 2;` 这种最常见排版全排除了。修掉后全库 394 → **482（+88），零丢失**，抽样 25 条全对。防「化合物 A、B、C、D」误判本来已有「相邻间隔 ≥2 字符」把关，负向断言是多余且有害的。
  2. **计算档词表/窗口太窄**：放宽窗口 24→60 + 补动词（多少/是多少/估计/几克）+ 补宾语（分子数/分压/硬度/解离度/含量/键长/波长/半径…），无信号档 0 → **112 条（6.5%）**，抽样 30 条全对。
- **合计机械增益仅约 200 条**（覆盖率 29.4% → ~34%），离 80% 目标很远。剩下 2,770 条需要语义裁定 + 人工。
- **自查纠偏（重要）**：动词频率表里的「合成 398 / 制备 189」是**全库出现次数**（含多问档、T3 档），
  不能拿来做决策。在真正缺类型的**无信号档**里实测：合成 91 题（6 题是「合成氨」类名词，85 条像真指令，
  集中在 ABOC）、制备 53 题（48 像真指令）、分离 28 题（27）、设计 21 题（17）。
  → 新增「合成」类题型的真实收益约 **50~90 条**，不是几百条。
  **教训：诊断表要按"缺口档"分段统计，全库计数会严重高估收益。**
- **待用户决策的三件事**：① 词表是否新增「合成/制备/分离/设计」这一类（实测 ~177 条，有重叠，
  现有 9 种原子题型里没有）；② T3 简答 542 条是否放行写入（现行规则规定不写，
  理由是「简答=没识别出题型」的委婉说法）；③ 多问档 701 条怎么办（287 条切段后全段落空）。

## 晚间：三模块升级 grill 定稿（管理与运用优化）

- **现状复盘**（quick_state_check.py）：4,182 题；question_type 1,757（42%，B6 推进后），缺 2,425（教材习题 1,729 / 04-题库真题 232 / 化学原理 192 / 有机 156 / 05-真题库 59）；真题 year 全覆盖；used_in 344（8.2%）全指向 4 份阶段测试卷。
- **teaching_level × difficulty 单调自洽**：基础 452（≈难度2）/ 巩固 1,317（≈3）/ 拓展 1,525（≈3–4）/ 竞赛 888（≈4–5）。
- **关键事实：来源≠难度**——教材习题目录横跨四档（205/827/1,179/598），真题小问跨四档（12/33/14/4）→ 按目录切模块必然错分，只能按元数据切。
- **grill 四决策**（用户逐项确认）：
  1. 核心场景 = 组卷输出（一期卷型：题型专练＋真题模拟＋知识点专题，不做重做卷）；
  2. 架构 = **一库三视图**，文件不搬家，模块归属 = teaching_level 唯一决定；
  3. 映射 = **切法 A**：习题集=基础+巩固 1,769 / 习题书=拓展 1,525 / 测试题=竞赛 888＋真题整卷模考（42/36/21）；
  4. 闭环 = **最小闭环**：卷尾失分复盘（模块+KP 两行）→ 工作台按 KP 抽未用题出补弱卷 → mark_used 回填；不做逐题对错记录。
- **存量定位**：31 章习题书与 4 份阶段测试卷为独立成品不动；「池 vs 成品」定义写进 SOP。
- **落地清单（待执行）**：Bases 三视图 → 工作台三预设（习题集：按章节小批量、答案随卷；习题书：KP 专项＋难度递进两种；测试题：梯度 2:5:3＋限时＋答案卷分离）→ 补弱卷流程 → SOP 补定义 → 校验器软检查（teaching_level 目录漂移，只告警不阻断）。
- 已知薄点：基础池仅 452 题（10.8%），视图上线后按模块看缺哪补哪；拓展与竞赛在难度 4 重叠，边界靠使用形态（练习态 vs 测评态）而非难度。
- 新增只读脚本：quick_state_check.py / quick_missing_analysis.py / analyze_module_structure.py（.workbuddy/scripts/）。
- MEMORY.md 精简重写（~7.8k → ~4.2k 字符，血泪坑三节合并去重），另补三模块定稿决策行。

## 晚间续：三模块视图落地（B7）

- **Bases 三视图**：新建 `02-数据库/{习题集,习题书,测试题}.base`，顶层 filter = 双仓 + type 双值 + teaching_level 条件；每文件三视图（全部 / 未用过的题 / 待填充）。`sim_module_pools.py` 验证：1,769 / 1,525 / 888，两两交集 0，并集 4,182，YAML 合法。⚠️ Bases 实际渲染仍需用户在 Obsidian 打开确认（离线无法验）。
- **池分布**：习题集未用 1,728；习题书未用 1,393（**化学原理仅 131 题，明显薄**）；测试题未用 717，全库 5 条待填充全在此池。
- **文档**：SOP §1.1 加「模块归属不靠目录」提示块；架构总览 §二 加「三模块视图与成品（2026-09-02 定）」小节（池 vs 成品定义、来源≠难度证据、补弱卷流程、基础池薄点）。
- **模板**：`模板-专题练习卷.md` v1.1 加 §7.1 失分复盘（卷尾两行字格式 + 补弱卷流程四步，不新增 frontmatter 字段）。
- **校验器**：`validate_kb.py` 加 `check_teaching_level_drift`——teaching_level 在所在二级目录为孤例（count==1，自愈式）→ Warning「层级-目录漂移」；存量孤例（经典例题/基础）进 `TL_DRIFT_KNOWN` 豁免；**仅全量口径启用**（--dir/--changed 部分扫描会误报）。`--full` 实测 **Error 0 / Warning 209 基线不动**；`test_tl_drift_check.py` 功能自测通过（4,119 条记录、0 告警、注入假孤例正确触发）。
- 新增脚本：sim_module_pools.py / check_crlf.py / test_tl_drift_check.py。
- **待用户**：① Obsidian 里打开三个新 .base 确认渲染与行数；② B3 三块 dataviewjs 实跑验证通过后，再做组卷工作台「三预设模式」（习题集/习题书/测试题）与补弱卷流程打通。

## 晚间再续：B6 收尾与提交（37ec643e）

- **三项"待用户决策"核实**：合成入 ATOMIC、T3S 强信号放行、同质多问写入——全部已实现进 infer_question_type.py（注释标明「2026-09-02 用户决策」），只是日志落后于实现。
- **五项规则升级落盘**（提交 37ec643e，548 文件 +1,096/-19）：选择题 OPT 误判修复(+88)、计算窗口 60+宾语反向(+112)、新增「合成」题型、T3S 强信号放行、同质多问写入与切段修复。545 个题目文件写入，抽样核验五档 ≥85%（选择 100/计算 93/合成 92/简答 87/推断 89）。qt_write_guard.py 行尾门禁 545/545 全过（混合行尾文件允许随邻居 +1）。
- **dry-run 证实规则法天花板**：可写存量=0，剩余 2,425 全在故意跳过档（无信号 1,325 / 多小问 738 / T3 弱信号 362）。覆盖率 29.4%→**42.0%**（1,757/4,182）。validate_kb --full 基线 209 持平。
- `00-首页/图片索引.md` 确认为 stat 缓存假 M（diff 为空），git add 刷新即消，未入提交。
- 诊断临时文件（_qt_*.txt ×9、qt_by_label.txt）不入库；孤儿目录 化学竞赛教程*/ 不动（用户决策：先只报告）。
- MEMORY.md 二次精简（5,673→约 2.9k 字符，回到 3,000 上限内；更新 question_type 天花板基线）。
- **下一轨道待用户选**：① 工作台三预设模式（gated on B3 Obsidian 验证）② 无信号 1,325 条 LLM 语义补全（新方法，预计 42%→55-65%）③ 转向其他遗留（习题书 V3 / 40 题 KP / 讲义断链）。

## 晚间三续：LLM 语义补全试点（5c3f2b19，用户拍板）

- **方法**：`llm_type_pilot.py` 复用 infer_question_type 内部 API（split_fm/stem_of/infer，口径与 main 一致），从无信号档 1,325 条按来源目录分层抽样 **125 条 / 77 组**（seed=42 可复现），LLM 逐条读题面判定 types+理由 → `llm_type_pilot_report.py` 合成 09-审计报告 试点报告（125 条明细全列出供抽查）。
- **结果**：写入率 **79.2%（99/125）**——计算 35 / 简答 20 / 方程式书写 14 / 推断 11 / 作图 11 / 选择 3 / 填空 2 / 合成 2 / 机理 1；留空 26 = 多问混合不同质 19（与 B2b 铁律一致）+ 题面缺失 4 + 题组关联 3。
- **三个新发现**：① **图片选项选择题**（选项为 Newman/结构图图片，题面无文字选项行）是规则法永久盲区（样本 3 条），LLM 补全核心增量；② **题面缺失/无设问 4 条**（ABOC 仅标题、汇智仅背景无设问、上海中学截断、经典例题 #112 空 stem）属数据质量问题，出清单单独修源，不属补全范围；③ **05-真题库有题组关联文件**（正文仅「完整题干请见 [[父题]]」），补全必须先排除或解析父文件。
- **放量预估**：无信号档可写 ~1,000 条 → 覆盖率 42%→**~66%**；T3 弱信号档 362 条同款判定可再推至 ~75%（多问混合是硬余量）。
- **待用户核验**：抽查报告明细确认精度 ≥90% 后放量（先排除题组关联+题面缺失 → 分批判定 → qt_write_guard 落盘 → 抽样复检）。

## 晚间四续：二轮升级 grill 启动（状态复核）

- 复核快查（check_status_quick.py 新增固化）：4,182 题口径与基线一致——status 100%（3377/792/8/5）、question_type 42.0%（1,757，缺 2,425）、year 21.5%（901）、concepts 17.1%（717）；非题目 md 142。
- validate_kb --full 复跑：6,410 文件 Error 0 / Warning 209，基线纹丝不动。
- 启动"管理与运用"二轮 grill：分支 = 主攻轨道排序 / Obsidian 验证关卡 / 三预设+补弱卷形态 / LLM 放量核验 / 管理自动化 / 遗留债务边界。

## 晚间五续：二轮 grill 定稿（全部分支关闭）

- **主攻轨道**：运用侧优先；LLM 放量并行后台；管理自动化收尾；遗留债务（习题书 V3/40 题 KP/讲义断链/孤儿目录）继续挂起。
- **验证关卡**：三 gate 现在全过（.base 渲染 / 工作台 dataviewjs 实跑 / LLM 试点抽 25 条核验）。
- **三预设参数**：习题集=书(file.folder)+submodule 二级、15 题、答案随卷；习题书=KP 精确命中、难度升序、15 题、答案卷尾；测试题=梯度 2:5:3 沿用（实测竞赛池 d2-3 114/d4 564/d5 210，50 题卷 10/25/15 三桶可满额）、限时 120min、独立答案卷文件、模考一期只用真题整卷（混题模拟卷二期，depends_on 仅 4 条不可自动化）。
- **补弱卷**：降档链式（测试→习题书→习题集，命中不足自动降档）、KP 精确、15 题、答案随卷、排除 used_in。
- **mark_used**：对话式一键（dry-run 预览→确认→write）；出卷即回填（占题防重语义）。
- **LLM 放量**：抽 25 条（按 9 类题型分层）核验 ≥90% 放行；一期只放无信号档 1,325（冲 ~66%），按来源目录分批 ≤300，每批 qt_write_guard+抽检；T3 弱 362 二期；题组关联/题面缺失 4 条剔除出修源工单。
- **管理自动化**：巡检 automation 已创建（id 7df7e47c，周一 08:00，validate --full + 状态快查与基线比对，异常才通知，正常静默落 09-审计报告）；入库闸门=validate_kb --changed 写进 SOP 硬步骤（不写新脚本）；废弃/待填充维持 B4 视图追踪。
- 关键事实：真章节号只在文件名（12 本书 12 种写法无字段）→ 章节维度落到 submodule；MEMORY.md 已按新决策重写（2,965 字符限内）。

## 晚间六续：未受阻执行件落地 + clean_stem 盲区发现

- **SOP 入库闸门**：§5.2 升级（`--changed` 显式文件列表、退出码 0 硬性 + `--full` 回归不高于基线 209）；§六 Agent 模板第 6 条同步。已用 `--changed` 实测今日两文件通过（0 error / 0 warning）。
- **Gate ③ 工具包**：`09-审计报告/question_type-试点抽查清单-25条-2026-09-02.md`——125 条按 9 类+留空分层抽 25（计算5/简答3/方程式2/推断2/作图2/选择3全/填空2全/合成2全/机理1/留空3），含核对标准、重点项（图片选项 3 条必须开原文件）、结论栏；通过线 ≥23/25。
- **放量清单**：`llm_batch_manifest.py` → `backups/llm_batch_manifest.json`。对账 **pool 1,315 + 题组关联 3 + 答案前置 6 + 图片题 1 = 1,325 ✓**；5 批（300×4+115），大组优先、组内文件名排序。
- **新发现（B6.5 候选）**：`clean_stem` 两类布局盲区——① `## 问题/题目/原题` 小节后单行短题干 25 条（假"过短"，已救回池里，如 34决理-1-2-2 全题干就一句话）；② **答案前置布局 6 条**（`## 参考答案` 在题目前面：4 个选择题合集带完整 ABCD 选项 + 讲义11.135 + Pb3O4验证），stem 被切到只剩标题 → 假"无信号"。修好 clean_stem 后选择题合集大概率**转规则可写**（选择，无需 LLM）。「题面真缺失→修源工单」实测归零——试点说的 4 条"题面缺失"实为背景无设问（LLM 留空即可）+1 条图片题。
- **教训**：自建脚本复用共享引擎的输出时，边界样本要抽查再信（<20 字排除规则差点把 32 个有题干的文件错误踢出放量池；抽查 2 个全误伤后改为按切短原因分类）。
- 待用户：B6.5 修不修（改共享引擎 clean_stem，放量前做收益最大；gate ③ 核验不受影响）。

## 晚间七续：B6.5 落地（用户拍板"放量前先修"）

- **clean_stem 补丁**（infer_question_type.py）：ANS_CUT 出现极早（≤300 字符）+ 答案块后有编号小节（NUM_SEC `^##+\s*\d`）+ 答案块前仅标题（≤200 字）→ 三条件同时满足才改取编号小节起的内容；普通"题干→答案"布局零影响。
- **管线**：dry-run --pending-out（恰好 4 文件）→ 逐条人工核验（全部 T1/选择、题干真实）→ qt_write_guard snapshot（pre_b65_20260902.zip）→ --write → verify 4/4 全过（插入 4、行尾零变化）→ validate --changed 4 文件 0 error/0 warning。
- **结果**：question_type 1,757→**1,761**（42.0%→42.1%）；缺 2,421 = 无信号 1,320 + 多问/T3 1,101。放量清单重生成：pool 1,314，答案前置桶收敛到 2 条真乱格式（讲义11.135 题面是图+答案卡交错 / Pb3O4验证），批次 300×4+114。
- **教训**：「题面真缺失→修源工单」实测归零——此前判为缺失的文件多数是布局误伤（答案前置/单行短题干）或图片题；**排除规则必须抽查反例再信**（<20 字规则差点误踢 32 个有题干的文件）。
