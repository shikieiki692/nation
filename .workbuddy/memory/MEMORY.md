# 妙妙屋题库·长期记忆

## 环境与常驻工具
- 系统 Python 3.12(`-X utf8`)；全量校验 `validate_kb.py --full`；`reconcile_counts.py` 实时账本。**数字随并行导入漂移，勿写死**。题号双 type 白名单：`题目`(04)+`真题`(05)。
- Obsidian 索引只看 `.obsidian/app.json` 的 `userIgnoreFilters`(10 项；含 kb-vault-mcp/；媒体仓库等含图库不可排除)。
- 常驻：`kp_link_patrol.py`(A~H 巡检，判据 A/C/D/H/G=0,E≤3)、`kp_dep_redirect.py`(弃用页改指，--scope=qb/kb/all)、`kp_bclass.py`(①-b 二次分类)、`jsyaml_verify.js`(js-yaml4 闸门)。三者纳入「题库周巡检(异常才报)」自动化(周一 08:00)。

## YAML 卫生铁律
- **js-yaml4 遇重复键/非法转义 THROW→整条文件在 Obsidian 消失(P0)**；PyYAML 不报错，只有 js-yaml 闸门能抓。
- 形态：值含裸 `: ` 或 `*`/`[`/`{` 开头→引号；**含反斜杠值一律单引号**；FM wikilink 路径 `/` 禁 `\`；FM 内禁独立行 `![[...]]`。
- **写 L2 前必须 ls/grep 验证 KP 名真实存在**(英文/带空格/带`-`名 90% 不存在)。

## 链接解析铁律
- 分两表：basename_map(文件名)优先、alias_map(title/alias)兜底。判链：有前缀按路径、无前缀按文件名全库搜。
- 撞车(活跃页 alias=弃用页文件名)：文件名优先照改；反之 key 是活跃页文件名则绝不改。
- 纯文本转链两步：①父页加 alias ②题库 `"token"`→`"[[token]]"`。
- 批量改指废弃页须白名单(价键理论/缺陷/白磷红磷磷酸/Wade规则族/糖/自由基加成/Sθ/*深化)；带锚点链接丢锚点需人工确认。存量断链(1643)不动。

## 批量改 md 防坑
- 重拼 fm：`t[e:]` 拼 `"---"+new_fm+body`；`t[e+4:]` 才补 `\n---`(不能补 `\n---\n`)；断言行数不变。
- bash 内联 python 吃 `\$`/反引号→含正则脚本 Write 成 .py 跑。切 fm 逐行找首个 `---`。读写 `open(newline="")`。改前 zip 快照+写后逐行 diff。
- 内联 heredoc 含 **emoji/非 ASCII** 正则会 mojibake 出假统计（2026-09-05 实锤：假报 104 emoji 实为 49）→ 一律 Write 成 .py 跑。
- 批量插行禁用 `for+手动 i 推进`（无效→重复消费行双倍），必须 while-walk；改后对账「题数=标记数=溯源行数」＋行数对比快照（2026-09-05 溯源行曾 98=49×2，快照救回）。
- glob `题-2[78][0-9]-*.md` 的 `[]` 是字符位，先 print 命中数。题号重名须带完整文件名/目录。同一文件多处 Edit 须串行(并行触发 stale 拒绝)。
- 闸门参数：validate_kb `--changed` 无参检出 0，须显式列文件；清单若文本模式写盘 `\n`→`\r\n`，bash 须 `tr '\r\n' '  '`（仅 `tr '\n'` 残 `\r`→exists 全 False 受检 1/17）；jsyaml_verify **无 --list 参数**(2026-09-05 grep 源码确认，运行即全量：题目库04+05＋03-知识点)；audit `--dir` 只认完整相对路径(别名"二分册"→受检 0)。**printf 拼路径坑**：格式串里 `\04-题库` 被当八进制转义吃成 NUL→路径静默损坏且闸门报"读取失败"，清单一律 Write 工具/python 写盘。

## 题库铁律
- `source_subject`(教材分科)≠`subject_module`(四选一：化学原理/结构化学/有机化学/元素与分析)。KP 文件名多英文(Lewis结构式/VSEPR理论)，写 L2 前 grep 验证。
- 审计黑名单：可逆箭头裸 `\rightleftharpoons`(禁 xlongequal；带条件用 `\underset{下}{\stackrel{上}{\rightleftharpoons}}`)；图片必须 `![[hash.jpg]]` 无 alt；半截 math(`$^{-1}$`)P2；OCR「<+字母」→`<` 后空格。校勘注禁逐字保留修复前形态。
- 查重校勘：同题异版互证；教程一分册答案区在 `竞赛教程第一分册_200-328.md`(写 L2 前先读)；题号写前 `find` 实测空闲段；OCR 下标/价态必验算。
- 二分册**全书完成**：批48~67＝17讲+测试一/二(374 题，题-1~1195 无重号)。pack 枚举仅 章节练习/模块习题集/综合模拟卷/预赛专项(书末测试→综合模拟卷)。KP 写前验证：[[消除反应]](非消去)；批验输出防 head 截断。竖排侧基转写用 Unicode 下标(—NH₂)禁半截下标(P2)；答案过短须「答案＋判据」；答案区 OCR 缺失按图核验归属＋标整理者补。

## 用户决策与手法
- 断链存量不动；质量优先难度；新题默认 10-待审核；三模块=一库三视图；巡检周一 0800 异常才报；每批 L2 完立即追加索引。
- 合并孪生页：先判重复 vs 分层(分层带「深化」)；用引用量定方向；加 alias 不够须实际改指；多数疑似孪生实则分层勿合。
- 新建 KP：零散走 alias；成体系独立域可破例建 1 页。废弃唯一机制 `status:deprecated`+`deprecation_reason`+`superseded_by`。
- ①-b 基调：token 有无第二道题用到才成 KP；命名变体(中英文/空格)是 D 类孤儿主源，导入前去空格+大小写归一。

## 当前开放待办
- 薄壳治理剩余按分诊表 §八 优先 refs≥15（分诊表为 7 月版，动手前重跑 shell_triage.py 取实时清单）。
- 平衡题25 官方原书复核（存疑标注已在，需用户提供原书）。
- 2026-09-05 盘点新增（晚间更新）：①撞名实为 **2 组**（有机/化学原理；结构化学/元素与分析课件侧不存在，此前误判）→ 题库侧 2 真撞名卷 95 处 used_in 已消歧加固(短链+全路径双值，commit 4200273b)；mark_used.py 已修（撞名自动升级全路径+末段等价跳过），新工具 fill_used_in.py 承接小问级 token 卷；②37 决理-1 5 题已全部填充完毕(commit edd3056e，31 张 PDF 直提答案图＋四闸门全绿)；③台账 24 条清账后仅剩 7 条「待他版/纸质原书」真实遗留；④无机第5版已整夹废弃标注。
- ✅ 2026-09-05 题库三任务：①化竟能力测试样板批撞车清理＝题-001/002 废弃改指题-276/278＋题-276 温度序列 630→600(亲验题图)＋S₄N₄ 改判三方互链(题-277/汇智-17/赵鑫光-58)＋同源变题题-078↔题-276 互链；②①-c 根治＝新题入库SOP §2.2e 禁链名单(配位化合物→配合物/等电子体原理→等电子体/理想气体状态方程→理想气体/EAN规则→18电子规则；4 弃用页全有规范 superseded_by，映射既成)；③有机化学阶段测试卷＝二分册 L9~L15 组 25 题(3★7/4★12/5★6，5 道真题源，评测层第三卷与原理 I/II 无共题)。commits 2742c36a/368a291c/782e947c 已 push。
- ✅ 2026-09-05 37决理-1 五题补全：31 张答案图 PDF 直提入库(fitz 坐标＋渲染页目验，兜 MinerU 漏提的 Q9/Q12/Q13)；1-2 题干本完整(37决小问全局连续编号 4~8)仅修 OCR 重复；共振结构.md updated 重复键 P0 顺手修(FM 末尾追加字段前必须查重)。commit edd3056e 已 push。
- ✅ 2026-09-05 真题域尾区填实工程启动：体检发现 35~39 决 59 题尾区五节为模板占位(CRLF 漏报教训)；38 决 7 题＋39 决 10 题已填实(commits 4f6cf19a/a6c8f23d 已 push，格式=考点抽象/知识点映射/思路5条/变式3条/易错4条，KP 全经验证)；**剩余 37 决 16→36 决 13→35 决 13 题**，体检脚本 `.workbuddy/tmp/audit_zhenti_template.py` 可复跑。
- ✅ 2026-09-05 导入收敛完成：126 commits 已 push；共振合并落地；一分册 L13~15 已转录(题-824~875，OCR 源在 06-外部资料导入/OCR/竞赛教程第一分册OCR源)；账本 5369 题。
- ✅ 验收三件套已跑毕(2026-09-04 全绿)。

## 13-教案(人教版必修一，另一工作流)
- 交付 `13-教案/必修一/`：**四章全成**，共 45 篇（30 课时 5+10+7+8 + 3 份总览类 + 12 份章节文档）；2026-09-05 收官：Word 导出 45 docx 0 error、18 张 Mermaid 概念图（`概念图源/`→`概念图PNG/`+总览画廊）、网页版 4 HTML + PDF备用 4 份、遮挡检测 18/18 零命中。入口 `13-教案/README.md`、`必修一 教案总览.md`。
- 规范：核心素养统领目标(2.1+2.2 不用旧三维)、含逐字稿、三层作业、公式 Unicode 优先禁半截 math、库内锚点 kp_links+exercise_source 保留(明细见项目日志)。
- **概念图铁律**：一律存 `.mmd` 源可重渲染；渲染后必跑遮挡检测；改图后必须同步刷新 md 的 `![[hash.png]]`（哈希名）。
- **导出管线**：md → staging `.workbuddy/tmp/jiaoan_docx_src/`(refresh_diagrams.py 把 PNG 算 sha256 写媒体仓库+刷 staging 副本的 hash 引用) → build-all-handout-docx.py `--batch-root --output-dir` → finish_docx.py 压缩 PNG 后 `open(dst,'wb')` 覆盖写回。**PNG 像素未变则 hash 不变**，staging 副本不必再刷；改 mmd 重渲染后用 Pillow 像素比对决定是否沿用旧 hash。
- **C14005**：四份带内嵌图 docx 在腾讯文档预览器打不开——已证为预览器转码缺陷（文件本身合法，LO 可转 PDF），**不要反复重出 docx**，改走 HTML/PDF/PNG。
- **回写 P0 已修（22:2x）**：4 个 `第N章 知识体系与概念关联图.md` 的 ASCII 块 → 18 张 PNG 嵌入(2/4/7/5)，3 块保留 ASCII（跨章骨架说明）。脚本 `.workbuddy/tmp/backfill_diagrams.py`：按 ASCII 围栏位置索引消费 PLAN[None/PNG 名]，dry-run 确认块数后再写。git diff 共 +18/−337 行，零破坏。
- 遗留：`必修一 知识主线总图` 3 个 ASCII 块（评估：与 ch1-02/ch2-02·03·04/ch3-02·03·04·06 重叠，边际价值低）；`30 篇 × 35 个板书块 / 1893 行`=Part 1 文字稿 + Part 2 站位图（整块转 Mermaid 会丢 90% 信息，待用户确认是否抽 Part 2）。是否续做必修二待用户指示。
