# 2026-09-16 工作日志

## 课程计划修复与重新导出（00:17-00:45）

接 09-15 续13 核验会话。用户转入 Agent 模式要求修复+重导出+补充优化。

**核验补充（上轮盲区收口）**：
- 表8/表9 路径与 § 引用首次验存：12 个课件路径全存在；§5.3/6.3/7.3/8.3/9.4/10.4/§11/§四/4-8/第16·19·21课 全命中。
- 教材来源全量（154 条去重/204 次）：11 种书名 0 命中为 0；**章号级**验存——无机第五版（第4-8/12-24章）、无机第6版Weller（第5/11-18/19-25章）、中级无机（第1-11章）、分析化学第六版（第3/5-6/7-10章）全部与 07-资料提炼 提炼页逐章对上。第五版与第6版混用系**有意主辅配合**（非错误）。
- 上海中学 7 讲无提炼页（第二分册八~十讲、第四分册十~十三讲）确认，但所在行均有 2-6 条并列替代来源 → 无单一依赖，素材链安全。与记忆 §七.① 待办吻合。
- 章级讲义支撑：63 章对 136 份讲义，11 条"无落点"全是 ALIAS 假阴性（醇和酚→醇醚胺酚等），非真缺口。

**实质改动（用户拍板：每行知识点追加）**：
- 发现真遗漏：表8 未体现今日新增的 §11.4 考试对接标准 → 表8 12 行知识点列追加「本章考试地图」（复习课 6 行）/「真题实战与命题趋势专项」（习题课 6 行）。
- 只改第 3 单元格，行数 405、9 表 330 数据行、docx vMerge 138 全不变。快照备份在 .workbuddy/tmp/courseplan_qa/courseplan.md.bak。
- 正式重导 docx（64,729 B），渲染 26→28 页，p27/p28 目检 §11.4 内容正确渲染、表9 完整。
- 手册勘误 2 处落地（双闸门 Warning 口径 + 分隔行判定集合须含 '|'）。
- 双闸门：js-yaml 1/1；validate_kb 受检 1 0E/0W（新报告 2026-09-16-validation.md，未覆盖 09-15）。

**提交**：`b2cd1e04a`（4 files / +39-14：课程计划 md+docx、交接手册、validate_kb 报告）。共享日志与他会话文件未动，未 push。

## §三重构 + 表1假期补充（00:39-01:10）

用户反馈 §三「写得不好」，要求优化补充；表1 高二寒暑假补校内 10 次课（经确认仅高二寒/暑两行）。

**§三 重构（3.1/3.2/3.3）**：
- 3.1 课型执行标准：§11.4 要求（复习课「本章考试地图」四小节 / 习题课「真题实战与命题趋势专项」两块）上提为说明文字，**消除上轮 12 行逐字重复**（每行 88-100 字符 → 回退为各章特有内容）。
- 3.2 表8 改 5 列：章｜课型｜知识点｜**课时**｜依据与课件（课时可见，不必回表9 查）；「依据与课件」列每行独立（复习课=§+复习课件，习题课=习题课件）。
- 3.3 表9 首次带表头行（原表头被脚本丢弃）。

**顺手修复的 3 个自坑**：
1. 上轮 patch_t8.py 的 `strip()` 副作用丢了管道符两侧空格 → 已修。
2. patch 脚本 `'\n'.join` 把 CRLF 转成 LF（全文件假改动）→ 二进制替换恢复，独立 LF 残留 0。
3. §三 说明文字的 `[[wikilink]]`/`**加粗**` 字面泄进 Word（污染 1/18）→ 脚本新增 `clean_md_text()` 清洗，污染归零。

**构建脚本增强**（build_courseplan_docx.py，+101/-21）：
- 新增 `review` 表型（5 列带表头，仅章列合并）+ `labeled4`（4 列带表头不合并）；表头非 MODULE_HEADER 的 4 列表不再被误当模块表丢表头。
- 正文段落首次可进 Word（`title is None` 守卫隔离 frontmatter，普通文字/列表/引用渲染为 10pt 段落）。

**结构变化（预期内）**：gridCol 35→36、vMerge 138→132（表8 末列不再按章合并）、段落 11→21。md 数据行仍 330。渲染 27 页，p1/p25/p26/p27 目检全过。

**闸门坑**：validate_kb --changed 误传 .py → 1E（frontmatter 缺失）/1W（docstring 里 `[[ ]]` 被当断链）全为假阳性；仅 md 重跑 0E/0W 覆盖。

**提交**：`34f0baff1`（3 files / +137-39）。未 push。

## 任务盘点（01:10，只读）

- **证伪一条**：§11.4 的两块（复习课「本章考试地图」/习题课「真题实战与命题趋势专项」）在课件侧已 **6/6 + 6/6 全部落地**，非待办；§11.4 文末"仅第一轮落地"的说法已过时。
- **§11.3 中段课 4 节确认未落地**（第一轮化学原理、第三轮有机各加中段复习+中段习题），`04-课件/复习课|习题课/` 下无任何中段课文件。
- **未推送 49 笔**（含本会话 34f0baff1、b2cd1e04a）；push 须 `git -c credential.helper=manager push origin master`。

## 物化补强线残留 3 项收官（01:14-01:28）

用户指示"把可以做的内容做了"，执行已登记无阻塞的最高优先级项。提交 `30e843e0e`（6 files / +79-14）。

1. **P0-B 表面热力学深化**（此前仅在 §8.0 重定、**漏进 §7 清单**，本次补登记并销项）：
   - `胶体与表面物理化学-超级充实版` 新增 §3.4「比表面热力学与分散度」。
   - 关键关系：比表面焓 $H^s = G^s + TS^s$、$S^s = -(\partial\sigma/\partial T)_p > 0$ → **$H^s > G^s$**，浸润热法必须用 $H^s$ 不能用 $\sigma$。
   - 两道真题（第40届初赛第6题）：6.1 分散度 $N=(R/r)^3$、$\Delta A=4\pi(Nr^2-R^2)$、$\Delta G=\sigma\Delta A=0.2248$ J；6.4 浸润热法 $A_s=Q_p/(mH^s)=165.2/(5\times0.1164)\approx284$ m²·g⁻¹。
   - **两个高频失分点**：用 $\sigma$ 代 $H^s$ → 459；漏除质量 $m$ → 1420（官方评分：后者只扣结果分）。
   - **OCR 校勘**：原卷 L906「间湿热法」＝**浸润热法**。
2. **§8.7 Atkins 超纲标注证伪**：18B 扩散控制反应 3 处标注（L102/L187/L201）改写，补 Stokes-Einstein 与扩散控制速率常数进 §六核心公式；18D/18E 维持超纲（无真题支撑）。
3. **§7.3 回填**：勾选最后一项，**§7 现无未勾选项**。

附带更正：`六大新课大章节总表` §11.4 落地说明——实测复习课 6/6、习题课 6/6 全部含考试维度，原文「仅第一轮落地」已过时。

**复用教训**：补强计划里"§8.0 重定"的项**必须同步登记进 §7 清单**，否则长期无人跟进（本次 P0-B 即因此挂了 3 天）。

## 推送收官（07:40）

- 推送前核对：分支 master、暂存区空、**远端不领先（0）** → 快进推送，无需 pull/rebase。
- `git -c credential.helper=manager push origin master` → `bf30d17ec..30e843e0e master -> master`，**50 笔全部推送成功**。
- 校验：`git ls-remote origin master` = `30e843e0e` = 本地 HEAD ✅。

**顺手修掉 tracking-ref 坑**：push 后 `git rev-list --count origin/master..HEAD` 仍报 50。根因确认——`.git/refs/remotes/` 是**空目录**（沙箱吞掉了 origin 子目录），而 `.git/packed-refs` 里 `refs/remotes/origin/master` 仍停在 09-15 写入的旧 SHA `430353157a`。
- 修复：备份 `packed-refs` → 用 `git rev-parse HEAD` 取**全 40 位 SHA** 重写 packed-refs（短 SHA 会报 `unexpected line`）。
- 修复后：待推送 0、远端落后 0、`git status -sb` 从 `[gone]` 恢复为 `## master...origin/master`，与 ls-remote 一致。
- ⚠️ 本仓库此坑会**反复出现**（每次 fetch/push 后 loose ref 被吞），下次判同步状态**一律以 `git ls-remote origin master` 为准**；查到"未推送 N 笔"先怀疑 packed-refs 过期。

## 备课线续接计划产出（15:22-15:45）

用户要「汇总当前任务 + 计划下一步」。先做一轮**实地勘查（非转述）**，再产出计划文件 `00-首页/活跃任务/计划-2026-09-16-备课线续接.md`（新建）。

**8 项现状实测（含对任务卡口径的修正）**：
- 7 讲提炼页：原书 OCR **齐备** → 第二分册 `（已压缩）…第2分册_201-353.md` 第八讲 L52-1518／第九讲 L1519-2323／第十讲 L2324-4029；第四分册 `…第4分册_201-385.md` 第十讲 L1-1328／第十一讲 L1329-3277／第十二讲 L3278-4099／第十三讲 L4100-4809。⚠️ 两份 OCR 尾部有重复目录/摘要块（如第2分册 L4030 之后），勿当正文。
- ⚠️ **落点修正**（新发现）：待办里「第九讲 离子反应」落点写的是 `_归档/2026-06-23-离子反应`——**归档＝弃用区**，且**现役讲义无独立离子反应页**；「第十讲 滴定分析」落点同样写成 `_归档/`，应改指现役 3 份滴定讲义（容量分析/氧化还原滴定/络合滴定）。
- 5 个教学洞察短链 = **7 处引用**（与任务卡口径吻合），全部落在 **6 份章后课的 `referenced_insights` 字段**（注：字段名是 `referenced_insights`，**不是** `teaching_insights`；每份只列 1-2 个"大类"页，与该课覆盖的 4-9 个专题不成比例）。
- 傅献彩引用范围**比口径大**：实测 **7 处**（2 份现役讲义 source_book/参考书目 + 3 处 `03-知识点/` + 1 备课大纲 + 1 新授课），`07-资料提炼` 提炼页 **0 份**。
- 中段课 4 节：确认 `04-课件/复习课|习题课/` 下**无任何中段产物**（现役 6+6 全为整轮/章末层）。

**计划分层**：P0-1 补 7 讲提炼页（命名用中文数字「第二/第四分册」，v1.2 模板，90-140 行）；P0-2 修短链 → **建议方案 A：新建 5 个大类索引页**（符合"章后课列大类入口"原意，一次修 7 处且未来可复用；方案 B 改细分页反而范围更大）；P1 三项待拍板（中段课 4 节、傅献彩、习题书裸下标**属另一条线勿混做**）；P2 滚动（深化精品化/课堂可用性/图片复勘）。

闸门：`jsyaml_verify.js --list` 受检 1 / 通过 1 / 失败 0（`validate_kb` 对 `00-首页/` 受检 0＝空跑，故未跑，避免冲掉旧报告）。

## P0-1 + P0-2 执行完毕（16:05-16:30）

**P0-1 补 7 讲提炼页（7 份 / 1,394 行，全部新建）**
- 第二分册：`电化学基础`(186) / `离子反应`(243) / `滴定分析`(264) —— 取材 `…第2分册_201-353.md` L52-1518 / L1519-2323 / L2324-4029。
- 第四分册：`醛与酮`(187) / `羧酸及其衍生物`(185) / `杂环化合物`(164) / `糖、氨基酸`(165) —— 取材 `…第4分册_201-385.md` L1-1328 / L1329-3277 / L3278-4099 / L4100-4809。
- **命名坑**：第十一讲先写成「羧酸及其衍生物与碳负离子反应」（照章标题全称），后按待办登记名改回「羧酸及其衍生物」；第十三讲先写「糖与氨基酸」，后按计划文件既有命名改回「糖、氨基酸」。**教训：新建页命名一律先回查待办/计划里的登记名，别照章标题自由发挥**。
- **轮次口径**：第二分册 `[第一轮]`（滴定/离子反应/电化学加挂）、第四分册统一 `[第二轮]` —— 照**同族既有 9+7 份**取值，不照学科推断。
- 收尾同步 3 处已完成：README 映射总表（6 处登记）、待办 §五 改「✅ 已补齐 7/7」并校订 `_归档/` 落点、课程计划无需改。
- **口径达成**：第一分册 10/10、第二分册 **10/10**、第三分册 10/10、第四分册 **13/13**。

**P0-2 教学洞察大类索引 5 份（方案 A）**
- 6 份章后课 `referenced_insights` 引 5 个大类名 → 7 处断链。新建 `教学洞察-{元素化学,分析化学,结构化学,化学动力学,酸碱理论}.md`。
- **关键设计（可复用）**：新建页 `type: 教学洞察索引`，**刻意不用 `教学洞察`** —— 否则会污染 `12-教学洞察/README.md` 顶部 `$=` dataview 的细分页计数（53 份）。README 已同步加注两类口径 + 新增 §六。
- **未采纳方案 B 的判据**：字段是**混层写法**（`第一轮化学原理复习` 同时引细分的 `教学洞察-化学平衡` 与大类的 `教学洞察-酸碱理论`）→ 证明「大类入口」是原设计，不是写错链。**改字段＝把正确写法改坏**。

**闸门**
- `jsyaml_verify.js --list`：P0-1 批 9/9 通过；P0-2 批 6/6 通过。
- `validate_kb.py --changed`：受检 9 / 6，均 0E/0W/0I。
- `validate_kb.py --full`：**7,728 文件 / 0E / 202W / 8,855 Info** —— 与 09-15 基线（0E/202W）持平，无新增告警；报告内已搜不到 5 个大类名 → 7 处断链全消。
- 自写 KP 链接自检（`.workbuddy/tmp/verify_kp_links_0916.py`，**先 Write 成 .py 再跑**）：frontmatter wikilink 115 次 / 去重 74 目标 → 0 缺失 / 0 弃用。

**🔴 新踩坑（写进长期记忆）**：`03-知识点/` 内有 `status: deprecated` 页（本次撞上 `沉淀溶解平衡` → `superseded_by: [[溶度积]]`；另有 `糖化学`）。**批量写 KP 链接不能只验「文件存在」**，弃用页在 Obsidian 里照样能解析，但属治理禁区 → 必须**同时读 frontmatter 的 `status`**。本次已改为 `[[溶度积]]`。

**提交口径提醒**：`02-数据库/quality-metrics.jsonl` 本次 `--full` 追加 74 行（append-only，跨会话混入）；`09-审计报告/auto-validation/2026-09-16-validation.md` 被 `--full` 整体重写（内容 100% 属本次）。两者按仓库惯例（历史多次提交过）一并入账，但**须如实说明混入**。

**项目侧同步**：新建 `00-首页/工作日志/2026-09-16.md`（仅覆盖本会话备课线，已注明他会话自补）。

**⚠️ 计划偏差（如实记）**：计划 §3.6 设的「每份 90–140 行」**未达成**，实产 164–264 行（约同族 1.3–2.0 倍；同族基线：第二分册 98–135、第四分册 96–133）。原因是保留原书数据表/机理/真题清单而非压成泛化复述。已在计划文件 §3.6 补记偏差 + 裁剪方案（压至 ~130 行/份）。**教训：计划里写篇幅区间时，要么先量同族真实分布，要么写"按同族基线浮动"，别拍一个数字**。

**提交与推送**
- 3 笔：`6552e1ef6`（P0-1，10 文件 +1666/-15）→ `afc86c98c`（P0-2，6 文件 +348/-2）→ `99d3e60ae`（账本，4 文件 +503/-12）。
- 推送：`git -c credential.helper=manager push origin master` → `30e843e0e..99d3e60ae`，`git ls-remote` 复核一致。
- **又踩 packed-refs 坑**：push 后 `git status -sb` 仍报 `[ahead 3]`（packed-refs 停在上次 SHA，loose ref 被沙箱吞）→ 按既有流程备份 + 用 **全 40 位 SHA** 重写 packed-refs → 恢复 `## master...origin/master`。**此坑每次 push 后必现，例行检查**。
- 提交账本时如实说明：`02-数据库/quality-metrics.jsonl`（append-only 跨会话）与 `.workbuddy/memory/2026-09-16.md`（120 行，本会话仅 35 行）**均无法按行隔离**。
- **未动**：`.workbuddy/memory/2026-09-13/14/15.md`、`MEMORY.md`（他会话 M）、`竞赛教程第二分册测试/`（他会话未跟踪）。

**技能固化**：新建 `~/.workbuddy/skills/family-page-backfill-and-hub-index/SKILL.md` —— 「同族页补页 + 混层短链消解」完整流程（五纪律 + 验存脚本 + 8 条坑清单）。本次两类任务的共同模式，后续提炼线批次可直接复用。
