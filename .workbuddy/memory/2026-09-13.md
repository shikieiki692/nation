# 2026-09-13 工作日志

## 线 1 收口：9-07 讲义归档断链修复（00:10）

用户确认「已 push」（实测远端未变，见下），继续执行线 1 收口。

### 执行结果

**commit `451443ee9`** — 58 files / 152 ins / 152 del

| 项 | 值 |
|---|---|
| 文件 | 备课大纲 45 + 备课思路 13 = **58** |
| 改动 | **152 增 / 152 删，完全成对** |
| 纯度 | 全部变更行含「04-课件/学生讲义」关键词，零混入 |
| 内容 | 纯路径修复：`学生讲义/xxx` → `学生讲义/{化学原理\|元素与分析\|有机化学\|_综合}/xxx` |

### 执行前终检（全绿）
- 目标文件数 58 ✓
- 增删成对 152/152 ✓
- 变更行纯度 100% ✓
- 暂存区无越界 ✓

### 执行后断链复查（重要）
提取 58 文件的 **72 个 `[[04-课件/学生讲义/...]]` 目标**：
- ✅ 71 个物理存在
- ⚠️ 1 个「断链」→ **经复核为脚本误报**

**误报详情**：`[[过渡-化学键衔接]]`（Obsidian **短链**，无路径前缀）
- 全库同名 `.md` **仅 1 个**（`04-课件/学生讲义/_归档/过渡-化学键衔接.md`）
- Obsidian 短链会全库匹配 → **能正常解析，并未断链**
- 我的脚本按显式路径拼接校验 → 必然误报

**4 个新子目录确认存在**：`化学原理/` `元素与分析/` `有机化学/` `_综合/`
→ 结论：本次是**修复断链**，71/72 验证通过，零真实断链

### 🔑 新沉淀方法论：Obsidian 链接校验必须区分短链与路径链
- **短链** `[[文件名]]` → 全库匹配唯一同名文件，**不能做路径拼接校验**（会误报）
- **路径链** `[[路径/文件名]]` → 可按路径校验
- 本次踩坑：脚本对短链做路径拼接 → 误报 1 个断链

---

## 提交记录（本日 2 笔）

| commit | 内容 | 规模 |
|---|---|---|
| `451443ee9` | 归档(9-07遗留)：讲义按学科分目录后的断链修复 | 58 files, 152/152 |
| `e2938dcc2` | 审计报告更新：线1 已收口 + 断链复查结论 | 1 file, 49 ins |

---

## ⚠️ 推送状态：用户报「已 push」，但远端未变

| 项 | 值 |
|---|---|
| 远端 master | `09cfd8b80`（**未变**） |
| 本地 HEAD | `e2938dcc2` |
| 落后/领先 | 本地领先 **15 笔**，远端落后 0 |
| reflog | **无 push 记录** |
| 远端分支 | 仅 `master`，HEAD 指向它 |

**结论：推送未到达 `github.com/shikieiki692/nation.git`。**
可能原因：用户在另一个工作副本/终端推送、或推送失败但提示被忽略、或推到了其他仓库。
→ 需用户复核其推送命令的实际输出。

---

## 遗留三线状态

| 线 | 状态 |
|---|---|
| 线 1 9-07 讲义归档 | ✅ **已收口**（`451443ee9`） |
| 线 2 教案线（11 D + 7 M + 29 ??） | ⏸ 待拍板（内容守恒 100%，属教案线职责） |
| 线 3 教材迁移（7068 D） | ⏸ 待确认无断链后提交（根因已查明：迁入 gitignore 的 媒体仓库/） |

---

## 讲义规范治理 P0-2 第一批：统计热力学 15 题参考答案（14:40）

决策前提（用户已拍板）：一份一批 / 不加题保持 15 题 / 存量结构不动。

### 交付
`04-课件/学生讲义/化学原理/统计热力学与Maxwell关系-超级充实版（自学完整）.md`
- 新增 `## 参考答案与提示（15 题）`，插在 `## 三级练习` 与 `## 延伸阅读` 之间（179 行，+183/−1）
- 顺带把 `exercise_levels` 从作废说法 `"基础5 + 进阶5 + 挑战5"` 改为 §四.1 列表式的 基础巩固/竞赛入门/真题挑战 5+5+5
- 4 处 `p^\circ` → `p^\theta`（1 处原有遗留 + 3 处新增），对齐该文件 `last_audit` 自述的 `$E^\theta$ 规范，清掉 precheck 全部 4 条 warning
- docx 重出：`06-学生侧材料/讲义/化学原理/统计热力学与Maxwell关系-超级充实版（自学完整）.docx`，oMath 501，答案节已确认入档

### 数值答案（脚本真算，`.workbuddy/tmp/verify_statmech_answers.py`）
| 题 | 结果 |
|---|---|
| 4 | Λ = 1.60×10⁻¹¹ m（16.0 pm），q^T = 2.45×10²⁹（q^T/N = 2.45×10⁶ ≫ 1 经典极限成立） |
| 7 | ΔG°(350 K) = **+62.2 kJ/mol**（双路校验：G-H 积分式 与 ΔH−TΔS 完全一致） |
| 8 | Θ_R = 2.777 K，q^R = **108** |
| 9 | U−U(0) = **7.48 kJ** |
| 10 | 离域比定域 **小 447 J·K⁻¹·mol⁻¹**（= R(ln N_A − 1)，ln N_A = 54.75） |
| 11 | Ar S_m = **154.9 J·K⁻¹·mol⁻¹**；改 298.15 K/1 bar 重算 = 154.8，与文献 154.8 吻合 |
| 12 | CO 残余熵 = R ln2 = **5.76 J·K⁻¹·mol⁻¹** |
| 13 | 激发态占比 **2.34%**（βε = 4.83，q = 1.0240） |

### 🔑 方法论沉淀
1. **数值答案必须脚本真算 + 找外部锚点校验**：Ar 的 Sackur-Tetrode 结果拿「298.15 K/1 bar → 154.8」对撞文献值，一次就验出公式/常数/单位三者都对；没有外部锚点的题（7/8/9/13）至少做**双路互校**（积分式 vs 定义式、分布比例 vs 比值式）。
2. **写答案前先通读正文公式区**：口径必须与讲义内公式逐字一致（如讲义用 `Λ = h/(2πmkT)^{1/2}`、`q^R = kT/(σhcB̃)`），不能另起炉灶。
3. **讲义内 `^\circ` vs `^\theta` 不统一会以 precheck warning 形式暴露**；该文件 `last_audit` 已声明 `$E^\theta$ 规范 → 以文件自述为准，不要跟历史遗留写法。
4. 自检脚本 `.workbuddy/tmp/check_statmech.py`：行尾 / `$` 奇偶 / 半截 math 扫描 / 结构 / FM 五合一，改完必跑。注意半截 math 检测器的「前后字符」判据会因 `re.finditer` 非重叠扫描而误报完整公式，**短 span 列表须人眼过一遍再下结论**。

### 待办
- P0-2 第二批 `胶体与表面物理化学`（15 题）、第三批 `物化综合计算`（15+3 题）

---

## ✅ 推送成功（10:02 确认）

**远端 master = `e2938dcc2`，与本地完全同步（领先 0 / 落后 0）。**
15 笔全部上去了。

### 过程复盘：从「卡住」到成功

**第一次失败报错**（用户终端）：
```
Writing objects: 100% (583/583), 108.62 MiB | 66.00 KiB/s, done.
error: RPC failed; curl 55 Send failure: Connection was reset
send-pack: unexpected disconnect while reading sideband packet
fatal: the remote end hung up unexpectedly
Everything up-to-date
```

**根因：单个 commit 的 pack 体积过大 + 网络慢**
- 用脚本算出 15 笔的累计增量体积，精确定位：
  - 第 1~9 笔（到 `e001b231a`）：**仅 11.6 MB**
  - **第 10 笔 `1662db31c`：一个 commit 暴涨 106.2 MB**（105 个 docx 全量重写，每个 0.3~3.7 MB）
  - 第 11~15 笔：仅 8.3 MB
- docx 是 zip 二进制，git 无法增量压缩 → 必须整份重传

**关键教训：`Everything up-to-date` 是误导**
- 它出现在 push 失败之后，容易被误读为"已经是最新"
- 真相：本地 remote-tracking ref 未更新，远端实际没变
- **唯一可信判据：`git ls-remote origin master` 的输出**

**「卡住」是假象**
- 上传 106 MB 大包时 Git **不打印实时进度**（尤其慢速连接）
- 66 KiB/s 下 106 MB ≈ 27 分钟
- 判据：`tasklist | grep git-remote` 若有进程且内存上百 MB，说明**正在传**

### 🔑 新沉淀：大 pack 推送的分批方案
1. 先算体积：`git rev-list --objects <base>..<commit>` + `git cat-file --batch-check=%(objectsize)`
2. 按体积分批，用 `git push origin <commit>:master` 逐批快进
3. 大包批配 `git config http.postBuffer 524288000`
4. 仍失败 → 换 SSH（`git remote set-url origin git@github.com:...`），不受 HTTP 包体限制

### 为什么 AI 不能代推（用户追问，已答）
1. 沙箱无 GitHub 凭据（无 token 变量、无 gh CLI、凭据管理器读取被安全策略拦截）
2. 沙箱代理 `127.0.0.1:65433` 下大流量上传会被 reset
3. **这是安全设计**：公开仓库写权限应只属于用户本人，不应让 AI 持有长期凭据

---

## 会话全面总结与体检 + 线3 断链预检（10:07~10:20）

报告：`09-审计报告/2026-09-13-会话全面总结与体检.md`（`742c38ab1` + `4ddf6555e`）

### 全面复核结果
| 项 | 状态 |
|---|---|
| 推送 | ✅ 远端=本地=`e2938dcc2` |
| 15 笔提交 | ✅ 本会话 11 + 并行会话 4，全部无越界 |
| 工作区 | 7627 → **7165** 项（M 从 485 降至 37，**−92%**） |

### 🟡 新发现（非本会话责任）
`auto-validation/2026-09-12-validation.md` 在我提交后（23:44:46）被自动流水线重写，
新增 **13 个 warning**。经查：
- 全部是 `11-模板/` 下模板文件的**占位符**（`[[KP链接]]`、`[[哈希名.jpg]]`、
  `[[B8FBFE0C.JPG]]`、`[[KP名]]`、`[[链接]]`）
- 实证 `11-模板/` **未被本会话任何提交触碰**
- → 属 `validate_kb.py` 对模板类文件**未豁免**的误报
- 建议：给 validate_kb.py 加模板豁免规则（`11-模板/**` 跳过断链检查）

### ✅ 线 3 断链预检通过（关键）
- 取 7068 D 中 6978 个图片的哈希名
- 扫全库 **9781 个活 md**，找出仍被引用的 **37 个（38 处）**
- 逐一核对 **全部存在于 `媒体仓库/`（11004 文件）→ 37/37 = 100%**
- **零丢失、零断链 → 线3 可安全提交**

### 沉淀方法论 4 条
1. **大 pack 分批推送**：`git rev-list --objects` 算体积 → `git push <commit>:master` 逐批
   （本次 15 笔中第 10 笔单 commit 占 106 MB / 102 份 docx）
2. **`Everything up-to-date` 是误导** → 唯一判据 `git ls-remote origin master`
3. **Obsidian 短链 vs 路径链**：短链 `[[名]]` 不能做路径拼接校验（会误报）
4. **迁移判定三步法**：getmtime 抛异常 → git status 无 ?? 新增 → check-ignore + find 定位

### 本会话提交累计 13 笔
（`ca60a2ac7` → `4ddf6555e`）

---

## 线 3 教材迁移：提交后终态复查与后续计划（11:05）

用户要求「再仔细检查一遍，然后制定接下来的计划」。完成**第三轮（提交后）核查**。

### 同步状态澄清（一处虚警）
- `git branch -vv` 显示 `[origin/master: gone]`，一度疑似远端引用丢失
- **实测为本地跟踪引用缺失，非远端问题**：`git ls-remote origin master` = `91d22c77d` ✅ 完好
- 本地 HEAD = 远端 = `91d22c77d`，**逐字节一致，完全同步**
- `git fetch` 报 `[new branch]` 成功但 `refs/remotes` 仍空（受管环境限制），**不影响数据实质**
- **结论：判定同步只认 `git ls-remote` 哈希比对，不看 `refs/remotes` 是否存在**

### 迁移入库完整性（全对齐）
一化高中化学 2564 / 人教版 2889 / 结构化学基础 1555 / 无机化学下册 3 md = **7011** ✅

### ⚠️ 新发现：旧位置冗余残留 4 张 jpg
- `结构化学基础/`（旧位置，本应清空）仍留 2 子目录各 2 张 jpg
- **三重证据**：① 新位置 `06-外部资料导入/结构化学基础/` 已有同名且已追踪；② blob 哈希逐字节相同（`cd73a8b6`/`7aebde57`/`baa5a874`/`4a86e278`）；③ `git status` 静默不报
- **根因**：迁移提交 `81301476b` 旧侧删除清单基于当时工作区快照，这 4 张在快照中缺席（疑历史 revert `7ddb6ff38` 树恢复副作用）→ 漏网孤儿
- **风险**：零数据风险，纯冗余，可 `git rm`

### 剩余项归属重判（关键修正）
- **37 个 M 中仅 11 个属本线（≤13h）**，26 个 mtime 42~181h 全属他线/历史遗留
  - 此前凭印象记"7 个 M 属他线"**系低估**
- 23 个 D：12 个 `_index.md` 孤儿索引（全库同类 77）+ 11 个教案线旧名（重构中）
- 3392 个 `??`：无机化学下册 1509（设计不入库）/ Mathematics for Physical Chemistry 1738（他线）/ 教案 119（教案线）

### 产出
- 新建 `09-审计报告/2026-09-13-线3教材迁移提交后终态复查与后续计划.md`（commit `87d8af5ef`，仅 1 个 A，干净）
- skill `git-bulk-move-migration` 新增「七点五、提交后复查」章节 + 3 条高频错误
  - ① 同步判定认 `ls-remote` 不认 `refs/remotes`；② 旧位置残留用 `git ls-files` 枚举（status 对静默一致文件是瞎的）；③ M 归属用 mtime 分窗
- 临时脚本 `chk_final_state.py` 已清理

### 待用户拍板
1. 残留 4 jpg 是否清理（建议做，零风险）
2. `无机化学下册` 1509 jpg 是否入库（决定 `.gitignore` 还是 add）
3. `_index.md` 孤儿索引：删除 or 新位置重建

---

## 线 3 后续执行：P0/P1 落地（11:30）

用户「按照你的思路继续推进，有决策项再问我」。执行中发现**两处原有误判并推翻**。

### 执行成果（3 个 commit，全部已推送）
| commit | 内容 |
|---|---|
| `fbf292a47` | 清理旧位置 4 张冗余 jpg（4 D，0 ins/0 del） |
| `ddf8d7ab6` | **补入无机化学下册 1509 张 jpg**（0 ins/0 del，29.3MB） |
| `56e151628` | 审计报告更新 |

终态：本地 = 远端 = **`56e151628`** ✅；`??` 3392 → **1883（−44%）**

### ⚠️ 误判一：下册 1509 jpg「设计不入库」→ 实为遗漏
- **横向对照四库**：一化 2558 / 人教版 2873 / 结构化学基础 1551 jpg **全入库**，
  唯独无机化学下册 **0**（jpg 物理 1509）
- **关键**：四库 md **全部用本地相对路径**引用图片（`![](xxx_images/hash.jpg)`），
  无一是媒体仓库哈希 → md 入库而 jpg 不入库 = **249 处引用永久断链**（磁盘校验 249 引用 0 缺失）
- **处置**：补入库（用户拍板「补入库（推荐）」）
- **教训**：这不是"设计"，是遗漏；判"设计 vs 遗漏"必须**横向对照同类样本**

### ⚠️ 误判二：`_index.md` 12 D「孤儿索引」→ 实为改名中间态
- 核同目录：**12/12 已有 `README.md`**（内容同质，均为"图片索引"），mtime **8月5~15日**
- 性质：全库性 `_index.md`→`README.md` 改名重构，**做了一半且从未提交**，属他线/历史遗留
- 全库还有 **77 个** `_index.md` 未重构（mineru 53 + 06-外部资料导入 23 + 99-归档 1）
- **处置**：从待治理清单**撤销**，本线不碰

### 方法论固化（skill `git-bulk-move-migration` 新增两节）
- 「**判"遗漏"还是"设计"：必须做同类横向对照**」——含两个实战案例
- 「**引用完整性：入库决策必须查 md 的引用方式**」——`grep '!\[[^]]*\]\([^)]*\)'`
- 高频错误清单 +3 条

### 最终状态：剩余 D(23)/M(37) **无一项属本线**
全部查明归属：教案线 11 D + 119 ??、他线 1738 ??、改名中间态 12 D、历史遗留 26 M

---

## 线 3 剩余项可行性评估与执行（11:50）

用户问「剩余项你可以做吗，仔细评估一下，有什么好的想法」。深挖后**发现 2 项实际可做**。

### 执行成果（3 个 commit，已推送）
| commit | 内容 |
|---|---|
| `c5387feac` | 补入 Mathematics for PC **1738 张 jpg**（0 ins/0 del，16.1MB） |
| `8ac33f353` | 讲义规范体系组 **11 文件**（501 ins / 84 del） |
| `ea2db8dfa` + `e767d55ee` | 评估报告 + 更新 |

**终态**：本地 = 远端 = **`e767d55ee`** ✅
**`??` 1883 → 145（−92%）**；`M` 37 → 26；`D` 23 不变（均属他线）

### ⭐ 核心发现：Mathematics for PC 与下册**同源病**
- 2 md 已入库、**1738 jpg 未入库**；引用语法是 **wikilink**（`![[hash.jpg]]`）
- **初始误判**：以为 wikilink = 图片在媒体仓库，可另存
- **实测推翻**：媒体仓库顶层 **0/105 命中**；**全库搜索 105/105 全部只在本地 `_images/`**
  → Obsidian wikilink 会**全库解析文件名**，故本地能显示，但 jpg 不入库 = 克隆后 **105 处断链**
- **结论**：与下册是同一个病（md 入/jpg 不入），**只是引用语法不同**（相对路径 vs wikilink）
- 1633/1738（94%）无引用，用户选**全量入库**（保持图集完整、口径一致）

### 第二项：讲义规范体系组 11 M（上一会话产物）
- 内容：`11-模板` 7 个（权威归属重构）+ `00-首页/讲义生产SOP`（字段口径唯一权威）
  + `02-数据库/quality-metrics.jsonl`（+145 行）+ 统计热力学讲义 md+docx（+186 行）
- **质量高**：README 重构权威归属表，明确各规范"唯一权威"，修正失效路径
- 用户确认提交（**不动历史重复**）

### 遗留议题（记录待议）
- `02-数据库/quality-metrics.jsonl` 有 **131 组完全重复行**（06-24 起累积，最多 x6），
  本次新增 145 行内 09-05 亦有重复。**追加式设计疑似脚本重复触发**，治理前需确认统计口径。

### 确认不可碰的三类
- `_index.md` 12 D + 12 ??：他线全库性改名重构（`_index.md`→`README.md`）做了一半
- `13-教案/必修一` 11 D + 119 ??：**深度重构中**（旧名删+新名增+合订版拆分）
- 历史遗留 26 M：mtime 42~181h，远超本会话窗

### 踩坑：Windows 写清单文件的换行污染
- 用 Python 写待 add 清单再 `while read` 喂 git add → **路径尾部带 `?`（\r）**，
  报 `pathspec '...md?' did not match`
- 解法：**直接 Python 调 `subprocess.run(["git","add","--"]+paths)`**，绕开 shell 层
- （呼应既有记忆：Windows 文本文件必须 `newline=""`）
