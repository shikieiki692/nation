# 2026-09-15 工作日志

## 习题书·排版线 — P0 批次（任务卡-2026-09-14-习题书裸下标收口与git健壮性复核）

### P0-1 git 对象库可信判据复核 ✅ 通过
- 新脚本 `11-模板/scripts/qa/verify_git_objects.py`（判据 = `git cat-file -e`，promisor 未取消时直接 ABORT）。
- 全覆盖 **157,063** 个可达对象 `cat-file --batch-check` → **missing = 0**（commit 533 / tree 11,683 / blob 144,847）。
- 分层抽样 **800**（commit 200 / tree 200 / blob 400，seed=20260915）逐对象 `cat-file -e` → **不可读 = 0**。
- `audit_packs.py`：108 pack，畸形/异常候选 **0**（微型 pack 84 个 7.59MB，健康）。
- 报告：`09-审计报告/git对象库可信判据复核-2026-09-15.md`。
- 注：卡上记 157,043 → 实测 157,063（HEAD 由 `f936daa68` 推进到 `430353157` 带来的正常增长）。

### P0-2 复用脚本固化 ✅ 13/13 SAME
- 12 个脚本从 `.workbuddy/tmp/` 迁入 `11-模板/scripts/qa/`（sha256 逐文件与 tmp 版一致）+ `qa/README.md`。
- 在 tmp 与 qa 两处各跑一次，**逐字节比对 stdout / 产物全部 SAME**（含 `gen_acd_list` 重生成的清单与 git HEAD 完全一致）。
- 更正卡上「63 token」→ 实测唯一 token **65**（A14 + B6 + C30 + D15）。

### P0-3 交接入口登记 ✅
- `00-首页/活跃任务/待办交接清单.md`：挂本卡 + 4 份报告 + 新增复核报告；纠正过时数字 **31 章/1,283 题/33×3 → 32 章/4,004 题/34×3**。
- js-yaml 闸门 2/2 通过。

### P0-4 `sync_readme_stats` 硬编码修复 ✅
- `11-模板/scripts/build_module_book.py`：章节数 `31 章` → `\d+ 章`（动态）；「未匹配」由 ⚠️ 打印提升为末尾汇总 + `RuntimeError`；改为「仅值变化才回写」（幂等）。
- 预演确认只改 README L12/L25/L26 三行、0 未匹配。
- 同类别处：`aggregate_precheck_warnings.py:61` 的「教师版 31 章」是 2026-08-30 一次性报告的标题文案，历史值，未改。

### ⚠️ 偏差：40届初赛源漂移（最重要的一条）
- 重跑 `build_module_book.py --strict --write --clean` 实测得 **4,007 题**（非卡上 4,004）：
  `04-题库` 工作区有**并行会话正在导入的第40届初赛 5 题 + 索引**（全为未跟踪 `??`），
  重建把它们吸入 → 习题书 md 被改写 **38 份**（越界）。
- 处置：`git checkout -- 04-课件/习题集` **回滚全部 38 份**；改为按**已提交 md 规模（4,004）**
  回写 README（`sync_readme_stats` 同组正则，0 未匹配，diff 恰 3 行）。
- **教训（写进流程）**：改习题书前**必须先** `git status --porcelain -uall -- 04-题库`。
  本次漏做，随即发现并回滚，**未污染任何提交**。
- 待办：题目导入会话提交第40届初赛后，习题书需重建 → 数字应变为 **32 章 / 4,007 题**。
- 副作用：`git checkout --` 把那 38 份 md 的物理行尾由 CRLF 归一为 LF（git 视角 clean，
  `.gitattributes` 本就声明 `eol=lf`，下次构建会写回 CRLF，无 git 影响）。

### 推送与 .gitignore（用户拍板后）
- 7 个 commit 已推：`430353157..0a0cf1706 master -> master`，推后 `HEAD = origin = 0a0cf1706`
  （a28f4a47c P0-1 / 8778817fb P0-2 / 53b8c92a4 P0-3 / ced9aefe9 P0-4 / e38382113 记忆 / c77b9d267 任务卡 / 0a0cf1706 gitignore）。
  推送用 `GIT_CONFIG_NOSYSTEM=1 GIT_TERMINAL_PROMPT=0 git -c credential.helper= -c credential.helper=manager push`，全程 <1 min。
- `.gitignore` 放行 `11-模板/scripts/qa/`：因 git 规则「父目录被排除时 ! 无法反向包含」，
  写法须为 `!11-模板/scripts/` → `11-模板/scripts/*` → `!11-模板/scripts/qa/` → `!11-模板/scripts/qa/**`。
  实测：qa/ 放行、其它 scripts/ 仍忽略、`.workbuddy/scripts/` 与 `pptx-workspace/` 不受影响、`??` 无新增噪声。

### P1 侦察（两项均未落盘）
- **P1-1 硬阻塞**：题源在红区 `04-题库`；且**重建被 40届初赛漂移挡住** —— 现在重建会把对方未提交题目吸进习题书。
- **P1-5**：讲义 82 处 = 我方可动 **60** + 他线 `化学原理/` **22**；教案 12 处 = 真 **5**（`M_A/M_B` 等）
  + **假阳性 7**（`has_images` ×4 / `image_count` ×3 是**行内 YAML 字段名**，非下标）。
- **⚠️ 扫描器盲区（新发现）**：`scan_bare5.py` 的正则基底要求 `[A-Za-z]` → **希腊字母基底漏检**
  （实证：教案 L227 的 `ρ_A/ρ_B` 未被命中，只有 `M_A/M_B` 被命中）。`130 处` 是**低估**。

## P1 执行（用户拍板：先改题源 + 先扩扫描器）

### 扩扫描器（修希腊字母盲区）
- `qa/scan_bare5.py` + `qa/gen_acd_list.py`：基底由 `[A-Za-z]` 放宽到含 `\u0370-\u03FF`
  (`\u1F00-\u1FFF`)。**规模显著上升**：
  习题书 130→**179**（A21/B47/C82/D29）；讲义 82→**119**（化学原理 22→50）；教案 12→**16**。
- 新增命中抽验全为真下标：`δ_H`×29、`δ_C`×3、`μ_B`、`σ_p/σ_m/σ_d`、`ν_max`、`ε_max`、`λ_max`、`ΔH_vap`。
- 新清单：`09-审计报告/习题书裸下标-ACD待批清单-2026-09-15.md`（09-14 版留档）。

### 修复器与落盘（组1/组2）
- 新增 `qa/fix_bare5.py`：复用 scan_bare5 遮蔽逻辑、只改可见区；**三重断言**
  （行数不变 / `$` 奇偶不变 / 无新增 `$$` / 控制字符 0）；`--apply` 才落盘；`--dump-skipped` 出组3 明细。
- **落盘 412 处（5 目标）**：题源 56 文件/164 处；师版 21/144；生版 10/33；讲义（**排除他线 化学原理/**）14/62；教案 1/9。
- 重扫验收：师版 179→**35**；讲义 119→57（余 50 属他线，可动仅 6）；教案 16→**7**（**恰为 has_images/image_count 假阳性**）。
- 组4：`题-034-2-1` 的 `title:` 改 `$\mathrm{SO_4^{2-}}$`（**文件名不动**）。
- 组3 清单：`09-审计报告/习题书裸下标-组3待批清单-2026-09-15.md`（22 处可直接落盘 / 6 类待拍板 / 7 假阳性 / 他线 50）。

### 关键教训（务必记住）
1. **`scan_bare5` 的 `X_a^b` 组合（如 `c_O₂^½`）只匹配到 `X_a`，`^b` 会漏** → 已在修复器中跳闸记入组3。
2. **相邻 `$…$$…$` 会被 pandoc 当 display math** → MAP 值不得让 `$` 相邻（`ρ_A/` 的斜杠放 math 外）；修复器加「无新增 `$$`」断言。
3. **`git add -u <dir>` 只暂存已跟踪的 M 文件**，是红区「只改既有文件、不带入他人 `??`」的安全加法。
4. 终端 `cut -cN` 截断超长 UTF-8 行会显示成 GBK 乱码 —— **文件本身没问题**，用 Read 复核即可。

### 待办
- 组3 6 类待用户拍板（kc_NO / S_N2X 疑讹字 / ρ_K·ρ_k / σ_h / 表头整行 / 图注整段）。
- 他线 化学原理/ 50 处需与讲义会话协调。
- 习题书 md 重建**推迟**至 40届初赛提交后。

## P1 续（组3 一节 + 第三维复检）

### 组3 一节落盘（用户点头，`fa0f1dd4f`）
- 给 `fix_bare5.py` 加 **X_a^b 组合支持**：PAT 只吃到 `X_a`，现把紧随的 `_…/^…` 一并吞入再查 MAP，
  表里没有才留给组3。**这是通用能力，去掉了一整类「组合式」盲区**。
- MAP +19 条：`c_O−M`/`c_NO−M`/`c_NO·`/`c_O₂^½`/`K^½`/`K^½)`/`c_KOH/mol·dm⁻³`/`p_Cl₂/Torr`/
  `Ad_E`/`N^N`/`ρN_A`/`xUCl_a`/`e^(−λt)` 系 + 讲义图注 `M^n+`/`ML^n+`/`M^(n-1)+`/`ML^(n-1)+`/`e^-`。
- 落盘 17 文件；重扫：师版 **35→18**；讲义 119→**51**（可动余 1）；教案 16→**7**（=7 假阳性）；幂等 0。

### ⚠️ 第三维复检（docx 侧）—— **口径再次不同**（重要）
- 用 `scan_docx_bare_script.py` 扫 102 份 docx：**33 份有残留**；剔除**图片路径假阳性**
  （`ABOC202505_200-397_image…` / `…_images/xxxx` 等，docx XML 里非可见文本）后 → **真残量 40 处行**。
- **md 侧仍看不到的三类**：
  1. **代码围栏内 ASCII 表**（`2-化学平衡.md` L926 `0 0.4 1.0 x_B`）→ 新增 `qa/scan_fence_bare.py`
     扫出师版 **3 处**（`x_B`×2 / `w_Sn`×1）。修法在「等宽段治理」范畴（去围栏 → 真表格）。
  2. **基底以 `)` 结尾**：`c(C₂H₆)^(1/2)` —— 注意括号内是 **Unicode 下标**，
     故 BASE 扩展须写成 `\([...SUB_UNI SUP_UNI]{1,10}\)`（我第一版只写 ASCII → 0 新增）。
  3. **BODY 字符集不含希腊字母**：`e^(−λt)` 的 token 只吃到 `e^(−`；`ε_415^Ti`、`f≈1−e^(−Δχ²/4)` 同理。
- **教训（第 3 次）**：`scan_bare5`（md 可见区）/ `scan_fence_bare`（围栏内）/ `scan_docx_bare_script`
  （Word 实文本）**三者口径都不同**，任何一维的「归零」都不等于交付物干净。**必须三维交叉**。

### 待办
- 组3 二节 6 类仍待拍板（`kc_NO` / `S_N2X` 疑讹字 / `ρ_K·ρ_k` / `σ_h` / 表头整行 / 图注整段）。
- 扩 BASE（括号内含 Unicode 下标）+ BODY（含希腊字母）后重扫 → 会再多一批。
- 围栏内 ASCII 表（师版 3 处）→ 归入等宽段治理另办。
- 他线 `化学原理/` 50 处；习题书 md 从题源整体重建仍推迟至 40届初赛提交后。

### 备注
- 本文件为共享记忆，**只追加**；不提交整个文件（含他人未提交内容）。

---

## 备课思路 · 未央竞赛课程计划（初二至高二）编制

- 输入：`C:\Users\蕾赛\Downloads\初三未央化学竞赛课程计划..docx`（**格式样板**）+ grill-me 三轮决策。
- 产出：`备课思路/未央化学竞赛课程计划（初二至高二）.md` + 同名 `.docx`（6 表全渲染，50KB）。
- **逐条核对样板的「教材来源」列**：普化第四版第 2–14 章、结构化学基础第五版第 1–10 章、上海中学竞赛课程第1分册十讲 + 第2分册十讲 —— **全部命中**知识库实有资产。
- **样板错误（已校正）**：样板引「无机化学下册第13章卤素/第15章氧族元素/第17章碳硅硼」，与本库**宋天佑版**冲突（宋版：13 硼族 / 14 碳族 / 15 氮族 / 16 氧族 / 17 卤素 / 18 氢和稀有气体 / 19 铜锌 / 20 钛钒 / 21 铬锰 / 22 铁系铂系）→ 全部按本库版本重写。
- **样板与真实进度错位（关键判断）**：样板把「化学基础知识+结构化学」放初三上；但 `暑期上课计划-初二竞赛班（22天）` 的别名正是「**初三竞赛衔接班**」，该班到初二暑假已走完 R1（结构化学初步 + 化学原理主干）→ 本计划重定义为：初三 = **R2**（元素+分析）、高一 = **R3**（有机+结构物化深化）、高二 = **R4**（真题/一轮复习/外出学习）。
- 学段轴：初二（1 段，合并）+ 初三/高一/高二各 4 段 = **13 段**；每学期 36 次 / 每假期 12 次。
- 模块：**保留样板 6 模块**（化学基础压缩 / 结构 / 物化 / 分析 / 无机 / 有机），每章加「轮次归属 + 📝🌟🔒 深度 + KB 覆盖状态」。

### 新增可复用技法（导出「非讲义」的 md → docx）
- `build-all-handout-docx.py --path` **只认 `04-课件/学生讲义/` 下的文件**；传 `备课思路/` 的文件会 `ValueError: ... is not in the subpath of ...`。改用底层管线。
- 正确链路：`pandoc <md> -o <docx> --reference-doc=11-模板/scripts/templates/custom-reference.docx` → `docx_utils.postprocess_pandoc_docx(OUT, center_title=True, add_page_numbers=True)`。
- 前置必须：① **剥 frontmatter**（否则 YAML 被当正文渲染）；② 清 `[[路径|别名]]` → `别名`（否则 Word 里留下 `[[ ]]` 语法）。
- **坑（新）**：**引用块（`>`）内的 Markdown 表格 pandoc 不渲染成表**，会被压成一行含 `|:---|` 的文本 → 表格必须移出 blockquote。
- 质检口径：`<w:tbl>` 数 + 每表 `<w:tc>` 数须与源表行列吻合；再查「残留 `[[` = 0」「未渲染 `|---` 行 = 0」。
- 脚本：`.workbuddy/tmp/export_courseplan_docx.py`。

### jsyaml_verify.js 用法坑
- 它的 `--list` 参数是**「文件清单的路径」**，不是文件本身；且清单内相对路径按 **cwd** 解析。
- 清单写到 `/tmp/` 会失败：bash 的 `/tmp` 被 node 解析成 `C:\tmp` → `ENOENT`。**清单文件必须落在仓库工作目录内**。

### 待办
- 本计划为「已完成规划层」，未提交 git；如需入库请单独 add（不与他线混提）。
- 执行层缺口（计划 §三 已登记）：`芳香反应` 独立讲义、`逆合成/路线设计` 主讲义、分析化学导论拆章、章后复习课/习题课层。

---

## 续：把 §二 从「大纲列表」改成「与样板同型的 4 列网格表」

用户要求「做成和原文件类似的表格类型，仔细一点」。

**样板的真实结构（重新逐格提取确认）**：不是大纲，而是 **7 张表**——表1 = 总体规划 3 列（时间/预计课时/课程模块，13 行）；表2–7 = **每模块一整张 4 列表：`章 | 节 | 知识点 | 教材来源`**，其中第 1、4 列用 `w:vMerge` 纵向合并（章 与 教材来源 各自成组）。

**改造口径**
- 章（`#### x.y`）→ 第 1 列；节（`- x.y.z 标题`）→ 第 2 列；知识点（节后 2 空格子项 / 节标题「：」后内容）→ 第 3 列，以「；」连接；教材来源 + KB → 第 4 列（样板无此列，按要求把「轮次 ｜ 深度 ｜ 覆盖」并入）。
- 合并组判定规则（自描述，无需额外标记）：**第 1 列同文本连续行成组；第 4 列「非空即起新组、空则续前组」**。这天然支持两种口径——模块 2/3/4 是逐章标注（4/4/2 组），模块 1/5/6 是模块级标注（整表 1 组）。
- 结果：6 张表 / **218 数据行** / 章合并组 40 / 来源合并组 13，与样板体量（222 行）相当。

**顺手修掉的真缺陷**
1. 源文件「/」分隔符写法**不统一**（有的 ` / `、有的紧贴），盲切会把 `sp/sp²/sp³`、`O₂/F₂`、`旋转轴/镜面` 切坏 → 改成**括号感知切分**（深度 >0 时不切）。
2. `6.7.8 芳香性判据（Hückel）/ 芳香性与反芳香性` 是唯一一个「节标题内含斜杠列表」，需拆成两行。
3. `5.11` 的两个子项没有编号（不是 `x.y.z` 形式），需单独识别（`（…）` 内内容当知识点；`/` 项改写成「：」形式）。
4. §0.3 表格里代码块内的 **ASCII 竖线** `上|下` → 换为 `上·下`（防表格断列）。

**pandoc → docx 的硬限制（本轮实测结论）**
- pandoc **完全不支持单元格合并**（markdown 无 rowspan/vMerge 语义）→ 必须 python-docx 后处理。
- 单元格内换行：`<br>` 被**静默吞掉**（前后文本拼成一行）；`` `<w:br/>`{=openxml} `` **可用，但需** `--from gfm+…+raw_attribute`。最终选择**单行 + `｜` 分隔**，保住 md 在 Obsidian 里的可读性。
- pandoc 生成的表格 **`tblPr` 里没有 `tblBorders`**（默认无框线）→ 必须自己注入。
- pandoc 的列宽来自表头分隔行破折号比例，但实测**常被写成等宽**（2640/2640/…）→ 直接改写 `w:tblGrid/w:gridCol` 最稳。

**导出脚本新增的后处理（`.workbuddy/tmp/export_courseplan_docx.py`）**
1. `tblBorders`（single/4/7F7F7F）+ `tblLayout fixed` + `tblW`；**元素顺序必须按 schema**（插在 `tblLook` 之前）。
2. 按表头签名分配列宽（7 个 profile）；模块表 = `1500/1850/2350/3712`（可用宽 9412）。
3. 表头行加粗居中 + 浅灰底纹 + `tblHeader`（**长表跨页自动重复表头**）；全行 `cantSplit`。
4. 第 1 列纵向合并（居中）、第 4 列纵向合并（**顶端对齐**，跨页时文字落在首页部分，居中会显得漂浮）。
5. 合并前先把续行的第 1 列文本清空（否则 `merge` 会把章名重复 N 次），合并后 `prune` 掉空段落。
6. 成品自检：**docx 正文里残留 `**` / wikilink / `\命令` 必须为 0**。

**新踩的坑**
- **CommonMark 右侧界定符规则**：`**…**` 的闭合 `**` 若**紧跟在全角括号 `）` 之后、且后面不是空白/标点**，则不算右界 → 字面输出 `**`。修法：改写为「」引号，或在闭合 `**` 后加空格。
- `docx_utils.postprocess_pandoc_docx` 会打印「目标被占用无法改名，已原地覆盖内容」——预览面板持着旧句柄，**内容仍是新的**，不必重跑。
- 目检链路（可用）：`soffice --headless --convert-to pdf` → MiKTeX 的 `pdftoppm -png -r 95` → 直接看图。**渲染目检能抓到纯 XML 检查抓不到的问题（如列宽过窄导致 4 行折行、`**` 字面泄漏）**。



---

## 续二：按样板 1:1 复刻版式（去掉所有「无关内容」）+ 模块内容按知识库校准

用户要求：「格式和原文件一样，不要出现其他无关内容，具体模块也结合知识库内容再调整一下」。

### 决策（grill-me 第 4 轮，2 问）
- **教材来源列 = 全部换成知识库实有**。样板引的《基础有机化学》（邢其毅第3~19章）与《分析化学》上册，**知识库里根本没有**；库内实有且已提炼的是：上中竞赛课程第1–4分册、普通化学原理第四版（第2/4–15章）、结构化学基础周公度五版（第1–10章）、**无机化学第五版（第12–24章，与库内宋天佑OCR同构）**、无机化学第6版（第5/11–25章）、分析化学第六版（第3/5–10章）、Clayden（第1–42章）、ABOC（第1–12章）、初赛讲义（第1–13讲）、人教版全套。
- **模块内容 = 补缺不重排**：保留样板章骨架与顺序，只补知识库明确要求而样板漏掉的 5 处。

### 样板版式（逐项提取，本次全部对齐）
| 项 | 值 |
|---|---|
| 页面 | A4 11906×16838；页边距 上下1440 / 左右1800；docGrid lines linePitch=312 |
| 字体 | 西文 Times New Roman，中文 宋体（**全程无加粗**） |
| 字号 | 标题 14pt 居中；章节标题与表格正文 **一律 10pt** |
| 段落 | 段后 0、行距 240（单倍） |
| 表格 | 框线 single sz=4 color=auto；单元格 vAlign=center + 段落 jc=center |
| 表1 | 3 列 1560/1275/2835，tblW auto，**有表头行** |
| 模块表 | 4 列，tblW=8391 dxa + tblLayout fixed，**无表头行**，第1/4列按「章」vMerge |
| 顶层结构 | 只有「一、总体课程规划」+「二、各模块内容规划」；无任何说明段 |

### 本次改动
- **删掉**（用户所指「无关内容」）：原 §〇编制依据与基线、§三知识库资产对接总表、§四执行说明、「阅读约定」引用块、各模块「定位」段、「模块级 KB」行、第4列里的轮次/📝🌟🔒/大纲✅⟳✗ 标记。
- 第 4 列回归**纯教材来源**（一行一条）。
- **表1 改回 3 列**，13 段（初二学年合并为 1 段 + 12 段）。
- 表1 学时：初二 46 次（= `班级授课计划-初二竞赛` 24 + `暑期上课计划` 22）。

### 补进去的 5 处缺口（共新增 10 个章）
1. 模块2：`有机分子结构初探`（库→第一轮结构化学初步 小章节6）
2. 模块3：`相平衡与胶体`（库→上中二分册第二讲 / Zchem 物化（下）4 / Atkins 主题04-05）
3. 模块4：`重量分析与分光光度法`（库→分析化学第六版 第7-10章 / Zchem 分析化学 L4）— 样板原本**整块缺失**
4. 模块5：`镧系与锕系元素`（库→无机化学第五版第23章 / 无机化学第6版第23章）
5. 模块6：`重排反应`、`周环反应`、`自由基反应`、`有机合成与金属有机`、`杂原子与生物高分子`、`有机波谱分析`（+ 另加 `有机结构基础与电子效应`、`活性中间体与反应机理基础`）共 8 章，全部对应库内第三轮有机 12 专题。

### 关键事实修正（靠库内实有版本证伪样板）
- **无机化学下册章号全错位**：样板「13章卤素/15章氧族/16章氮磷砷/17章碳硅硼/19章s区/23章ds区」；库内实有（=无机化学第五版=宋天佑）是 **12碱金属碱土金属 / 13硼族 / 14碳族 / 15氮族 / 16氧族 / 17卤素 / 18氢和稀有气体 / 19铜副族锌副族 / 20钛副族钒副族 / 21铬副族锰副族 / 22铁系铂系 / 23镧系锕系 / 24新兴领域**。已逐条按库内重排。
- **人教版初中「第十一单元」**：样板写「盐 化肥」（旧版），库内 2024 版是「**化学与社会**」。
- 核化学在库内归 **无机化学第五版第23章**（该章定位原文即「f区元素 + 核化学」），据此补进「原子结构」章的来源串。
- 上中竞赛课程讲次全部核准：第一分册10讲、第二分册10讲（第九讲=离子反应）、第三分册10讲（元素）、第四分册13讲（有机）。

### 新版式导出（弃用 pandoc，改**直接用 python-docx 从 md 构建**）
`.workbuddy/tmp/build_courseplan_docx.py`。为什么弃 pandoc：本次要 1:1 复刻外部 docx 的**字号/字体/居中/无表头/无粗体**，pandoc 会带上自己的样式层和 Heading 样式，后处理反而更绕。
- `vMerge` **手写 XML**（不用 `cell.merge()`）：组首行 `w:vMerge w:val="restart"`，续行空 `w:vMerge`；单行组不加 vMerge（与样板一致）。这样避开 `merge()` 追加段落的坑。
- 插入顺序：`w:vMerge` 必须插在 `w:tcW`/`w:gridSpan` 之后、`w:vAlign` 之前。
- `w:tblGrid` 必须紧跟在 `w:tblPr` 之后（`tblPr.addnext(grid)`）。
- md 侧：模块表保留一行 `| 章 | 节 | 知识点 | 教材来源 |` 表头（保证 md 语法合法 + Obsidian 可读），**构建时丢弃**；表1 保留表头。
- md 单元格内多条目用 `｜` 分隔，构建时**拆成多个段落**（复刻样板「一格多段」）。
- **章列宽度**：样板 737 缇（≈1.3cm）会把「氧化还原反应」折成 4 行。先试 1100 仍折 2 行，**最终 1450/1750/2550/2641**（总宽仍 8391）才做到「6 字章名一行放下」。

### 验收
- js-yaml 4 闸门：**受检 1 / 通过 1 / 失败 0**
- docx：7 张表 / 框线 7 / vMerge 起点 102 / 全部 rFonts=宋体+TNR / 10pt×1296 + 14pt×1 / 残留 `[[`=0 `**`=0
- 渲染目检 19 页（soffice→pdf→pdftoppm），确认标题居中、表1 三列、模块表无表头、章/来源纵向合并、章名不折行

### 待办
- 本计划未提交 git（属「已完成规划层」，按惯例单独 add）。

### 续三、收官独立复核（换维度，不重跑构建脚本）
写 `_final_audit_courseplan.py` 直接解包成品 docx 与读 md 源，**独立于构建脚本**再查一轮。结论：
- **表规模**：7 表；表1 = 14行×3列 grid[1560,1275,2835]；表2~7 = 4列 grid[1450,1750,2550,2641]，全部无表头 / fixed。与样板列数、表头有无、固定布局一致。
- **顶层结构**：剥掉所有 `<w:tbl>` 后再找 `<w:p>` → 得到 9 段：标题(sz=28)×1 + 「一、总体课程规划」「二、各模块内容规划」+ 6 个模块小标题（全部 sz=20）。**与样板顶层逐条对齐**。
- **框线**：`document.xml` 内 `<w:tblBorders>` = 7。⚠️ **踩坑记录**：初版去 `styles.xml` 里 grep `tblBorders` 得到「未命中」假警报——样板是走 `styleId="ae"`（Table Grid 样式引用），我是直写 `w:tblPr/tblBorders`，**两条路径视觉等价**，核对时应在 document.xml 里数。
- **合并/对齐**：vMerge restart 102 / cont 418 / vAlign center 664 / `<w:b/>` = 0（全程无加粗，与样板一致）。
- **字号**：sz=20 共 1296 + sz=28 共 1，无杂散字号。rFonts 1297 条全部 宋体+TNR，不合规 0。
- **污染残留**：docx 内 `[[`=0 `**`=0 `\ce{}`=0 `\begin{}`=0 emoji=0 半角`$`=0。
- **md 侧**：329 行 / H1×1 H2×2 H3×6 / 表格行 287。⚠️ `[[` 出现 7 处**全在 frontmatter `related:` 字段**（库内正常引用，非污染；docx 因不导出 frontmatter 故为 0）。已 `test -f` 逐一验在：7 个引用目标全部存在（11910/61974/24783/76855/50383/65785/21114 bytes）。
- **闸门**：js-yaml 4 → 受检 1 / 通过 1 / 无frontmatter 0 / 失败 0。

清理：删除本轮全部临时产物（`tmp/render/pg-01..19.png`、`plan.pdf`、`plan.docx`、`bak_courseplan.md`、`export_courseplan_docx.py`、`to_table_plan.py`、`orig/`、`orig_dump.txt`、`_final_audit_courseplan.py`、`_cp_list.txt`），**只保留 `tmp/build_courseplan_docx.py`** 供复用。

skill 回写：`md-tables-to-docx-merged` 新增踩坑 11（框线两条实现路径 / 核对查对文件）、12（顶层段落必须先剥表再找 `<w:p>`）、13（md 的 `[[` 分 frontmatter 与正文两种，前者正常且须验存）；§五 自检块补入框线计数与顶层结构打印。

---

## 续四、课程计划第二轮增补（grill-me 决策 6 项 + 8 项补缺）

### 触发
用户：「再仔细斟酌一下还有没有什么可以优化或者填充的内容。@skill:grill-me」

### 本轮新读的 KB 依据
`六大新课大章节总表`（§3/§4/§5.1/§6/§7.1/§9.1/§10/§11）｜`新课大章节-第三轮有机化学`｜`新课大章节-第三轮结构化学与物理化学深化`｜`上课顺序（详细内容参考2024考纲）`｜`第三轮总体备课框架`（专题 13/14/15-16）｜**`班级授课计划-初二竞赛`**（24 次课，已完成 8 次，Session 9-24 逐课排课）｜**`暑期上课计划-初二竞赛班（22天）`**（Day 1-11 结构收口 / Day 12-21 化学原理主干 / Day 22 总测）。

### 决策（AskUserQuestion 两轮，共 6 问）
1. 模块2/3 **补全 6 章**（第三轮深化层）。
2. 模块6 **保留 22 章，节级去重**（重复内容从官能团章移除、收进方法层章；章序不动）。
3. 表1 课时 **仅初二行写「待定」**，其余 12 行沿用样板 36／12。
4. 章后复习课／习题课层 **暂不处理**。
5. 表1 初二「先留空待回填」→ 落实为「待定」。
6. 两个桥章 **严格建 6 章，用视角分工**：桥A＝表征手段（XRD／选频／谱图判读），桥B＝点群与场语言／选律／Tanabe-Sugano；配合物结构深化只讲 CFT-CFSE-推断。

### 实际改动（8 项补缺 + 1 项拆分）
- **模块1**：+章「化学计算规范」（有效数字与单位／守恒法与计算技巧）。KB 原名为「化学计量与计算规范」，因「化学计量」已由本模块「物质的量」章覆盖，新章聚焦「计算规范」。
- **模块2**：① **原子结构拆出「元素周期律」独立章**（KB §6.2 明确要求在目录层拆开；库内有独立讲义）——拆出的新章插在原子结构之后（原位，非追加）；② 分子结构章 +节「分子的对称性与点群」（《结构化学基础》第4章被引却无对应节）；③ 晶体结构章 +节「晶体的X射线衍射」（暑期 Day 8 有布拉格方程）；④~⑦ +4 章：晶体结构深化（3节）／配合物结构深化（4节）／结构表征与波谱桥（3节）／配位光谱与对称性桥（3节）。
- **模块3**：① 热化学节 +「Born-Haber循环」（六大总表 §5.1 顺位4）；② 化学反应速率和化学平衡章 +节「速率方程与反应级数的测定」（§5.1 顺位5；原动力学仅 3 节偏薄）；③~④ +2 章：化学动力学深化（3节，积分速率方程与半衰期／复杂反应近似处理／速率反应理论深化）／热力学与平衡深化（3节，化学势与相平衡／平衡的定量深化／溶液与依数性综合）。
- **模块5**：+章「元素推断与情境化综合训练」（3节；§7.1 顺位8 收束单元 ＋ 上课顺序新增专项，38届"炼丹"22分／39届"砷化学"15分）。
- **模块6**：① 芳烃章 +节「芳香性与休克尔规则」；② **节级去重**——「炔烃和二烯烃」删「共轭作用」「共振式」两节（移入「有机结构基础与电子效应」的「电子效应」节，知识点扩充为"诱导／共轭／π,π-共轭与π—π共轭／超共轭／烯丙式与乙烯式卤代烃／共振论"）；「卤代烷」删「有机金属化合物」节（移入「有机合成与金属有机」的「有机金属试剂」节）。
  → **核实结论**：SN1/SN2 本无重复——KB 专题3（活性中间体与机理基础）与专题4（亲核取代与消除）本就是两个专题，SN1/SN2 归专题4（卤代烷＋烯烃章），「活性中间体」章只讲中间体类型／能量曲线／弯箭头规范。
- frontmatter `related` 由 7 条扩到 15 条（补入 6 个新课大章节页 + 2 份初二授课计划），提升溯源。
- 表1「初二学年（含寒暑假）」→「初二学年」：1560 缇时间列放不下 10 字会折两行，**加宽会把折行压力转嫁到「课程模块」列（"外出学习（结构化学为主）"12 字本来刚好放下）**，故选择缩短标签。

### 新体量
表1 14 行（表头+13）｜模块表 16／45／32／9／65／117 = **284 数据行**（原 266）｜共 21 页。

### 知识库内部矛盾（本轮发现，按"新者优先"处理）
1. **物化深化已搬去第四轮**：`第三轮总体备课框架`（2026-08-03）明写「专题15-16 物化深化 → 已移第四轮 4-7」，而六大总表（v1.3，2026-07-11）仍列在第三轮。→ 6 章照补，但「化学动力学深化／热力学与平衡深化」实为第四轮内容。
2. **表1 高一模块顺序**：`上课顺序`（旧草案）说 R3 是"结构深化先、有机后"，六大总表却把有机编为大章节 5、结构深化为 6。→ 按六大总表（有机先），现表1 不动。
3. **两个桥章彼此重叠**且与「配合物结构深化」的配位光谱／对称桥梁节重叠。→ 已用第 6 项决策切边界。

### 验收
- js-yaml 4：**受检 1 / 通过 1 / 失败 0**
- docx：7 表／框线 7／vMerge R120 C448／sz20×1431 + sz28×1／`<w:b/>`=0／vAlign 730／字体不合规 0；污染 `[[`=0 `**`=0 `\ce{}`=0 emoji=0 LaTeX命令=0
- md：361 行／H1×1 H2×2 H3×6／正文 `[[`=0（15 条全在 frontmatter）
- 渲染 21 页目检：表1 三列含表头、初二「待定」不折行；模块2「元素周期律」已独立成章；深化 4 章与物化深化 2 章落地正确；炔烃章去重后 5 节；芳烃章含「芳香性与休克尔规则」

### 工具侧教训（已回写 skill）
**用 `pdftotext` 定位中文关键词会假阴性**——「晶体的X射线衍射」「配合物结构深化」在 PDF 文本里被折行拆开，报"未找到"，但 `document.xml.count(关键词)` 全中。**核对"新增内容是否落地"应以 document.xml 为准，pdftotext 只用于排版目检定位。**
清理：删除 `tmp/render2/`、`tmp/_cp2.txt`，仍只保留 `tmp/build_courseplan_docx.py`。

---

## 续三：章后层「考试维度」注入（立范式）

**用户 4 项决策（AskUserQuestion 全选推荐）**
1. 考试理念基准 = **初赛主线**：`近五年真题命题拆解分析`（35-39 届）+ `01-考纲导航/基础要求-*.md` 双基准；决赛要求仅作 R4/R5 延伸。
2. 本批范围 = **先立 1 章范式**（大章1 化学原理，复习课 + 习题课各 1 份），验收后再批量推。
3. 中段升级 = **化学原理 + 有机化学各补 2 节**（共 4 份中段课，依总表 §11.3）。
4. 其他三项 = **计划 + 章节页 + 讲义三处落地**（导论拆章／无机新兴领域第24章／有机区域选择性）。

**摸底结论（本批最重要的一条）**
章后层**最低配置其实已达成**——6 大章 ×（1 复习课 + 1 习题课）= 12 份，均 2026-06-30 建成，有明确品质标准。**不是"缺文件"，是"缺考试维度"**。grep 实测（`命题|真题|趋势|考纲|初赛|决赛`）：

- 复习课：`第一轮化学原理复习` **0**、`第二轮分析化学复习` **0**，其余 1-7 处
- 习题课：`第一轮化学原理习题课` 1、`第三轮有机化学习题课` 1，仅 `第一轮结构化学习题课` **23**、`真题模拟拆解` **47** 较高

另：总表 §11.3 要求大章1、大章5 升级为 4 节（中段复习+章末复习+中段习题+章末习题），现各只有 2 节。

**本批改动（3 个文件）**

1. `04-课件/复习课/第一轮化学原理复习.md`
   - FM 加 `exam_syllabus`（8 条考纲条目）/`exam_evidence`/`exam_trend`/`exam_weight`，`updated` → 2026-09-15
   - 新增 **`## 一点五、本章考试地图`**：1.5.1 考纲条目对照（含"哪些同模块条目属大章2 边界"提醒）、1.5.2 近五年命题分布（35-39 届分值量级）、1.5.3 命题趋势三条、1.5.4 题型权重与复习优先级（★ 排序）
   - §一 学习目标加第 6 条（考试视角）；尾注加「考试对接标准」
   - **编号用「一点五」→ 零编号重排、零既有 §N 引用破坏**（库内已有 §零点六 先例）

2. `04-课件/习题课/第一轮化学原理习题课.md`
   - FM 加同组字段 + `real_exam_count: 5`；§一 表加「真题实战」行（自编 15 + 真题 5 = 20）
   - 新增 **`## 六、真题实战与命题趋势专项`**：6.1 真题实战组 5 题（直引 `05-真题库/` 真题卡，逐题给"解题链／陷阱／迁移价值"）；6.2 新题型专项「给材料学习」（近年材料主题表 + 解题四步法：找状态量→配模型→搭链路→查边界 + 1 道模拟 39 届风格的普鲁士蓝改编题）
   - 原 §六→**§七**、原 §七→**§八**；讲评顺序表加 **「考试权重」列**并写明"讲评优先级 vs 考试权重是两个维度"
   - 尾注加考试对接标准

3. `备课思路/六大新课大章节总表.md`
   - 新增 **§11.4 章后层的考试对接标准（2026-09-15 增补）**——把范式沉淀到权威规则处
   - `updated` → 2026-09-15，`index_version` v1.3 → **v1.4**

**验证**
- js-yaml4 闸门：**受检 3 / 通过 3 / 无 frontmatter 0 / 失败 0**
- `$` 计数 160 / 160（偶）；未闭合 `$` 行 = 0
- **14 个新增链接全部验存**（8 考纲条目 + 5 真题卡 + 1 分析文件）
- 结构核对：§一点五 与 §六 位置正确、编号顺延正确（§七讲评、§八课后推荐）

**发现的库内问题（未动，供后续决策）**
`11-模板/模板-复习课.md`、`模板-习题课.md` 均为 **v1.0（2026-05-09）**，且 `模板-习题课` 用「题组式」结构，与实际 6 份章后课的「A/B/C 分层式」**不一致** → 库内章后层的真实标准载体是**各文件尾注**，不是模板。本轮**刻意未改模板**，避免制造第三套口径；建议后续统一整顿。

**未做（后续批次）**
① 其余 5 章按范式注入；② 4 份中段课（大章1、大章5）；③ 其他三项三处落地（计划层 + 六份章节页 + 讲义）。

---

# 【本会话】第40届初赛真题拆题入库 —— 全部完成（10 题 / 83 子问）

## 交付物
`04-题库/真题/第40届初赛/`：**10 个题文件 + 索引.md**（共 2414 行，88 张图引用，**断链 0**）
`07-资料提炼/提炼-第40届初赛试题解析.md`（八节全填，`review_status: 已处理`）

| 模块 | 题 |
|---|---|
| 无机和结构化学 | 1 磷化学(19) / 4 金刚烷→金刚石(27) / 5 密堆积(30) |
| 化学原理 | 2 生物相容性(21) / 3 氨基酸态氮(15) / 6 钙钛矿纳米晶(30) / 7 NRR(24) / 9 CO₂化学(35) |
| 有机化学 | 8 有机基本概念(20) / 10 反芳香性(26) |

## 批次与闸门（每批 js-yaml + validate_kb 全绿 0E0W）
B0 骨架（目录+163图复制+索引+提炼页骨架）→ B1(1,2) → B2(3,4) → B3(5,6) → B4(7,8) → B5(9,10) → 全量验收（js-yaml **11/11**，validate_kb 12 文件 **0E0W**）

## 关键做法（遵守硬约束）
1. **不改源文件**：全程只读 `40届初赛/**`，OCR/图片/表格修复全部在**新题文件内**完成 → 完全避开与并行会话的写冲突（该目录仍有 173 项未提交）
2. **图片规范**：源为 MinerU 相对路径 → 全部转 `![[06-外部资料导入/真题/images/<hash>.jpg]]`（**全路径**，照第39届最新规范；第37届裸 hash 是旧规范）。B0 用 `cp -n` 复制（并行会话也在复制，避免覆盖）
3. **一大题一文件**，按模块分科子目录，命名 `题-040-<题号>-<主题词>.md`

## 🔴 拆题中发现的原文缺陷（均按「修正 + 标注」处理，未动源文件）
| 题 | 缺陷 | 处理 |
|---|---|---|
| 8 | **8.4.1 答案印作 "a、b、c、c"**（选项 c 重复） | 判定应为 **a、b、c、e**（(e) 连续热交换是分馏核心机理），已修正+标注 |
| 6 | **6.3.1 系数 OCR 错**：`∂h²`/`1×0.376` | 按答案 0.69 nm 反推应为 **3h²/3×**（1× 算出 0.40≠0.69），已修正+标注 |
| 6 | **6.2.4 答案残缺**（只有算式无数值） | 补算 a≈**6.19 nm**；对照提示值 Eg=2.200 得 5.53 nm 与原文一致，互为印证 |
| 6 | 6.4 单位 `N·m⁻³`/`m³g⁻¹` 错、"间湿热法"→浸润热法 | 新文件内改正 |
| 9 | 9.1 共振式**图为空**、9.3.2.3 仅"1分"无图、9.3.2.4 退化为文字 | 照抄+标注 `⚠️ 原文残缺` |
| 10 | `Famday`→**Faraday**、`Hüokol`→**Hückel**；10.3 表格 `10.3.12` 系"题号+化合物标号"粘连 | 已改正+标注 |
| 3 | 数据表第二、三次以"…"省略 | 标注 `⚠️ 原文如此`，计算以第一次数据为准 |
| 7/9/10 | 7.2.2、9.2.1、10.4 未标分 | 标注 `⚠️ 原文未标分` |

## 图片处理
- 源 **163** 张 → 全部复制到 `06-外部资料导入/真题/images/`（零缺失、抽样字节一致）
- 题文件引用 **88** 张，**75 张未被源文件正文引用**（MinerU 版面碎片/重复裁切）→ 按「不硬塞」原则**未塞入题文件**，清单存 `.workbuddy/tmp/unused40.txt`，已在提炼页「未引用图片记录」列明

## 复用工具
`.workbuddy/tmp/build_q9.py` / `build_q10.py`（纯作图题自动生成：按源行号精确嵌入图片，避免手工出错）——**41 图的第9题就是靠它完成的**，可复用于其他"答案即大量图片"的题。

## 后续（补强优先级，已写入提炼页与计划页）
**9 CO₂(35分,全缺) > 3 甲醛法(15分,全缺) > 7 NRR(24分,全缺) > 4 > 1 > 8 > 5**
第9题分值最大且覆盖最弱（19 份有机讲义中 CO₂ 命中全是泛词）；拆出的真题正好作例题反哺讲义。
**用户硬约束仍生效**：讲义练习题一律取题库实题，题源不足就降规格+标注，**严禁自编**。

## git（未提交）
本会话产物：10 题文件 + 索引 + 提炼页 + 163 图复制，均**未 git 提交**（等并行会话收敛）。`40届初赛/` 173 项未提交、HEAD `df5bd830f`。

---

# 【本会话·续】第40届初赛「收入题库」——复检 + 登记（B6）

## 交付：正式登记入题库索引体系
| 文件 | 改动 |
|---|---|
| `04-题库/题库总索引.md` | 初赛真题表加 **第40届\|10** 行；章节名 25-39→**25-40届**；真题 636→**646**、初赛 162→**172**、总 4,186→**4,196**；加「题目数＝历史口径」脚注；`updated`→09-15 |
| `04-题库/真题/README.md` | 初赛表加 **第40届初赛\|11**；「15届」→**16届**；覆盖范围 25-40届；文件数 854→865 |
| `04-题库/题库架构总览.md` | 真题行 687→698、25-40届；加 09-15 校准注 |
| `04-题库/README.md` | 题源总表 真题 837→**847**、合计 4,655→**4,665**；目录树加 第40届初赛/11、31→32 场次、865→876 文件；`updated`→09-15 |
| `04-题库/真题/第40届初赛/索引.md` | 加模块分布；dataview `subject`→**`source_subject`** |
| `07-资料提炼/提炼-第40届初赛试题解析.md` | 状态→**已定稿**；加 §六 B6 行；加「字段规范化修正」表；`problems_extracted: 10` |

## 🔴 复检揪出并修复：3 题会「待分类」静默掉出模块习题书
用 `build_module_book.py --dry-run` + 直接调 `gather_questions/classify_by_keywords`（探针 `.workbuddy/tmp/probe_route40.py`）实测，**修正 4 题 6 处字段**：

| 文件 | 字段 | 原→新 | 依据 |
|---|---|---|---|
| 题-040-4 | subject_module / submodule | 元素与分析→**结构化学**；结构化学与键焓估算→**晶体结构** | 金刚石属晶体结构；同型 `题-025-7-金刚石晶胞键连`（结构化学） |
| 题-040-5 | subject_module / submodule | 元素与分析→**结构化学**；晶体结构（密堆积与多型体）→**晶体结构** | 同型 `题-039-5-XeFₓ晶胞`（结构化学） |
| 题-040-8 | submodule | 有机化学基本概念→**结构基础与波谱分析** | 须路由到有机章节 |
| 题-040-10 | source_subject·module | 化学原理→**有机化学** | 与目录及 subject_module 一致；原值系与第9题误串 |

修正后 10 题全部落章（化学原理3 / 元素与分析2 / 结构化学2 / 有机化学3），**待分类＝0**。其余 7 题 submodule 虽为描述式但含关键词、路由正确，未动（最小变更）。

## 复检结论（全绿）
- **查重**：10 题题干特征串在 `04-题库` 5,508 文件中**外部命中 0**（新题，无重复）
- **逐文件体检**：行尾全 LF、`$` 全偶（24~352）、88 图**零断链**、KP 全 47 个**零缺失**、必填字段齐全、title＝文件名
- **章节结构**：10 题均为 `题目/参考答案/知识点映射/解题思路/易错分析`（+🔗同大题小问），参考答案 1,462~4,744 字非空
- **闸门**：js-yaml **12/12**；`validate_kb --changed` 0E0W；`--full` **7,714 文件 0E/202W ≤ 基线 209**；`check_pack_rule.py` 退出码 0（无硬违规）
- `04-题库/真题总索引.md` 为**纯 dataview**（无手工届次表），10 题自动收录

## 发现（存量，未擅改，仅记录）
1. **索引「题目数」列是 2026-05 旧口径**：各届初赛实为「10 个大题文件」，但总索引写 25/51/72…（既非文件数也非小问数，不可复算）。第40届按现行结构填 **10** 并加脚注；建议单独一轮统一 15 届口径。
2. **`subject` 字段漂移**：真题文件 `subject`＝0 / `source_subject`＝662，但库内 **31 个 `索引.md` 的 dataview 仍写 `subject AS 学科` → 「学科」列恒空白**。已改我这份，其余建议批量修。
3. `04-题库/题库架构总览.md` 的「04-题库 4,258」等总量数与实测（5,508）差距大，本次未动（无法归因）。

## 记忆维护
`.workbuddy/memory/MEMORY.md` 超注入上限被截断 → **精简重写 v2（16,168B → 9,141B，-43%）**：合并 §五/§六、压缩 §八 课程规划、新增「题库字段分工 + submodule 是路由字段 + 索引口径漂移」三条口径；铁律全保留。

## git（仍未提交）
本会话新增/改动：10 题文件 + 索引 + 提炼页 + 4 个题库索引文档 + 163 图复制。按并行纪律**未 add/未 commit**。

---

# 【本会话·续2】题库索引系统性修复（用户授权动红区）

## 交付 A：`subject` → `source_subject`（37 文件 / 42 处）
题库区 2026-09-02 已把 `subject` 改名 `source_subject`，但 **37 个索引的 dataview 仍写 `subject` → 「学科」列恒空白**。脚本 `.workbuddy/tmp/fix_subject.py`（**仅在 dataview 代码块内替换 + 负向断言避开 `source_subject` + 行数守恒 + 备份**）一次修完：
- `题库总索引.md`（4 处）· 31 个届次 `索引.md` · 化学原理 Ch02/Ch03 · 教材习题 无机化学第5版 · 经典例题 ×2
- 复检残留 **0**；备份 → `.workbuddy/backups/20260915-subject-fix/`

### 🔑 关键：**不能跨区改**
`subject` 在**非题库区仍是有效字段**：`03-知识点` 937 页、`07-资料提炼` 207 页、`04-课件` 34 页都用 `subject`（`source_subject` 计数为 0）。全库另有 **20 个文件**（00-首页/02-数据库/03-知识点/04-课件/07-资料提炼/10-索引与统计/11-模板）的 dataview 引用 `subject` —— **这些是对的，一处未动**。改名只针对题库。

## 交付 B：「题目数」口径统一（初赛 16 届）
**核出真相**：**决赛的「题目数」全是准的**（与实际文件数完全吻合：13/9/9/10/9/9/8/8/8/53/17/19/22/10/10）；**只有初赛是 2026-05 遗留的小问级旧值**（第25届记 36、实为 10 个大题文件）。脚本 `.workbuddy/tmp/fix_counts.py` 统一：
- `题库总索引.md` 初赛表 16 行 → 实测值（10/11/9/11/11/10/10/10/10/10/10/10/10/10/10/10）
- `真题/README.md` 初赛表 16 行 → 文件数（+索引）
- 16 个初赛 `索引.md` 的「共收录 N 道题目」静态文本同步
- 脚注重写为可验证定义；`真题/README` 头总量 865→**697**（实测）；`04-题库/README` 目录树 876→697 及三处初赛旧值

## 闸门
js-yaml **41/41**（38 + 3）；`validate_kb --changed` 分批（≤8）×5 + 1 → **全 0E0W**；`--full` 全量回归已跑。

## 未动 / 待办
- 20 个非题库区 `subject` dataview：**正确，不动**
- `04-题库/README` 题源登记总表「真题 847」与实测 646 差距大、`省预赛 11` 与实测 272 md 不符 → **未归因，待单独核**
- `题库架构总览.md`「04-题库 4,258」与实测 5,508 差距大 → 同上

## git
本会话累计（含此前）：10 题文件 + 索引 + 提炼页 + 题库总索引 + 真题/README + 题库README + 架构总览 + 37 个索引修复 + 163 图。**仍未 add / commit**（用户指示）。

---

# 【本会话·续3】补强第9题 → 新建《二氧化碳化学》讲义

## 题源勘查（第 5 次「命中≠可用」教训）
| 判据 | 结果 |
|---|---|
| 原始关键词命中 | 题库 1,593 文件 |
| 题干含 CO2 主题词 | 94 |
| **真正可用（题干非空 + 答案完整 + 主题相关）** | **10 题** |
| CO2 **专属**真题 | **仅 3 道**：第40届第9题(35分)、第38届7-1、第35届4 |

**不可用清单（重要）**：
- `题-34决理-2-2-1/2/3`（CO₂ 插入 Ti / M–N / Fe-Zr）→ **题干为空**（只有"见原卷图示"）
- `题-34决理-1-2-1/1-2-3`（CO₂ 传感器 / 海水浓度）→ **题干为空**
- `题-042-ABOC`（CO₂ 自由基负离子催化循环）→ **题干为纯图片**
- 环状碳酸酯 / CO₂ 插入 M–X / CO₂ 电催化还原 → **0 命中**

## 交付：`04-课件/学生讲义/有机化学/二氧化碳化学-超级充实版（自学完整）`
- **1027 行 / 3.4 万字**；docx 已出（oMath 631 / 48 图）
- 知识主线 5 节：CO₂ 分子与晶体 → 水溶液平衡与捕集 → 活化与插入 → **自由基阴离子与光氧化还原（核心）** → 资源化与工业情境
- 例题 4（第40届第9题 / 第38届7-1 / 第35届4 / 第28届决赛6）+ 练习 6（679 成键 / 864 石灰岩 / 786 镁燃烧 / 078 CO-CO₂ 控制 / CO₂对 NaOH 标定 / ABOC-041 光化学）
- **全部 10 题均为题库实题，零自编**；按用户规则**按实际题量落地**并在 §9.3 标注「题源边界 + 补做条件」

## 构建做法（可复用）
- 用 **builder 脚本**（`.workbuddy/tmp/build_co2.py`）组装：**知识主线人工撰写 + 例题/练习从题库抽取**（保真，不手抄，避免长答案转录出错）
- 抽取清洗四件套：去 `<details>/<summary>`、去 `> 来源/> 小问关联` 行、**内部标题降级 `###`→`####`**、折叠空行
- 讲义惯例核查：**图片用纯哈希**（讲义区 805:77）、**不用 `<details>` 折叠**（仅 1 份用过）→ 已按惯例调整

## 踩坑
1. **`## 九` 重复**：builder 加了节标题、tail 文件又自带 → 去一处
2. **练习编号错位**：原按 竞赛→基础 排序，与知识节 `练 N` 引用不符 → 改为 **基础→进阶** 并同步改 5 处交叉引用
3. **precheck 教师向泄漏 2 条**：`> **本讲定位**`（blockquote 判据含"本讲定位"）+ `### 高频易错清单`（heading 判据含"易错清单"）→ **移入 HTML 注释**（注释内不报警）+ 改名「核心速查卡」→ **0 error / 0 warning**

## 闸门
js-yaml 1/1；validate_kb **0E0W**；precheck **0E0W**；`$` 972 偶；48 图零断链；63 链接零缺失；裸 LF。

## 题库答案问题（**非本轮新增**）
`题-028决-6`（第28届决赛第6题）相数 $\varphi=5$ → **应为 $\varphi=4$**（气相是一个相）。
🔧 **更正**：此条**早已收录**于 `09-审计报告/题库答案问题清单-2026-09-14.md` **总表第 3 条**（🔴 高，2026-09-14 的 P0-B 轮发现），**不是本轮新增**——本轮只是把它**落成讲义内的校勘注**。清单仍为 8 条，未变。

## 未提交
讲义 md + docx + 计划页 §7.2c 回填，均**未 git 提交**。

---

# 【本会话·续4】章后层：其余 5 章注入考试维度（批次 1/5 完成）

## 标准依据
`六大新课大章节总表 §11.4`（2026-09-15 立）：
- **复习课**加 `## 一点五、本章考试地图`，位置**紧随学习目标**；四小节 = 考纲条目对照 / 近五年命题分布 / 命题趋势（≤3条）/ 题型权重与复习优先级（**按考试权重排序，非教材顺序**）
- **习题课**加 `## 真题实战与命题趋势专项`；两块 = 真题实战组（≥5 题，直引库内真题卡＋届次）/ 新题型专项（"给材料学习"：材料主题表 + 四步法：找状态量→配模型→搭链路→查边界）
- FM 挂 `exam_syllabus`/`exam_evidence`/`exam_trend`/`exam_weight`（习题课改挂 `real_exam_count`）
- 样板：`第一轮化学原理复习` + `第一轮化学原理习题课`

## 本轮交付（批次 1/5：第一轮结构化学）
| 文件 | 改动 |
|---|---|
| `04-课件/复习课/第一轮结构化学初步复习-v2.md` | 插入 §一点五（考纲 04/09/10/11/12；35-39 届晶体＋配位命题分布；趋势三条：计算型→定性推理型｜前沿材料常态化｜配合物重心上移；题型权重表＋复习顺序）；FM 加 exam_* 4 项、`updated`→09-15 |
| `04-课件/习题课/第一轮结构化学习题课.md` | 插入 §真题实战与命题趋势专项（**6 道真题**：38届FeCr晶胞／38届SrSb分数坐标／39届XeFₓ晶胞＋3 道省预赛；逐题解题链/陷阱/迁移价值；"给材料学习"材料表＋四步法）；FM 加 `real_exam_count: 6` + exam_* 、`updated`→09-15 |

**真题实战组选题**：**3 道近三年**（38 届 2＋39 届 1）满足标准要求；其余 3 道为省预赛（钙钛矿／配合物／晶体场）。
**闸门**：js-yaml 2/2；validate_kb **0E0W**；两文件 `$` 均偶（1156 / 64）。

## 发现的既有问题（**存量，未改**）
- `第一轮结构化学初步复习-v2.md` L631 `[[配位场理论]]` **断链**——全库无该页，最近替代为 `03-知识点/化学原理/晶体场理论`。**疑似不等价（配位场理论 LFT ≠ 晶体场理论 CFT）→ 不猜测替换**，留待人工确认。
- `第一轮结构化学初步复习.md`（无 -v2）**已 `status: deprecated`** → 活动版是 **-v2**；写方案时注意别改到废弃版。
- 复习课/习题课**无 docx 产物**（非学生讲义线，`build-all-handout-docx.py` 不覆盖）→ 无需导出。

## 剩余（批次 2-5，8 份文件）
第二轮元素化学复习/习题课 · 第二轮分析化学复习/习题课 · 第三轮有机化学复习/习题课 · 第三轮结构物化深化复习/习题课
数据源：`备课思路/近五年真题命题拆解分析` §二（有机）/§三（晶体）/§四（配位）/§五（元素）/§六（物化）。⚠️ **分析化学无专节**，需从 §六 与真题卡另行归纳。

---

# 【本会话·续5】章后层考试维度注入 —— 全部收官（批次 2-5，8 份）

## 交付总表（本轮 8 份 + 上轮 2 份 = 全部 10 份）
| 章 | 复习课 §一点五（+行） | 习题课 §真题实战（+行） | 闸门 |
|---|---|---|---|
| 第一轮结构化学 | §1.5.1–1.5.4 | 6 题 + 给材料学习 | ✅（上轮） |
| 第二轮元素化学 | 考纲 13+4 前置／35-39 分布／三趋势／权重 | **6 题**（39-1 雄黄、39-7 N₅⁺、38-1 灰狼、37-1 GaN、36-1 纳米硅、35-5 推断） | 10/10 · 0E0W |
| 第二轮分析化学 | 考纲 18/19/20 全部／分布／三趋势／权重 | **6 题**（39-4 碘量法、38-2 KMnO₄、37-2 滴定、36-4 MIₙ、30-5 MOₓCl_y、误差） | 10/10 · 0E0W |
| 第三轮有机化学 | 考纲 21-56 归五组／分布／三趋势／权重 | **6 题**（39-10 NHC-硼、39-8 Bent、38-10 埃索美拉唑、37-10 Brook、36-10 十氢合萘、36-9 [5+2]） | 10/10 · 0E0W |
| 第三轮结构物化深化 | 跨模块（14-17+12+动力学+06/07/08+决赛）／分布／三趋势／权重 | **6 题**（38-5 FeCr、39-2 普鲁士蓝、39-6 储氢、38-6 醋酸亚铬、37-4 Pt、36-6 非水电池） | 10/10 · 0E0W |

**每份均**：FM 加 `exam_syllabus`/`exam_evidence`/`exam_trend`/`exam_weight`（习题课改挂 `real_exam_count: 6`）、`updated`→09-15、tags 加「考试对接」；复习课 §一点五 紧随学习目标、习题课新 §六 插在「讲评顺序」前并**顺延原节号 + 同步交叉引用**。

## 关键做法（可复用，建议固化为 skill）
1. **先读范式再动手**：样板 = `第一轮化学原理复习` / `第一轮化学原理习题课`（§11.4 标准落地的两份）
2. **动手前必查两件事**：① 目标文件的**标题编号形态**（决定是新节"零顺延"还是要重编号）；② 目标文件里的 **`§N` 交叉引用**（插入后须同步改）
3. **「一点五」的编号技巧**：复习课标题序为 `一、学习目标 / 二、…` 时，插 `一点五、本章考试地图` 可实现**零顺延**（字面含义即"一与二之间"）
4. **链接防断**：写入前先建"全库 basename 索引"校验 KP/条目页存在性（本轮 10 份**新增链接 83 个全部零缺失**）
5. **归档校验**：`$` 奇偶 + 章节重名 + 行尾 + emoji + 链接，一次脚本全出（`.workbuddy/tmp/check_units.py`，参数化）

## 数据源要点
`备课思路/近五年真题命题拆解分析`：§二 有机（物理有机从附加题→独立大题、立体化学三级跳、人名反应让位机理推断、给材料学习固化、自由基系统化）｜§三 晶体结构（计算型→定性推理、前沿材料常态化）｜§四 配合物（初赛场效应/邻基参与、决赛 SMM+σ/π 轨道分析）｜§五 元素（传统退潮、非经典键、化学史情境）｜§六 物化（多步联立、分值暴增）。
**⚠️ 分析化学无专节** → 已从 `05-真题库/真题-分析-*`（8 卡）与近五届题文件（39-4/38-2/37-2 等）另行归纳。

## 🔴 发现的系统性存量断链（**未修**，需决策）
章后课 FM 的 `referenced_insights` 引用 **5 个不存在的大类页**：`教学洞察-元素化学`／`-分析化学`／`-化学动力学`／`-结构化学`／`-酸碱理论`。
而 `12-教学洞察/`（**54 项**）**全为细分主题页**：`教学洞察-元素化学深度与结构推断综合`、`教学洞察-元素推断综合训练`、`教学洞察-化学动力学基础/深化`、`教学洞察-容量分析基础与酸碱滴定`、`教学洞察-氧化还原滴定与沉淀滴定` 等。
→ 属**命名策略问题**（建大类索引页 **vs** 改短链指向细分页），涉及多份课件，未擅改。
另：`05-真题库` 出现在 2 份习题课正文，是**目录级链接**（非断链，属链接审计已知假阳性）。

## 沿用上一轮的两条注意
- `第一轮结构化学初步复习.md`（无 -v2）已 `deprecated`，活动版是 **-v2**
- 复习课/习题课**无 docx 产物**（不属学生讲义线，`build-all-handout-docx.py` 不覆盖）→ 本轮无需导出

## git（仍未提交）
本轮改 8 份课件；累计本会话：第40届 10 题 + 索引 + 提炼页 + 4 个题库索引 + 37 个索引修复 + 163 图 + 章后课 10 份 + 补强讲义 1 份。**未 add / 未 commit**（用户指示）。

---

# 【本会话·续6】收敛页整理（用户反馈「搞得这么复杂」后的减负动作）

## 诊断：复杂度来自「只加不减的状态块堆积」
| 文件 | 整理前 | 整理后 | 降幅 |
|---|---|---|---|
| `00-首页/状态摘要.md` | 408 行 / **57,113 字符** / 111 块 | 314 行 / **16,627 字符** | **−71%** |
| `00-首页/活跃任务.md` | 276 行 / **17,843 字符** | 187 行 / **4,767 字符** | **−73%** |

两页的"状态块"按会话轮次**倒序堆积、从不清理**（状态摘要的块区占全文 82% 字符，最老到 2026-08-05）。
**判据**：`> **<emoji> <标题>（MM-DD）**：` 为状态块；`> ⛔ **…GATE**`、`> **职责**`、`> ⚡ **执行协议**` 等**规则块不属于状态块，必须保留**。

## 整理动作（只归并、不删改）
- 归档文件 1：`00-首页/归档/状态摘要-历史状态块-2026-08-05至09-10.md`（96 块，原样保留，41,577 字符）
- 归档文件 2：`00-首页/归档/活跃任务-历史状态块-2026-08-24至09-04.md`（6 块）
- 两页**保留**：最近状态块（09-11~09-13）+ 本日新块（09-15）+ **全部规则区与固定节**（执行协议 / GATE / 使用规则 / 任务面板 dataview / 模块关系 / 入口路由 / 会话检查点）
- 各加一行「📦 历史状态块已归档 → [[归档页]]」指引
- 脚本 `.workbuddy/tmp/converge.py`（备份 + 切分守恒断言 + dry-run/--apply）；备份 → `.workbuddy/backups/20260915-convergence/`
- 闸门：js-yaml **4/4**（含 2 归档 + 2 收敛页）；工作日志 1/1

## 同时补齐的项目侧落点
- **新建 `00-首页/工作日志/2026-09-15.md`**（项目正规落点此前缺 09-14/09-15）——六节：第40届拆题 / 索引修复 / 章后层 / 物化补强 / 收敛页整理 / 存量问题 + 改动清单
- 之前本会话的日志只写在 `.workbuddy/memory/`（内部），**项目侧 `00-首页/工作日志/` 才是对外可查的落点** → 以后每轮收尾应同步

## 🔑 新学到的两条
1. **收敛页会无界堆积**：`状态摘要.md` 每次会话在顶部加块，111 块后达 5.7 万字符。**建议每次大轮次收尾顺手归档旧块**（保留最近 3-5 块），否则入口页越读越慢。
2. **`活跃任务.md` 任务面板是 dataview 自动聚合**（`WHERE type = "活跃任务卡" AND status = "active"`）→ 无需手工维护；但**盘点发现 4 张卡缺 `status`/`type` 字段**（`待办清单-2026-09-01`、`断链内容建设backlog`、`待办交接清单`、`图片剩余待手绘清单`）→ **不会出现在面板里**（第 4 类静默失效）。

## 任务卡盘点（25 张）
- **active 4**：KP待完善项非文字任务清零(P1,09-13) · 习题书裸下标收口与git健壮性复核(P1,09-15) · 计划-图片识别后续任务(P2,**08-07 已停滞两月**) · validate_kb每周运行(P3,09-02)
- **blocked 7**：其中 3 张为 **06 月**（非正文剩余工作落地 06-19、学生反馈闭环 06-13、纸质版排版测试 06-19）→ 疑似失效
- **completed 8** · **缺 status 4** · 待用户确认 1
- ⚠️ 本会话的多项工作（第40届拆题、章后层）**未建任务卡**（SOP 要求新任务先建卡）

## 未动（存量，待决策）
`教学洞察-大类名` 5 个短链无对应页 · 题库 README/架构总览总量数无法归因 · 06 月 blocked 卡是否失效

---

# 【本会话·续7】git 提交（5 笔 / 85 项）

| commit | 内容 | 文件数 |
|---|---|---|
| `7ee9c6075` | feat(题库): 第40届初赛 10 题拆题入库 | 12 |
| `3481f88e3` | fix(题库): 索引系统性修复 + 第40届登记 | 40 |
| `472df0563` | feat(课件): 章后课考试维度注入 10 份 | 10 |
| `ff8208242` | feat(讲义): 物化补强——新建 2 + 内增 3 | 13 |
| `ef9496ba0` | chore(00-首页): 收敛页归档瘦身 + 工作日志 + 规范漂移 | 10 |

工作区 1,680 → **1,595**（净减 85，吻合）。**未 push**（并行会话活跃 + 仓库纪律）。

## 🔑 方法：如何判定「文件归属本会话」（并行会话隔离）

**mtime 是决定性判据**。本次差点误提交样板 2 份（`第一轮化学原理复习.md` / `第一轮化学原理习题课.md`）：
- 内容特征与我的工作**完全同构**（§一点五 + 本章考试地图 + exam_* + 真题实战 + real_exam_count）→ 单看内容无法区分
- 但 `stat` 显示 mtime = **20:24 / 20:25**，而那正是我做收敛页整理的时刻（我的件是 19:52）→ **并行会话正在改**

三条排除标准（本次实际用到）：
1. **mtime 落在本会话动作窗口之外**（尤其与其他文件同分钟但内容无关）
2. **内容含本会话未曾做过的动作**（如样板那份我没碰）
3. **提前用 git status 全量扫描目录**，确认「该目录下改动是否全属本会话」——本次 `04-题库`(40)、`00-首页`(6)、`04-课件/学生讲义`(5)、`06-学生侧材料/讲义`(5) 四目录经核均为本会话全量，可整目录 add；而 `备课思路`(4) 含并行件（`近五年真题命题拆解分析.md` + 未央课程计划 2 份）→ **改显式清单 add**

另：`06-外部资料导入/真题/images/`（501 张）在 git status 中**无条目** → 已被跟踪且无改动（并行会话已纳入），无需操作。

## 提交纪律执行
- 每笔**先 add → `git diff --cached --name-only | wc -l` 核对数量 → 再 commit**（禁 add→commit 一条龙）
- commit message 用 **Write 写成文件** + `git commit -F`（避免 heredoc 中文 mojibake）
- 5 笔按线拆分，互不混装

---

# 【本会话·续8】35-40 届复盘（命题分析扩届 + 课程计划补新章 + 章后课章1同步）

## 背景与范围
- 触发：用户「结合近5年的考试题目(35-40)届，看看还有没有可以优化的内容」—— **届次上限由 39 推到 40**
- 第40届初赛（2026-09-15 入库）的提炼页 + 题库 10 题文件已在库，可作全量素材
- 三层同步：①命题分析扩至 35-40 届 ②课程计划补 40 届暴露的新章新节 ③章后层（大章1 化学原理）同步

## 六届分值底座（统一口径）
| 届次 | 相关大题 | 分值/卷面 | 占比 |
|:---:|:---|:---:|:---:|
| 35 | 第2题 多样的氧物种 | 6/100 | 6% |
| 36 | 第6题 非水溶剂电极电势与热力学 | 14/100 | 14% |
| 37 | 第3题 海水中锂的提取 | 10/110 | 9% |
| 38 | 第7题 CO₂ 空气捕集 | 10/110 | 9% |
| 39 | 第2题 普鲁士蓝 + 第6题 储氢 | 56/242 | 23% |
| 40 | 第2题 生物相容性 + 第6题 钙钛矿 + 第7题 NRR | 75/247 | **30%** |

**口径铁律**：39-40 届原卷自带「N分,M%」；35-38 届取提炼页分值表。两段体量（100-110 vs 242-247）**差异成因库内未能证实** → 跨届比较一律用**卷面占比**，不用绝对分。

## 三层改动
1. `备课思路/近五年真题命题拆解分析.md`（16528 → 34446 B / 500 行）
   - §一 事实更正：「35-39届决赛提炼全部缺失」→ **「骨架级」**（7 份提炼页仅「考纲映射 + 拆题清单」两节），加「覆盖度说明(2026-09-15 修订)」注明旧结论「当时成立、现已过期」
   - §二~§五 逐小节补 40 届行（11 处）；§2.1 新增 2026 两组考点映射
   - §六 整节重做（统一口径）；§七 缺口 1-6 补 40 届验证 + **新增 Gap #7-#14**：表面热力学 / 量子限域估算 / 分离纯化 / 光氧化还原与 HAT / 反芳香性与自旋多重度 / 多型体记号 / PCET / 掩蔽与空白校正
   - §八 执行顺序改 2026-09 版；§九 由「从零提炼」改为「为已有骨架补审题趋势」
2. `备课思路/未央化学竞赛课程计划（初二至高二）.md`：**数据行 284 → 300**（+16）
   - 新 3 章：表面与界面化学（3节）/ 量子化学初步（3节）/ 有机分离与纯化（5节）
   - 新 5 节：多型体与堆积变体 / 电催化与 PCET / 掩蔽·解蔽与空白校正 / 光氧化还原催化与 HAT / 反芳香性·自旋多重度与 Breslow 中间体
   - 各模块行数：化学基础 16 / 结构化学 46 / 物理化学 39 / 分析化学 10 / 无机化学 65 / 有机化学 124
   - 教材来源回溯库内实有提炼（上海中学竞赛课程第一分册第七讲、中级无机化学第8章等）
3. 章后课章1：复习课 §1.5.2 命题分布补 40 届、§1.5.3 趋势三条→四条、§1.5.4 +2 行题型权重；习题课真题组 5→8 题（含 3 道 40 届真题）、四步法→五步法

## 闸门结果
- **js-yaml**：受检 4 / 通过 4 / 失败 0
- **validate_kb**：受检 4 / Error 0 / Warning 1 —— `近五年真题命题拆解分析.md` 的 `stage='第四轮'` 不在允许列表。**判定为存量**（首版 2026-06-06 即有，非本次引入），未静默修改
- **docx 结构**：7 表 / 框线 7 / vMerge R126 C474 / sz20×1523 + sz28×1 / `<w:b/>`=0 / vAlign center 768 / rFonts 1524 / 污染（`[[`、`**`、emoji）全 0 / 顶层 9 段与样板对齐
- **渲染目检**：22 页，pg-05（多型体）/ pg-08（表面与界面+量子化学）/ pg-10（掩蔽解蔽）/ pg-17（反芳香性）/ pg-21（光氧化还原与HAT）/ pg-22（有机分离与纯化）版式正确、无折行

## 踩坑（两条）
1. **同文件多处 Edit 若在同一工具批次并发发出 → 整批丢失**：3 处小节标题 Edit（改「五年命题演进」→「(35-40届)」）同批发出，核查时残留仍为 3 → 必须**串行**逐个重发才生效
2. **bash 内联 python 的正则转义被吞**（`\%`/`$`）→ 老坑复现，改写为临时 .py 文件再跑
3. 分值口径修正前曾误判：原 §六 写「39 届 ~20-25 分」，实际 40 届化学原理达 75 分 → **凡跨届分值必须先抓原卷自带标注**

## 提交（3 笔，均未 push）
| commit | 内容 | 文件数 |
|---|---|---|
| `ff2f21ba5` | docs(备课思路): 未央课程计划 3 章 5 节 | 2 |
| `04dbe080f` | docs(备课思路): 命题分析扩 35-40 届 | 1 |
| `c7f257357` | feat(课件): 章后课章1 同步 40 届 | 2 |

提交纪律：每笔 **add → 核 `git diff --cached --name-only | wc -l` → commit -F <消息文件>**（消息用 Write 落盘，避免 heredoc 中文 mojibake）。

## 🔴 并行会话核查（重要结论）
- 章后层 **6 大章 × 2 = 12 份**被两会话拆开完成：**并行会话提交 `472df0563`（5 章 10 份）**，本会话补章1 的 2 份（`c7f257357`）→ 合计 12 份齐
- **⚠️ 关键缺口：并行会话那 10 份仍是 39 届口径**（其分析源是本会话扩届前的版本）→ 这 10 份的「命题分布 / 命题趋势 / 题型权重」**尚未同步 40 届**，须另开一轮补齐
- `MEMORY.md` 第 52 行「考试维度注入已收官（5/5）」为并行会话 20:19 写入，其「5/5」指 5 章，不覆盖章1；口径与本会话 `exam_syllabus`/`exam_evidence` 部分重合但不完全一致

## 🟡 发现的存量问题（未处理，超出本会话范围）
1. `06-外部资料导入/真题/images/` 磁盘 **501** 张、git 已跟踪 **338** → **163 张未跟踪**，其中 **88 张正是第40届题库 md 引用的图**。`7ee9c6075` 声称「88 图零断链」指的是磁盘可解析，但**图片未入 git 库**（`git ls-files --error-unmatch` 对 88 张全部未命中）
2. 仓库根目录两个散落目录未跟踪：`竞赛教程第二分册测试/`（1256 文件）+ `40届初赛/`（166 文件），**共 1422 个文件** —— 疑为 OCR/渲染脚本误写根目录，需并行会话确认归属
3. `.workbuddy/memory/2026-09-13.md` / `2026-09-14.md` 处于 `M` 状态（非本会话改动），未提交

## 方法沉淀
- **判别文件归属本会话**：`mtime` 是决定性判据。本次章后课两份（20:24/20:25）与并行会话的 10 份（19:52–20:18）内容**完全同构**（§一点五 + 本章考试地图 + exam_* + 真题实战），单看内容无法区分，只能靠时间窗切分
- **提交前必跑**：`git status --porcelain -uall` 全量扫 → 对每个目录先确认「改动是否全属本会话」→ 是则整目录 add，含并行件则改显式清单

---

# 【本会话·续9】课程计划搞实（有机 45 处知识点补全 + 初二课时 + 来源可回溯核对）

## 触发
用户：「继续推进吧，把课程计划搞好」。先做实评估（不凭印象动手），再摆决策树、自答可定项、只问需拍板的 3 问。

## 评估用的四条判据（可复用）
1. **与样板逐格比**：`从 docx 读表 → 与 md 逐行对齐`，查「样板有 / 本地有」「两边都空」两类差异
2. **表格完整度**：知识点列空值计数（分模块）
3. **占位符扫描**：`待定 / TBD / TODO / 待补`
4. **来源可回溯**：按各提炼的 `chapter:` 字段反查讲次覆盖

## 评估结论（课程计划）
| 维度 | 状态 |
|:---|:---|
| 结构 1:1 复刻 | ✅ 7 表 / 13 学段 / 表行 [14,16,46,39,10,65,124] |
| 模块覆盖 | ✅ **超集**：本地有机 124 节 vs 样板 89 节，新增 39 节；样板"缺"的 4 处（有机金属化合物 / 共轭作用 / 共振式 / 乙酰乙酸乙酯和丙二酸乙酯）实为**改名或移位**，均有对应 |
| 知识点列 | ⚠️ 有机 **45 处为空** —— **样板表7 本身就空 45 处**，此前 1:1 沿袭 |
| 表1 | ⚠️ **初二学年课时 = 「待定」**（样板无此行，扩到初二后新加） |
| 教材来源 | 🟡 7 讲无 `07-资料提炼/书籍提炼/` 对应页（详见下） |

## 三问决策（用户三选均采纳推荐）
1. 初二学年课时 → **填「24次」**（依据 `班级授课计划-初二竞赛` 的 `total_sessions_planned: 24`，3h/次）
2. 有机 45 处知识点 → **全部补齐**
3. 7 讲缺提炼页 → **登记为待办，本轮不动**

## 执行
### A. 补齐 45 处有机知识点
- 依据库内实有来源：上海中学竞赛课程（第四分册）**烷烃与环烷烃 / 烯烃 / 二烯烃与炔烃 / 芳香烃 / 卤代烃与有机金属化合物 / 醇、醚与环氧化物 / 胺与酚** 各讲提炼 + Clayden／ABOC 对应章
- 覆盖 23 章 45 节：命名类、物理性质类、结构类（苯的结构／环的张力／旋光性／手性）、反应类（定位规律／还原氧化／芳基重氮盐）
- **落盘方式：写脚本按行号+节名双校验替换**（不是 45 次 Edit）——断言 45/45 命中、行数 378→378 守恒、改动行恰 45、落盘前备份到 `.workbuddy/backups/courseplan-md-*.bak`
- 结果：git 显示 **46 insertions / 46 deletions**（45 处知识点 + 1 处课时），与预期完全吻合

### B. 表1 初二学年 待定 → 24次
### C. 登记 7 讲待补提炼页
- 写入 `备课思路/备课与教学思路待办.md` 新增 §五「🟡 资料提炼层缺页（2026-09-15 登记）」，含 7 讲与库内替代落点对照表；`updated` 同步 2026-09-15

## 闸门与验收
- **js-yaml**：受检 2 / 通过 2 / 失败 0
- **validate_kb**：受检 2 / Error 0 / Warning 0
- **docx 结构**：7 表 / 表行 [14,16,46,39,10,65,124] / `<w:tblBorders>` 7 / `<w:tblLayout>` 7 / `<w:gridCol>` 27 / `<w:b/>`=0 / sz20×1658 / vMerge 600
- **渲染目检**：**25 页**（原 22 页）；pg-01（表1「24次」）/ pg-15（有机起始：烷烃三节已填）/ pg-22（胺与杂环）/ pg-25（有机分离与纯化收尾）版式与全表一致、无折行溢出

## 🔑 两条新踩坑（都是「先怀疑脚本」的实例）
1. **md 读入保留 CRLF 时 `split("\n---\n")` 切不开 frontmatter** → 会把 frontmatter 的 `[[wikilink]]` 误算成正文污染。正确做法：`re.match(r"^---\r?\n.*?\r?\n---\r?\n", md, flags=re.S)` 剥离。
2. **`xml.count("w:tblBorders")` 会同时命中开闭标签**（得 14 而非 7）。正确做法：数 `<w:tblBorders>`。
→ 本轮两次「❌」**全部是脚本假阳性**，文档本身无问题。**报告前必须先自证脚本**。

## 7 讲来源缺口明细（本轮不动，已登记）
| 分册 | 缺的讲次 | 库内替代落点 |
|:---|:---|:---|
| 第二分册 | 第八讲 电化学基础 | `学生讲义/化学原理/电化学基础-超级充实版（自学完整）` |
| 第二分册 | 第九讲 离子反应 | `学生讲义/_归档/2026-06-23-离子反应` |
| 第二分册 | 第十讲 滴定分析 | `学生讲义/_归档/2026-06-23-滴定分析` |
| 第四分册 | 第十~十一讲 醛与酮／羧酸及其衍生物 | `学生讲义/有机化学/醛酮羧酸` |
| 第四分册 | 第十二~十三讲 杂环化合物／糖、氨基酸 | `学生讲义/有机化学/杂环糖氨基酸` |
→ 可回溯口径：第一分册 10/10、第三分册 10/10（6~10 讲合并于「过渡金属」提炼）、第二分册 7/10、第四分册 9/13

## 提交（2 笔，未 push）
| commit | 内容 | 文件 |
|---|---|---|
| `0bf292a30` | 课程计划补齐（45 知识点 + 初二 24 次 + docx 重建） | 2 |
| `a3d3d0778` | 登记 7 讲资料提炼层缺页待办 | 1 |

## 未决（用户已明确本轮不做）
- 7 讲提炼页补写（可从未被提炼的学生讲义反向提炼）
- 仍待另开一轮：**章后层 10 份（并行会话 472df0563）同步 40 届**
## 续10 · 章后层 5 章 10 份同步 40 届（2026-09-15 21:30–21:50）

**背景**：上一轮发现并行会话 `472df0563`（09-15 20:53）注入考试维度的 5 章 10 份仍是 39 届口径，本轮补齐。

**改动**（10 份，75 处精确替换；脚本落盘、逐处断言命中 1 次、落盘前备份）
- 复习课 5 份（25 处）：§1.5.2 标题改「命题分布（35-40 届）」+ 补 40 届行；**§1.5.3 标题「命题趋势三条」→「四条」** + 补第 4 条；§1.5.4 题型权重补行；复习顺序补一条。
- 习题课 5 份（30 处）：§六 导语三条→四条；§6.1 真题组 6 题→7／8 题（新增 40 届真题，链接全部验存）；补逐题讲评要点（解题链／陷阱／迁移价值）；§6.2 给材料学习表补 40 届行；「解题四步法」→「解题五步法」（新增第 0 步）。
- frontmatter 10 份（20 处）：exam_trend 补第 4 条；exam_weight 换 35-40 届口径并写入 40 届分值／卷面占比；习题课 real_exam_count 6→7／8。

**40 届映射（卷面 247 分，源：07-资料提炼/提炼-第40届初赛试题解析）**
结构化学 第5题30+第4题27+第1题19 = 76 分(31%)｜元素化学 第1题19分(8%)｜分析化学 第3题15分｜有机化学 第8/9/10题=81分(33%)｜结构物化深化 第4/5/6/7题=111分(45%)

**踩坑（真问题，由换维度复检抓到）**
1. **§1.5.3 标题漏改**——内容加了第 4 条趋势，标题仍写「命题趋势三条」；55 处替换脚本只列了 1.5.2 标题、未列 1.5.3 标题 → 复检 5 项 FAIL，补改 5 处后全绿。
   → **教训：批量做「N 条 → N+1 条」时，标题与内容必须同批改**，只改内容会留下自相矛盾的标题。
2. **validate_kb 分批跑会互相覆盖报告**——先跑 8 files 再跑 2 files，报告只保留最后一批（`--changed` 是覆盖写）；必须**一次传全部文件**重跑，报告口径才完整。
3. 本轮用脚本替换而非多轮 Edit，规避了「同文件多处 Edit 并行丢失」的老坑。

**闸门**：js-yaml4 受检 10／通过 10／失败 0；validate_kb 受检 10／Error 0／Warning 0。
**换维度复检 8 项全通过**：§1.5 口径 / §六 口径 / 题数与表行一致 / `$` 配平（10 份均偶）/ 表格列数 0 不齐 / 40 届链接 8 条验存 / 污染 0 / 40 届分值自洽。
**提交**：`af4d7d81e`（10 份 + auto-validation 报告，11 files，+153/−71；未 push）。

**遗留（未动）**：① 第 40 届 88 张图未入 git（`06-外部资料导入/真题/images/` 磁盘 501 vs 已跟踪 338）；② 仓库根目录 2 个散落未跟踪目录（1422 文件）；③ 5 份复习课 §1.5 导语仍写「近五年命题走向」（与已改标题略有出入，但与已定稿的化学原理那份口径统一，暂保持一致）。

## 续11｜收尾：第40届图库入库 + 两散落目录定性（2026-09-15 22:05）

**请求**：用户「把最后收尾项做了吧」（承接上轮列出的两项存量）。

**已做（提交 `920586870`）**：把 `06-外部资料导入/真题/images/` 的 **163 张未跟踪图**入库。
- 依据：这批图被**已提交文件**引用 —— `04-题库/真题/第40届初赛/**`（10 题，88 处）+ `04-课件/学生讲义/有机化学/二氧化碳化学-超级充实版（自学完整）.md`（41 处，去重后共 89 张）。缺图＝远程断链。
- 验证：`images/` 磁盘 501 / git **501**、未跟踪 **0**；89 张引用图 `git ls-files --error-unmatch` 全部命中。
- 仅新增图片，未改任何 md；未碰并行会话的 3 个 M 文件（`memory/09-13.md`、`memory/09-14.md`、`02-数据库/quality-metrics.jsonl`）。

**新发现（推翻上轮定性，未动手，转用户定夺）**：
1. `40届初赛/` **不是残留** —— `04-题库/真题/README.md` + 10 个第40届题文件（**均已提交**）引它作「原始文件」（`[[40届初赛/第40届化学奥林匹克初赛答案11144113321211]]`），**删它＝11 处断链**。其 163 张图与 canonical **完全重复**（交集 163/163、仅在散落目录 0）。
2. `竞赛教程第二分册测试/` **不是残留** —— `化学测试1题目.md`（5 章 / 33 卷能力测试 = 16A+17B）内容**全库唯一、疑似未整合**（题库·第二分册目录无任何「能力测试」条目）；`答案1.md` 与已跟踪的 `06-外部资料导入/OCR/竞赛教程第三分册OCR源/化竞测试1答案.md` 重叠。源 PDF（`OCR/化学测试1题目.pdf`、`OCR/答案1.pdf`）亦未入库。
3. **两目录均落在** `任务卡-2026-09-14-习题书裸下标收口与git健壮性复核.md` §1 的 **❌ 不碰（题目导入会话）** 区 → 未擅自改动。
4. 全库未跟踪 **1585** = 竞赛教程第二分册测试 1258 + 40届初赛 164 + images 未跟踪 163，**三处全中**；已改动仅 3 个 M（他会话）。

**口径（再次踩到）**：`git status --porcelain` 顶层折叠非 ASCII 路径为八进制转义 → 统计一律 `-z` 或 `core.quotepath=false`；`_img_ref.py` 第一版即因非 `-z` 失真。

**决策与执行（用户拍板）**：
- ① `40届初赛/` → **整目录入库**：`76f6f5c87`（1 md + 163 图，1444 insertions）。11 处「原始文件」引用（`04-题库/真题/README.md` + 10 题）即刻可解析。
- ② `竞赛教程第二分册测试/` → **暂不处理**，仅在 `00-首页/活跃任务/待办交接清单.md` 记一笔：`9eede7cf2`（新增「🟡 根目录散落导入目录 · 待归属确认」一节）。理由：`化学测试1题目.md` 疑似未整合，删/移前须先确认归属会话。
- 收尾后工作区：未跟踪 **仅剩** `竞赛教程第二分册测试/` 1258 个；已改动仅他会话 3 个 M（`memory/09-13.md`、`memory/09-14.md`、`quality-metrics.jsonl`）。

**闸门 + 一个陷阱**：
- js-yaml：受检 1 / 通过 1 / 失败 0 ✅
- validate_kb：**受检 0**（`00-首页/` 不在其校验域内 → **空跑**，非真通过）；其报告文件被这次空跑覆盖为「changed:1 / 受检 0」，**把上一轮有意义的 10 files 记录冲掉了** → 已 `git checkout --` **还原**（保住 r10 的 10 files）。
- 教训：**validate_kb 的报告是覆盖写，空跑也会覆盖** → 改非域内文件后跑它，要么事后 `git checkout` 还原报告，要么别跑。

**本会话本轮 4 笔提交**（均未 push）：`920586870`（163 图）/ `39aefc697`（日志续11）/ `76f6f5c87`（40届初赛入 git）/ `9eede7cf2`（交接清单记账）。

---

## 续12 · 全库待办现状核查（2026-09-15 23:05，用户问「当前还有什么要做的任务」）

> 结论：本会话线（未央课程计划 / 命题分析 / 章后层 12 份 / 图库与 40届初赛入库）**已全部收尾**，无遗留。
> 全库待办归为 4 类，其中一类是**新发现的、可立即做且依据已就绪**的项。

### 一、🟢 可立即做：物化补强计划仍有 3 项未闭环（该页 `status: 执行中`）

来源：`备课思路/第40届初赛-物化讲义覆盖率审计与补强计划.md`（30,519 B，最后修改 09-15 22:16，由并行会话 `b647b8adc` 更新）。

**① §7.1 P0-B 剩余「表面热力学深化」——未做（最高价值）**
- 依据（§8.0 重定）：以**第40届第6题 6.1 / 6.4** 为落点——比表面吉布斯自由能、分散度计算（$\Delta G=\sigma\Delta A$）、**浸润热法测比表面积**。
- 依据已就绪：第6题原文已入库（`40届初赛/第40届化学奥林匹克初赛答案11144113321211.md` L748 = 「6.1 比表面吉布斯自由能与分散度」，题面参数 17.89 mJ·m⁻²、1 mm→1 nm）。
- 讲义现状（实测）：`胶体与表面物理化学-超级充实版` 有 §3.1 表面张力与表面自由能（**概念级**）、§4.3 BET 比表面积（**BET 法**）；但 **「浸润热」0 命中、「比表面积测定」0 命中**，分散度仅 1 处（非该计算模型）→ **确未覆盖**。
- 题源（实测）：`浸润热` 题库 1 / `比表面积` 题库 6 / `表面张力` 题库 5 / `比表面自由能` 题库 1 → **题源存在**，满足「不编题」约束。
- 落点建议：`胶体与表面物理化学-超级充实版` 增章（其 §六「多组分热力学与界面」正是 6.1/6.4 的理论基础）。

**② §8.7 教材提炼页「超纲」标注未修正——与第40届矛盾**
- `07-资料提炼/书籍提炼/提炼-Atkins物理化学-主题17-19-动力学与表面过程.md`（最后修改 **09-01**，未修）
  - L102：`18B 扩散控制的反应…竞赛中极少直接考查`
  - L187：`主题18B（扩散控制反应）…标注为超纲，不并入正式知识点`
  - L201：`主题18（反应动力学）的大部分内容（18B/18D/18E）对决赛属于超纲`
- 矛盾事实：**第40届初赛第 2 题（21 分）恰恰考了 Stokes-Einstein 扩散系数**（P0-A 已据此建讲义）。
- 应做：把「超纲」标注改为「第40届初赛第2题已考（21分），需并入」，并在页内加修订说明。

**③ §8.6 P2-9 傅献彩《物理化学》素材源不可回溯**
- `07-资料提炼/` 中 **0 份**傅献彩提炼页（仅 `Zchem 相平衡与胶体` 提炼里提及 1 次）。
- 但 **2 份 live 讲义** `source_book` / 参考书单引用它：
  - `化学原理/物化综合计算-超级充实版（自学完整）.md` L1019
  - `化学原理/统计热力学与Maxwell关系-超级充实版（自学完整）.md` L4（frontmatter `source_book`）、L585
  - （另 3 份 `_归档/` 旧讲义亦有引用，可不计）
- 两条路：补导入傅献彩提炼页 **或** 改 `source_book` 为实际在库素材。

**④ §7.3 状态回填未做**：`- [ ] 每块完成后按第五节验收口径闭环，并回填状态` 仍未勾选；§7.2c/d/e 的完成态未回流到 `00-首页/` 与待办页。

### 二、🟡 交接清单 Active（5 张，均等外部条件或用户拍板）
1. 习题书裸下标收口与 git 健壮性复核（P1）——P1 待用户拍板：A/C/D **130 处裸下标**、学生讲义 82 处、清回收站
2. 题库现状评估与扩充计划（P1）——Phase 2 外部下载，**需用户提供 PDF**
3. 计划-图片识别后续任务（P2）——名著候选池视觉核验
4. 任务卡-validate_kb每周运行（P3）——周一 08:23 cron 自动跑

### 三、🟡 待归属（1 项）
- 根目录 `竞赛教程第二分册测试/`（未跟踪 1,258 文件 = 2 md + 1,256 jpg）——属**题目导入会话**；含疑似未整合内容（`化学测试1题目.md` 5 章 33 卷能力测试，全库唯一）。已记 `待办交接清单` §🟡。

### 四、⛔ Blocked（6 张，等外部条件）
KP待完善项（21 项需 ChemDraw/Gaussian/真人）/ PPT制作试点 / 学生反馈闭环 / 学生材料清理 / 纸质版排版测试 / 非正文剩余工作。

---

# 【本会话·续8】第40届第3/7题对口补强 + git push 成功

## 一、新建《容量分析与酸碱滴定-超级充实版（自学完整）》（1386 行）

**触发**：第40届第3题（酱油中氨基酸态氮含量的测定，15 分）
**关键发现（结构性缺口）**：分析化学线其余四块（氧化还原滴定／络合滴定／分光光度法／有效数字与误差）**均有**超级充实版，**唯独缺"容量分析与酸碱滴定"**（原版已在 `_归档/`）→ 本讲补齐。

- 知识主线八节：容量分析基础 → 酸碱滴定原理 → 指示剂选择 → 终点误差 → 弱酸强化滴定（硼酸-甘露醇）→ **甲醛法测氨基酸态氮（核心）** → 空白试验与质量控制
- **例题 4 道**（第40届第3题／克氏定氮与甲醛法／甲醛测定／第32届决赛甲醛光度法）+ **练习 19 道**（基础 8／进阶 8／真题挑战 3）——全部题库实题、**零自编**
- 题源清点：该领域全库**可用实题 146 道**（37 道命中 ≥2 子主题），题源充足
- 闸门：`$` 1158 偶；17 图零断链；54 链接零缺失；js-yaml + validate_kb 0E0W；docx 0 error

## 二、电化学基础内增 §十「电催化与 PCET——以 N₂ 还原为例」（+423 行，1144→1566）

**触发**：第40届第7题（NRR，24 分）
- 内容：N₂ 难还原三因 → Mo 配合物催化三步 → **PCET 的"为什么"**（质子给电子铺路）→ 非水体系 $\mathrm{Fc^+/Fc^0}$ 参照换算
- 例题 2（第40届第7题／第36届第6题非水电池）+ 练习 3 实题；原 §十~§十三 顺延为 §十一~§十四
- 题库原文 `\varphi^\circ`（**标准态**，非摄氏度）按库内规范改 `\varphi^\theta`

## 三、🔴 两个新踩的坑（都已修）

### 坑 1：bash 内联 Python 把 `\varphi` 吃成 VT(0x0b)+`arphi`（7 处）
- 现象：js-yaml 报 `non-printable characters (21:869)`
- 根因：`'\\varphi'` 在 bash 双引号内先被 shell 降为 `\varphi`，再被 Python 当转义 `\v`（VT）解释
- **修复**：`.workbuddy/tmp/fix_varphi.py`（Write 成 .py 再跑）
- **重申铁律**：含反斜杠／正则的脚本**必须 Write 成 .py 再跑**，不用 bash 内联

### 坑 2：图片必须同时进 `媒体仓库/`，否则 docx 丢图
- 现象：`[WARN] 1 image(s) lost: expected 17, docx 16`
- 根因：`build-all-handout-docx.py` 的 `VAULT_MEDIA = <root>/媒体仓库`，**只从这里找图**；而第40届拆题时 163 张图只复制到 `06-外部资料导入/真题/images/`
- **处理**：把第40届第7题引用的 6 张 + 酸碱讲义 1 张补进 `媒体仓库/`（该目录已 gitignore，docx 自包含故不入库）
- **遗留提示**：第40届题库题文件用的是**全路径** wikilink（Obsidian 可解析），题文件不导 docx 故无碍；但**任何讲义引用第40届图时都要先补进 `媒体仓库/`**

## 四、git 提交与 push（用户要求）

**commit 6**：`b647b8adc` feat(讲义): 第40届第3/7题对口补强（5 文件 / 1835 insertions）

**push 成功**：`df5bd830f..bf30d17ec`（远端与本地一致）
🔑 **关键**：必须 `git -c credential.helper=manager push origin master`
- 根因：全局 `~/.gitconfig` 末行 `[credential] helper =`（**空值**）覆盖了 PortableGit 默认 `helper-selector`（selected=manager）→ 直接 push 报 `could not read Username`
- 凭据在 Windows 凭据库（`git:https://github.com`）
- SSH 两条路都不通：22 端口被网络阻断；443 端口能连通但本机 key 未被 GitHub 授权
- 验证用 `git ls-remote origin master`（tracking ref 不可信）

本会话累计 **6 笔提交**，已全部推送。

### 续12 · 课程计划「核化学」补入 + 首次全表超集核验（23:08–23:20）

用户问「课程计划确定完成了？」→ 不凭印象、实跑三重核验，**查出 1 处真缺口**。

- **超集核验（本会话五轮来首次做）**：样板 7 表逐行 `(章,节)` 反查本地 → 严格未命中 19 处、节名级未命中 8 处。逐条 grep 定性：**7 处为写法/归属差异**（`九年级上/下`=`初三上/下`、样板单元格内换行 `ⅠA␊ⅡA␊0族`、样板把「元素周期律」并入原子结构章而本地单列、样板错字 `硒和蹄`→`硒和碲`、`共轭作用/共振式` 改名），**1 处真缺**：样板表3「核化学简介」整节未写（`核化学/放射性衰变/核反应` 在课程计划内 **0 命中**）。
- **补入**：原子结构章末（多电子原子结构之后，与样板行序一致）加「核化学简介」行（5 知识点：放射性衰变和放射系｜放射性衰变定律与半衰期｜放射性活度｜核结合能｜核裂变与核聚变）；章首来源追加「无机化学第五版第 23 章 镧系元素和锕系元素」（库内该提炼页为核化学主落点、20 处命中；普化第15章元素化学 14 处）。**教材来源可回溯 ✓**（6 个相关提炼页全部存在）。
- **数字**：md 378→**379** 行；数据行 **300→301**（结构化学表 46→**47**）；docx 62053→**62157** B；渲染 **25 页**（未变）。
- **验收**：js-yaml 1/1；validate_kb 受检 1 / **0E / 0W**（跑完 `git checkout` 还原报告，保 10 files 批量记录）；md↔docx **逐格一致**（7 表）；XML 7表/框线7/固定布局7/gridCol 27/**vMerge 602**/粗体0/污染0/emoji 0；pg-02 目检核化学行与来源换行正常。
- **提交**：`e1ec196d2`（2 files, +2/−1）。
- **教训（已写入 skill）**：一致性审计的四条判据全是**正向**（本地格填得对不对），**"整节漏写"只能靠反向（样板→本地）核验发现**——上轮补齐 45 处知识点时漏掉整节，正因缺这一条。已补 `sample-doc-conformance-audit` **判据 5** + 三口径坑（vMerge 去重破坏列对齐 / 产物无表头行需 off=0-1 两种对齐 / `｜`↔换行归一化）。
- **注**：`09-15.md` 为多会话共享文件，其末尾「本会话累计 6 笔提交，已全部推送」为**他会话**所写，非本会话。

---

# 【本会话·续9】任务盘点 + MEMORY.md 精简 v3

## MEMORY.md 精简（8,044 → 7,631 字符 / 15,161 → 14,222 字节）
并行会话又补入内容使文件回升。本轮**补全了四条新口径**（净增）并压缩 §七 表述：
- 新增：**push 必须 `-c credential.helper=manager`**（含根因与失败路径）｜**图片必须先进 `媒体仓库/`**（docx 只从这里取图）｜**题源勘查三查铁律**（题干非空／答案完整／题干含主题词；5 次踩坑总结）｜**并行会话隔离判据 = mtime**｜**收敛页无界堆积**与日志双写
- 压缩：§七 课程规划层（保留全部事实点，去过程叙述）
- 脚本 `.workbuddy/tmp/slim_memory.py`（§七 整体替换 + 字符/字节/行统计）

## 本轮为「盘点」性质（未改动库内文件）
盘点结论见回复。关键发现：
- 补强线 **P0-A/C/B + 多组分×表面 + 第9/3/7题 全部收官**；`多组分相图` 的**立项依据已被推翻**（第6题实为钙钛矿纳米晶，不考相图）→ 应关闭而非继续
- 章后层 **12 份已收官**（6 章 × 2）；§11.3「中段课」升级建议**尚未做**（第一轮化学原理、第三轮有机各需中段复习+习题）
- 本地领先远端 **3 笔**（并行会话的 e1ec196d2 / 465d62e14 / f87f5ea9d）
- 工作区 1,261 项（绝大多数为并行会话）

### 续12（23:23–23:40）课程计划新增「三、复习课与习题课规划」

**请求**：复习课和习题课计划也尝试规划一下。

**三项决策（用户选定）**：① 统一 2 节档位（6 大章 × 复习+习题 = 12 节，不做大章 4 节升级）；② 章后层为主 + 收束衔接（不另立课时）；③ 不动表1，新增一章。

**落地**：
- 课程计划新增 `## 三、复习课与习题课规划`。表8 = 6 大章 × 2 课型（12 行 4 列），课型定位与内容要点取自《六大新课大章节总表》各章「章后课型」小节（§5.3 / §6.3 / §7.3 / §8.3 / **§9.4** / **§10.4** —— 注意有机与结构物化是 9.4/10.4，不是 9.3/10.3），末列挂 12 份现役产物路径。表9 = 「分层定位」4 行（课内习题环节 / 章后复习课与习题课 / 阶段测验与结业测 / 真题模拟与收束）。
- 表1 与原有 6 张模块表**零改动**（保持与样板逐格对应）。
- **容器选择的关键判断**：新表**复用模块表四列体例**（章｜节｜知识点｜教材来源），使 docx 构建脚本无需定制即可正确渲染（第 1/4 列按章竖合并、第 3 列按 `｜` 分段）。

**修了构建脚本一个真缺陷**：`build_courseplan_docx.py` 原按 `is_module = hdr==MODULE_HEADER` 二分（表1 式 3 列 / 模块式 4 列），**任何自定义表头的 4 列表会被误判进「表1」三列分支，第 4 列被静默丢弃**。改为按**列数**分派（3 列 = summary 保留表头 / 4 列 = module 丢表头），且竖合并仅限 `is_module`（真模块表）。表9（自定义表头）由此正确保留第 4 列、且不触发合并。

**验收**：js-yaml 1/1；validate_kb 受检 1 / 0E / 0W；md↔docx **逐格一致（9 表 / 0 不一致）**；docx 结构 9 表 / `<w:tblBorders>` 9 / `<w:tblLayout>` 9 / `<w:gridCol>` 35 / `<w:b/>` 0 / vMerge restart **138**（= 上轮 126 + 表8 的 6 组 × 2 列）；污染（`[[`/`**`/emoji）全 0；渲染 **26 页**（+1），p25–26 新增两表版式正常（第 1 列 7 字名折行与既有模块表同款）。引用验存：12 份章后课产物 + 4 份依据文件 + 7 个章节锚点全部命中。

**踩坑**：① bash 内联 python 含反引号再次触发 shell 解析失败（老坑，须 Write 成 .py）；② validate_kb **单文件运行又把报告覆盖成 1 files**（上轮已记"报告覆盖写"，这次重踩），已 `git checkout` 还原保住 10 files 记录。

**提交**：`a70391e95`（md +26/−0；docx 62,157 → 64,187 B）。
