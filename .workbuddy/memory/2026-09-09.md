
## 习题书嵌图不匹配清零（续昨日重组待办，本会话）

- 起点：validate_docx_structure（昨日修复虚检后）真实暴露 9 份 docx 嵌图 < md 嵌图。深挖出三类根因：
  ① 1-化学基础与计量 教师版：源 md 3 行游离 `$$`（转录残缺）→ precheck 掩码错位整章误报 381 处；径改删 3 行零内容损失。
  ② 3-化学动力学 教师版：行内公式断行腰斩 `\frac`→`rac{`（4 处，行首 \f 被吃家族）；径改合并还原。
  ③ 2-分子结构与化学键 等：OCR 残行 `/>…\( \begin{array}…` 未闭合 → pandoc raw-TeX 前向扫描吞掉后续图片（单章 31/254）；**管线级修复**：_preprocess_markdown 末尾新增行级数学平衡守卫（$$ 块外自动补 `\)`/`\end{env}`，$$ 块内多行 array 不干预）。
- 重建 教师/学生/打印版 4 章 ×3 版本，全部 strict-images 通过；第三篇 twin 旧平名三件套归档（消歧前缀名为准）；validate_docx_structure 终态 **docx=105 mismatch=0 skipped=0**。
- 脚本防复发加固：build-all-handout-docx batch_root 模式此前忽略 --file（定向重建变全量重建）已修；build_print_student 适配拍平布局 + --file 过滤（注意 main 里 dst 必须是绝对路径，曾因相对路径把文件写进仓库根——已修并清理）。
- commit `076aae049`（54 文件）；README「已知待办」已改写为清零状态；遗留 1 个存量 Warning（3-化学动力学 L3155 混入 H1 讲次标题，习题书内容线）。
- 排查手法沉淀：docx media 是 rId 名无法反查 hash → 用 sha256（源图字节）对 docx media 字节集合做差集定位丢失图，再按行号回看 md 上下文找共性——两次命中（`/>` 行、断腿 rac 行）。

## H1 混入全清（续：习题书线「继续完善」）

- 全习题书排查非章节 H1：6 文件、四类形态。①游离下一讲标题+导语（3-化学动力学 双版第9讲、3-主族 双版第四讲）→ 删除；②题面内例题 H1（4-过渡元素 例19.2~19.8）→ 加粗降级；③分组标题 H1（3-烷烯炔 章末习题/自学练习）→ 加粗降级。
- 源头在红区题-047 等题库文件答案块（下一讲导语随转录带入）——红区不动，习题书 md 下游径改，README 挂账说明。
- 坑：修复脚本曾顺手压缩连续空行导致 3-主族 被删 500 行（OCR 源本有大量多连空行）——git checkout 回滚后改最小改动版（只删目标行）。教训：批量修 md 时「顺手规范化」必须克制，diff 对账（9+/15-）确认才提交。
- 涉及章 docx 同步重建（教师4+学生2+打印2）；validate_kb 0 Error/0 Warning/标题跳跃 0；validate_docx_structure docx=105 mismatch=0。
- commit `895149a35`。至此习题书线三连收口：重组(25308ae03)→规范(21e8edffd)→嵌图清零(076aae049)→H1清零(895149a35)，均未 push。

## git push 同步（本会话收官）

- 习题书线 5 个 commit（0273c48df 基线 → 895149a35 H1清零）全部推送成功：`077ac48d8..895149a35 master -> master`，`git ls-remote` 验证远端 HEAD = 895149a35。
- push 解法：`GIT_TERMINAL_PROMPT=0 git -c credential.helper= -c credential.helper=wincred push origin master`（绕开 helper-selector；wincred 里已存 shikieiki692 凭据，秒推）。
- 环境坑（记入跨项目记忆）：本环境 fetch 后写不进 `refs/remotes/origin/*`（目录被清、update-ref 静默失败），tracking 显示 `[gone]` 属显示噪音，同步状态以 ls-remote 为准。
- 工作区遗留未提交项保持不动：并行会话的 2 个习题集 md 断链修复、.workbuddy/tmp 脚本与隔离的 pandoc tmp 残留。
