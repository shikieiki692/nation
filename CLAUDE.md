# 妙妙屋 - Agent 执行规范

## 核心原则

- 所有 KB 文件操作必须通过 MCP 工具（`kb_*`），禁止原生 Read/Write/Edit/Bash
- 先读规范再操作，避免重复读取
- 小修改用 Edit，不要 Read 全文再 Write

## Token 节约规则（强制）

### 结构化学题库操作

操作 `04-题库/教材习题/结构化学基础/` 下任何文件前，**必须按顺序执行**：

1. **读速查**：`kb_read` 读取 `04-题库/教材习题/结构化学基础/题库格式速查.md`
2. **读摘要**：`kb_read` 读取 `07-资料提炼/习题提炼/结构化学基础-习题章节摘要.md`（获取原始题目内容）
3. **禁止全读 OCR**：775KB 的 OCR 源文件只在摘要不足时才读取特定章节（用 `limit` 限制行数）

### 通用规则

| 规则 | 说明 |
|------|------|
| Read 加 limit | 除非明确需要全文，否则用 `limit=100` |
| 用 Glob+Edit | 小修改直接 Edit，不要先 Read 再 Write |
| 先检查再读取 | 用 `kb_read` 检查文件是否变化 |
| 批量 Grep | 搜索时用 Grep 而非逐文件 Read |
| 避免重复读取 | 文件刚读过且未修改，跳过读取 |

### 量化目标

| 操作 | 目标 | 路径 |
|------|------|------|
| 单题审计 | <5K tokens | 速查 + 摘要 + 目标文件 |
| 批量审计(10题) | <30K tokens | 速查 + 摘要 + 批量 frontmatter |
| 创建新题 | <10K tokens | 速查 + OCR 特定章节(limit) + 写入 |
| 知识点修正 | <5K tokens | 速查 + 目标文件 + Edit |

### 题库关键文件

| 文件 | 用途 | 大小 |
|------|------|------|
| `04-题库/教材习题/结构化学基础/题库格式速查.md` | YAML 模板、命名规范 | 4KB |
| `07-资料提炼/习题提炼/结构化学基础-习题章节摘要.md` | 308 题的题目索引和关键数据 | 20KB |
| `04-题库/教材习题/结构化学基础/题库审计清单.md` | 6 维度检查、P0/P1/P2 分级 | 3KB |
| `07-资料提炼/习题提炼/化学原理-章节摘要.md` | 376 题的章节概览和知识点 | 15KB |
| `07-资料提炼/习题提炼/有机化学-章节摘要.md` | 103 题的专题概览和知识点 | 12KB |
| `07-资料提炼/习题提炼/分析化学-章节摘要.md` | 20 题的专题概览和知识点 | 5KB |

## MCP 工作流

```
kb_session init → kb_gate create → kb_gate confirm → [kb_read/kb_write/kb_edit] → kb_gate verify → kb_gate close → kb_gate archive
```

## 图片规范

- 格式：`![[图片文件名.jpg]]`
- Ch1-6 图片目录：`结构化学习题与解析/.../_1-150_images/`（1177+ 文件）
- Ch7-10 图片目录：`结构化学习题与解析/.../_151-266_images/`（473+ 文件）

## 禁止事项

- 禁止全读 OCR 源文件（775KB）→ 用 20KB 摘要替代
- 禁止每个文件一个 agent → 批量操作
- 禁止 Read 全文后再 Write → 用 Edit
- 禁止不读速查就操作题库
